"""Oracle brick — thin, domain-agnostic verification oracle.

Verifies findings by dispatching to a registry-resolved verifier with a
generic fallback. The engine never branches on a domain literal; domains
are pure registry-overlay DATA (bd python-factory-216ti Contract B).
"""
