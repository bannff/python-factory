"""Backend registry tests — DEPRECATED.

The adapter registry is superseded by individual brick adapters
(cache, graph, storage). These tests are kept as stubs.
"""


def test_registry_deprecated():
    """Adapter registry is deprecated — use individual brick adapters."""
    assert True
