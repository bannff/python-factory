"""Backend composition tests — DEPRECATED.

Cross-brick composition (cache + graph + storage) is now handled
by each brick independently. These tests are kept as stubs.
"""


def test_composition_deprecated():
    """Backend composition is deprecated — bricks compose via MCP."""
    assert True
