"""Companion X chat agent — shared business logic (prompt, tool selection).

Adapters in runtime/adapters/ handle the HOW (Strands local, AgentCore AWS).
This module handles the WHAT (system prompt, curated tool list).
"""
