---
inclusion: manual
---
# Brick Inventory & Status

> **Ownership lens:** [`.kiro/steering/python-factory.md`](../../.kiro/steering/python-factory.md) is normative. Agent owns the Strands runtime and evidence-promoted catalog; Dataset validates/materializes immutable blueprints with bounded idempotent internal retries; Workflow durably governs cross-brick attempts and global stopping; Evals owns frozen challenge policy, evidence-bound reports, typed deficiencies, acceptance, and promotion. Rows below inventory current capabilities and presets, not static team ownership.

> **Last Updated**: 2026-06-10 (oracle verification brick, Req 15.2/15.3, branch `feat/python-factory-2rg5j-depth-verify-opkind`. **bd:python-factory-216ti (Contract B, oracle/verify)**: added the domain-agnostic `components/oracle/` verification brick; current counts derive from `BRICKS_INDEX.yaml`. Thin, domain-agnostic verification oracle: `VerifierPort` Protocol + registry-overlay dispatch (built-ins-win) with a `GenericFallbackVerifier` that returns the finding state unchanged and never raises — zero domain branching. MCP tool `oracle_verify_finding(finding_id, domain="", backend="")` fetches the finding, dispatches to the resolved verifier, and persists the outcome via the graph brick's atomic write — all by MCP tool-name indirection, no cross-brick imports. Placed in its own brick (NOT security — that would be `if domain=="security"` by the back door; NOT graph — graph owns only the atomic write) per meta-architect verdict `ee6a3b0e`. The `SecurityPentestVerifier` (`components/security/runtime/adapters/oracle_verifier.py`) is registered into the oracle as **pack DATA** via `factory.oracle.interface.register_verifier("security", ...)` from `security/server.py` — it re-runs pentest by calling `security_pentest_scan` / `spawn_registered_graph` by name, with ZERO `import factory.security` (grep+AST canary guards this). GRAPH brick gained `graph_set_finding_state` (atomic single-write, closes the r1pbn/xn0j9 read-modify-write race) + a neutral `state` field {candidate,verifying,verified,refuted} on findings, layered ABOVE security's SuspectedVuln/ProvenExploit labels. Blockchain-anchored provenance oracle DEFERRED to vNext (bd:python-factory-v689r). **bd:python-factory-rk4hc (Contract A, opKind)**: mcp_utils gained the `@op_kind` trait (closed set {shell,read,write,authoring}) — see mcp-tools.md. Implementer `a64b7d58` / `7ced0628`; foreman 38c/4b, zero new violations. Prior: 2026-06-02 (epics bd:python-factory-4bqcz + bd:python-factory-mqhta + bd:python-factory-9m5e3 — `spawn_registered_graph` native @tool (`strands_registered_graph_spawn.py`): registry-driven graph spawn by `graph_id`, resolves topology from `get_default_graphs`, only `AgentNodeRef` nodes supported, graceful text error on unsupported types; `SummarizingConversationManager` (bd:mqhta) now attached in `_build_specialist` so every spawned sub-agent compresses its own rolling context independently; `cleanup_offload_dir()` (bd:9m5e3) exported from `strands_subagent_spawn.__all__` for process-wide offload dir teardown. Prior: 2026-06-02 (epics bd:python-factory-t8o1g + bd:python-factory-x2k0b — native Strands `spawn_swarm` + `spawn_graph` async-generator `@tool`s, alongside bd:python-factory-czpw.1's `spawn_subagent`. `spawn_swarm(agent_ids, task)` assembles `strands.multiagent.Swarm` from registered personas, SESSIONLESS, streams via the existing `tool_stream` rail; `spawn_graph(agent_ids, edges, task)` uses `GraphBuilder → Graph` for directed pipelines (`edges=[{"from":id,"to":id},...]`). Both ride `translate_strands_event`'s `multiagent_node_stream` branch (G2) — each node streams into its OWN sub-bubble (`message_id=f"swarm-{node_id}"`). `AgentSkills plugins=` fix: `_build_specialist` (`strands_subagent_spawn.py`) and `_get_or_create` (`strands_mcp_chat.py`) both pass `plugins=[skills_plugin]` at `Agent` ctor time, NOT `hooks.add_hook()` — `AgentSkills` is a `Plugin` (`init_agent`), not a `HookProvider` (`register_hooks`); `add_hook` was crashing. Wired in `build_chat_tools(include_spawn=True)` alongside `spawn_subagent`. No new BRICK.yaml / BRICKS_INDEX.yaml flag — native `@tool`s, not declared MCP surfaces. Walkthrough: `.agents/recipes/domain-agnostic-substrate.md` Step 1c. Prior: 2026-06-01 (epic bd:python-factory-czpw — conversational chat-agent CREATE/SPAWN/DELETE/SKILLS, branch `feat/python-factory-d4roe-unify-agent-registry`, **UNCOMMITTED working tree** (not on `main`). The Companion-X chat agent can now manage a team of sub-agents conversationally, Strands-natively. **czpw.1 (orchestrator `e58afdb2`)**: NEW `spawn_subagent(agent_id, task)` — a NATIVE async-generator Strands `@tool` (`runtime/adapters/strands_subagent_spawn.py`), the "Agents-as-Tools" pattern done natively, NOT a Swarm / MCP wrapper / `register_dynamic_tool` (strands-expert verdict `209fb838`, REVISED from `ea6fc0be` — the load-bearing decision). Resolves a persona via `unified_personas`, builds a SESSIONLESS Strands `Agent` (`agent_id=cfg.id`, tools scoped to `AgentConfig.tools` or the shared chat MCPClient when empty, skills via `build_skills_plugin`), and `async for ev in specialist.stream_async(task): yield ev` — Strands auto-wraps each yield as a `tool_stream` event. Registered via `build_chat_tools(mcp_client, include_spawn=True)`; sub-agents get `include_spawn=False` (no recursive self-spawn). `translate_strands_event` (`runtime/adapters/strands_mcp_chat_stream.py`) gained a `type=="tool_stream"` branch that unwraps `tool_stream_event.data` + `tool_use.toolUseId`, recurses, and re-tags inner text `message_id=f"subagent-{tcid}"` so the sub-agent renders in its OWN chat bubble (this was the local gap that previously dropped sub-agent streaming). **`agent_create_agent` UN-GATED for chat**: `agent_agent_create_agent` removed from `companion/tools.py::EXCLUDED_TOOLS`; the `agent_` prefix passes `chat_tool_filter`, so the chat LLM can now create personas on the fly. The chat-exclusion guardrail (from verdicts `9d6a73fb` / `6dde9717`) was REVERSED BY EXPLICIT USER WAIVER (on record, meta-architect verdict `dbae0646`); the `@authoring` decorator STAYS as the operator kill-switch (`SUPER_AGENT_ENABLE_AUTHORING_TOOLS`). **czpw.2 (orchestrator `a0c13526`)**: NEW `agent_delete_agent(agent_id)` `@authoring` tool (`mcp/authoring.py`) — mirrors create, rejects built-in ids, `delete_yaml_config("agent", id)` + `agent_registry.load()` so the persona vanishes from `unified_personas` live; `not_found` on miss; completes create/spawn/delete CRUD. NEW `agent_list_skills` `@deterministic` tool (`mcp/skills_tools.py`, registered always-on in `server.py`) — globs `skills/*/SKILL.md` YAML frontmatter → `{skills:[{id,name,description}], count}` (13 skills on disk today); lets the chat agent discover skills and compose real specialists. Prompt (`runtime/companion/prompt.py`) gained a "Creating, Spawning, and Managing Agents" section teaching `agent_list_skills` FIRST → compose with `skills[]`/`tools[]` → `spawn_subagent` → `agent_delete_agent`; removed the bluffing `agent_launch_swarm` "Workflow Routing" guidance (`agent_launch_swarm` stays chat-EXCLUDED). strands-expert `6b9c1482` (delete gap + full Strands primitive audit + list_skills). **No BRICK.yaml / BRICKS_INDEX.yaml change** — `agent_list_skills` / `agent_delete_agent` register dynamically and `spawn_subagent` is a native `@tool`, not a declared surface (verified). Honest Strands coverage (strands-expert `6b9c1482` Q2): chat now has skills discovery+composition, create/spawn/delete, FileSessionManager, plugins, MCPClient, FE-tool injection; still EXECUTOR-ONLY (not chat-reachable): Swarm/GraphBuilder composition, ContextOffloader, ConversationManager, structured output — known gaps. Walkthrough: `.agents/recipes/domain-agnostic-substrate.md` Step 1c. Prior: 2026-06-01 (epic bd:python-factory-d4roe — Kiro-style runtime agent authoring + per-thread persona selection, branch `feat/python-factory-d4roe-unify-agent-registry`. **Phase 0 (`8c2bd674`)**: unified persona registry — built-ins are CODE (`AGENTS_TYPED`), user personas are DATA behind a new `RegistryStore` Protocol (`runtime/ports.py`) with `DiskRegistryStore` / `InMemoryRegistryStore` adapters (`runtime/adapters/registry_store.py` — the AgentCore seam); `registry/unified.py::unified_personas` (built-ins-first, built-in-wins-on-collision via `merge_personas`) is the single read path both `resolve_agent_config` and `get_agent_registry` call (previously disjoint, so runtime-authored agents were invisible to chat); `AgentRegistry.load()` seeds `AGENTS_TYPED` then overlays the store; meta-architect `9d6a73fb`. **Phase 1 (`a4221680`)**: `agent_create_agent` `@authoring` MCP tool (`mcp/authoring.py`) — thin facade over `write_yaml_config("agent", config)` + `agent_registry.load()` so a user persona resolves for chat + discovery with NO restart; rejects built-in id collisions; gated by `SUPER_AGENT_ENABLE_AUTHORING_TOOLS` AND in `companion/tools.py::EXCLUDED_TOOLS` so it's never an LLM chat tool (mirrors `agent_launch_swarm`) — NOTE this chat-exclusion was later REVERSED by user waiver under bd:czpw.1 (see the czpw block at the top of this Last Updated). **Bug 67qvz (`5148afe1`)**: `AgentConfig.id` Pydantic `field_validator` enforces `^[a-z0-9][a-z0-9_-]{0,127}$` (REJECT not coerce) — closes a case-fold data-loss between disk/in-memory stores on case-insensitive filesystems AND a `FileSessionManager` session-id path-traversal vector; `authoring/validation.py::_ID_RE` tightened; meta-architect `56b267c3`. **Phase 2 (`a4b0be66`)**: per-thread persona SWITCH — selection rides `RunAgentInput.forwardedProps.companion_x_agent_id`, `ChatAgentPort.stream` gained `agent_id: str | None = None` threaded to `_get_or_create`, `close()` drops all `*-{thread_id}` slots, registry miss → clean `RUN_ERROR`; new `GET /api/personas` (passthrough to `agent_get_agent_registry`, mounted in `bridge.py`) backs the FE `/` persona palette (`frontends/next-dashboard/components/chat/persona-palette.tsx`); `COMPANION_X_CHAT_AGENT_ID` stays the default (byte-identical when `forwardedProps` absent); meta-architect `a19ef414`, strands-expert `6e22d1bb`. No new BRICK.yaml flag (the tool registers dynamically). Walkthrough: `.agents/recipes/domain-agnostic-substrate.md` Steps 1b + 4b. Prior: 2026-06-01 (P3 wave-3 follow-up: bd:python-factory-ziwqs retracted bd:python-factory-zj026 per strands-expert verdict `68ea2c63` (consult #5) + meta-architect verdict `afdb867a` (consult #6, identified 9 surfaces residue + 2 surfaces genuine taxonomy_registry domain residue deferred to Wave 2 P4). Dropped `AgentConfig.domain_class:str=""` field — Strands ships `agent_id` natively, `domain_class` was a duplicate. `LearningRecallPlugin.__init__(agent)` now reads `self._agent.agent_id` at invocation (Strands canonical primitive at `strands/agent/agent.py:227`). `StrandsMCPChatAgent._get_or_create` now passes `agent_id=cfg.id` to the Strands `Agent` ctor — **the load-bearing fix; was missing, so per-agent recall was structurally broken pre-ziwqs even with `domain_class` set** (Strands' default sentinel `'default'` leaked through). Empty `agent_id` or `'default'` omits the tags filter (broad recall back-compat). The agent IS the domain — skills + tools + system_prompt + agent_id are the entire expertise contract; no separate `domain_class` knob. OUT-OF-SCOPE per-run lineage `domain_class` retained on `LearningEventPayload` (events, dual-emit pattern with `AliasChoices("domain_class","vuln_class")`), `mcp_utils.correlation._CANONICAL_KEYS`, `games_process_workflow_rl(domain_class=...)`, `games_write_experiment_report(...)`, `evals_persist_score(domain_class=...)` — those are run-level taxonomy, distinct from per-agent recall. Wave 2 P4 follow-up filed separately to retire the `taxonomy_registry` + `graph://schemas/taxonomy/{domain}` resource template, relocating taxonomy declaration onto `AgentConfig.taxonomy` field. implementer `a2d50fdb`, qa-tester `3c837989` APPROVE: 43 targeted PASS, 1399 agent regression PASS, foreman zero-new. Prior: 2026-05-31 (P3 burndown wave 2: bd:python-factory-26e2a (Tier-1 native `tags` push-down on `neo4j` + `neo4j_embedding` adapters via the shared `_neo4j_filters.build_filter_clauses` helper; chromadb stays Tier-2 per `amem_helpers.flatten_meta` JSON-encode limitation; runtime post-filter retained as defense-in-depth + Tier-2/3 fallback. implementer `2a6a0a78`, qa-tester `e1b2fb39`). bd:python-factory-b2d2o (`memory_retrieve` gains `metadata: dict[str, str] | None = None` per-key AND filter with **asymmetric default `metadata={}≡None`** — distinct from `tags=[]`'s strict-empty match-nothing — per meta-architect verdict `f279063c` Q4. Cross-filter `tags ∧ metadata` = intersection. **Critical Cypher injection guard**: `MemoryQuery.metadata` keys must match `^[a-zA-Z_][a-zA-Z0-9_]*$` via `re.fullmatch` — Python's `re.match` `$` anchor accepts a trailing `\n`/`\r`/`\t`, which QA `8862f537` discovered as a bypass on the meta-architect's HARD Q7-SECURITY pre-condition; implementer `66d4b831` landed the `re.match → re.fullmatch` single-line fix at TWO sites. Defense-in-depth: Pydantic `_validate_metadata_keys` field_validator AND adapter-side `_neo4j_filters.safe_property_key`. Precedent: `components/graph/.../runtime/adapters/_neo4j_finding.py::safe_label`. implementer `56e85bbd` (initial) + `66d4b831` (security fix), qa `8862f537` BLOCK→APPROVE). bd:python-factory-nley3 (test-only Hypothesis flake fix on `test_p5_preserves_user_text` — assertion now searches for `query` AFTER `_HEADER` via inlined slice; `test_learning_recall_domain_class.py` carries a sibling deterministic regression. implementer `e9b0c851`). Wave-2 push-down convention for both `tags` and `metadata`: Tier-1 = `memory` (in-memory) + `neo4j` + `neo4j_embedding` native; Tier-2 = `chromadb` post-filter only; Tier-3 = `mem0` / `zep` / `cognee` post-filter only. Prior: 2026-05-29 (vs1vu+zj026 wave: per-domain RL recall closed end-to-end. bd:python-factory-vs1vu (P2 fix, meta-architect verdict `2efd2a40` Q9): `components/games/src/factory/games/runtime/game_pipeline.py:189` `_store_learnings` now passes `metadata={"tags": [...]}` instead of a top-level `tags=` kwarg that `MemoryQuery` silently dropped (latent since #550 renamed `vuln_class → domain_class`). bd:python-factory-zj026 (P3 closure, strands-expert verdict `46479612` Q1+Q2+Q8): `AgentConfig.domain_class:str=""` field added to `runtime/registry_contracts.py`; `LearningRecallPlugin.__init__(agent, domain_class="")` is now constructor-bound and forwards `tags=[f"{domain_class}-learnings"]` to `memory_retrieve` only when non-empty. `build_chat_plugins(agent, domain_class="")` and `StrandsMCPChatAgent._attach_plugins(thread_id, agent, domain_class="")` thread it through; client-side `_has_learnings_tag` filter retained as defense-in-depth. Empty default = today's broad recall byte-identical. Pinned by `test_per_domain_recall_e2e.py`, `test_chat_persona_domain_class_e2e.py`, `test_learning_recall_domain_class.py`, `test_store_learnings_tags.py`. qa-tester `9c4846a4` APPROVE_WITH_NOTES. Prior: 2026-05-29 (P3 burndown wave landed: bd python-factory-qm07q (graph per-domain taxonomy registry + `graph://schemas/taxonomy/{domain}` resource template) + bd python-factory-lin6p (`memory_retrieve` `tags:list[str]|None` filter via runtime post-filter, all 7 adapters inherit) — implementer `7675a162`, qa `96d9e8ac` APPROVE_WITH_FOLLOWUPS, meta-architect `2efd2a40`, strands-expert `46479612`. epic python-factory-hadbi: domain-agnostic substrate prep landed across three bricks (qa-tester APPROVE_WITH_FOLLOWUPS `1e7ce24b`, 1918/1919 broad regression). **Follow-up wave (PRs #600 + #601, bd python-factory-twxj0 / tmlrx / st587 / sm1va / le9gu / 7ut47 / 2sjyz / 2tgo1 / d4r37 / ttru9)**: `domain_class` / `count_labels` / `taxonomy_edges` now thread through the MCP boundary (`games_process_workflow_rl`, `games_write_experiment_report`, `graph_get_workflow_summary`, `evals_persist_score`), the canonical `LearningEventPayload.domain_class` field (alias `vuln_class`), `mcp_utils.correlation._CANONICAL_KEYS`, and per-node `skills:list[str]` on `AgentNodeRef` + `SwarmAgentConfig` via the neutral `runtime/adapters/skills_attach.py::build_skills_plugin` helper (strands-expert verdict `1c5df3c7`). FE consumers in `frontends/next-dashboard` (`canvas/welcome-view.tsx`, `lib/runtime-metrics-summary.ts`) read `domain_class` with `vuln_class` fallback. **agent — bd python-factory-hadbi.1**: Companion-X chat persona is now driven by the agent registry. New `COMPANION_X_CHAT_AGENT_ID` env var (default `companion-x-default`, LOUD-FAIL on miss per strands-expert verdict `9b6d46cd`) selects an `AgentConfig` from `AGENTS_TYPED` (`registry/defaults.py`); the `companion-x-default` registration in `defaults_companion_x.py` carries the verbatim original `COMPANION_X_PROMPT` so chat behavior is byte-identical to pre-pivot. `AgentConfig` (`runtime/registry_contracts.py`) gained a `skills:list[str]` field (default `[]`); the chat adapter (`runtime/adapters/strands_mcp_chat.py` + `strands_mcp_chat_persona.py::build_skills_plugin`) attaches Strands `AgentSkills` only when `cfg.skills` is non-empty — closes the prior SDK-First flag from `eb92d1b5`. Cache key + `Agent.name` + `FileSessionManager.session_id` all use `f"{agent_id}-{thread_id}"` (meta-architect verdict `9a4aba3c`) so swapping the env yields a clean session boundary with no history bleed across personas. New BRICK.yaml flag: `companion_x_registry_persona`. **graph — bd python-factory-ecph9 + python-factory-le9gu**: typed reads `get_findings_for_run` / `get_recent_findings` accept `taxonomy_edges: list[TaxonomyEdgeSpec] | None = None`. `None` = back-compat security CWE/OCSF defaults; `[]` = no taxonomy join (rows have no taxonomy keys); custom edges drive caller-supplied row columns from `target_props`. New `TaxonomyEdgeSpec` Pydantic model lives in `components/graph/src/factory/graph/runtime/models.py`; Cypher builder helpers in `runtime/adapters/_neo4j_finding.py` + `_taxonomy.py`. MCP tools `graph_get_findings_for_run` and `graph_get_recent_findings` surface this as `taxonomy_edges: list[dict] | None = None` accepting raw dicts; `coerce_edges` validates per call. `graph_get_workflow_summary` accepts both `taxonomy_edges` and `count_labels` per call so the FE / domain agents can drive the summary panel without bespoke endpoints. The graph brick is now domain-agnostic at the typed-read boundary — no domain-specific labels in adapter code. New BRICK.yaml flag: `taxonomy_edges_polymorphic_reads`. **games — bd python-factory-qer1z**: RL pipeline reads `config["domain_class"]` (was `vuln_class`); back-compat fallback chain is `domain_class → vuln_class → game_type` (`game_pipeline.py::_store_learnings`). Memory tags now dual-emit `[domain_class, "scan-learnings", f"{domain_class}-learnings", run_id]` — old `scan-learnings` preserved for back-compat with the `LearningRecallPlugin` filter, new `{domain}-learnings` enables per-domain recall (e.g. `wine_pairing-learnings`). `_collect_findings(invoker, run_id, wtype, count_labels=None, taxonomy_edges=None)` takes per-call label tuple + edges; `DEFAULT_COUNT_LABELS = ("Finding", "ProvenExploit")` preserves security defaults. Same pattern in `workflow_report_graph.py::graph_counts(invoker, run_id, count_labels=None)` and `workflow_report_findings.py::build_findings_detail(invoker, run_id, count_labels=None)`. `games_process_workflow_rl` and `games_write_experiment_report` now accept the same `domain_class` / `count_labels` / `taxonomy_edges` triple at the MCP boundary so callers driving the RL loop from outside `games_create` skip the per-game `config` round trip. Future domain agents (wine, workout) drive their own RL loop via `config={"domain_class": "your_domain", "count_labels": [...]}` or direct MCP args with no engine code change. New BRICK.yaml flag: `domain_class_rl_loop`. End-to-end walkthrough: `.agents/recipes/domain-agnostic-substrate.md`.) Prior: 2026-05-28 (bd python-factory-u3xy: chat-agent read-side RL closure landed. New `LearningRecallPlugin` (`components/agent/src/factory/agent/plugins/learning_recall.py`, 183 LOC) attaches to the persistent chat `Agent` via `HookProvider` on `BeforeInvocationEvent`. Every turn: extracts the last user message text, calls `memory_retrieve(query=text[:256], user_id="kiro-agent", limit=5, min_relevance=0.3)` through the `tool_invoker` service registry, filters results to entries whose `metadata.tags` end in `-learnings` (the RL-loop signature emitted by `memory/runtime/learning_emit.py`), and prepends a "Prior runs (recall, system-supplied):" block to the user message in place via `event.messages` mutation — which is the only field `BeforeInvocationEvent._can_write` whitelists (Strands SDK 1.40, `strands/hooks/events.py:62-64`). The mutation lands in conversation history via `FileSessionManager` (bd-vfgf), so recall persists with the question it grounded and replays cleanly on resume. On miss / failure: silent no-op (mirrors `collaboration.py` pattern). Closes the loop end-to-end alongside bd-o7t8 `learning.applied` event (write-side observability) and bd-vfgf session persistence — the agent now actually USES what prior runs learned. Test invariants P1-P6 pinned in `components/agent/test/factory/agent/test_learning_recall.py`. Agent BRICK.yaml gains the `learning_recall_plugin` feature flag. Prior: 2026-05-27 (bd python-factory-evra + bd python-factory-0s6w: RL persistence contract gap closed across SAST + DAST workflow paths. SAST validators (`idor-validate/SKILL.md` PHASE 3, plus consolidator step in hybrid + swarm `_CONSOLIDATOR_PROMPT`) now persist `entity_type='Finding'` (was `'ValidatedFinding'`) with explicit `entity_id='finding-{run_id}-<n>'` and a `PROMOTED_FROM` edge back to the source `SuspectedVuln` — single-promotion-point design preserved. SAST validator tool surfaces (`_VALIDATOR_TOOLS` in `defaults_sast_open.py` and `defaults_code_scan.py`) gained `graph_add_entity`, `graph_add_relationship`, `graph_get_findings_for_run` (workflow-path bare names, no `strands_tools.` prefix per `executors/_workflow_manager.py:114-124` filter contract). Hybrid + swarm consolidators rely on `_mcp_tools` auto-injection (`graph_nodes.py:83`, `swarm.py:158-160`) — their `tools=` lists were intentionally left untouched; only the `_CONSOLIDATOR_PROMPT` step 6 was amended to specify `entity_type='Finding'`. DAST testers (`dast-open-scan/SKILL.md` PHASE 4) now persist `entity_type='ProvenExploit'` (renamed from `ConfirmedVuln`) with `CONFIRMS` edge from SAST seed `Finding` and `CHAINS` for attack-chain components. DAST `_DAST_TOOLS` in `defaults_dast_open.py` gained `graph_add_entity`, `graph_add_relationship`, `graph_find_entities`, `graph_get_findings_for_run`, `memory_store`, `memory_retrieve`. Collateral fix: `sast-open-scan/SKILL.md` PHASE 0 stale `graph_find_entities(entity_type='ConfirmedVuln')` renamed to `'ProvenExploit'`. Persistence contract pinned by 12 property tests in `components/agent/test/factory/agent/test_persistence_contract_properties.py` (covers entity-type relabel, validator-side tool-list expansion, hybrid/swarm prompt amendment, bare-name invariant via Hypothesis @given over 8 workflow-path tool lists, and `ValidatedFinding` extinction across registry/ + skills/). Recipe: `.agents/recipes/rl-pipeline.md` Persistence Contract section.) Prior: 2026-05-25 (bd python-factory-vw04 + bd python-factory-115z: Companion-X chat is now canvas-aware end-to-end. **vw04 (FE-only)** lifts workbench state to a `<WorkbenchProvider>` mounted above `<CopilotKitProvider>` and adds `<CanvasContextBridge />` which calls CopilotKit v2's `useAgentContext` (renamed from v1's `useCopilotReadable`) to push `{activeView, activeTabId, openTabs, recentTools[≤10]}` into agent context every turn — CopilotKit forwards as `RunAgentInput.context[]`, the api base prepends each entry to the user message as a labelled `Canvas context (system-supplied, FE-pushed):` block via `bases/api/runtime/ag_ui_input.py::prefix_context`. **115z (full round-trip)** adds `<FrontendTools />` which registers `fe_navigate_canvas` via `useFrontendTool`; CopilotKit forwards as `RunAgentInput.tools[]`. The api base validates each entry against the new `FrontendToolSpec` Pydantic model and threads it through to `ChatAgentPort.stream(thread_id, message, fe_tools=...)`. New `FrontendToolPlugin` (`components/agent/src/factory/agent/plugins/frontend_tool_plugin.py`) registers stub `_StubAgentTool`s on the per-Agent `ToolRegistry.dynamic_tools` for the turn via `BeforeInvocationEvent` and strips them in `AfterInvocationEvent` — the legitimate Strands SDK API since `BeforeModelCallEvent.tool_specs` is read-only. The stub yields a deferred sentinel `{_frontend_pending: true, name, args}` and sets `request_state["stop_event_loop"] = True`; Strands ends the turn cleanly, AG-UI surfaces the canonical `TOOL_CALL_END` + `TOOL_CALL_RESULT` pair, FE handler runs locally (e.g. `workbench.switchView(view)`), CopilotKit re-POSTs `/ag-ui/run` with the full message history including the `role=tool` reply, Strands resumes from there. FE tool names use the `fe_` prefix to avoid collision with the ~650 MCP brick tools. Closes the canvas-aware chat loop alongside the bd-eyuj / bd-47sl / bd-tpfk thinking trio. Agent BRICK.yaml gains the `frontend_tool_plugin` feature flag. New recipe: `.agents/recipes/canvas-aware-chat.md`.) Prior: 2026-05-24 (bd python-factory-tpfk: Companion-X chat agent now wires Claude extended thinking upstream — new `COMPANION_X_CHAT_THINKING_BUDGET` env var (int, Bedrock min 1024) makes the Strands chat adapter construct `BedrockModel` with `additional_request_fields={"thinking": {"type": "enabled", "budget_tokens": N}}` when the chat model is Sonnet 3.7+ or Opus 4+ (Haiku/Nova/Llama/GLM5/GPT-OSS no-op). This is the missing upstream piece that closes the bd-47sl + bd-eyuj + bd-tpfk trio end-to-end: bd-eyuj already emits `REASONING_MESSAGE_*` from the AG-UI mapper and bd-47sl already renders them as the v2 Thinking panel, but neither fired until the chat adapter actually requested thinking from Bedrock. Agent BRICK.yaml gains `extended_thinking` feature flag. Prior: 2026-05-23 (bd python-factory-eyuj: ui chat-stream mapper now emits canonical AG-UI `REASONING_MESSAGE_START/CONTENT/END` trio for `ChatStreamEvent.reasoning_text`, replacing legacy `STEP_STARTED("reasoning") + CUSTOM("reasoning_text")`. New `AGUIStreamState.reasoning_message_id` tracks the open block; reasoning closes on next non-reasoning event for Claude 4 interleaved-thinking support, or on `done` / `error`. Frontend (bd python-factory-47sl) cut over to CopilotKit v2 — `CopilotChatReasoningMessage` auto-renders the trio as the Thinking panel). Prior: 2026-05-22 (bd python-factory-y9tq: GT scoring unified in evals brick. `evals/runtime/gt_scorer.py` now polymorphic via `match_on` tuple param (SAST: `("cwe","file")`, DAST: default `("cwe","file","method","path")`); new `evals_persist_score` @operational MCP tool persists P/R/F1 to sqlite `eval_results` collection via `storage_doc_insert` (idempotent doc_id `eval-{run_id}`). DELETED: `games/runtime/_rl_gt_edges.py` (Neo4j edge writer — user no longer runs Neo4j locally) and `games/runtime/sast_scorer.py` (algorithm twin of evals scorer). REMOVED EVENT: `rl.gt.edges.written` no longer emitted. Longitudinal F1 history is now a `storage_doc_query` against sqlite, not a Cypher query against `EVALUATED_AGAINST` edges. Prior: 2026-04-13 agent: skills list adds class-agnostic `sast-open-scan` / `dast-open-scan` for new `sast` / `dast` open workflow registrations — bd c3hx + vy9r + 1v2t; 2026-04-12 agent: GLM 5 replaces Nova 2 Lite as SAST scanner and DAST IDOR tester; GLM 5 replaces Sonnet as SAST validator; 2026-03-11 events: the rewards dispatch path now calls `learning_compute_reward`; Learning's `gt-findings` adapter delegates to `games_process_workflow_rl`, while telemetry, chat, generic, memory, and blockchain fan-out remain separate subscribers; removed games_check_convergence — use metrics brick)

## Factory Status

Current component and base counts are generated from `BRICKS_INDEX.yaml`; do not hard-code totals here. The deprecated `backend` entry remains cataloged below, and the removed `flet_dashboard` base remains represented only by the Flet adapter in the UI brick.

## Exemplar Component

**workflow** - The canonical structural reference for bricks. Has:
- ✅ runtime/ports.py (Protocol interfaces)
- ✅ mcp/ with full primitives (deterministic, operational, authoring, resources, prompts)
- ✅ Modular server.py (delegates to mcp/ modules)
- ✅ interface.py exports create_server + Runtime

This structural exemplar status does not grant Workflow ownership of intelligent within-attempt Agent work.

## Components

All active components are compliant with factory tenets:
- BRICK.yaml present and cited in BRICKS_INDEX
- runtime/ subdirectory with ports.py (Protocol interfaces)
- Clean architecture (runtime/, mcp/, interface.py, core.py)

| Component | Description |
|-----------|-------------|
| agent | Owns registered personas and skills, scoped models/tools, structured outputs, and LangChain agents and bounded LangGraph execution for bounded within-attempt reasoning. Companion-X may compose the smallest objective-specific team through these Agent surfaces; only repeatedly proven topologies belong in the small evidence-promoted template catalog. The existing `kind: workflow` / `FACTORY_REGISTRY` / `WorkflowExecutor` route is documented current-state migration debt, not the target ownership model; Workflow remains the durable cross-brick controller. Ships security presets via `defaults.py`: 2 swarms (security-recon ×3 agents, code-review ×2 agents) + 3 standalone agents (security-analyst, eval-runner, compliance-checker). AWS pentest swarm (`defaults_aws_pentest.py`): 4-agent pipeline (aws-recon → iam-auditor → exploit-tester → report-writer) targeting LocalStack environments via `sandbox.execute`. Skills: `pentest-ops` (grounded pentest workflow patterns), `aws-pentest-ops` (AWS-specific pentest targeting LocalStack), `sast-open-scan` (class-agnostic SAST playbook covering 9 vuln classes + PHASE 6 attack chaining), and `dast-open-scan` (class-agnostic DAST playbook covering 9 vuln classes + PHASE 6 attack chaining). Persistent chat agent (`ChatAgentPort`) powers the Companion X sidebar — `reason()` routes to it by default, with direct `SwarmExecutor`/`GraphExecutor` dispatch for explicit swarm/graph hints. Chat agent now accepts FE-side tool registrations via `FrontendToolPlugin` + `_StubAgentTool` stubs (bd python-factory-115z) — `ChatAgentPort.stream(fe_tools=...)` registers stubs on the per-Agent `ToolRegistry.dynamic_tools` for the turn, the stub yields a deferred sentinel + `stop_event_loop=True`, AG-UI surfaces the call as the canonical END+RESULT pair, the FE handler runs locally, and CopilotKit v2 re-POSTs with the full message history so Strands resumes from the new `role=tool` reply. Chat agent also consumes pushed canvas context via the AG-UI `RunAgentInput.context[]` field (bd python-factory-vw04) — each entry is prepended to the user message by the api base as a labelled `Canvas context (system-supplied, FE-pushed):` block. Together with the bd-eyuj / bd-47sl / bd-tpfk thinking trio, bd-vw04 + bd-115z close the canvas-aware chat loop end-to-end. Feature flag: `frontend_tool_plugin` in `BRICK.yaml`. Optional `COMPANION_X_CHAT_THINKING_BUDGET` env var (int, Bedrock min 1024) enables Claude extended thinking on the chat agent — applies only when the chat model is Sonnet 3.7+ or Opus 4+ (Haiku/Nova/Llama/GLM5/GPT-OSS are no-op); the chat adapter constructs `BedrockModel` with `additional_request_fields={"thinking": {"type": "enabled", "budget_tokens": N}}`, which makes upstream emission of `REASONING_MESSAGE_*` events (bd-eyuj) actually fire and the v2 Thinking panel (bd-47sl) light up — closes the bd-47sl + bd-eyuj + bd-tpfk trio end-to-end. Feature flag: `extended_thinking` in `BRICK.yaml`. Chat agent also closes the read-side of the RL feedback loop alongside bd-o7t8 `learning.applied` event and bd-vfgf `FileSessionManager` (bd:python-factory-u3xy) — every chat turn the new `LearningRecallPlugin` (`plugins/learning_recall.py`) calls `memory_retrieve` and prepends `*-learnings` hits into the user message via `BeforeInvocationEvent` (the only field `_can_write` whitelists per Strands SDK 1.40); the mutation persists with the question it grounded via `FileSessionManager`. On miss / failure: silent no-op. Feature flag: `learning_recall_plugin` in `BRICK.yaml`. Semantic MCP tool selection via `companion/tools.py` (Neo4j vector search with static prefix fallback). **Swarm agents automatically receive MCP tools via native Strands `MCPClient` (`runtime/adapters/strands_mcp_graph.py`)** — in-memory transport to the aggregator, scoped by `tool_filters` predicate against `SWARM_TOOL_ALLOWLIST`. Tool selection matches both `prefix_` and `prefix.` naming conventions (e.g. `sandbox.provision`, `sandbox.execute`). Async variants: `invoke_graph_async` uses permanent subprocess dispatch (`scripts/run_graph.py`) to avoid GIL contention — the subprocess reads `required_bricks` from graph config to scope MCP tool loading. `invoke_swarm_async` (@operational) runs in-thread. Both return `workflow_id` immediately; callers poll progress with existing `get_workflow_status`. Vuln-class-agnostic SAST code scan graph `rt-sast-scan` (`defaults_code_scan.py`): 3 parallel agents (GPT OSS, Sonnet, GLM 5) → `hierarchy-analyzer` (Sonnet — compares sibling endpoints for inconsistent auth patterns, flags hierarchy_inconsistency findings) → consolidator with 2/3 voting consensus → validator (GLM 5 — adversarially challenges each confirmed finding), parameterized by `{{vuln_class}}` context variable. GLM 5 (`zai.glm-5`) is a 745B MoE frontier agentic model from Z.AI — best validator (100%) and best DAST IDOR (90%); added to ENSEMBLE in `models.py`. Red team DAST IDOR graph `rt-scan-idor` (`defaults_redteam_idor.py`): 3 parallel agents (GPT OSS, Sonnet, GLM 5) independently test IDOR vectors; evals/metrics/rewards run via event subscriptions. SAST prompt includes STEP 0 — RETRIEVE PRIOR LEARNINGS (`memory_retrieve` for prior TP/FP patterns before scanning). `vuln_class_config.py` maps 6 vuln classes (IDOR, XSS, SQLi, SSRF, CSRF, Path_Traversal) → CWE families, OWASP refs, and code patterns for graph parameterization; all 6 classes include `param_blocklist` (safe params to skip) and `taint_sinks` (dangerous sink calls to trace — e.g. `repository.findById` for IDOR, `innerHTML` for XSS, `cursor.execute` for SQLi, `requests.get` for SSRF, `open(` for Path_Traversal; CSRF has empty sinks since it's control-absence, not taint-flow). `security.trace_taint` consumes these sinks for polymorphic taint analysis. First skill: `idor-code-scan` (5 code patterns, Java/Python/Node guidance, structured 3-cycle reflection workflow). Persistence-promotion contract (bd:python-factory-evra + bd:python-factory-0s6w): SAST validators promote `SuspectedVuln` → `Finding` via `idor-validate/SKILL.md` PHASE 3 (single-promotion-point design — targeted `defaults_code_scan` keeps writes validator-only, `_CONSOLIDATOR_TOOLS` stays `["think"]`); DAST testers persist `ProvenExploit` directly in `dast-open-scan/SKILL.md` PHASE 4 (renamed from `ConfirmedVuln`). Workflow-path validator tools (`_VALIDATOR_TOOLS` in `defaults_sast_open.py` + `defaults_code_scan.py`) include `graph_add_entity`, `graph_add_relationship`, `graph_get_findings_for_run` as bare names per `executors/_workflow_manager.py:114-124` filter contract. DAST `_DAST_TOOLS` in `defaults_dast_open.py` adds the same plus `graph_find_entities`, `memory_store`, `memory_retrieve`. Hybrid + swarm consolidators (`defaults_code_scan_hybrid.py`, `defaults_code_scan_swarm.py`) rely on `_mcp_tools` auto-injection (`graph_nodes.py:83`, `swarm.py:158-160`) — their `tools=` lists are intentionally untouched and only `_CONSOLIDATOR_PROMPT` step 6 specifies `entity_type='Finding'` explicitly. Contract pinned by 12 property tests in `components/agent/test/factory/agent/test_persistence_contract_properties.py`. Red-team variants follow the same shape (bd:python-factory-t754 + bd:python-factory-ksqd): `rt-scan-vulns` SCAN_VULNS_SWARM in `defaults_redteam_exploit.py` now writes `entity_type='Finding'` (was `'Vulnerability'`); `redteam-pentest` (PENTEST_IAM_AUDITOR + PENTEST_SECURITY_VALIDATOR in `redteam_playbooks.py`) and the `aws-pentest-ops` skill PHASE 4 now persist `entity_type='ProvenExploit'` to graph before `memory_store`. Collateral: `events/runtime/dispatch_args.py::_count_findings` reads the `Finding` label (no longer `Vulnerability`). Substrate-prep — Companion-X chat persona is now driven by the agent registry (bd:python-factory-hadbi.1): `COMPANION_X_CHAT_AGENT_ID` env (default `companion-x-default`, LOUD-FAIL on miss per strands-expert verdict `9b6d46cd`) selects an `AgentConfig` from `AGENTS_TYPED`; `defaults_companion_x.py::COMPANION_X_DEFAULT_AGENT` carries the verbatim original `COMPANION_X_PROMPT` so chat behavior is byte-identical to pre-pivot. `AgentConfig` (`runtime/registry_contracts.py`) gained `skills:list[str]` (default `[]`); the chat adapter (`runtime/adapters/strands_mcp_chat.py` + `strands_mcp_chat_persona.py::build_skills_plugin`) attaches Strands `AgentSkills` only when `cfg.skills` is non-empty — closes the prior SDK-First flag from `eb92d1b5`. Cache key + `Agent.name` + `FileSessionManager.session_id` all use `f"{agent_id}-{thread_id}"` (meta-architect verdict `9a4aba3c`) so swapping the env yields a clean session boundary with no history bleed across personas. Per-node `skills:list[str]` (bd:python-factory-2tgo1) extends the contract to `AgentNodeRef` and `SwarmAgentConfig` so graph + swarm registrations attach `AgentSkills` per node — 17 prompt-skill registrations populated across `defaults_*.py`. The neutral helper lives at `runtime/adapters/skills_attach.py::build_skills_plugin` (strands-expert verdict `1c5df3c7`: do not import a skills helper out of a chat-namespaced module — the executor plugin factories must take a domain-neutral dependency); `strands_mcp_chat_persona.py` re-exports it for back-compat. Future domain agents (wine, workout) just register their own `defaults_<domain>.py`, append to `AGENTS_TYPED`, and flip the env. Feature flag: `companion_x_registry_persona` in `BRICK.yaml`. Per-domain RL recall closure (bd:python-factory-ziwqs, retracts bd:python-factory-zj026, qa `3c837989` APPROVE): Strands' canonical `agent_id` primitive (`strands/agent/agent.py:227`) IS the recall namespace — `domain_class` was a duplicate per strands-expert verdict `68ea2c63` + meta-architect verdict `afdb867a`. `LearningRecallPlugin.__init__(agent)` reads `self._agent.agent_id` at invocation time. `StrandsMCPChatAgent._get_or_create` now passes `agent_id=cfg.id` to the Strands `Agent` ctor — **the load-bearing fix; was missing, so per-agent recall was structurally broken pre-ziwqs regardless of any `domain_class` setting** (Strands' default sentinel `'default'` leaked through). Empty `agent_id` or Strands default `'default'` omits the tags filter (broad recall back-compat). The agent IS the domain: skills + tools + system_prompt + agent_id are the entire expertise contract; no separate `domain_class` knob. Pinned by `test_per_domain_recall_e2e.py`, `test_chat_persona_domain_class_e2e.py`, `test_learning_recall_domain_class.py`. End-to-end walkthrough: `.agents/recipes/domain-agnostic-substrate.md`. Unified persona registry (epic bd:python-factory-d4roe, branch `feat/python-factory-d4roe-unify-agent-registry`): built-ins are CODE (`AGENTS_TYPED`), user personas are DATA behind the new `RegistryStore` port (`runtime/ports.py`) with `DiskRegistryStore` / `InMemoryRegistryStore` adapters (`runtime/adapters/registry_store.py` — the latter is the AgentCore seam). `registry/unified.py::unified_personas` (built-ins-first, built-in-wins-on-collision via `merge_personas`) is the single read path BOTH the chat resolver (`resolve_agent_config`) and the discovery surface (`get_agent_registry`) call — pre-d4roe these were disjoint so a runtime-authored agent was invisible to chat; `AgentRegistry.load()` seeds `AGENTS_TYPED` then overlays the store, and `SuperAgent.initialize` wires the registry's store process-wide via `set_default_store` (meta-architect verdict `9d6a73fb`). New `agent_create_agent` `@authoring` MCP tool (`mcp/authoring.py`, bd:d4roe.2) creates a persona on-the-fly — a thin facade over `write_yaml_config("agent", config)` + `agent_registry.load()` so it resolves for chat + discovery with NO restart; rejects built-in id collisions, gated by `SUPER_AGENT_ENABLE_AUTHORING_TOOLS`. (At d4roe time it was ALSO listed in `companion/tools.py::EXCLUDED_TOOLS` so it was never an LLM-callable chat tool, mirroring `agent_launch_swarm` — that chat-exclusion was later REVERSED by user waiver under bd:czpw.1; see the czpw note at the tail of this row.) `AgentConfig.id` is charset-locked `^[a-z0-9][a-z0-9_-]{0,127}$` via a Pydantic `field_validator` (REJECT not coerce, bd:python-factory-67qvz — closes a case-fold data-loss between disk/in-memory stores on case-insensitive filesystems AND a `FileSessionManager` session-id path-traversal vector; `authoring/validation.py::_ID_RE` tightened to match). Per-thread persona SWITCH (bd:d4roe.3): selection rides `RunAgentInput.forwardedProps.companion_x_agent_id`, `ChatAgentPort.stream` gained `agent_id: str | None = None` threaded to `_get_or_create` (cache slots `f"{agent_id}-{thread_id}"`), `close()` drops all `*-{thread_id}` slots, registry miss → clean `RUN_ERROR`; new `GET /api/personas` (thin passthrough to `agent_get_agent_registry`, mounted in `bases/api/.../bridge.py`) backs the FE `/` persona palette (`frontends/next-dashboard/components/chat/persona-palette.tsx` → `core.setProperties({companion_x_agent_id})`). `COMPANION_X_CHAT_AGENT_ID` stays the default (byte-identical when `forwardedProps` absent). meta-architect `9d6a73fb` / `56b267c3` / `a19ef414`; strands-expert `6e22d1bb`. No new BRICK.yaml flag (the tool is registered dynamically). Conversational sub-agent CRUD (epic bd:python-factory-czpw, same branch, UNCOMMITTED): the chat agent now has native create/spawn/delete/list-skills CRUD over a team of sub-agents, Strands "Agents-as-Tools" done natively. `spawn_subagent(agent_id, task)` (bd:czpw.1) is a NATIVE async-generator Strands `@tool` (`runtime/adapters/strands_subagent_spawn.py`) — resolves a persona via `unified_personas`, builds a SESSIONLESS Strands `Agent` (`agent_id=cfg.id`, tools scoped to `AgentConfig.tools`, skills via `build_skills_plugin`) and streams `specialist.stream_async(task)` — NOT a Swarm, NOT an MCP wrapper, NOT `register_dynamic_tool` (strands-expert `209fb838`). Registered via `build_chat_tools(mcp_client, include_spawn=True)`; sub-agents get `include_spawn=False` (no recursive self-spawn). `translate_strands_event` (`runtime/adapters/strands_mcp_chat_stream.py`) gained a `type=="tool_stream"` branch that unwraps + recurses + re-tags inner text `message_id=f"subagent-{tcid}"` so the sub-agent renders in its OWN chat bubble. `agent_create_agent` is now CHAT-CALLABLE — `agent_agent_create_agent` removed from `companion/tools.py::EXCLUDED_TOOLS`; the security-exclusion guardrail was WAIVED BY EXPLICIT USER DECISION (meta-architect `dbae0646`, reversing `9d6a73fb`/`6dde9717`), `@authoring` kept as the operator kill-switch. NEW `agent_delete_agent(agent_id)` `@authoring` tool (`mcp/authoring.py`, bd:czpw.2) mirrors create (rejects built-ins, `delete_yaml_config` + `agent_registry.load()`, `not_found` on miss) — completes CRUD. NEW `agent_list_skills` `@deterministic` tool (`mcp/skills_tools.py`, always-on in `server.py`) globs `skills/*/SKILL.md` → `{skills,count}` (13 on disk). Prompt section "Creating, Spawning, and Managing Agents" added; bluffing `agent_launch_swarm` "Workflow Routing" guidance removed. strands-expert `6b9c1482` (delete gap + Strands primitive audit + list_skills); orchestrator `e58afdb2` (czpw.1) / `a0c13526` (czpw.2). No BRICK.yaml change (these register dynamically; `spawn_subagent` is a native `@tool`). Walkthrough: `.agents/recipes/domain-agnostic-substrate.md` Step 1c. |
| auth | Authentication via Keycloak and AWS Cognito. Its 15 public native MCP v2 tools (4 deterministic, 7 operational, 4 authoring) accept strict same-brick Pydantic v2 flat inputs and return `ToolResult[OutputDTO]` envelopes. Expected authentication denials, absent backend/principal, configuration failures, and refresh/revoke/exchange/userinfo outcomes remain successful typed data with stable safe error codes; only unexpected or unclassified backend faults fail the envelope. |
| ~~backend~~ | ~~DEPRECATED~~ — duplicates cache, graph, storage (see #239) |
| blockchain | Agent economy ledger with wallets, transfers, bounties, and hash-chained blocks (MockLedger in-memory adapter). **24 strict native MCP v2 tools** (13 deterministic, 9 operational, 2 authoring) use same-brick Pydantic v2 ingress and `ToolResult[OutputDTO]` egress. Normal domain-negative outcomes remain typed successful data (`found=false` or a domain `success`/`ok=false` plus error); only unexpected faults fail the envelope. Authoring tools are always discoverable, while economy seeding is behavior-gated by `BLOCKCHAIN_ENABLE_AUTHORING_TOOLS=1`. `blockchain_get_views` returns the declared Economy & Ledger view with transaction, activity, graph-context, and bounty inspectors. |
| browser | Browser automation with pluggable protocol adapters. Its 18 public native MCP v2 tools (5 deterministic, 9 operational, 4 authoring) retain their names and flat kwargs, validate ingress with strict same-brick Pydantic v2 DTOs, and return `ToolResult[OutputDTO]` v1 envelopes. Successful payloads are in `data`; a missing session remains a successful typed result (`found=false`), while unexpected failures use the failed envelope. Resources and prompts retain their native MCP shapes. |
| builder | Internal builder tooling access — read code repos, search packages, inspect pipelines via Midway auth (same pattern as veritas/sipp) |
| cache | Ephemeral caching (memory, Redis) |
| config | Centralized configuration (env, file, SSM, infra-env) with layered auto-bootstrap, `get_infra()` convenience API, and AWS identity probing |
| dataset | Durable asynchronous generation and immutable dataset materialization. Its 16 native MCP v2 tools are 9 deterministic and 7 operational; all retain flat kwargs, enforce strict Dataset-local Pydantic v2 ingress, and return `ToolResult[OutputDTO]` envelopes. Normal domain outcomes remain typed data rather than envelope failures. The catalog covers blueprint and ScenarioPack lifecycle, durable jobs and artifacts, CAN training-bundle materialization and pipeline discovery, approved DBC discovery/resolution, evidence-backed failure-pattern reads, and Graph projection. |
| evals | Agent benchmarking and evaluation with frozen policies and evidence-bound evaluation reports (12 LLMAJ evaluators, experiments, ActorSimulator, serialization). `evals_evaluate_can_model(y_true, y_pred, y_score=None)` (@deterministic) is a thin wrapper over `runtime/adapters/can_evaluator.py::evaluate_can_model` — returns bundled accuracy/precision/recall/f1 always, plus auroc/auprc/brier when `y_score` is given and both classes are present. Called by the machine_learning brick's `train_top_can_ids` after each per-CAN-ID LightGBM fit (bd:python-factory-sbhyq.4); distinct from the separate 6-evaluator `evals_evaluate_computational` (can_auroc/can_auprc/can_brier/can_lead_time/can_false_alarm/can_episode_recall) harness, which remains standalone-callable. Red-team benchmark harness: `BenchmarkRunner` orchestrates start target → health check → invoke swarm → collect findings → score against ground truth (P/R/F1) → persist metrics. 5 benchmark/scoring tools: `evals_run_benchmark` (@operational — run a benchmark against a vuln app target), `evals_list_benchmark_results` (@deterministic — list past runs with P/R/F1 summary), `evals_compare_benchmarks` (@deterministic — side-by-side delta with regression flag), `evals_score_gt` (@deterministic — unified deterministic P/R/F1 scorer with optional `match_on` list param parameterizing the match key tuple; SAST callers pass `["cwe","file"]`, DAST defaults to `("cwe","file","method","path")`, narrow DAST can pass `["cwe","method","path"]`. Path params normalized to `{param}` for fuzzy matching. Replaces the old games `sast_scorer.py` algorithm twin), `evals_persist_score` (@operational — routes P/R/F1 through canonical `evals_record_run` as an immutable `evaluation_score_projection` at `eval-score-{run_id}`; it cannot replace the full `evaluation_run` record at `eval-{run_id}`. Terminal documents carry a schema version, terminal state, and deterministic content hash: equal retries match and divergent writes conflict. Storage commit precedes pointer-only `eval.completed` and Graph projection. `evals_run_tool_chaos` (@operational) uses native Strands ToolSimulator/ChaosExperiment with a developer-owned allowlisted catalog, bounded evidence, and paired baseline/fault artifacts. Red-team and multimodal execution remain blocked pending authorization, sandbox, redaction, retention, audit, and kill-switch infrastructure; accepts `domain_class:str=""` (bd:python-factory-tmlrx) alongside `vuln_class` for per-domain longitudinal queries). `ComposeTargetAdapter` (`benchmark_target`) rotates vulnerable app targets (vampi, idor_warehouse, crapi, juice_shop). |
| events | Event streaming and message bus (memory, SQLite, Redis, EventBridge/SQS). **20 strict native MCP v2 tools** (9 deterministic, 7 operational, 4 authoring) accept flat same-brick Pydantic v2 inputs (`extra="forbid"`, strict) and return `ToolResult[OutputDTO]`; successful data is always in `result.data`. Expected domain-negative outcomes—such as a missing event or history replay target—remain successful typed payloads (`found=false` or `ok=false` with `error`), while unexpected faults fail the envelope. SQLite adapter (`EVENTS_BACKEND=sqlite`) provides lightweight file-based session-scoped persistence using Python's built-in `sqlite3` — WAL mode for concurrent access, truncates on startup. `learning_compute_reward` returns the strict v1 Learning result; the rewards dispatch handler validates it and emits `reward.computed` using authoritative `scalar`, `reward_value`, `wallet_id`, `verdict`, and `scoring`. Learning's `gt-findings` adapter is the only path that delegates to `games_process_workflow_rl`; telemetry, chat, generic, memory, and blockchain fan-out remain separate subscribers. `raw` source data is evidence only. `metrics_record`/`metrics_detect_drift` handle convergence tracking. Event-driven, polymorphic: no per-workflow bespoke logic. `LearningEventPayload` accepts `workflow_id` (canonical) and `graph_id` (deprecated alias kept for back-compat) interchangeably via Pydantic `AliasChoices` + a pre-validator that backfills the missing key from the present one and rejects mismatched values; producers dual-emit during the deprecation window. Carries a canonical `domain_class` field with `validation_alias=AliasChoices("domain_class","vuln_class")` (bd:python-factory-twxj0) so producers that only set `vuln_class` still populate the domain-agnostic key. Emits two new closure-observability learning events: `learning.applied` (read-side closure, fired from memory retrieve via `learning_emit.py`, bd:python-factory-o7t8) and `workflow.improvement` (priority-7 subscription `auto-improvement-on-reward-computed` → handler `learning_handlers/improvement.py`; baselines vs N=10 prior runs from sqlite `eval_results`, ±0.05 verdict thresholds with module-level `VERDICT_*` constants in `learning_contracts.py`, bd:python-factory-kq6u). |
| foreman | Polylith scaffolding, workspace structure management, and governance. Its 14 public native MCP v2 tools are 9 deterministic (`get_capabilities`, `health_check`, `describe_config_schema`, `foreman_info`, `foreman_check`, `foreman_guardian_check`, `foreman_get_repo_guardrails`, `foreman_build_bricks_index`, `foreman_resolve_dependencies`) and 5 operational (`foreman_create_component`, `foreman_create_base`, `foreman_write_bricks_index`, `foreman_create_project`, `foreman_sync_brick_deps`). Every tool preserves flat kwargs, uses strict Foreman-local Pydantic v2 ingress, and returns `ToolResult[OutputDTO]`; consumers inspect `result.ok` and read successful payloads from `result.data`. See [the Foreman README](../../components/foreman/README.md). |
| games | Turn-based game engine with pluggable rules and RL-ready environment. Security game adapters: `sql_injection`, `xss_hunter`, `command_injection`, `ssti`, `idor_detective`, `path_traversal`, `finding_triage`. `games_create` accepts optional `config` dict (ground_truth, max_actions, etc.). `games_security_move` provides structured vuln-hunting actions (analyze_code, trace_dataflow, submit_finding, check_config, test_endpoint, read_file). Post-game pipeline (`game_pipeline.py`) has 4 steps: graph transcript → blockchain bounty → evals scoring → memory learnings (RL feedback loop via `memory_store` with vuln_class tags for next-iteration retrieval). Per-workflow RL loop (`workflow_rl.py`): `games_process_workflow_rl` (@operational) scores findings against GT, mints blockchain rewards proportional to F1, stores learnings in memory. Auto-detects workflow type from graph_id (sast/dast/recon). Takes: graph_id, run_id, vuln_class, workflow_type. Calls the unified evals scorer via MCP (`evals_evals_score_gt` with `match_on=["cwe","file"]` for SAST, default for DAST), then fires `evals_evals_persist_score` to write the P/R/F1 row to the sqlite `eval_results` collection — best-effort, never raises. Longitudinal F1 history ("F1 over the last 10 runs for IDOR on WebGoat") is now a `storage_doc_query` against sqlite, NOT a Cypher query against Neo4j edges (the `_rl_gt_edges.py` writer and `rl.gt.edges.written` lifecycle event were deleted in bd-y9tq because the user no longer runs Neo4j locally). Unified experiment reporting (`workflow_report.py`): `games_write_experiment_report` (@operational) queries graph for findings/exploits, runs RL scoring via `process_workflow_rl`, batch-records metrics, and writes JSON report to `challenges/experiments/`. Takes: run_id, workflow_type, target_app, vuln_class, framework, agent_count, duration_seconds, extra_metrics. Convergence detection: Use `metrics_detect_drift` from metrics brick — the bespoke `games_check_convergence` tool was removed. Substrate-prep — RL pipeline now reads `config["domain_class"]` (was `vuln_class`) with a back-compat fallback chain `domain_class → vuln_class → game_type` (`game_pipeline.py::_store_learnings`, bd:python-factory-qer1z). Memory tags dual-emit `[domain_class, "scan-learnings", f"{domain_class}-learnings", run_id]` — old `scan-learnings` is preserved so the chat-agent `LearningRecallPlugin` filter keeps working for security flows, the new `{domain}-learnings` tag enables per-domain recall (e.g. `wine_pairing-learnings`). `_collect_findings(invoker, run_id, wtype, count_labels=None, taxonomy_edges=None)` accepts a per-call label tuple; `DEFAULT_COUNT_LABELS = ("Finding", "ProvenExploit")` keeps the security default. Same pattern in `workflow_report_graph.py::graph_counts(invoker, run_id, count_labels=None)` and `workflow_report_findings.py::build_findings_detail(invoker, run_id, count_labels=None)` (the latter requires the primary slot stay `"Finding"` because the column shape `_build_finding` reads is keyed to the typed-findings envelope). `domain_class` / `count_labels` / `taxonomy_edges` now also thread through the MCP boundary: `games_process_workflow_rl(graph_id, run_id, vuln_class="", domain_class="", count_labels=None, taxonomy_edges=None, workflow_type="auto", target_app="", session_id="")` and `games_write_experiment_report` accept the same triple per call (bd:python-factory-twxj0 + python-factory-tmlrx + python-factory-st587), so callers driving the RL loop from outside `games_create` (event handlers, custom orchestrators) skip the per-game `config` round trip. Future domain agents drive their own RL loop via `config={"domain_class": "your_domain", "count_labels": [...]}` or direct MCP args with no engine code change. Feature flag: `domain_class_rl_loop` in `BRICK.yaml`. |
| graph | Knowledge graph operations with unified taxonomy schema. Workflow-run lineage tools (`graph_get_run_topology`, `graph_list_recent_runs` — both `@deterministic`) reconstruct the connected sub-graph for a single `WorkflowRun` from telemetry-sink data, ordering invocations by the canonical `ToolInvocation.sequence` property. Powers the Companion-X graph viewer's run filter. Typed run-scoped read suite (all `@deterministic`, replace ad-hoc Cypher): `graph_get_findings_for_run`, `graph_count_entities_by_run`, `graph_get_workflow_summary`, `graph_get_recent_findings`, `graph_get_target_app`, `graph_get_tool_invocations_for_run`. The global `graph_list_recent_tool_invocations` (`@deterministic`, bd python-factory-c39g) returns the most recent `ToolInvocation` rows joined one-hop with the parent `Session` and sorted `created_at` DESC — drives the Companion-X Timeline tab and replaces the prior Cypher escape hatch. Capability flags: `run_scoped_typed_reads`, `recent_tool_invocations`. Both `networkx` and `neo4j` adapters are first-class for these reads. Substrate-prep — typed reads are now domain-agnostic at the join boundary (bd:python-factory-ecph9): `get_findings_for_run` and `get_recent_findings` accept `taxonomy_edges: list[TaxonomyEdgeSpec] | None = None`. `None` = back-compat security CWE/OCSF defaults (`CLASSIFIED_AS → CWECategory`, `CONFORMS_TO → OCSFEventClass`); `[]` = no taxonomy join; custom edges drive caller-supplied row columns from `target_props`. New `TaxonomyEdgeSpec` Pydantic model lives in `components/graph/src/factory/graph/runtime/models.py`; Cypher builders in `runtime/adapters/_neo4j_finding.py` + `_taxonomy.py` keep this module under 200 LOC. MCP tools `graph_get_findings_for_run` and `graph_get_recent_findings` surface this as `taxonomy_edges: list[dict] | None = None` accepting raw dicts; `coerce_edges` validates per call. `graph_get_workflow_summary` (bd:python-factory-le9gu) also accepts both `taxonomy_edges` and `count_labels` per call and forwards them into the underlying `get_findings_for_run` / `count_entities_by_run` invocations so the FE / domain agents can drive the summary panel without bespoke endpoints. Adapter code now carries no domain-specific labels — a wine agent passes `[{"relationship_type":"TASTED_AS","target_label":"Varietal","target_props":{"id":"varietal_id"}}]` and gets the same row shape contract. P3 burndown (bd:python-factory-qm07q, this PR): per-domain taxonomy registry at `runtime/taxonomy_registry.py` (151 LOC) — `register_extension(domain_id, *, node_types, relationship_types, conventions=None)` (append-only, raises `ValueError` on collision), `get_extensions()` (deepcopy snapshot), `resolve_domain_taxonomy(domain_id)` (`"security"` returns `SECURITY_TAXONOMY` verbatim, registered → security base merged with extension, unknown → `None`), `merge_taxonomies(*tax)` (last-write-wins helper), `reset_extensions()` (test-only). Surfaced via new `graph://schemas/taxonomy/{domain}` MCP resource template in `mcp/resources.py` — `{domain}=security` returns the security default, registered domains return base+extension merged, unknown returns 404-shape JSON `{"error": ..., "available_domains": ["security", ...]}`. Module-global dict + `reset_extensions` chosen over `mcp_utils.set_service` per meta-architect verdict `2efd2a40` Q3 — same shape as `mcp_utils.correlation._CANONICAL_KEYS`. Feature flag: `taxonomy_edges_polymorphic_reads` in `BRICK.yaml`. |
| http | HTTP client abstraction |
| integrations | External service connectors |
| kb | Dual-store vector search: local Neo4j HNSW + remote AWS Bedrock KB (GraphRAG). Also supports ChromaDB and BM25 text fallback (rank-bm25 BM25Plus). `kb_search_remote` queries the remote Bedrock KB without touching the local store. |
| llm_gateway | Direct LLM calls with providers |
| learning | Domain-agnostic reward-source seam with two strict flat MCP tools, typed ToolResult v1 egress, native schema/docs resources, four built-in sources, and Games/Evals adapters behind MCP invoker boundaries. |
| logger | Logging and monitoring |
| machine_learning | ML experiment tracking, fine-tuning, and model artifacts. ExperimentTracker and FineTuningPort protocols. Dataset generation is owned by the dataset brick; ML consumes immutable dataset artifact inputs. The attempt-idempotent CAN lifecycle now supports `lightgbm` (backward-compatible default), `lnn`, and `chronos` through `ml_train_can_portfolio(attempt_id, dataset_request, model_family, top_n_can_ids, training_config, model_config, experiment_name)`: it reconciles `dataset_materialize_can_training_bundle` and consumes the completed terminal's exact family refs. LightGBM binds `contract`/`x_2d`/`y`; LNN binds `contract`/`x_3d`/`y`/`timespans`, derives `model_config` from the exact timing ref, and rejects caller timing/backbone/LoRA overrides; Chronos binds `contract`/`x_3d`/`y` plus a caller-supplied sealed local `model_config.local_backbone_ref`. Native jobs use deterministic effect IDs and immutable replay records, so retries reconcile an interruption after record publication but before effect-receipt persistence. Equal attempt retries replay the terminal; divergent reuse returns `conflict`. `ml_issue_can_passports` remains candidate authority; the separate authoring-gated `ml_verify_and_promote_can_passport` publishes or returns exact revision 2, and `ml_predict_neural_passport` cold-loads that passport after restart (with request-scoped exact live timing additionally required for LNN). Feature flags: `polymorphic_native_can_lifecycle`, `crash_reconciled_native_can_effects`. CAN keystone training (`can_keystone_helpers.py::train_top_can_ids`) now best-effort calls the evals brick's `evals_evaluate_can_model` MCP tool (via `get_service("tool_invoker")`) after each per-CAN-ID LightGBM fit, merging the bundled accuracy/precision/recall/f1 (+auroc/auprc/brier when scores present) into `job.metrics` — never raises, logs a warning on failure (bd:python-factory-sbhyq.4). `TimeSeriesTrainingJob` (`runtime/ports.py`) gained additive `val_y_true`/`val_y_pred`/`val_y_score` fields, populated only by the LightGBM `SklearnTimeSeriesAdapter` path (torch/TimeGAN/memory adapters leave them empty). Timeseries trainer default backend is now `sklearn` (REAL training; `ML_TIMESERIES_BACKEND` env override, `memory` = tests only — it fabricates seed-hash metrics) via `runtime/timeseries_factory.py` (bd:python-factory-zgg1x). Training runs persist best-effort to the storage doc collection `ml_training_runs` (doc_id `mlrun-{job_id}`, `runtime/adapters/training_run_store.py`) from `ml_train_timeseries` + `train_top_can_ids`; `training_run_reader.py` computes read-time trend/regression enrichment (mirrors evals doc_store_reader). Dashboard tools: `ml_get_dashboard_summary`, `ml_list_training_runs`, `ml_get_training_run`, `ml_get_run_regression` (`mcp/dashboard_summary.py`). `ml_get_views()[0]` is the brick-declared "ML Models" view (trend chart + experiment health + training runs + fine-tuning jobs); the RL learning-runs projection moved to views[1]. Promoted neural inference uses `ml_predict_neural_passport` → `load_neural_passport_scores` → the literal native loader. LNN/LTC requires a strict request-scoped local `.npy` timing ref (URI + exact digest), finite positive numeric shape `(len(live X), sealed window_size)`, normalized by immutable training-derived `timing_scale`; live digest/shape are execution evidence and are never compared with training timing provenance. Candidate conformance still replays exact prepared training X/timing. LSTM, TCN, and PatchTST omit timing and reject it when supplied. Companion-X and the brick exact-pin `ncps==1.0.1`. MLX-native LSTM/TCN jobs are Darwin-arm64-only and carry the frozen lifecycle identity `mlx/0.31.1/mlx.nn.Module.load_weights/mlx-safetensors/local-mlx-isolated-v1`. Their immutable `${ML_MODEL_PASSPORT_ROOT}/models/mlx/<digest>` tree contains exactly `model.safetensors` and canonical `factory_model.json`; durable inference rejects `ml_predict_timeseries` and requires exact promoted revision-2 `ml_predict_neural_passport` authority after isolated revision-1 conformance. |
| mcp_utils | Shared MCP utilities (mcp_enabled: false). Includes process-level service registry (`set_service`/`get_service`) for cross-brick callbacks without direct imports. Provides async-safe envelope context (`set_envelope`/`get_envelope`/`get_principal_id`) for per-request principal propagation. Exposes the shared `POLL_NOISE` frozenset (`poll_noise.py`, bd python-factory-c39g) — tool names that the SSE poll itself produces (`find_entities`, `graph_find_entities`, `graph_get_stats`, `graph_add_entity`, `graph_add_relationship`) — so any read site can filter them out and avoid a self-amplifying feedback loop. `correlation._CANONICAL_KEYS` includes `domain_class` (bd:python-factory-sm1va) so cross-thread propagation carries the new domain bucket without aliasing onto `vuln_class` (the meta-architect Q2 verdict allows divergent values). |
| memory | Agent memory with 7 backends. Its 16 live native MCP v2 tools use strict same-brick Pydantic v2 ingress and `ToolResult[OutputDTO]` egress; categories remain 12 untagged, 3 operational, and 1 deterministic. Consumers check `result.ok` and read successful payloads from `result.data`. Feature flag: `typed_mcp_boundary`. Neo4j adapter supports HNSW vector search and A-MEM Zettelkasten evolution (RELATED_TO edges, EVOLVED_FROM audit trail). ChromaDB A-MEM adapter with implicit embeddings. Features: graph_memory, memory_evolution, semantic_memory, memory_consolidation. `user_id` is optional on `memory_store`, `memory_retrieve`, `memory_delete_user`, `memory_consolidate`, `memory_evolve` — falls back to envelope context `get_principal_id()` when authenticated. `memory_evolve` backfills A-MEM evolution on memories stored without LLM access (e.g., expired credentials). `runtime/learning_emit.py` hooks `memory_retrieve`: when results contain ≥1 memory tagged `*-learnings`, emits the canonical `learning.applied` event (read-side RL closure, idempotency_key `learning_applied:{run_id}:{agent_id}:{sha256(query)[:16]}`, bd:python-factory-o7t8). P3 burndown wave 1 (bd:python-factory-lin6p): `memory_retrieve` accepts `tags: list[str] | None = None` filter — `None` = no filter (back-compat), `[]` = match nothing (strict empty, distinct from `None`), non-empty = ANY-match against `metadata.tags`. `MemoryQuery.tags` field added to `runtime/models.py`; filter lives at `MemoryRuntime.retrieve` post-filter layer (`_matches_tags` helper) so all 7 adapters inherit it for free without per-adapter changes (strands-expert verdict `46479612`, meta-architect verdict `2efd2a40` Q8). **P3 burndown wave 2 (this PR):** bd:python-factory-26e2a layers Tier-1 native `tags` push-down on `Neo4jMemoryStore.retrieve` + `Neo4jEmbeddingMemoryStore._vector_search` via `AND ANY(t IN m.tags WHERE t IN $tags)`, refactored through the shared `_neo4j_filters.build_filter_clauses` helper. Tier-2 `chromadb` skips push-down (`amem_helpers.flatten_meta` JSON-encodes metadata into one string column — `where={"tags":{"$in":[...]}}` would compare against literal JSON); the runtime post-filter covers it and is retained on Tier-1 as defense-in-depth. bd:python-factory-b2d2o adds a generic `metadata: dict[str, str] | None = None` filter (per-key AND-join) with the **same Tier-1/2 split** plus an **asymmetric default**: `metadata={}` is treated as no constraints (≡ `None`), distinct from `tags=[]`'s strict-empty match-nothing — per meta-architect verdict `f279063c` Q4. Cross-filter `tags ∧ metadata` = intersection (apply order: tags → metadata). **Cypher injection guard at TWO sites** (`MemoryQuery._validate_metadata_keys` field_validator + adapter-side `_neo4j_filters.safe_property_key`), both via `re.fullmatch` on `^[a-zA-Z_][a-zA-Z0-9_]*$`. The `re.match` original was a trailing-newline bypass that QA `8862f537` discovered and the implementer fixed in the same wave (single-line `re.match → re.fullmatch` at both sites). Precedent: `components/graph/.../runtime/adapters/_neo4j_finding.py::safe_label`. **Per-domain RL recall closed end-to-end (bd:python-factory-vs1vu + bd:python-factory-zj026 superseded by bd:python-factory-ziwqs, qa `9c4846a4` then `3c837989` APPROVE):** writer side — `games/runtime/game_pipeline.py::_store_learnings` now passes `metadata={"tags": [...]}` so dual-emitted tags actually land in `MemoryQuery.metadata.tags` where the lin6p filter reads (was a top-level kwarg silently dropped since #550, meta-architect verdict `2efd2a40` Q9). Reader side — `LearningRecallPlugin(agent)` reads `self._agent.agent_id` at invocation (Strands canonical primitive at `strands/agent/agent.py:227`) and forwards `tags=[f"{agent_id}-learnings"]` to `memory_retrieve` when `agent_id` is a real id (not `'default'`/`''`/`None`); the chat adapter binds `agent_id=cfg.id` to the Strands `Agent` ctor — **the load-bearing fix; was MISSING pre-ziwqs, so per-agent recall was structurally broken regardless of the prior `domain_class` field** (Strands' default sentinel `'default'` leaked through). bd:python-factory-ziwqs retracted zj026's `AgentConfig.domain_class` field per strands-expert verdict `68ea2c63` + meta-architect verdict `afdb867a`. Client-side `_has_learnings_tag` filter retained as defense-in-depth. Empty/`'default'` `agent_id` keeps today's broad recall byte-identical. |
| metrics | Portfolio-level metrics, risk scoring, drift detection, and time-series tracking. **27 canonical native MCP v2 tools** (15 deterministic, 7 operational, 5 authoring), 8 resources, 3 prompts. Every tool enforces strict, flat Pydantic v2 ingress and returns a typed `ToolResult[OutputDTO]` envelope; normal domain outcomes (including disabled authoring and partial portfolio-ingestion results) remain successful envelopes with their outcome in `data`. Canonical surface includes `metrics_get_all_snapshots`, `metrics_get_views`, and `metrics_seed_sample_data`. Ports: MetricsStore, MetricsComputer. Features: metric_definitions, time_series_recording, aggregation_snapshots, drift_detection, trend_analysis, portfolio_coverage, risk_scoring, taxonomy, portfolio_ingestion, graph_lifecycle, regression_gating. Regression gating compares current values against stored baselines and returns BLOCK/WARN/PASS signals. Extensible taxonomy (domain/category/source_brick/input_tool) enables cross-brick metric federation. Graph persistence via MCP (graph brick). `interface.py` exports `default_runtime()` — a zero-arg factory returning a fully-wired `MetricsRuntime` with default in-memory adapters and auto-seeded definitions, for cross-brick contract tests that need a real runtime without violating the no-cross-imports rule. |
| notification | Notification delivery |
| oracle | Domain-agnostic verification oracle (bd:python-factory-216ti Contract B, Req 15.2). `VerifierPort` Protocol + registry-overlay dispatch (built-ins-win) with a `GenericFallbackVerifier` that echoes the finding's state unchanged and never raises — zero domain branching, zero domain literals, zero cross-brick imports. `oracle_verify_finding(finding_id, domain="", backend="")` (@operational) fetches the finding from the graph by MCP tool-name indirection (`graph_graph_get_entity`), dispatches to the resolved verifier (generic fallback on miss), and persists the neutral outcome via the graph brick's atomic `graph_graph_set_finding_state` write. Placed in its OWN brick — NOT security (that would be `if domain=="security"` by the back door), NOT graph (graph owns only the atomic write, SRP) — per meta-architect verdict `ee6a3b0e`. Doctrine mechanic #7 home: the vNext blockchain provenance anchor (`LedgerPort.anchor_provenance`) docks here, DEFERRED to vNext (bd:python-factory-v689r). The security pack's `SecurityPentestVerifier` (`components/security/runtime/adapters/oracle_verifier.py`) is **pack DATA** — registered via `factory.oracle.interface.register_verifier("security", ...)` from `security/server.py`, it re-runs pentest by calling `security_pentest_scan` / `spawn_registered_graph` BY NAME with ZERO `import factory.security` (grep+AST canary guards this). `oracle_verify_finding` carries `@op_kind("verify")` defensively — `"verify"` is not yet in the `OP_KINDS` closed set (bd:python-factory-rk4hc), so the local shim no-ops until it lands. Tools documented in `mcp-tools.md`. |
| payments | Payment processing with 14 strict native MCP v2 tools (7 deterministic, 3 operational, 4 authoring). Every tool retains its flat kwargs, validates ingress with Payments-local Pydantic v2 DTOs, and returns `ToolResult[OutputDTO]`. Credentials use secret inputs and are never returned; public output allowlists operational status while excluding customer PII, metadata, raw webhook payloads, upstream errors, and client secrets. `payments_process_webhook` parses and dispatches an unverified Stripe-shaped payload only—it neither verifies a signature nor changes payment status. |
| permissions | Policy-based access control (YAML, Cedar, AWS Verified Permissions) |
| sandbox | Remote execution environment provisioning with a strict same-brick Pydantic v2 boundary: all 30 canonical native MCP v2 tools accept strict flat DTO inputs and return `ToolResult[OutputDTO]` envelopes (11 deterministic, 15 operational, 4 authoring). The four authoring tools remain discoverable but are behavior-gated by authoring mode; the live status is currently disabled. `sandbox.list_stacks` has env-only ingress (`env_id`); `sandbox.describe_stack` and `sandbox.delete_stack` additionally require `stack_name`. Supports cfn_deployment, manifest_provisioning, and profile-based provisioning. Adapters: `mock` (in-memory), `docker` (local containers), `aws_ec2`/`aws_ssm` (EC2+SSM), `localstack` (docker compose with LocalStack + awscli sidecar, `ENFORCE_IAM=1`), `cfn_provisioner` (`CfnProvisioner` — translates `SandboxManifest` to CFN via existing `cfn_defaults` generators, implements `ProvisionerPort` protocol in `ports.py`), `compose_provisioner` (`ComposeProvisioner` — multi-container apps via docker-compose overlay; handles `java_server` and `container` compute types, WireMock stubs for Coral/REST service deps, implements `ProvisionerPort`). LocalStack adapter emulates IAM, Lambda, S3, DynamoDB, SQS, SNS, API Gateway, CloudFormation, STS, SSM. Profile-based provisioning: `sandbox.list_profiles` (@deterministic — returns builtin target profiles: webgoat, dvwa, vampi, juice_shop, idor_warehouse with image, ports, health_check_url); `sandbox.provision` accepts optional `profile` param to resolve a builtin profile and provision a Docker container with the profile's image, ports, and entrypoint (auto-terminates any existing sandbox first). CFN tools: `sandbox_deploy_cfn`, `sandbox_list_stacks` (operational), `sandbox_describe_stack`, `sandbox.translate_cfn`, `sandbox.generate_cfn_from_recon` (deterministic; generates golden-default CFN templates from Veritas `search_resources` output — 13 resource types: DynamoDB, SQS, S3, IamRole, Lambda, SNS, ECSCluster, ApiGateway, ApiGatewayMethod, KMS, Secret, SSMParameter — for LocalStack deployment when no CDK package exists). Manifest provisioning tools: `sandbox.validate_manifest` (deterministic — validates a `SandboxManifest` dict, checks resource type support), `sandbox.plan_provision` (operational — generates a `ProvisionPlan` with ordered steps from manifest without executing), `sandbox.apply_provision` (operational — deploys CFN resources, custom stacks, and init scripts from a plan into a sandbox env via `cfn_ops`). `sandbox.workspace_dir` returns a scoped temp directory (`/tmp/factory-sandbox/{env_id}/artifacts/`) for artifact staging during recon — created on provision, cleaned on terminate. Service mock configuration via `sandbox.apply_service_mocks` (`ServiceMockConfig` for AAA/Odin/CloudAuth/Coral/Turtle mocking — called after CFN deployment). Challenge templates in `projects/companion_x/challenges/localstack-iam-privesc/` (IAM privesc CFN) and `projects/companion_x/challenges/localstack-ssrf-lambda/` (SSRF Lambda CFN) with ground-truth JSON for eval pipelines. Set `SANDBOX_ADAPTER=localstack`. Graph persistence via `SANDBOX_PERSISTENCE=graph` env var (Neo4j via MCP, same pattern as games/metrics/security). |
| security | Polymorphic security operations with graph-backed finding persistence, CWE taxonomy classification, OCSF event schema conformance, Ground Truth ingestion (`security_ingest_gt_entry`) for the IDOR pipeline (KB + graph), deterministic endpoint scanning (`security.scan_endpoints` — regex-based HTTP endpoint discovery across spring_mvc, jax_rs, flask, express, django; returns structured inventory with method, path, file, line, params, framework), deterministic confidence classification (`security.classify_confidence` @deterministic — 5-level structured system: Strong_Safe, Medium_Safe, Weak_Suspicious, Likely_Vulnerable, Confirmed; inputs: agent_consensus, total_agents, has_taint_trace, has_mitigating_control, has_code_evidence, is_sensitive_operation, historical_tp_rate, confidence_score; returns: confidence_level, confidence_score, reason, level_index; no LLM needed), deterministic interprocedural taint tracing (`security.trace_taint` @deterministic — regex-based, no LLM; inputs: source_code, param, taint_sinks (JSON array), taint_sources (JSON array), endpoint, file_path; returns: {endpoint, param, file, hops[], hop_count, sink_reached, auth_gap, auth_checks[]}; SAST Phase 3 calls this deterministically before LLM reasoning), and pentest scanning (5 tools: `security_pentest_scan`, `security_pentest_status`, `security_pentest_results`, `security_pentest_cancel`, `security_pentest_list_jobs`). Pentest supports 6 scan types: port_scan (nmap), vuln_scan (nuclei), web_fuzz (gobuster), sql_inject (sqlmap), http_probe (curl), nuclei_scan. `SandboxPentestAdapter` runs tools inside Docker via sandbox brick MCP. Auto-persists findings to graph with CWE classification. Swarm preset `pentest-assessment` (4 agents: recon-agent → vuln-scanner → exploit-tester → report-writer) in `defaults_pentest.py`. |
| storage | Unified blob, document, SQL, and raw graph abstraction. Its 25 canonical local native MCP v2 tools retain flat names/kwargs, validate with strict same-brick Pydantic v2 DTOs, and return `ToolResult[OutputDTO]`; consumers check `result.ok` and read payloads only from `result.data`. Misses and negative mutations are normal successful payloads (`doc_get.found=false`, `graph_get_node`/`graph_update_node` with `found=false` and `error="not_found"`, and `deleted=false` for blob/document/graph deletes), not envelope failures. `doc_create_or_match` is atomic and immutable: its successful `data.status` is one of `created`, `matched`, `conflict`, or `unsupported`; equal retries match while divergent content conflicts without mutation. Document store supports SQLite (`STORAGE_DOC_BACKEND=sqlite`, stdlib `sqlite3`, persists to `./.storage/docs.db`), TinyDB, MongoDB, Neo4j. |
| telemetry | Observability via OpenTelemetry (OTLP + console + storage persistence). Its 25 canonical native MCP v2 tools (8 deterministic, 10 operational, 6 authoring, plus `telemetry_get_views`) use strict same-brick Pydantic v2 ingress and return `ToolResult[OutputDTO]`. Callers retain flat kwargs, inspect `result.ok`, and consume successful outputs only from `result.data`. Normal domain-negative states—uninitialized runtime/OTel, unknown span, disabled authoring, and authoring validation errors—are successful envelopes with `data.ok=false`; unexpected operational faults are failed envelopes. The typed `telemetry_get_views` response carries declarations in `data.views`; capture outputs remain JSON-safe scalar status/count values rather than raw OTel spans. |
| test | Trusted-local test execution, bounded JUnit comparison, and strict typed MCP contracts. All 14 tools preserve flat kwargs/defaults, use same-brick strict Pydantic v2 DTO ingress, and return concrete `ToolResult[OutputDTO]`; MCP projections redact credentials, paths, and external JUnit identities. Categories: 5 deterministic, 4 operational, 5 authoring. The surface declares 7 resources (including the `test://docs/{doc_name}` template), 3 prompts, and requires `fastmcp>=3.0.0,<4`. |
| ui | Polymorphic UI rendering (324 Heroicons, HTMX/React/JSON/Flet/AG-UI adapters, session bridge for live agent UI, AG-UI SSE streaming). Chat-stream mapper emits canonical `REASONING_MESSAGE_START/CONTENT/END` trio for `ChatStreamEvent.reasoning_text` (bd:python-factory-eyuj) — replaces the legacy `STEP_STARTED("reasoning") + CUSTOM("reasoning_text")` shape. Reasoning blocks bracket correctly across Claude 4 interleaved thinking: a block opens on the first reasoning chunk and closes on the next non-reasoning event (or on `done` / `error`). |
| veritas | Security knowledge graph (Veritas) — Cypher queries against 6B+ node AWS resource graph. 9 tools, 4 resources, 5 prompts. |
| workflow | Durable cross-brick attempts and stopping control (SQLite, DynamoDB/Step Functions). Its 22 native MCP v2 tools (10 deterministic, 8 operational, 4 authoring) use strict same-brick Pydantic v2 ingress and return `ToolResult[OutputDTO]`; callers retain flat kwargs, check `result.ok`, and read successful typed payloads from `result.data`. |

## Bases

| Base | Description |
|------|-------------|
| api | HTTP API entry point (REST + GraphQL) |
| blueprint | System configuration, service discovery, CDK generation & CI/CD pipelines |
| mcp_server | Unified MCP aggregator with 9 progressive-discovery meta-tools (capabilities, health, list_bricks, get_brick_tools, get_brick_resources, get_brick_prompts, call_brick_tool, read_brick_resource, render_brick_prompt). Lazy-loads bricks on demand; eagerly counts tools at startup. Force-loads the telemetry brick early so OTel is initialized before any tool calls. All tool invocations are wrapped with OTel spans (`runtime/instrumentation.py`) — gracefully no-ops when opentelemetry is not installed. Supports AgentCore Runtime wrapping via `BedrockAgentCoreApp` in companion_x. `_parse_tool_name` routes both `brick_tool` and `brick.tool` dot-notation names to the correct brick. `get_brick_tool_names` preserves dot-notation names without mangling. |
| worker | Background task execution (Celery, Dagster, Fargate SQS) |

## Compliance Checklist

Every brick must have:
1. ✅ `BRICK.yaml` with correct metadata
2. ✅ Entry in `BRICKS_INDEX.yaml`
3. ✅ `runtime/ports.py` with Protocol interfaces
4. ✅ Files under 200 LOC
5. ✅ No cross-component imports (use `factory.<other>.interface`)
6. ✅ MCP primitives (tools, resources, prompts) if mcp_enabled

## Verification

```bash
# Full compliance check (import integrity, branch, LOC, BRICKS_INDEX)
foreman_guardian_check

# Tests
uv run pytest
```

## Adapter Support (Memory vs External)

Most bricks support a **memory adapter** for local dev without Docker. Key external dependencies:

| Brick | Memory Adapter | External Adapter | AWS Adapter | Requires |
|-------|----------------|------------------|-------------|----------|
| auth | ✅ mock | Keycloak | ✅ Cognito | Docker (keycloak) / AWS credentials |
| blockchain | ✅ mock_ledger | Neo4j (via graph brick MCP aggregator) | — | Nothing (in-memory default) |
| builder | ✅ mock | Midway (code.amazon.com, pipelines.amazon.com) | — | Midway cookie (~/.midway/cookie) |
| cache | ✅ memory | Redis | ✅ DynamoDB, ElastiCache | Docker (redis) / AWS credentials |
| config | ✅ env/file/infra-env | SSM | ✅ AWS Identity (STS) | AWS credentials |
| evals | ✅ custom | frozen policies and evidence-bound evaluation reports, benchmark_target (ComposeTargetAdapter — vuln app rotation) | — | AWS credentials (Bedrock). Benchmark harness: Docker (compose targets) |
| events | ✅ memory | SQLite (`EVENTS_BACKEND=sqlite`, `EVENTS_SQLITE_PATH`), Redis | ✅ EventBridge, SQS | SQLite: nothing (stdlib). Docker (redis) / AWS credentials |
| graph | ✅ networkx | Neo4j | ✅ Neptune | Docker (neo4j) / AWS credentials |
| kb | ✅ memory | ChromaDB, Neo4j | ✅ Bedrock KB (GraphRAG via Neptune Analytics) | Local or Docker / AWS credentials (Bedrock + Neptune Analytics in ML account). Activate: `kb.backend=bedrock`. KB ID from env `KB_BEDROCK_KB_ID` or SSM `/art/ml/bedrock/gt-kb-id`. |
| llm_gateway | ❌ | Bedrock/OpenAI/Anthropic | — | API keys |
| logger | ✅ console | File/structured | ✅ CloudWatch Logs | AWS credentials |
| machine_learning | ✅ mock | MLflow | ✅ SageMaker | AWS credentials |
| memory | ✅ memory | Neo4j (HNSW embeddings, A-MEM evolution), ChromaDB (A-MEM), Mem0, Zep, Cognee | — | Docker (neo4j) or API |
| metrics | ✅ memory | — | — | Nothing (in-memory default) |
| notification | ✅ mock | SMTP/Twilio/Slack | ✅ SNS, SES | Credentials / AWS credentials |
| oracle | ✅ generic-fallback (in-process) | — | — | Nothing (dispatches to graph via MCP tool-name indirection) |
| payments | ✅ mock | Stripe | ✅ Marketplace Metering | API keys / AWS credentials |
| permissions | ✅ mock | OPA | ✅ Verified Permissions | AWS credentials |
| sandbox | ✅ mock | Docker, LocalStack, CfnProvisioner, ComposeProvisioner, Graph (Neo4j via MCP) | ✅ EC2, SSM | Docker (containers). LocalStack: `SANDBOX_ADAPTER=localstack`, `docker compose` (LocalStack + awscli sidecar). CfnProvisioner: manifest→CFN via `ProvisionerPort`. ComposeProvisioner: multi-container apps (java_server, container) + WireMock stubs via `ProvisionerPort`. Graph: `SANDBOX_PERSISTENCE=graph` env var |
| security | ✅ memory | Graph (Neo4j via MCP) | — | `SECURITY_PERSISTENCE=graph` env var. Pentest: `PENTEST_ADAPTER=sandbox` (default) uses sandbox brick Docker containers |
| storage | ✅ local_fs/memory/networkx/sqlite (doc) | S3/MongoDB/Postgres/Neo4j | ✅ DynamoDB, Aurora Serverless (SQL) | SQLite doc: nothing (stdlib, `./.storage/docs.db`). Docker or AWS for others |
| telemetry | ✅ console / storage | OTLP | ✅ CloudWatch, X-Ray | Collector endpoint / AWS credentials |
| veritas | ✅ memory | Midway (encrypted Cypher) | — (future IAM SigV4) | Midway cookie (`mwinit`) |
| worker | ✅ memory | Celery/Dagster | ✅ Lambda, SQS, Fargate SQS | Docker or AWS |
| workflow | ✅ mock | Temporal | ✅ DynamoDB, Step Functions | AWS credentials |

**AWS adapters** (13 of 13 complete, #336): Each brick gets a single `aws.py` adapter with a `service` parameter selector. Backend classes live in a `backends/` subfolder to stay under 200 LOC. All AWS adapters expose `infrastructure_spec()` returning structured resource requirements.

**CDK generation** (#336 Phase 3): The blueprint base converts `infrastructure_spec()` dicts into deployable CDK L2 Python projects. The pipeline is: normalizer (3 spec shapes → `NormalizedResource`) → resolver (VPC detection, IAM generation → `InfraBlueprint`) → CDK renderer (per-brick stacks, network stack, app.py). Supports 15 AWS services. See `bases/blueprint/runtime/` for implementation.

**CI/CD pipeline generation** (#336 Phase 3): The blueprint base generates GitHub Actions workflows with OIDC auth. Default stages: test → synth → deploy-staging → deploy-prod. No hardcoded AWS credentials — uses `aws-actions/configure-aws-credentials@v4` with role assumption.

**For local dev without Docker**: Use memory/mock adapters. Most recipes can run this way.
