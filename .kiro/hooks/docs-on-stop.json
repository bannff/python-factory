{
  "version": "v1",
  "hooks": [
    {
      "name": "Docs on Stop",
      "description": "After agent execution, checks if changes require documentation updates across traditional docs, agentic docs, and brick metadata.",
      "trigger": "Stop",
      "action": {
        "type": "agent",
        "prompt": "DOCS CHECK — one line only. No files changed → skip silently. MCP tools/interfaces/brick metadata changed → '• doc-writer: update <files> — <what changed>'. Nothing changed → 'Docs clean.'"
      },
      "enabled": false
    }
  ]
}