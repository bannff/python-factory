# Row briefs — pre-read research for upcoming checklist rows

**Written by:** the prep agent (a separate, read-only chat; no code, no tests, no servers).
**Read by:** the builder loop, at the start of any slice on a row that has a brief here.
**Never edit `kirocrew-feature-map.md` or `tasks.md` from the prep chat** — this file is the only hand-off.

Each brief answers, for one row, the questions the builder otherwise spends the first part of a cycle on:

1. **What it does (owner's-eye view)** — 2–4 sentences, plain language. What does a user click, what happens.
2. **Upstream behaviour contract** — from the cited page + handler + endpoints in the read-only clone (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src`): inputs, outputs, states, error/empty cases, anything the UI promises. Cite file paths + line ranges. **Never paste KiroCrew code.**
3. **Companion-X mapping** — which existing brick(s)/MCP tools already cover parts of it (grep `components/*/src/factory/*/mcp/` and `interface.py`); what is genuinely missing; where the UI slot is (route/tab/section under the owner's IA: Agent Capabilities tabs, Settings sections, Terminal dock, Chat).
4. **Simplicity check** — the smallest change that satisfies the owner's words and the upstream contract; flag anything that would need a new store/model/brick (the builder must consult a meta-architect for those).
5. **Acceptance** — what "PRESENT+WORKS" looks like for this row: the MCP round trip to prove, the screenshot to take.
6. **Owner questions** — only if genuinely blocking; otherwise none.

Order = builder priority order. Mark a brief `READY` when complete; the builder consumes READY briefs only.

---

## Settings (priority 2 — in progress by the builder; brief the rows it has NOT reached)

<!-- 97 security is mid-flight in the builder (approval list) — skip it. -->

### Row 98 — `secrets` · status: READY
1. **What it does (owner's-eye view)** — Settings → Secrets lists credential names without revealing values. The owner can add or replace a named integration/custom secret and delete one; agents and integrations consume references rather than receiving a vault browser.
2. **Upstream behaviour contract** — The panel distinguishes managed integration credentials from custom entries, shows an empty state and setup guide, masks every value, and confirms destructive removal (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/SecretsPanel.tsx:70-230`). `GET` returns names/metadata only, `POST` validates and upserts, and `DELETE` removes by name; vault read failures are surfaced rather than rendered empty (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/secrets.py:133-321`).
3. **Companion-X mapping** — Put this in the existing Settings gear as the Secrets section. Storage already has encrypted, owner/tenant/provider/connection-scoped generation- and version-fenced credential slots plus write/read/revoke/rekey MCP, but those tools are deliberately service-only to Auth (`/Users/wdaniero/workplace/python-factory/components/storage/src/factory/storage/mcp/credential_slots.py:25-91`; `/Users/wdaniero/workplace/python-factory/components/storage/src/factory/storage/runtime/adapters/credential_slots_sqlite.py:26-88`). Missing: an owner-facing vault catalog, generic named-secret semantics, masked metadata DTOs, and a UI-safe write/delete boundary that never returns plaintext.
4. **Simplicity check** — Reuse Storage encryption and CAS; add the smallest owner-facing secret-catalog contract and Settings panel. Do not expose `credential_slot_read` to the UI or create another crypto store. Because this adds a new owner-visible model over a service-only security boundary, consult the meta-architect and security reviewer.
5. **Acceptance** — Through authenticated MCP: create `DEMO_TOKEN` → list shows its name/kind but no value → replace it with the expected revision → delete it → list no longer contains it; a second owner cannot see it. Screenshot Settings → Secrets in both populated and empty states, with values masked.
6. **Owner questions** — None; Secrets is already owner-declared core.

### Row 100 — `releases` · status: READY
1. **What it does (owner's-eye view)** — Settings → Releases tells the owner what Companion-X version is running, whether an update exists, which release channel is selected, and what changed. Applying an update must follow Companion-X's own packaging/deployment story, not KiroCrew branding or git-layout assumptions.
2. **Upstream behaviour contract** — The UI separates check failures from apply/download failures, shows channel/capability state, progress, cancellation, release history, and changelog (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/ReleasesPanel.tsx:1-260`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/AboutPanel.tsx:34-120`). The backend returns an install-specific update capability, lists changelog/releases, supports channel/auto-update controls, and refuses unsupported install layouts rather than pretending to update (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/updates.py:419-574`, `:1239-1338`, `:1528-1822`).
3. **Companion-X mapping** — Add a Releases section under the single Settings gear. Metrics can expose status/trends and Config exposes schemas, but no Factory component owns product-release discovery or application (`/Users/wdaniero/workplace/python-factory/components/metrics/src/factory/metrics/mcp/deterministic.py:42-90`; `/Users/wdaniero/workplace/python-factory/components/config/src/factory/config/interface.py:1-20`). The current About panel version/health is only the read-only seed.
4. **Simplicity check** — First slice: truthful running version/build, update capability (`unavailable` is valid), check result, and changelog from Companion-X release metadata. Do not port KiroCrew's git-pull updater. A release channel/update state model is new cross-cutting ownership, so consult the meta-architect before adding persistence or apply behavior.
5. **Acceptance** — MCP returns current version/build, selected channel, capability, and deterministic `up_to_date`/`update_available`/typed-error status; UI renders all without raw exceptions. Screenshot Settings → Releases showing current version and either a real available release or a truthful unsupported/no-update state.
6. **Owner questions** — None; exact distribution mechanics can remain capability-driven while the core read/check experience lands.

### Row 99 — `developer` · status: READY
1. **What it does (owner's-eye view)** — Settings → Developer exposes advanced, explicitly optional controls: developer UI, feature previews, and which already-configured gateway the dashboard is using. It is not a second Agent Capabilities page and must not duplicate model/provider controls.
2. **Upstream behaviour contract** — Developer Mode gates diagnostics-oriented UI; Feature Previews are named client flags with independent opt-ins; gateway selection reports current/local state and errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/DeveloperPanel.tsx:1-230`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/FeaturePreviewsSection.tsx:1-220`). These controls are primarily client/config state, with no dedicated row endpoints.
3. **Companion-X mapping** — Keep the existing Settings → Developer section. Agent already exposes runtime/agent capability registries and Browser exposes runtime sessions, but neither is a developer-preference store (`/Users/wdaniero/workplace/python-factory/components/agent/src/factory/agent/mcp/tools.py:24-110`; `/Users/wdaniero/workplace/python-factory/components/browser/src/factory/browser/mcp/deterministic.py:30-94`). Missing: owner-scoped preview flags and a truthful runtime endpoint display.
4. **Simplicity check** — Add only controls that have real consumers: developer-console visibility, a small allowlisted preview registry, and read-only gateway origin. Do not invent previews or permit arbitrary endpoint injection. Reuse the existing UI preference persistence; consult meta-architect only if gateway switching is made live at runtime.
5. **Acceptance** — Toggle a real preview through MCP, reload, and prove the associated gated UI changes; turn it off and prove reversal. Screenshot the Developer section showing current gateway and the real preview list.
6. **Owner questions** — None.

### Row 101 — `about` · status: READY
1. **What it does (owner's-eye view)** — Settings → About identifies the running Companion-X build and health, then lets the owner produce a redacted diagnostics bundle when troubleshooting. It must never label fabricated values as a build ID.
2. **Upstream behaviour contract** — About shows version/build links and health, while Report Problem collects diagnostics with progress, failure, and downloadable-result states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/AboutPanel.tsx:120-360`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/ReportProblemCard.tsx:1-210`). Collection builds a redacted zip and returns a constrained download URL; download only streams a previously generated diagnostics filename (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/diagnostics.py:1-94`).
3. **Companion-X mapping** — Extend the existing Settings → About section. Brick health/capability counts already come from the UI/progressive MCP catalog, and Metrics has health/snapshot tools (`/Users/wdaniero/workplace/python-factory/components/metrics/src/factory/metrics/mcp/deterministic.py:42-72`). Missing: authoritative build identity and a bounded, redacted diagnostics artifact/download flow.
4. **Simplicity check** — Keep the current version/health cards; add build identity from package/build metadata and generate diagnostics as a normal versioned Artifact or outbox file. No new store is needed if Artifacts owns the bundle metadata; security review is required for log/config collection and redaction.
5. **Acceptance** — MCP returns version/build/health; collect diagnostics returns a downloadable artifact whose archive contains only allowlisted files and no seeded secret; invalid/expired IDs fail safely. Screenshot About before and after successful collection.
6. **Owner questions** — None.

### Row 83 — `overview` · status: READY
1. **What it does (owner's-eye view)** — Settings → Overview is the health landing page: current service state and compact counts, with drill-ins to Memory and Usage. It should summarize, not duplicate the full management panels.
2. **Upstream behaviour contract** — The wrapper routes overview drill-ins while the page renders health/system cards, loading/error states, and links into memory/usage (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/OverviewPanel.tsx:1-150`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/OverviewPage.tsx:1-240`). `/api/status` is the light health snapshot; `/api/system` supplies runtime/process detail (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers_system.py:145-260`).
3. **Companion-X mapping** — Retain the single Settings Overview section and add drill-in cards rather than rail icons. Metrics already provides registry, snapshots, trends, and all-snapshot reads (`/Users/wdaniero/workplace/python-factory/components/metrics/src/factory/metrics/mcp/deterministic.py:42-90`); Workflow also provides a dashboard summary (`/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/views.py:20-72`). Missing is only a composed owner-facing summary and links to rows 43/52.
4. **Simplicity check** — Compose existing health and summary MCP reads in the UI; do not create an Overview brick or duplicate underlying state.
5. **Acceptance** — A single page load performs real MCP reads, displays health and counts, and its Memory/Usage cards navigate to working drill-ins; one failed source renders a scoped error while other cards remain. Screenshot the complete Overview.
6. **Owner questions** — None.

### Row 86 — `display` · status: READY
1. **What it does (owner's-eye view)** — Settings → Display controls visual theme, density, language, and Terminal presentation/default behavior. Changes apply immediately and survive reload.
2. **Upstream behaviour contract** — The panel supports system/light/dark/custom themes, language, density-related presentation, terminal font family/size, default shell, and completion toggle with optimistic/loading/error handling (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/DisplayPanel.tsx:75-210`). Theme CRUD/config is separated from general dashboard config (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/themes.py:9-148`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/core.py:590-650`).
3. **Companion-X mapping** — Extend the existing Settings → Display section and current `ui_get_display_preferences`/`ui_update_display_preferences` contract (`/Users/wdaniero/workplace/python-factory/components/ui/src/factory/ui/mcp/display_preferences.py:1-150`). Theme and terminal font already work; missing density, language, terminal family/default shell, and completion consumers.
4. **Simplicity check** — Extend the current owner-scoped preference DTO/store; only add fields that are immediately consumed by the root layout or Terminal. Do not add a new theme/config brick.
5. **Acceptance** — MCP update changes density/language/terminal settings with revision fencing; reload preserves them and the live dashboard/Terminal visibly consumes each setting. Screenshot Display with non-default values and Terminal showing the selected font behavior.
6. **Owner questions** — None.

### Row 89 — `shortcuts` · status: READY
1. **What it does (owner's-eye view)** — Settings → Shortcuts lists every keyboard command that actually works and lets the owner override supported bindings. Conflicts must be visible before saving.
2. **Upstream behaviour contract** — The panel groups searchable commands, shows platform-correct bindings, edits/reset overrides, and persists them through dashboard config (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/ShortcutsPanel.tsx:1-240`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/files.py:4298-4740`). Empty search and request failures are distinct states.
3. **Companion-X mapping** — Keep Settings → Shortcuts. The current panel already derives a truthful reference from real consumers; UI preferences already persist Chat/Display data (`/Users/wdaniero/workplace/python-factory/components/ui/src/factory/ui/mcp/chat_preferences.py:1-150`; `/Users/wdaniero/workplace/python-factory/components/ui/src/factory/ui/mcp/display_preferences.py:1-150`). Missing: a shared shortcut registry, override DTO, collision validation, and consumers reading overrides.
4. **Simplicity check** — First centralize only the shortcuts currently documented, persist an override map in the existing UI preference record, and have those consumers resolve through it. No new brick/store; consult meta-architect only if the registry crosses packages beyond UI.
5. **Acceptance** — Change one binding through MCP, reload, prove old binding no longer fires and new one does; conflicting/reserved binding is rejected with a typed field error; reset restores default. Screenshot search plus an overridden row.
6. **Owner questions** — None.

### Row 90 — `skills` · status: READY
1. **What it does (owner's-eye view)** — Settings → Skills controls whether installed skills may enter agent context and shows the context-budget impact. Browsing/authoring skills remains in Agent Capabilities → Skills, preserving the owner's one-icon IA.
2. **Upstream behaviour contract** — The Settings panel is a control-plane view over installed skills and budget; the canonical Skills surface handles browse/read/add. Injection control is withheld for always-on or non-owned skills, and budget/loading/error states are explicit (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/SkillsPanel.tsx:1-180`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/SkillsTab.tsx:41-100`). Budget data comes from a dedicated handler (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/skill_budget.py:1-220`).
3. **Companion-X mapping** — Keep authoring in Agent Capabilities → Skills and use Settings only for enablement/budget. Agent already lists, reads, and adds bounded skills (`/Users/wdaniero/workplace/python-factory/components/agent/src/factory/agent/mcp/skills_tools.py:24-110`). Missing: per-owner enabled state, prompt-budget estimation, and launch-time filtering.
4. **Simplicity check** — Add enablement metadata and a deterministic budget estimator beside Agent's existing skill registry; do not duplicate skill files or create a Settings-owned registry. If persistent per-owner enablement changes the Agent skill model, consult the meta-architect.
5. **Acceptance** — Disable one eligible skill via MCP, prove it remains browsable but is absent from assembled context, reload and re-enable it; budget totals update deterministically. Screenshot Settings → Skills with enabled/disabled rows and budget summary.
6. **Owner questions** — None.

### Row 92 — `browser` · status: READY
1. **What it does (owner's-eye view)** — Settings → Browser configures the agent's browser runtime: availability, engine, and attach mode/token status. Browser operation remains in the dashboard Browser panel and requires its per-session Globe authorization.
2. **Upstream behaviour contract** — The panel probes CLI availability, runs background install with polled steps, downloads Chromium/Firefox/WebKit, configures built-in versus attach mode, and never reads a saved token back into the form (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/BrowserPanel.tsx:27-160`). Handlers expose status, install, token set/clear, and engine install with explicit unavailable/progress/error states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/messaging.py:3283-3693`).
3. **Companion-X mapping** — Settings → Browser stays under the one gear. Browser already has launch, navigate, content, screenshot, click, type, evaluate, waits, sessions, and authoring profiles (`/Users/wdaniero/workplace/python-factory/components/browser/src/factory/browser/mcp/operational.py:25-105`; `/Users/wdaniero/workplace/python-factory/components/browser/src/factory/browser/mcp/authoring.py:44-95`). Missing: host runtime probe/install, engine inventory/selection, and attach-token status.
4. **Simplicity check** — Do not duplicate browser action tools. Add a small runtime-management contract around the configured adapter; token values are write-only and should use Secrets/credential storage. Runtime installation and executable policy are security-sensitive, so consult meta-architect/security before adding install.
5. **Acceptance** — MCP status reports adapter/engine/availability/token-present; engine change is reflected in a subsequent real `browser.launch`; token write returns only presence. Screenshot Browser settings and a separately proven Browser-panel session.
6. **Owner questions** — None; Browser is owner-declared core.

### Row 93 — `computer-use` · status: READY
1. **What it does (owner's-eye view)** — Settings → Computer Use enables and scopes native macOS app automation. It explains exactly what is protected: secure fields are inaccessible, capture is suppressed around them, and only explicitly permitted gestures may move the real pointer.
2. **Upstream behaviour contract** — The panel presents enablement, limits/scope, platform/availability, and clear safety copy with save/error states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/ComputerUsePanel.tsx:1-260`). Config reads are browser-authenticated, config writes update the keystone/limits, invoke is machine-only loopback/internal-secret, and frames are a separate live-view ingress (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/computer_use.py:1-30`, `:534-575`, `:812-1005`).
3. **Companion-X mapping** — Add the real control to the existing Settings → Computer Use section. No component MCP/interface currently owns native desktop automation; Browser is not an acceptable substitute (`/Users/wdaniero/workplace/python-factory/components/browser/src/factory/browser/interface.py:1-20`). Missing: a Computer Use brick/host adapter, config contract, invocation boundary, and dashboard live-view integration.
4. **Simplicity check** — This is genuinely a new capability brick with a native macOS adapter and strict no-op/unavailable adapter elsewhere. Keep enablement host-owned and do not expose raw accessibility privileges to arbitrary MCP callers. Meta-architect and security review are mandatory.
5. **Acceptance** — With host capability enabled, MCP lists an app, reads a safe accessibility tree, activates a non-secure control, and refuses secure-field read/write/capture; disabled state refuses invocation. Screenshot Settings config plus a harmless app tree/result, never a password window.
6. **Owner questions** — None; Computer Use is owner-declared core.

### Row 96 — `privacy` · status: READY
1. **What it does (owner's-eye view)** — Settings → Privacy tells the owner what telemetry is collected, whether anonymous beaconing is active, where data stays, and provides a real opt-out where supported. Copy must not promise that conversation or personal content is excluded unless enforcement proves it.
2. **Upstream behaviour contract** — The panel renders collection and beacon status separately, discloses local versus outbound behavior, supports loading/error states, and reflects unavailable controls truthfully (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/settings/PrivacyPanel.tsx:1-240`). Backend status reports local metric collection and anonymous-heartbeat state (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/telemetry.py:1553-1665`).
3. **Companion-X mapping** — Keep Settings → Privacy. Telemetry already has OTel-native exporters, configuration schemas, and summary resources (`/Users/wdaniero/workplace/python-factory/components/telemetry/src/factory/telemetry/mcp/resources.py:22-101`; `/Users/wdaniero/workplace/python-factory/components/telemetry/src/factory/telemetry/runtime/otel_setup.py:15-84`). Missing: owner-facing effective-policy/status and a verified opt-out applied to exporters/beacons.
4. **Simplicity check** — Expose effective configured state first; only show a toggle if it actually disables outbound exporters/beacons. Extend Telemetry rather than creating a Privacy store. Security/privacy review is required for field-level claims.
5. **Acceptance** — MCP status enumerates collection and each outbound destination without credentials; opt-out disables a test exporter/beacon and survives reload; local operational logs remain truthfully disclosed. Screenshot the effective-state panel before/after opt-out.
6. **Owner questions** — None.

### Row 102 — cross-cutting Settings behaviour · status: READY
1. **What it does (owner's-eye view)** — Settings written in the browser survive a changed dashboard origin/port or relocated desktop profile by backing up an explicit safe allowlist to the host. Safety acknowledgements and secrets are intentionally excluded.
2. **Upstream behaviour contract** — The client hydrates only on a profile with no local value, pushes allowlisted changes, tolerates storage/network failures, and never syncs arbitrary localStorage (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/lib/uiPrefs.ts:1-220`). `GET` returns the allowlisted preference map; `PUT` is a merge patch where `null` removes a key (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/ui_prefs.py:1-80`).
3. **Companion-X mapping** — This is infrastructure behind the one Settings gear, not a section or rail row. Chat and Display already share owner-scoped revision-CAS preferences (`/Users/wdaniero/workplace/python-factory/components/ui/src/factory/ui/mcp/chat_preferences.py:1-150`; `/Users/wdaniero/workplace/python-factory/components/ui/src/factory/ui/mcp/display_preferences.py:1-150`). Missing: client hydration/sync and an explicit durable-key allowlist spanning supported settings.
4. **Simplicity check** — Reuse the existing UI preference record and add an allowlisted bulk read/merge contract. Do not mirror all localStorage, tokens, approval acknowledgements, drafts, or transient panel state. Meta-architect review is appropriate if multiple bricks claim keys.
5. **Acceptance** — Save allowlisted preferences on origin A, open clean origin B, hydrate them once, then prove local explicit values win; a disallowed seeded key never leaves the browser. Screenshot is not meaningful for the mechanism, so capture Settings Display before move and the identical restored state after move plus MCP evidence.
6. **Owner questions** — None.

## Task runner and subagents (priority 3 — Terminal PTY + subagents)

### Row 80 — Subagents · status: READY
1. **What it does (owner's-eye view)** — A chat can launch background agent work and show each run's live/terminal state, output, failures, and controls to steer or stop it. The activity view is subordinate to Chat; it is not another rail destination.
2. **Upstream behaviour contract** — ActivityViewer lazily loads redacted output, distinguishes running/done/error/stopped/approval states, retries failed reads, and exposes selection/steering controls (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/chat/ActivityViewer.tsx:60-180`). Spawn validates task/crew/approval mode, status/list redact output, steer only targets running work, and stop-all scopes to the parent chat (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/messaging.py:209-581`, `:746-1055`).
3. **Companion-X mapping** — Use Chat activity viewer plus existing `spawn_background`/`steer_background`; Workflow owns durable attempts (`/Users/wdaniero/workplace/python-factory/components/agent/src/factory/agent/mcp/spawn_background_tools.py:18-64`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/operational.py:35-135`). Missing: per-run cancel in the viewer, terminal-output/detail read, continue/resume UI, and aggregate badge parity.
4. **Simplicity check** — Compose Agent launch/steer with Workflow get/list/cancel/resume; do not create a Subagents brick or duplicate run storage.
5. **Acceptance** — Spawn from chat → observe running → steer once → cancel or complete → reload and read terminal result from the same origin session; unauthorized owner sees nothing. Screenshot Chat activity with at least one running and one completed row.
6. **Owner questions** — None.

### Row 77 — Task Runner · status: READY
1. **What it does (owner's-eye view)** — The owner pastes or uploads a task spec, asks for a plan, reviews it, then starts a durable autonomous run. Runs can pause, resume, cancel, retry, rename, delete, export, or move results into Chat.
2. **Upstream behaviour contract** — ProjectsPage has inline/file inputs, explicit Plan versus Run, cancellable planning, roster/workspace selection, readable loading/errors, and run selection (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ProjectsPage.tsx:42-140`). The handler supports status/start/cancel/pause/delete/rename/retry, plan/execute, YAML export, and chat handoff; inline content or a path is required and private-memory refinement is refused (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/taskrunner.py:79-109`, `:195-352`, `:721-930`).
3. **Companion-X mapping** — Place this under Agent Capabilities (Task Runner tab) and link active runs into Chat/Terminal; do not restore KiroCrew's Apps-group rail row. Workflow already starts/lists/gets/cancels/resumes durable runs and exposes tasks/activity (`/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/operational.py:35-135`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/views.py:25-72`). Agent supplies intelligent planning. Missing: TaskSpec/Plan product model and plan-to-workflow adapter.
4. **Simplicity check** — Implement a thin Task Runner facade over Workflow and Agent; do not create a parallel executor. Because TaskSpec/Plan persistence is a new model, consult the meta-architect.
5. **Acceptance** — Submit inline spec → plan → edit/confirm → start Workflow → observe tasks → cancel/resume or complete → reload history → open result in Chat. Screenshot Task Runner list and active detail.
6. **Owner questions** — None.

### Row 78 — Task detail · status: READY
1. **What it does (owner's-eye view)** — Selecting a Task Runner run opens its plan, current step, gates/approvals, activity, comments, errors, and available lifecycle actions. It must distinguish a failed load from an empty run.
2. **Upstream behaviour contract** — ProjectDetailPage renders status/steps, pending approvals, activity/comments, retry/pause/execute/export/chat actions, and scoped loading/error/empty states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ProjectDetailPage.tsx:1-300`). Project handlers return one project plus activities/comments and enforce not-found/conflict outcomes (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers_project.py:1-420`).
3. **Companion-X mapping** — Drill in from Agent Capabilities → Task Runner. Workflow already exposes run, task list, activity, graph context, cancel/resume/step (`/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/operational.py:51-135`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/views.py:25-49`). Existing chat approval cards handle real interrupts. Missing: Task-facing projection, comments, and action composition.
4. **Simplicity check** — Build a view projection over Workflow records and reuse Artifact-style comments only if their ownership semantics fit; do not duplicate Workflow state. A new cross-domain comments model requires meta-architect review.
5. **Acceptance** — Open a real run and show ordered steps/activity; execute one lifecycle action and observe revision/status change; pending approval uses the existing chat card; bad ID renders not found. Screenshot a populated detail page.
6. **Owner questions** — None.

### Row 79 — Spec refinement · status: READY
1. **What it does (owner's-eye view)** — Before execution, the owner can ask the agent to tighten an ambiguous task spec. The agent may ask bounded questions, then returns a revised spec/plan for explicit review rather than auto-running it.
2. **Upstream behaviour contract** — ProjectsPage preserves refinement state and answers across navigation and clearly separates planning/refining/running (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ProjectsPage.tsx:106-190`). The backend starts background refinement, supports status polling and answers, validates required text, and refuses unsupported private-member refinement (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/taskrunner.py:79-109`, `:1096-1290`).
3. **Companion-X mapping** — Keep it inside Agent Capabilities → Task Runner. Agent can perform intelligent decomposition and Workflow can persist the attempt; no dedicated refinement MCP exists (`/Users/wdaniero/workplace/python-factory/components/agent/src/factory/agent/interface.py:1-70`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/operational.py:35-93`). Missing: typed refinement/question/answer state and UI.
4. **Simplicity check** — Model refinement as a bounded Workflow whose output is a TaskSpec draft; reuse existing chat/question-card/interrupt machinery. Do not create an independent long-running service. Consult meta-architect for the shared TaskSpec model.
5. **Acceptance** — Ambiguous input yields at least one typed question → answer resumes the same Workflow → revised spec appears without execution → owner starts it separately. Screenshot question and final refined preview.
6. **Owner questions** — None.

### Row 81 — Worktrees · status: READY
1. **What it does (owner's-eye view)** — A completed suggestion card can create an isolated git worktree and open a new Companion-X session scoped to it. The original checkout and session remain unchanged.
2. **Upstream behaviour contract** — Chat follow-up carries a standalone prompt and offers create-worktree versus same-session/skip; backend validates repo/base/path, creates the worktree, then returns the new project/session target with readable conflict errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ChatPage.tsx:8780-8885`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/worktree.py:1-360`).
3. **Companion-X mapping** — Surface this on Chat follow-up cards. Session already creates/binds project-scoped sessions (`/Users/wdaniero/workplace/python-factory/components/session/src/factory/session/mcp/operational.py:27-64`); Devtools already confines git/file operations to a validated project root (`/Users/wdaniero/workplace/python-factory/components/devtools/src/factory/devtools/interface.py:1-80`). Missing: one typed worktree-create operation and the follow-up card action.
4. **Simplicity check** — Add the operation to Devtools and compose it with `session_create`/`session_bind_project`; no Worktree brick or store. Treat git mutation as authoring and retain protected-branch/path policy.
5. **Acceptance** — From a real repo follow-up, create worktree → verify returned absolute path is under the configured worktree root → new session is bound there → duplicate/invalid base fails without partial session. Screenshot follow-up card and resulting session/project label.
6. **Owner questions** — None.

## Schedules and loops (priority 4a)

### Row 55 — Schedule · status: READY
1. **What it does (owner's-eye view)** — Agent Capabilities → Schedules lists recurring and one-shot jobs, lets the owner create/pause/resume/run/delete them, and shows execution history. It supports agent turns, deterministic scripts, and bounded commands without adding a rail icon.
2. **Upstream behaviour contract** — SchedulePage provides list/calendar/history views, templates, folders, search, create/edit detail, explicit empty/error states, and job actions (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/SchedulePage.tsx:47-180`, `:560-900`). Backend validates time forms, creates/updates/deletes, toggles, triggers/cancels, and returns paged history/detail (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/cron.py:351-472`, `:560-811`, `:1621-1806`).
3. **Companion-X mapping** — Keep the existing Agent Capabilities → Schedules tab. Scheduler already supports interval/one-shot/cron agent tasks, list/get, revision-fenced pause/resume/remove/trigger, and unique fire records (`/Users/wdaniero/workplace/python-factory/components/scheduler/src/factory/scheduler/mcp/operational.py:16-86`; `/Users/wdaniero/workplace/python-factory/components/scheduler/src/factory/scheduler/mcp/deterministic.py:16-42`). Missing: script/command kinds, create/edit form, and history projection.
4. **Simplicity check** — Finish UI over current lifecycle first. Script/command execution changes Scheduler's model and security boundary; consult meta-architect/security and route durable attempts through Workflow rather than executing inside Scheduler.
5. **Acceptance** — UI creates each supported kind → list/get → trigger exactly once → history links to Workflow attempt → pause/resume/delete with revisions. Screenshot populated schedule list and one detail/history view.
6. **Owner questions** — None.

### Row 61 — Session control · status: READY
1. **What it does (owner's-eye view)** — Trusted app/agent callers can create a session, read it, send or steer a message, and stop active work without opening the Chat UI. The same owner/session rules as direct Chat apply.
2. **Upstream behaviour contract** — The service accepts create/read/send/stop requests, resolves slot identity, queues when immediate steer is unavailable, and returns not-found/conflict outcomes without duplicating sends (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/session_control.py:1-420`). The Chat stop endpoint interrupts only the targeted slot (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/chat_handlers.py:4242-4565`).
3. **Companion-X mapping** — Session already exposes create/get/list, revision-fenced lifecycle, and exactly-once steer mailbox (`/Users/wdaniero/workplace/python-factory/components/session/src/factory/session/mcp/operational.py:27-184`; `/Users/wdaniero/workplace/python-factory/components/session/src/factory/session/mcp/deterministic.py:1-80`). Missing: stopping the active Agent/Workflow attempt and a single facade for external app callers.
4. **Simplicity check** — Compose Session identity/mailbox with Agent/Workflow cancellation; do not add stop state to Session itself. Preserve send IDs and owner scope.
5. **Acceptance** — External caller creates → reads → sends/steers exactly once → stops running attempt → reads terminal state; replayed send ID does not duplicate; cross-owner access is not found. No screenshot is required for API-only behavior; capture Chat reflecting the externally delivered message and stop state.
6. **Owner questions** — None.

### Row 58 — Monitor loops · status: READY
1. **What it does (owner's-eye view)** — From Chat, the owner arms a bounded same-session monitor for an external condition, sees cadence/budget/status, updates it, and stops/restarts/clears it. Each wake returns to the same conversation and halts deliberately on its exit condition or cap.
2. **Upstream behaviour contract** — The popover validates cadence/runtime/turn/token/provider-error limits and provider targets, with separate create/update/stop/restart errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/components/SessionAutomationPopover.tsx:35-120`). Backend stores terminal outcomes and exposes list/by-slot/create/update/stop/clear/restart plus legacy loop/fire routes (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/autonudge.py:469-771`, `:817-1065`).
3. **Companion-X mapping** — Put the control in Chat, not Schedules navigation. Workflow already has durable loop start/get/list/cycle and pause/resume/stop; Scheduler owns cadence (`/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/loop_operational.py:14-53`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/loop_deterministic.py:13-34`). Missing: update/restart/clear, provider-error/token budgets, same-session completion injection, and popover.
4. **Simplicity check** — Extend Workflow loop contracts and compose Scheduler; do not create a Monitor store or let Scheduler own stopping logic.
5. **Acceptance** — Arm monitor from session → observe scheduled cycle → update cadence/budget → inject result into origin → stop and restart → prove cap halts it. Screenshot Chat popover plus active/terminal status.
6. **Owner questions** — None.

### Row 59 — Session ledger · status: READY
1. **What it does (owner's-eye view)** — An agent records a small durable work checkpoint for its own session so compaction/reload does not erase current objective, progress, blockers, and next action. There is no separate page.
2. **Upstream behaviour contract** — The API reads the ledger for the authenticated session and records schema-bounded state; ownership is session-derived, not accepted from arbitrary body fields (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/session_ledger.py:1-300`). Empty ledger is valid; malformed/oversized records are rejected.
3. **Companion-X mapping** — Session owns durable session identity and Workflow owns attempts, but neither exposes a compact agent-authored checkpoint (`/Users/wdaniero/workplace/python-factory/components/session/src/factory/session/interface.py:1-16`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/interface.py:1-10`). Missing: ledger DTO/store/tool and prompt/context injection.
4. **Simplicity check** — Add one revision-fenced JSON checkpoint per Session, bounded and owner/session scoped, then inject its latest value during context assembly. This is a new Session model/store field, so consult the meta-architect.
5. **Acceptance** — Record → read → replace with expected revision → restart → read identical state; oversized, forged-session, and stale writes fail. No standalone screenshot; show the session detail/activity readout or terminal MCP round trip.
6. **Owner questions** — None.

### Row 60 — Work ledger · status: READY
1. **What it does (owner's-eye view)** — A conductor and its spawned workers share a structured checklist: workers report only their assigned item; the conductor reads aggregate status and the acceptance evidence needed to decide completion. No page is required in this milestone.
2. **Upstream behaviour contract** — The handler exposes conductor read/record/brief and worker report operations, binds each caller to its role/item, bounds schemas, and prevents a worker from rewriting siblings (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/work_ledger.py:1-420`). Empty and partial work are first-class states.
3. **Companion-X mapping** — Agent background runs and Workflow tasks already establish conductor/worker/run identities (`/Users/wdaniero/workplace/python-factory/components/agent/src/factory/agent/mcp/spawn_background_tools.py:18-64`; `/Users/wdaniero/workplace/python-factory/components/workflow/src/factory/workflow/mcp/views.py:31-49`). Missing: shared work-item schema, worker-scoped report mutation, and conductor aggregate read.
4. **Simplicity check** — Prefer a Workflow run-scoped work-item projection over a new brick. Because this adds durable shared records and role authorization, consult the meta-architect and security reviewer.
5. **Acceptance** — Conductor creates/reads brief → two workers report their own items → sibling mutation is refused → aggregate shows both outcomes/evidence after restart. No standalone screenshot; show Task detail/activity projection if mounted.
6. **Owner questions** — None.

### Row 56 — Cron secret grants · status: READY
1. **What it does (owner's-eye view)** — A script schedule can request specific vault secrets; the owner approves, denies, or revokes each grant from that job's Secrets panel. The schedule receives only approved names at execution, never a vault listing or plaintext preview.
2. **Upstream behaviour contract** — Job detail shows pending/approved/denied grants and explicit revoke confirmation. Grant writes are owner-only, audited, tied to the exact job/script identity, and reject stale or unknown requests (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/SchedulePage.tsx:900-1160`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/cron.py:1341-1525`).
3. **Companion-X mapping** — UI belongs under Agent Capabilities → Schedules detail. Storage already holds encrypted credential slots but access is service-only to Auth (`/Users/wdaniero/workplace/python-factory/components/storage/src/factory/storage/mcp/credential_slots.py:25-91`); Scheduler has owner-scoped job identity (`/Users/wdaniero/workplace/python-factory/components/scheduler/src/factory/scheduler/mcp/operational.py:16-86`). Missing: secret-request/grant records and execution-time projection.
4. **Simplicity check** — Store only secret references/grant decisions on Scheduler; resolve approved values through a privileged broker immediately before Workflow execution. Never copy secrets into Scheduler records, prompts, history, or Terminal. New grant model requires meta-architect/security review.
5. **Acceptance** — Script requests one secret → run is blocked/omits it before approval → approve → next run receives it through environment without logging → revoke → later run lacks it; cross-job grant reuse fails. Screenshot job Secrets panel with masked names/statuses.
6. **Owner questions** — None.

### Row 57 — Template update signal · status: READY
1. **What it does (owner's-eye view)** — A schedule created from a template remembers that template and prompt snapshot. If the template later changes, job detail shows a dismissible notice so the owner can decide whether to update the job.
2. **Upstream behaviour contract** — Template comparison is deterministic and the notice names the source template without silently changing the job (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/utils/schedulePresets.tsx:1-220`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/SchedulePage.tsx:1160-1260`). Create/list carry `source_preset` and `source_template_prompt` metadata (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/cron.py:560-720`, `:2559-2680`).
3. **Companion-X mapping** — Render in Agent Capabilities → Schedules detail. Scheduler records already persist task and scheduling metadata, but not template provenance (`/Users/wdaniero/workplace/python-factory/components/scheduler/src/factory/scheduler/runtime/lifecycle.py:13-52`). Notification can optionally signal changes, but should not own comparison.
4. **Simplicity check** — Add optional template ID/version or digest plus creation snapshot to Schedule; compare against the live template registry at read time. This changes the Schedule model, so consult meta-architect; no separate store.
5. **Acceptance** — Create from template → no notice → mutate template version/prompt → notice appears → dismiss without modifying job → explicit update changes task and clears notice. Screenshot changed-template notice.
6. **Owner questions** — None.

### Row 62 — Agent chat egress · status: READY
1. **What it does (owner's-eye view)** — An agent can post, edit, and delete its own messages in an allowed external chat/DM while preserving redaction and destination policy. This is an agent tool, not a dashboard page.
2. **Upstream behaviour contract** — POST/EDIT authorize tracked channel or owner DM and redact display text/blocks; DELETE validates channel/message but upstream documents a weaker target boundary (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/messaging.py:2031-2130`, `:2921-2995`). Errors distinguish missing target, unauthorized destination, provider failure, and unsupported channel.
3. **Companion-X mapping** — Notification already publishes owner-scoped in-app/delivery messages, but has no edit/delete of prior external messages (`/Users/wdaniero/workplace/python-factory/components/notification/src/factory/notification/mcp/operational.py:1-180`; `/Users/wdaniero/workplace/python-factory/components/notification/src/factory/notification/interface.py:1-40`). Missing: provider message receipt identity and authorized edit/delete tools.
4. **Simplicity check** — Extend Notification provider ports with send/update/delete receipts and enforce the same destination authorization on all three, improving on upstream's delete gap. New provider capability/receipt model requires meta-architect/security review.
5. **Acceptance** — Send to allowlisted test channel → edit same bot-authored receipt → delete it; foreign target/message and redaction-canary attempts are refused or sanitized. No screenshot needed beyond provider/test-channel evidence and MCP receipts.
6. **Owner questions** — None.

## Artifacts (priority 4b)

### Row 63 — Artifact library · status: READY
1. **What it does (owner's-eye view)** — Agent Capabilities → Artifacts is the saved library for widgets, HTML, markdown, images, and webapps, with search, folders, sorting, create/import, move, pin, and delete. Artifacts do not regain a rail icon.
2. **Upstream behaviour contract** — The page distinguishes loading/error/empty/filtered-empty, supports gallery/table/tree views, validates imports, and provides folder lifecycle/move actions (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ArtifactsPage.tsx:35-180`, `:600-980`). Backend lists/creates artifacts and lists/creates/updates/deletes folders with conflict/not-found behavior (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/artifacts.py:1078-1370`, `:3035-3415`).
3. **Companion-X mapping** — Existing Agent Capabilities → Artifacts already uses the Artifacts brick. Typed MCP covers get/list/versions/folders, save/update/revert/tombstone, and folder create/rename/move/delete/artifact move (`/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/deterministic.py:16-60`; `/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/organization.py:16-75`). Missing parity is UI breadth, import/pin/filter polish, not a new store.
4. **Simplicity check** — Finish the existing tab against existing MCP; add only missing pin/import contracts to Artifacts. Do not create a Library brick or duplicate Storage.
5. **Acceptance** — Save two artifacts → create nested folder → move/list/filter → rename folder → safe-delete folder and verify reparenting → tombstone artifact. Screenshot populated library in tree or gallery view.
6. **Owner questions** — None.

### Row 64 — Artifact detail · status: READY
1. **What it does (owner's-eye view)** — Selecting an artifact opens its content, metadata editor, version history, activity, folder/tags, and companion chat for iterative changes. Restoring a prior version creates a new current version rather than deleting history.
2. **Upstream behaviour contract** — Detail handles native/iframe/image rendering, editable kinds, dirty-state guards, version picker/revert, activity, folder move, errors, and a bound companion chat (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ArtifactDetailPage.tsx:53-220`, `:295-520`). Backend gets/patches, lists/reads versions, and lists events with not-found/conflict validation (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/artifacts.py:1564-1715`, `:2180-2245`).
3. **Companion-X mapping** — Keep detail under Agent Capabilities → Artifacts. Existing MCP supplies get/update/versions/revert and immutable event emission (`/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/operational.py:24-78`; `/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/deterministic.py:20-43`). Missing: companion-session binding and complete activity projection.
4. **Simplicity check** — Compose Artifact detail with Session using artifact slug metadata; do not store chat transcript in Artifacts. Activity should consume artifact events already emitted. No new brick/store.
5. **Acceptance** — Edit with expected revision → version increments → restore old version → new live snapshot appears while all versions remain → open/reuse one companion session. Screenshot content plus version/activity sidebar and companion chat.
6. **Owner questions** — None.

### Row 65 — Artifact comments · status: READY
1. **What it does (owner's-eye view)** — The owner or agent can comment on an artifact, reply in one-level threads, mark agent work for review, resolve, and delete. Selecting content anchors a comment to that exact excerpt/location.
2. **Upstream behaviour contract** — Detail captures iframe/native selections, opens a comment popover, highlights anchors, and keeps sidebar/thread state synchronized (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ArtifactDetailPage.tsx:8-50`, `:520-850`). Backend lists local/remote comments, posts/replies, edits/statuses/deletes, bounds text, and reports remote-sync degradation separately (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/artifacts.py:3683-3865`).
3. **Companion-X mapping** — Existing Artifact detail comments already use typed post/reply/mark-review/resolve/delete operations (`/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/comments.py:17-65`; `/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/authoring.py:25-72`). Missing: anchor DTO/persistence and selection bridge.
4. **Simplicity check** — Extend the existing comment model with optional bounded anchor metadata; keep unanchored comments unchanged. This changes a durable model, so consult meta-architect; no separate annotations brick.
5. **Acceptance** — Select text → post anchored root → reload highlight → reply → mark review/resolve → delete; stale/missing anchor degrades to thread without hiding comment. Screenshot selected anchor and open thread.
6. **Owner questions** — None.

### Row 67 — Publishing · status: READY
1. **What it does (owner's-eye view)** — From Artifact detail, the owner chooses a configured publication provider, publishes/unpublishes, adjusts sharing, refreshes remote state, and rechecks warnings. The UI always shows provider and sync status.
2. **Upstream behaviour contract** — PublishHub loads provider capabilities, handles destination/options, confirm/progress/error, and recorded notices; detail exposes refresh/unpublish/sharing (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/components/PublishHub.tsx:1-320`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ArtifactDetailPage.tsx:850-1110`). Backend validates provider names, bounds remote calls, records publication/sharing state, and distinguishes unavailable/conflict/not-published errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/artifacts.py:108-122`, `:2389-2665`).
3. **Companion-X mapping** — Publishing belongs in Agent Capabilities → Artifacts detail. Artifacts currently has local lifecycle/events only (`/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/interface.py:1-6`; `/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/operational.py:24-86`). Missing: provider port/registry, publication receipt/state, sharing, refresh/reprobe.
4. **Simplicity check** — Add a provider-neutral port to Artifacts and one local/mock adapter before real providers. Publication state is a new durable model and external side effect, so meta-architect/security review is required.
5. **Acceptance** — List providers → publish to test adapter → artifact records receipt/public URL → refresh/reprobe → update sharing → unpublish; replay/conflict and provider timeout are typed. Screenshot PublishHub and publication banner.
6. **Owner questions** — None.

### Row 66 — Remote artifacts · status: READY
1. **What it does (owner's-eye view)** — The owner browses provider-hosted documents and opens one in place to read and comment without first copying it into the local library. Provider identity and read/write capability are always visible.
2. **Upstream behaviour contract** — Remote detail loads provider/external ID, renders remote content/comments, handles unavailable/not-found/permission/sync failures, and supports provider-backed comments where allowed (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/RemoteArtifactDetailPage.tsx:1-300`). Artifact handlers browse/read remote items and reconcile remote comments without treating provider failure as an empty local result (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/artifacts.py:2683-2895`, `:3683-3807`).
3. **Companion-X mapping** — Reach from Agent Capabilities → Artifacts as a remote source, not a rail route. Local Artifacts lifecycle and comments exist, but no remote provider adapter/identity (`/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/deterministic.py:16-60`; `/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/comments.py:17-65`).
4. **Simplicity check** — Reuse the publishing provider registry if its capability model supports browse/read/comments; otherwise define one shared ArtifactProvider port, not parallel publish/remote abstractions. New adapter and mirrored-comment identity require meta-architect/security review.
5. **Acceptance** — Browse test provider → open remote item → list/post comment → refresh without duplicate mirror → permission failure remains an error. Screenshot remote detail with provider badge and comments.
6. **Owner questions** — None.

### Row 68 — Deploy · status: READY
1. **What it does (owner's-eye view)** — The owner deploys a webapp artifact to a public HTTPS URL in their own AWS account, selecting a saved non-secret profile and seeing preview/scan/progress/result. Credentials remain in the owner's AWS configuration, not Companion-X.
2. **Upstream behaviour contract** — Deploy page manages config/profiles, selects an artifact/profile, performs readiness checks, distinguishes preview/errors/running/success, and links the final URL (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/ArtifactDeployPage.tsx:1-360`). Backend gets/updates config, manages profiles, validates artifact/kind/profile, and starts deployment with typed failures (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/deploy/handlers.py:1-420`).
3. **Companion-X mapping** — Link from Agent Capabilities → Artifacts detail/library. Artifacts supplies content/version identity, but no deploy component exists (`/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/mcp/deterministic.py:20-43`; `/Users/wdaniero/workplace/python-factory/components/artifacts/src/factory/artifacts/interface.py:1-6`). Storage credential slots must not ingest AWS credentials.
4. **Simplicity check** — Create a deployment capability with provider port and AWS adapter, while Workflow owns durable deployment attempts. Store profile name/region/site config only; invoke SDK/CLI using the owner's configured AWS profile. New brick/model and infrastructure side effects require meta-architect/security review.
5. **Acceptance** — Preview valid webapp/profile → scan passes → start Workflow → status reaches public URL → artifact version/profile are recorded; missing AWS login yields actionable refusal and no resources. Screenshot preview and successful result (or isolated adapter proof before live AWS).
6. **Owner questions** — None; live AWS confirmation remains an execution-time approval, not a design blocker.

## Memory, lessons and usage (priority 4c)

### Row 43 — Memory browser · status: READY
1. **What it does (owner's-eye view)** — Settings → Overview → Memory shows paged facts, directives, and episodes with search/type filters, single correction/forget, and bulk preview/apply. The owner can inspect what the assistant remembers instead of editing opaque files.
2. **Upstream behaviour contract** — MemoryTab composes store/status/tools; MemoryRecordsEditor supports server paging, selection across pages, structured correction, replace/forget preview, conflict proposals, loading/error/empty states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryTab.tsx:1-190`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryRecordsEditor.tsx:18-180`). Handlers list revisioned records and perform preview/apply with stale detection (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory_edit.py:1-520`).
3. **Companion-X mapping** — Use a Memory drill-in under Settings Overview. Memory MCP already gets/lists/stats, retrieves with filters, stores, and deletes (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/mcp/deterministic.py:21-60`; `/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/mcp/operational.py:34-112`). Missing: stable record revisions, paging, update, cross-page selection, and bulk preview/apply.
4. **Simplicity check** — Extend Memory's existing ports/models; do not create a browser-specific store or consolidate adapters. Durable revisions/bulk plans alter the model, so consult meta-architect.
5. **Acceptance** — Page/search/filter → edit one with revision → preview bulk replace/forget → apply exact preview → stale mutation conflicts → reload reflects changes. Screenshot populated browser and bulk preview.
6. **Owner questions** — None.

### Row 44 — Store picker · status: READY
1. **What it does (owner's-eye view)** — The Memory page lets the owner switch between Global Memory and each crew/member's private store, clearly showing who owns the selected store and its counts. Changing stores refreshes every child panel together.
2. **Upstream behaviour contract** — Store listing is owner-gated, has independent loading/retry/error handling, and store query keys consistently scope records, backups, carve, retired data, and member panels (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryStoreCard.tsx:16-90`). Backend enumerates only visible stores with type/owner/count/status (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory_admin.py:1-260`).
3. **Companion-X mapping** — Place inside the Memory drill-in. Memory already derives partitioned user IDs and validates memory scope/owner (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/interface.py:1-17`). Missing: typed `memory_list_stores` metadata/count operation and shared selected-store routing.
4. **Simplicity check** — Enumerate partitions through Memory's existing configured store registry; do not create a Store Picker brick or infer private stores by scanning raw backend data.
5. **Acceptance** — MCP lists global plus two owner-visible private stores; switching changes record/stats queries; forged/private store is not found. Screenshot picker expanded and one private store selected.
6. **Owner questions** — None.

### Row 46 — Explore memory · status: READY
1. **What it does (owner's-eye view)** — Explore groups a selected memory store by a facet such as source, type, project, or time and lets the owner drill into one group. It is a navigational summary, not a second graph product.
2. **Upstream behaviour contract** — MemoryCarveCard selects facet, loads bounded counts, drills into matching rows, and distinguishes unsupported facet/empty/error (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryCarveCard.tsx:1-260`). Backend returns count or paged rows for an allowlisted facet and selected store (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory.py:857-920`).
3. **Companion-X mapping** — Add a card inside Settings Overview → Memory. Memory list/retrieve provide raw records but no facet aggregation (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/mcp/deterministic.py:44-60`; `/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/mcp/operational.py:77-87`).
4. **Simplicity check** — Add one deterministic `memory_group_by_facet` over the selected adapter with an allowlisted enum and bounded results. No new store/model; every existing adapter must either implement or truthfully report unsupported.
5. **Acceptance** — Group same store by two facets → counts equal filtered record totals → drill-in opens browser filter → unsupported facet is rejected. Screenshot grouped card and drill-in.
6. **Owner questions** — None.

### Row 49 — Episodic search · status: READY
1. **What it does (owner's-eye view)** — The owner searches past episodic memories, filters by tags, pages results, opens detail, and can forget one. Search failure is not presented as no matches.
2. **Upstream behaviour contract** — Memory UI integrates episodic search/list/delete with explicit query, pagination, tags, loading/empty/error, and selected-store scope (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryTab.tsx:190-430`). Backend supports query/tag search, paged list, and delete (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory.py:1728-1812`).
3. **Companion-X mapping** — Reuse Settings Overview → Memory browser. `memory_retrieve` already searches by query/type/category/tags/metadata and `memory_delete` removes by ID (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/mcp/operational.py:77-96`). Missing: cursor paging and explicit episodic projection/detail.
4. **Simplicity check** — Extend retrieval with cursor/sort and pin `memory_type=episodic`; do not create an Episodic brick or separate index.
5. **Acceptance** — Seed episodes → query/tag returns ranked bounded results → next page has no duplicates → delete one → it disappears after reload; backend error renders retry. Screenshot search results and filters.
6. **Owner questions** — None.

### Row 51 — Memory graph · status: READY
1. **What it does (owner's-eye view)** — Settings → Developer → Memory shows a read-only entity/relation projection over the selected memory store with observability counts and drill-in. It does not replace or consolidate Memory and KB.
2. **Upstream behaviour contract** — MemoryGraphTab loads nodes/edges and observability, supports selection/filter/empty/error, and treats malformed/missing graph data as unavailable rather than success (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryGraphTab.tsx:1-320`). Backend returns bounded graph and observability views (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory.py:2036-2075`, `:2282-2360`).
3. **Companion-X mapping** — Put this in Settings → Developer, consistent with owner IA. Graph already has entity/neighborhood/path/stats and dashboard view MCP (`/Users/wdaniero/workplace/python-factory/components/graph/src/factory/graph/mcp/deterministic_core.py:38-108`; `/Users/wdaniero/workplace/python-factory/components/graph/src/factory/graph/mcp/views.py:16-38`). Memory remains source of truth; graph is a projection.
4. **Simplicity check** — Build a read-only Memory→Graph projection using existing M5.5 rails; no new graph store or consolidation.
5. **Acceptance** — Add related memories → projection yields stable nodes/edges → select node opens corresponding memory → empty store renders empty graph → foreign store refused. Screenshot populated graph with selected node.
6. **Owner questions** — None.

### Row 52 — Usage · status: READY
1. **What it does (owner's-eye view)** — Settings → Overview → Usage shows token, turn, duration, model/provider, and cost-equivalent trends over time, with session/turn drill-down. Unknown cost stays unknown rather than zero.
2. **Upstream behaviour contract** — UsageTab supports range selection, aggregate cards/charts, session/turn rows, loading/partial/error/empty states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/UsageTab.tsx:1-360`). Backend exposes aggregate usage, provider usage, and turn detail with bounded windows (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/usage.py:1-420`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/telemetry.py:1268-1340`).
3. **Companion-X mapping** — Drill in from Settings Overview. Metrics already provides registry/snapshot/trend/taxonomy and measurement tools; Telemetry stores OTel data (`/Users/wdaniero/workplace/python-factory/components/metrics/src/factory/metrics/mcp/deterministic.py:42-90`; `/Users/wdaniero/workplace/python-factory/components/telemetry/src/factory/telemetry/mcp/resources.py:72-96`). Missing: normalized agent-turn usage projection and owner/session filters.
4. **Simplicity check** — Project existing Agent/LLM telemetry into Metrics; do not add a Usage store. Preserve provider-native units and nullable prices.
5. **Acceptance** — Run two model turns → usage MCP returns accurate per-turn and aggregate counts → range/model/session filters reconcile → unknown price remains null. Screenshot Usage summary and turn table.
6. **Owner questions** — None.

### Row 48 — Backups · status: READY
1. **What it does (owner's-eye view)** — The owner snapshots the selected memory store, lists backups, stages a restore, cancels it, and sees that final application occurs safely at restart when required. Existing live memory is never silently overwritten.
2. **Upstream behaviour contract** — MemoryBackupsCard lists/creates backups and presents stage/cancel/restart states with confirmations and errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryBackupsCard.tsx:1-300`). Backend scopes backups to a store, validates artifact identity, and persists staged restore/cancel state (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory_admin.py:260-620`).
3. **Companion-X mapping** — Add to Settings Overview → Memory. Storage has blob/document/SQL/graph adapters but Memory exposes no snapshot manifest operation (`/Users/wdaniero/workplace/python-factory/components/storage/src/factory/storage/interface.py:1-28`; `/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/interface.py:1-17`).
4. **Simplicity check** — Define a Memory backup manifest and delegate bytes to Storage blob; restore through a staged, digest-verified Workflow. New backup/staged-restore model requires meta-architect/security review.
5. **Acceptance** — Snapshot → mutate store → stage restore → cancel leaves mutation → restage/apply → original digest/content returns; foreign/tampered backup refused. Screenshot backups list and staged restore banner.
6. **Owner questions** — None.

### Row 50 — Embeddings · status: READY
1. **What it does (owner's-eye view)** — The Memory page shows whether vector recall is available, which embedding model/dimension is active, setup/reindex progress, and lets the owner choose a supported model. It explains failures without disabling basic memory.
2. **Upstream behaviour contract** — VectorMemoryCard and model card render model availability, legacy mismatch, setup/re-embed progress, retry, search, and typed error states (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/VectorMemoryCard.tsx:29-120`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/EmbeddingModelCard.tsx:11-70`). Backend validates/applies model, reports status/progress, and starts setup/reconciliation (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory.py:1213-1510`).
3. **Companion-X mapping** — Add a card under Settings Overview → Memory. Memory already ships multiple adapters, including an embedding-enhanced Neo4j adapter with vector index and failure events (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/runtime/adapters/neo4j_embedding.py:15-69`). Missing: adapter-neutral status/model catalog, enable/reindex Workflow, and UI.
4. **Simplicity check** — Preserve adapter breadth; expose capabilities/status through Memory and run reindex as Workflow. Do not force Neo4j or download a heavy local model by default. Model configuration/reindex state requires meta-architect review.
5. **Acceptance** — Status → choose supported test model → Workflow reports progress → vector retrieval works → unavailable adapter truthfully reports unsupported while lexical/basic memory remains healthy. Screenshot status/progress card.
6. **Owner questions** — None.

### Row 45 — Member memory V2 · status: READY
1. **What it does (owner's-eye view)** — Each crew/member has a private memory store the owner can browse, correct, bulk-edit, inspect recall for, back up, and explicitly seed from selected starting knowledge. Nothing crosses into another member implicitly.
2. **Upstream behaviour contract** — MemberMemoryPanel exposes records/recovery/explore, immutable binding, recall evidence, explicit bounded copy, and per-store loading/error state; MemoryDocCard shows content/details without leaking internal keys into the main card (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemberMemoryPanel.tsx:24-180`; `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryDocCard.tsx:1-220`). Backend applies store ownership, recall and seed limits, partial outcomes, and no-overwrite provenance (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory_member.py:1-520`).
3. **Companion-X mapping** — Reach through the Store picker in Settings Overview → Memory; crew binding stays Agent-owned. Memory already derives/validates private partition IDs (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/interface.py:3-17`) and Crews already carry memory-store binding (`/Users/wdaniero/workplace/python-factory/components/agent/src/factory/agent/mcp/crew_tools.py:75-170`). Missing: store enumeration, private record editing, recall evidence, and explicit seed.
4. **Simplicity check** — Extend Memory partitions and reuse row 43 editor/backups/explore. Never build a separate Member Memory brick or collapse stores. Seed/recall provenance changes durable models, so consult meta-architect/security.
5. **Acceptance** — Create two members → seed selected items into one → browse/edit/recall it → second member/global store unchanged → restart preserves isolation/provenance. Screenshot picker and private member panel.
6. **Owner questions** — None.

### Row 47 — Replaced experiences · status: READY
1. **What it does (owner's-eye view)** — The owner can inspect experiences retired from recall because a newer experience replaced them, then restore one if retirement was wrong. Restored content re-enters recall with provenance.
2. **Upstream behaviour contract** — MemoryRetiredCard pages retired records, shows replacement/reason/time, restores with confirmation, and distinguishes empty/error/loading (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/MemoryRetiredCard.tsx:1-280`). Backend lists by selected store and restores with conflict/not-found checks (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/memory_admin.py:620-900`).
3. **Companion-X mapping** — Put under Recovery in Settings Overview → Memory. `memory_evolve` already performs experience evolution, but Memory has no retired-record read/restore contract (`/Users/wdaniero/workplace/python-factory/components/memory/src/factory/memory/mcp/operational.py:100-114`).
4. **Simplicity check** — Expose the evolution engine's retirement lineage through Memory and add revision-fenced restore; do not keep a second archive. Durable lineage/restore semantics require meta-architect review if not already represented by adapters.
5. **Acceptance** — Evolve replacement → old experience absent from recall and present in retired list → restore → it is recallable and lineage remains auditable; stale restore conflicts. Screenshot retired list and restore result.
6. **Owner questions** — None.

### Row 53 — WakaTime coding activity and export · status: READY
1. **What it does (owner's-eye view)** — Settings → Overview → Coding Activity shows WakaTime stats for a selected range and exports project-grouped hours as CSV or JSON for reporting/invoicing. Missing credentials/config produces setup guidance, not zero hours.
2. **Upstream behaviour contract** — WakaTimeTab selects ranges, shows summary/project breakdown, export format/date range, loading/error/empty, and browser download (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/WakaTimeTab.tsx:1-320`). Handler validates dates/ranges/formats, calls the configured service, and returns stats or downloadable CSV/JSON with typed auth/provider errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/wakatime.py:1-420`).
3. **Companion-X mapping** — Add a drill-in under Settings Overview; no new rail row. Integrations has provider patterns but no WakaTime MCP; Metrics can render resulting trends (`/Users/wdaniero/workplace/python-factory/components/integrations/src/factory/integrations/interface.py:1-20`; `/Users/wdaniero/workplace/python-factory/components/metrics/src/factory/metrics/mcp/deterministic.py:54-90`). Missing: WakaTime adapter, secret reference, stats/export contracts.
4. **Simplicity check** — Add a WakaTime adapter to Integrations and project into Metrics; generate CSV/JSON on demand, not in a new store. Use Secrets references and never log API keys. New provider adapter requires meta-architect/security review; do not make this paid dependency a prerequisite for core usage.
5. **Acceptance** — With mocked/free account adapter: fetch range → reconcile project totals → export CSV and JSON with identical hours → invalid range/auth is explicit and secrets absent from output/logs. Screenshot populated Coding Activity plus export controls.
6. **Owner questions** — None; lack of a configured WakaTime account is a runtime setup state, not a design blocker.

### Row 54 — Portability · status: READY
1. **What it does (owner's-eye view)** — Settings → Import & Export previews and exports a complete, versioned Companion-X memory/config bundle, then previews/imports one with explicit per-kind counts and merge behavior. Existing data is not silently overwritten.
2. **Upstream behaviour contract** — PortabilityTab offers export, file selection, preview, confirmation, progress/result, and clear unsupported/corrupt/version mismatch errors (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/website/src/pages/overview/PortabilityTab.tsx:1-340`). Backend exports a bounded bundle, previews without mutation, and imports validated kinds with typed results (`/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src/src/kiro_crew/dashboard/handlers/portability.py:1-520`).
3. **Companion-X mapping** — Keep this in the single Settings → Import & Export section. Migration already owns immutable preview/start/get plans and Memory/Scheduler/Lessons import adapters; Workflow owns durable execution (`/Users/wdaniero/workplace/python-factory/components/migration/src/factory/migration/mcp/lifecycle.py:1-220`; `/Users/wdaniero/workplace/python-factory/components/migration/src/factory/migration/interface.py:1-20`). Missing: export manifest/bundle, native Companion-X adapter, and config-kind coverage.
4. **Simplicity check** — Extend Migration with a versioned native adapter and export contract; reuse existing preview/Workflow execution and merge-only policy. Do not build a second import engine. Export schema/secret exclusions require meta-architect/security review.
5. **Acceptance** — Export fixture data → inspect manifest contains no secrets → preview on clean store reports exact counts → import → re-export semantically matches → replay does not duplicate → corrupt/newer bundle fails before mutation. Screenshot export card and import preview/result.
6. **Owner questions** — None.
