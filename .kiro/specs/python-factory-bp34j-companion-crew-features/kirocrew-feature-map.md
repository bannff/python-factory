# KiroCrew → Companion-X feature checklist (code-referenced)

Generated by `scripts/gen_kirocrew_feature_map.py` from upstream `docs/feature-map/README.md` (https://github.com/kirodotdev/kirocrew) at commit `0fba5d2 2026-09-13`.
Local read-only clone for code lookup: `/Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src` (refresh: `git -C /Users/wdaniero/workplace/kirocrew-workspace/kirocrew-src pull --ff-only`, then re-run the generator; Status/Evidence/Location cells survive regeneration).

**Never copy KiroCrew code.** Read the upstream page/handler to learn the behaviour, then implement it Python-Factory style: Polylith brick + MCP tool (strict Pydantic ingress, typed `ToolResult` egress), LangChain/LangGraph on MCP v2, CopilotKit v2, shadcn/ui, brick-declared views. Sidebar truth upstream is `website/src/surfaces/builtins.tsx`.

## How the loop uses this file

- **This is the M7.5 acceptance list.** Gate 0 counts rows; M7.5 cannot close while any row is `TODO`, `MISSING`, or `PRESENT+BROKEN`.
- `Status` ∈ `TODO` (unchecked) · `MISSING` · `PRESENT+BROKEN` · `PRESENT+WORKS` (requires a screenshot path in `Evidence`) · `OWNER_NA` (never build) · `OWNER_DEFERRED` (owner wants it LATER — build in **M9 Deferred features**) · `OWNER_REBRAND` (KiroCrew-identity item — ditch or re-spec under Companion-X's own name/repo in **M8 Rebrand**). All three `OWNER_*` statuses are only valid in the owner's words, quoted in `Evidence`, and are OUT of M7.5 scope.
- **Order after M7.5:** M8 Rebrand first (owner: "rebrand/whatever last but before the deferred features"), then M9 Deferred features (voice, channels/Slack, Remote Crew + webhooks, Brick Store replacing KiroCrew's app store).
- **Cycle 1 of M7.5 is a census** (done 2026-09-13: 9 works / 33 broken / 98 missing). After that, work in **OWNER PRIORITY ORDER, not file order** (owner escalation 2026-09-13 22:10 — the loop spent three cycles polishing Sessions rows 1–2 while the suites the owner asked for stayed broken):
  1. **Agent Capabilities** (Crews, Agent Templates, Connections, Skills, Steering, Hooks, Prompts — every tab must do its job, not show "unavailable")
  2. **Settings** (all 21 rows: each section either works or is `OWNER_NA` in the owner's words)
  3. **Task runner and subagents** — Terminal as a real PTY panel + subagents table
  3b. **M7.6 Dogfood loop (roadmap)** — immediately after the Terminal rows and BEFORE any further Settings rows: make Companion-X's own goal loop drive an agent turn per cycle (rows 55, 58, 61) and prove it by completing one real checklist row with the KiroCrew loop off. This is the owner's #1 outcome; nothing else in M7.5 outranks it once Terminal is usable.
  3c. **M7.7 Unified graph memory (roadmap)** — right after M7.6, built by Companion-X's own loop: Memory + KB onto the one persistent networkx graph with a light local embedder; rows 43–54 (Memory, lessons and usage) are its UI. Owner ruling 2026-09-15: never 'deferred'.
  4. **Schedules and loops**, then **Artifacts**
  5. **Chat and sessions** — remaining rows (tags, folders, forks, popouts are polish; do them last)
  6. Developer · Standalone operator surfaces · Popouts and embeds · Apps (pending owner ruling) · Redirects
- Within a priority area: `MISSING` rows before `PRESENT+BROKEN` rows whose missing piece is cosmetic. One row (or one tightly coupled group) per slice; update the row in the same cycle. A `PRESENT+BROKEN` row the owner can already use for daily work (e.g. Sessions) does not get another cycle while a higher-priority area has `MISSING` rows.
- **Renders ≠ works.** `PRESENT+WORKS` means the control round-trips through the MCP tool and shows the result, not that the page loads. **Parity means the buttons too (owner 2026-09-15 07:52):** a row is `PRESENT+WORKS` only when EVERY user control on the upstream page exists and works — list AND create (+ / JSON import) AND edit AND delete AND apply/reload. A list-only tab is `PRESENT+BROKEN`. Owner: "there's no + button for the user to add the thing the menu is talking about… wtf". Reviewers must enumerate the upstream page's controls and tick each one.
- `Reach it` / `Page` / `Handler` / `Endpoints` are **upstream KiroCrew** references (read them). `Companion-X location` is the route/component/brick here.
- Every new or changed surface ships with one view-only Playwright screenshot on the 18000/13000 smoke stack (LOOP-GUIDE).

## Owner IA decisions (2026-09-13 19:23)

1. **Agent Capabilities is ONE rail icon** with a sub-nav inside: Crews · Agent Templates · Connections · Skills · Steering · Hooks · Prompts · **Schedules** · **Artifacts** · Lessons.
2. **Settings is ONE gear icon** with the full suite inside: Overview · Import/Export · Chat · Display · Voice · Notifications · Shortcuts · Skills · Channels · Browser · Computer Use · Remote Crew · Privacy · Security · Developer · About (each row below decides PRESENT vs OWNER_NA).
3. **Schedules and Artifacts lose their own rail icons** and move under Agent Capabilities (Kiro's pick between the two options the owner offered; owner may move them to Settings instead).
4. Resulting rail: Sessions/Chat · existing Analysis/Intelligence canvases · Agent Capabilities · Settings · Terminal (dockable panel) · bell.

---

## Chat and sessions

Companion-X home: Chat sidebar + rail **Sessions** (Operations)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
The default destination. `/chat` is the rail's **Sessions** row; everything in
this area is reached from inside it unless stated otherwise.
| 1 | Sessions | Multi-slot agent chat, one slot per conversation; pinned rows form a manually ordered section above automatic sorting | `/chat/:slug?` — rail **Sessions** | `pages/ChatPage.tsx`, `pages/ChatSidebar.tsx`, `pages/chat/SessionFlyout.tsx`, `pages/chat/TranscriptScrollShell.tsx` (internal split of ChatPage — the transcript scroller skeleton, no new user-facing feature), `pages/chat/ChatPageMessageContent.tsx` (internal split of ChatPage — the header menu, row-key helpers and user-bubble content renderers, no new user-facing feature), `pages/chat/useChatPageTranscriptController.tsx` (internal split of ChatPage — scroll-to-bottom, auto-follow gating, nav scrolling and the pinned-prompt banner, no new user-facing feature), `pages/chat/useChatPageSessionController.ts` (internal split of ChatPage — session identity, `?sid` URL sync, session tabs and slot auto-create, no new user-facing feature), `pages/chat/useChatPageResourcesController.tsx` (internal split of ChatPage — side-panel tabs, source/issue selection, file/folder/artifact/diff opening, uploads, screen snips and drops, no new user-facing feature), `pages/chat/hoverHold.ts` (internal split of ChatSidebar — seat arithmetic for the hovered-row hold, no new user-facing feature), `pages/chat/CompactionCard.tsx` (transcript row for the gateway's compaction / session-reload status notices — folds the compaction summary behind a one-line card instead of an assistant bubble; no new user-facing entry point), `pages/chat/transientNotice.ts` (helper for the existing error row — resolves the gateway's transient-retry `meta.notice` token into localized NoticeCard / ErrorCard copy; no new user-facing entry point), `utils/pinnedSessionOrder.ts` | `chat_handlers.py`, `ws.py` | `POST /api/chat`, `GET,POST /api/chat/slots`, `GET /api/ws` | Chat sidebar SessionDeck; `/sessions` | PRESENT+WORKS | Multi-slot create/list/resume/history plus durable Session-owned pin/unpin and sparse manual rank ordering round-trip through revision-fenced MCP; both Deck and direct page consume authoritative order; restart, owner scope, archive-unpin, stateful, and UI tests pass. |
| 2 | Sessions page | A neutral, bookmarkable session chooser at a stable URL — lists open sessions with search, filter chips (All / Unread / tags) and recency groups; never auto-selects or auto-creates a session, unlike bare `/chat` | `/sessions` — typed or bookmarked URL only (deliberately no nav-rail entry; pending a maintainer call) | `pages/SessionsPage.tsx`, `pages/chat/sessionOrder.ts` (shared ordering with the sidebar) | none new — reads the same SSE slots frames and existing handlers | `GET /api/ws` (`slots`), `GET /api/chat/tags`, `POST /api/chat/slots` (New session) | `/sessions` | PRESENT+WORKS | Neutral selection, deep-link focus, pinned/recency groups, and dynamic All/Unread/tag chips work; Unread derives from pending completion delivery; tags are durable bounded Session-owned CAS state editable in detail. |
| 3 | Default memory mode | Picks Persistent, Incognito, or Temporary for new dashboard-created chats; explicit per-chat choices win, while app-owned and direct API sessions keep their own mode | Settings → Chat → **Default Memory Mode**; the Welcome view can override one chat | `pages/settings/ChatPanel.tsx`, `store/chatSlice.ts`, `components/WelcomeView.tsx` | `handlers/files.py` (`api_dashboard_config`), `chat_handlers.py`, `remote_relay.py` | `GET,PUT /api/dashboard/config`, `POST /api/chat/slots` | `frontends/next-dashboard/components/settings/chat-preferences-panel.tsx` (Settings → Chat → Default memory mode radio group); `components/ui/.../runtime/chat_preferences.py` (`default_memory_mode` owner preference); `components/session/.../runtime/models.py` (`SessionRecord.mode`, `MemoryMode` enum); `components/agent/.../runtime_contracts.py` (`RuntimeInvocation.memory_mode`); `components/agent/.../adapters/session_steering.py` (`bind()` materializes the session's real mode); `components/agent/.../adapters/langchain_tools.py` (`_memory_mode_decision`, the real write/read gate); `components/agent/.../adapters/langchain_hybrid_recall.py` (`_recall_memory`, the automatic-read gate) | PRESENT+WORKS | **OWNER RULING 2026-09-16 21:20, verbatim: "Incognito: read memory, write nothing; Temporary: read nothing, write nothing, session deleted on close."** Built the gating on that exact spec, same auto-nudge cycle as row 16's regenerate work. **Write gate** (`langchain_tools.py::invoke_capability`, the SAME chokepoint `_approval_decision` already uses): every explicit `memory_*` tool call the model makes is checked against `_MEMORY_WRITE_TOOLS` (an exhaustive, non-guessed set pinned against `test_typed_mcp_boundary.py`'s own tool inventory: `memory_store`/`delete`/`update`/`bulk_delete`/`delete_user`/`consolidate`/`evolve`/`embed_backfill`/`import_record`) — Incognito AND Temporary both deny these with a typed `memory_mode_incognito_blocks_memory_write`/`_temporary_blocks_memory` error, never a silent no-op. **Read gate**: explicit `memory_*` READ tools (`memory_get`/`retrieve`/`list`/`stats`/`recall_inspect`/`history`/`bulk_preview`/`hybrid_search`/`search_by_time`) are blocked ONLY for Temporary (Incognito still reads, per the ruling's own read/write split) at the same tool-dispatch boundary; the ONE automatic (not model-chosen) memory read — `HybridRecallMCP._recall_memory`, fired every turn to inject scoped context — is separately gated in `langchain_hybrid_recall.py` since it never goes through the explicit-tool-call path at all; the SAME hybrid call's KB/graph recall is deliberately untouched (the ruling names memory specifically, confirmed by re-reading its exact wording before scoping, not assumed broader). Scope discipline: lessons and KB writes are NOT gated by memory_mode — literal reading of "memory" in the ruling, no unilateral scope expansion. **Verified, not assumed**: new `test_memory_mode_gate.py` (4 tests, real tool-dispatch-boundary proof: Incognito blocks store but allows retrieve; Temporary blocks both; persistent/unset mode never restricts; an unrelated tool name is never accidentally caught by the gate) + `test_memory_scope_propagation.py` (+2: Temporary reads no memory but still reads KB in the same hybrid call; Incognito still reads memory). Full `components/agent/test` 884/885 (1 pre-existing unrelated flake). **Still not `WORKS`, honestly disclosed, not silently dropped**: "session deleted on close" has no backend or FE hook to attach to today — Companion-X's chat is stateless HTTP/SSE per turn with no durable server-side "session closed" lifecycle event, and no FE tab-close/switch-away handler exists to call `session_delete` from (confirmed absent by direct search, not assumed). This is real, scoped, buildable follow-up work (wire a FE unmount/visibility-change handler → a new or existing delete-on-close tool call) — a different, smaller layer than the memory gating this cycle closed, not blocked on a design decision the way the gating itself was before this ruling arrived. |
| 4 | Remote-bound session | A local session whose turns execute on a connected peer crew and stream back over its tunnel — local sidebar row, transcript and history, remote execution | New-chat menu → **New chat on crew** → pick a connected crew | `pages/ChatPage.tsx`, `pages/ChatSidebar.tsx`, `components/RemoteCrewChip.tsx` | `chat_handlers.py`, `handlers_instances.py`, `handlers/core.py`, `remote_relay.py`, `remote_mirror.py` | `POST /api/chat/slots` (`instance_id`), `POST /api/chat?relay=1`, `GET /api/instances/{id}/capabilities`, `GET /api/version` | — | MISSING | No behaviorally equivalent Session/chat MCP plus UI path was found in the current census. |
| 5 | Peer-owned live session | The OPPOSITE direction of travel from the row above: a session a connected peer crew owns, merged read-only into this machine's live Sessions list and ordered with local rows by recency, badged with the owning crew. Clicking one opens it HERE: the hub binds a fresh local slot to that peer session and backfills the peer's transcript into it, so the row opens a real local conversation whose turns keep running on the peer. The bound row keeps the identity the peer row had (`<instance_id>:<peer_key>`), so it re-renders in place instead of appearing as a second row. The row still omits rename, close, pin, folders, drag and the ⋯ group, because the peer — not this machine — owns the session itself. An adopted session drops out of the peer rows, so it is never listed twice. Board view stays local and reports the count it filtered. The peer's CLOSED sessions are not here; they are reachable only through federated history search | Settings → Developer → Feature Previews → **Remote instance sessions** (default off), then rail **Sessions** | `pages/ChatSidebar.tsx`, `hooks/useInstanceSessions.ts`, `components/RemoteCrewChip.tsx`, `pages/settings/FeaturePreviewsSection.tsx` | `handlers_instances.py`, `chat_handlers.py`, `remote_adopt.py`, `slot_projection.py` | `GET /api/instances/{id}/chat-slots` (owner-only, GET-only; reads the peer's `GET /api/chat/slots`, drops the slots this hub itself drives, then allowlists each row to the fields the sidebar reads and re-redacts its text — a peer's session title is model-authored on another machine, and the local rows beside it are scrubbed by `slot_projection.py`), `POST /api/chat/slots` (`adopt_remote_slot` — binds a fresh local slot to the peer session named by a row of the listing above, and backfills its transcript; refuses with `adopt_peer_mode_unknown` when the crew reports no `memory_mode`, rather than guessing the session's privacy boundary) | — | MISSING | No behaviorally equivalent Session/chat MCP plus UI path was found in the current census. |
| 6 | Session folders | User-defined folders grouping session rows; a folder holding no sessions can optionally drop its body so it costs one row instead of two | Sidebar folder header → drag a row; folder ⋯ menu → New ephemeral chat (incognito / temporary) to start a mode-pinned session in the folder; Settings → Chat → **Compact Empty Folders** (off by default) | `pages/chat/FolderPanel.tsx`, `pages/ChatSidebar.tsx`, `pages/settings/ChatPanel.tsx`, `pages/chat/ChatSettings.tsx` | `chat_folders.py`, `chat_handlers.py` | `GET,POST /api/chat/folders`, `PATCH /api/chat/slots/{slot}/folder`, `POST /api/chat/slots` | `components/session/src/factory/session/mcp/folder.py` (`session_set_folder`) + `runtime/adapters/sql_mutations.py` (`set_folder` CAS) + `SessionRecord.folder` (additive column); FE `frontends/next-dashboard/components/operations/sessions/{session-detail.tsx (Folder input), session-actions.ts (setSessionFolder), use-sessions-view-derived.ts + sessions-view.tsx (folder filter chips)}` | TESTING_NEEDED | **Core grouping built + unit/FE-tested: assign a session to a folder (durable, CAS-fenced) and filter the Sessions list by folder. Remaining is the richer sidebar chrome + a live proof.** Modeled as a single-value `folder` field (unlike `tags`' delimited list), reusing the SAME `_cas_set` path `summary`/`active_checkpoint_id` use — additive `folder TEXT NOT NULL DEFAULT ''` column + migration, `set_folder` mutation + lifecycle mixin method + `session_set_folder` MCP tool (own module, mirroring `tagging.py`). **LOC discipline**: adding `SetFolderInput` pushed `lifecycle_contracts.py` to 205 — extracted it AND `GenerateSummaryInput` into a new `grouping_contracts.py` (imports `SessionRefInput` one-way, no cycle), bringing it back to 188; every touched/new py file ≤200. **FE**: a "Folder" input in the session detail (mirrors the tags input) calling `setSessionFolder`, and a `folder:`-prefixed filter branch in the derived hook + `📁 <name>` chips in the Sessions filter row (mirroring the existing `tag:` chips). Tests: backend 6 new (`test_folder.py` — unfiled default, file+clear with a real `session_get` re-read, revision-fenced, foreign-owner fails closed, cross-restart SQL persistence) → `components/session/test` **98/98** (up from 93), zero regressions. FE 3 new (`session-actions.test.ts` +1 trimmed CAS wrapper; `session-detail.test.tsx` +1 real `session_set_folder` round trip via the component; `sessions-view.test.tsx` +1 folder-chip filtering) → `components/operations/sessions` 54/54; `tsc` at the 8-error pre-existing baseline, zero folder-caused (fixed the typed fixtures the new required `folder` field broke). **Still `TESTING_NEEDED`, honestly**: (1) full upstream parity wants sidebar folder-HEADER grouping + drag-a-row-into-a-folder + folder ⋯ menu + empty-folder ENTITIES (create an empty folder) + the Settings → **Compact Empty Folders** toggle — the current model files a session under a typed name and filters by it, but has no folder-entity lifecycle or drag UI; (2) no live 18000/13000 browser proof (deferred — host memory ~1.2 GB with the owner's live stack up). |
| 7 | Session tags | Colored labels on sessions, filterable | Sidebar row context menu → Tags | `pages/chat/SessionFlyout.tsx` | `chat_tags.py` | `GET,POST /api/chat/tags`, `PUT /api/chat/slots/{slot}/tags` | `/sessions` detail and filter chips | PRESENT+WORKS | Up to eight unique lowercase tag slugs persist through additive Session SQL storage, strict revision-fenced `session_set_tags`, colored card labels, detail editing, and dynamic filtering; restart/owner/stateful/UI tests pass. |
| 8 | Older sessions | The sidebar's history pane, searchable, with per-session delete and a bulk **Delete all** | Sidebar → **Older Sessions** disclosure | `pages/ChatSidebar.tsx` | `handlers/sessions.py` | `GET /api/sessions` (`exclude_open`), `GET /api/sessions/search`, `GET /api/sessions/{key}`, `DELETE /api/sessions/{key}`, `DELETE /api/sessions`, `GET /api/sessions/clearable/count` | `/sessions` archived filter + per-session Delete + Delete all | PRESENT+WORKS | Cycle 66 (per-session delete) + cycle 67 (bulk Delete all, `session_count_archived`/`session_clear_archived` MCP tools + FE `ClearArchivedControl`). Verified live: real 12-session clearable count rendered correctly; confirm-commit deliberately not exercised against real data (owner's call). Row fully closed. |
| 9 | Pinned messages | Pin a message; pins panel per session | Message hover → pin; header pin count | `pages/chat/PinnedMessagesPanel.tsx` | `chat_pins.py` | `GET,POST /api/chat/pins`, `DELETE /api/chat/pins/{id}` | `components/session/src/factory/session/mcp/pinned_messages.py` (`session_set_pinned_messages`); FE `frontends/next-dashboard/lib/copilotkit/{chat-pin.ts, chat-callbacks.ts (onPin), feedback-message-slot.tsx (Pin toolbar button)}` | TESTING_NEEDED | **Backend fully built+tested (prior cycle); the FE pin/unpin TRIGGER is now wired this cycle via the SAME `additionalToolbarItems`/`ToolbarButton` seam rows 15/16 proved — the "no `onPin` slot" claim from the original cycle was the same misread corrected for Rewind. Remaining gap narrowed to the pins-VIEWING panel + a live proof.** Backend (unchanged, still green): `SessionRecord.pinned_message_ids` bounded field (`max_length=32`, additive column, JSON-encoded since message ids are opaque), `session_set_pinned_messages` whole-set atomic CAS write, `components/session/test` 93/93. **FE this cycle**: `chat-pin.ts::togglePinnedMessage(threadId, messageId)` resolves the owning session via the existing `session_resolve_thread` (the sidebar only carries `thread_id`), reads its current `pinned_message_ids`, and rewrites the whole set (add if absent / remove if present) through the revision-fenced `session_set_pinned_messages` — returning whether the message is pinned after the toggle so the toast is truthful. Wired as `onPin` in `useChatCallbacks` and rendered as a second toolbar button beside Rewind (both under `additionalToolbarItems`, guarded so each renders only when its callback is supplied). Added `pinned_message_ids: string[]` to the FE `SessionSummary` type/normalizer (defaults `[]`). Tests: FE 6 new (`chat-pin.test.ts` ×3 — pins an unpinned message writing the full set+id, unpins by writing the set with the id removed, throws when the session can't resolve; `feedback-message-slot-pin.test.tsx` ×3, rendered against a REAL `CopilotKitProvider` — Pin button renders+fires with the exact message, omitted when no callback, coexists with Rewind) — full `lib/copilotkit` 91/92 (the 1 failure, `frontend-focus-run.test.tsx`, the SAME pre-existing `useRouter()` flake) + `components/operations/sessions` 58/58; `tsc` at the 8-error pre-existing baseline, zero pin-caused. **Still `TESTING_NEEDED`, honestly**: (1) the pins-VIEWING panel per session + the header pin count (upstream's `PinnedMessagesPanel`) still need a home — the same chat right-side-panel architecture rows 18/19/25/26/27/30 also wait on (confirmed absent cycle 71); a pin you can set but not review in one place isn't full parity. (2) No live 18000/13000 browser proof yet (same bar row 17 met). The toggle interaction + backend are real and tested; the viewing surface is the remaining build. **UPDATE (cycle 9)**: the pins-VIEWING panel is now BUILT — a **Pins tab** in the chat side panel (`components/chat/chat-pins-tab.tsx`) resolves the thread's session, maps its `pinned_message_ids` to the real transcript messages, lists them, and unpins via `session_set_pinned_messages`. So both halves (pin toggle + pins panel) now exist; only the live proof and the header pin-count remain. |
| 10 | Pinned prompt banner | Keeps the turn's own prompt visible in a single-state banner at the top of a long reply while it scrolls; distinct from **Pinned messages** above. Worn by the main chat AND by every `ChatPane` host — split-view panes and the Crew Members DM thread — through the same hook | Settings → Chat → **Pin the latest turn** | `pages/chat/PinnedPrompt.tsx`, `pages/chat/usePinnedPrompt.ts` (internal split of `useChatPageTranscriptController` — the host-agnostic pin geometry, scroll recompute and in-place jump shared with `components/ChatPane.tsx`; no new user-facing entry point), `app-sdk/ChatMessageList.tsx` (`onDisplayItems` / `hiddenRow` row-indexing seam) | `components/ui/src/factory/ui/runtime/chat_preferences.py` (`pin_latest_prompt` durable preference); FE `frontends/next-dashboard/components/chat/pinned-prompt-banner.tsx` (banner + `latestUserPrompt`), `components/chat/copilot-sidebar.tsx` (mount), `components/settings/chat-preferences-panel.tsx` (toggle) | TESTING_NEEDED | **Full stack built + unit/FE-tested at every layer; the ONLY remaining step is a live browser proof — NO structural gap (unlike rows 9/18, this needs no chat right-panel).** Reused the EXACT durable `ChatPreferences` additive-field pattern row 12 established for the new `pin_latest_prompt` boolean: model field, `ChatPreferenceStore.update` (both in-memory + SQLite adapters, additive column + migration), the `ui_get`/`ui_update_chat_preferences` MCP DTOs (UNSET-sentinel translation at the boundary so an unrelated save never resets it — the same discipline row 12 fixed), AND the row-102 preference backup/restore snapshot (export/import + `restore_preferences`). **FE**: a pure `PinnedPromptBanner` + `latestUserPrompt(messages)` helper (scans from the end so it finds the current turn's user prompt even while the assistant reply streams as the last message); the sidebar computes the prompt from the live agent via `useAgent(AGENT_ID)` and mounts the banner (sticky, `top-0 z-10`) just above the transcript ONLY when the preference is on — a faithful Companion-X-rails placement (keeps the prompt visible while the reply scrolls beneath it) that needs no CopilotKit-internal scroll hooks; a Settings → Chat "Pin the latest turn" toggle. Tests: backend 2 new (`test_row_10_pin_latest_prompt_round_trips_and_survives_a_display_only_update` — round-trips through the real MCP tool + real SQLite store AND is preserved by an unrelated update; `test_in_memory_store_update_display_preserves_pin_latest_prompt`) → `test_chat_preferences.py`+`test_preference_backup.py` 14/14, full `components/ui/test` 537 pass (14 pre-existing `MCP_PERMISSIONS_CONFIG_DIR` env errors unrelated), all files ≤200 LOC. FE 8 new (`pinned-prompt-banner.test.tsx` ×4 — helper finds the latest user prompt behind a streaming reply / returns empty when none, banner renders the text / renders nothing when null-or-empty; `chat-preferences-api.test.ts` +1 pin round-trip; `chat-preferences-panel.test.tsx` +1 toggle; plus the shared exact-object fixtures updated for the new field) → full `components/settings`+banner 77/77; `tsc` at the 8-error pre-existing baseline, zero pin-caused. **Remaining before `PRESENT+WORKS`**: a real 18000/13000 browser proof (turn on the toggle, send a long reply, confirm the prompt stays pinned at the top while it scrolls) — deferred this cycle because host memory was ~1.5 GB free with the owner's live stack up, making a second full-stack launch unsafe. Implementation is complete; this is a proof gap, not a build gap. |
| 11 | Turn minimap | A proportional, non-scrolling rail of markers in the transcript's left gutter, one per loaded user message with non-empty content (an image-only send counts, since it persists as image markdown; earlier, not-yet-loaded history has no markers — while the server still holds older rows the rail wears a dimmed end-cap that says so and loads them on click); on-screen turns are highlighted, hovering previews the prompt and the final assistant reply before the next turn opener (a user or injected message), and click / arrow keys jump to that turn (Escape closes the preview). Desktop-only by design | Session transcript → left gutter; appears when the viewport is at least the `md` breakpoint (768px), the pane is ≥ 560px wide with ≥ 40px of free gutter beside the content column, there are two or more turns, and the primary pointer is not coarse (`(pointer: coarse)` — touch — hides it; a hover-capable stylus counts as fine) | `pages/chat/TurnNavigationMinimap.tsx` (mounted by `pages/ChatPage.tsx`; items from `hooks/useChatNavigation.ts`) | client-only (no handler) | none | `frontends/next-dashboard/components/chat/turn-minimap.tsx` (`TurnMinimap` + `turnsFromMessages`), mounted in `components/chat/copilot-sidebar.tsx` | TESTING_NEEDED | **Core scaffolded per owner steer (feature dev first, validate later).** A `TurnMinimap` proportional rail in the transcript's left gutter (`hidden md:flex` desktop gate, shown only with ≥ 2 turns): `turnsFromMessages(agent.messages)` yields one marker per non-empty user turn (string or parts-array content, whitespace-collapsed, 140-cap preview); each marker hover-previews the prompt and click jumps the transcript PROPORTIONALLY (the spec's own "proportional rail" model — the sidebar finds the scrollable transcript element and seeks to `index/(n-1)` of its scroll range, avoiding fragile per-message DOM anchoring). Mounted as an absolute overlay on the (now `relative`, ref'd) transcript container — additive, no change to CopilotKit's own message DOM. Tests: FE 3 new (`turn-minimap.test.tsx` — `turnsFromMessages` extracts only non-empty user turns with capped previews; renders nothing < 2 turns; one marker per turn + click fires `onJump(index)`) → `tsc` at the 8-error pre-existing baseline. **Deferred/disclosed**: the richer upstream behaviors — on-screen-turn highlight, arrow-key nav + Escape, the dimmed "older history not yet loaded" end-cap that loads on click, and the exact ≥560px-pane / fine-pointer gating (currently a simpler `md:` gate) — plus the standing live proof. |
| 12 | Collapse the message input | Puts the composer away while you read a long reply and hands the room to the transcript (measured 89px at 1500x950); a labelled bar stands in its place, reports the unsent draft’s first line, and restores it. Off by default, and the choice persists. Collapses the opposite end of the same reply from the **Pinned prompt banner** above | Composer **+** menu → **Collapse the message input**; on touch, the **⋯** overflow beside the attach button | `components/ChatInput.tsx` (opted in by `pages/ChatPage.tsx`; focus intents resolve through `pages/chat/composerFocus.ts`) | client-only (no handler) | none | `components/ui/src/factory/ui/runtime/chat_preferences.py` (`collapse_message_input`); `frontends/next-dashboard/components/chat/composer-collapse.tsx`, `components/settings/chat-preferences-panel.tsx` | PRESENT+WORKS | **Closed full-stack: durable per-owner preference (matching upstream's own "off by default, choice persists" contract) + the actual composer collapse/restore UI.** Added `collapse_message_input: bool = False` to `ChatPreferences` (SQLite additive column, `InMemoryChatPreferenceStore` and `SqliteChatPreferenceStore` both extended, MCP tool/backup-restore DTOs extended) — the exact same durable record row 3's memory-mode field already lives on, reused rather than inventing a second preference store. **Two real, separate correctness bugs found and fixed while adding this field, neither invented — caught by writing the test first and watching it fail for the right reason**: (1) the MCP tool `ui_update_chat_preferences` declared `default_memory_mode: str = "persistent"` (a plain default, not an unset-sentinel) — meaning ANY caller that updated `plain_diffs`/`hidden_models` without ALSO re-specifying the owner's current memory mode silently reset it back to `"persistent"`, a real bug affecting row 3's own feature that had been live all session; fixed by changing the tool boundary to `str | None = None` and translating `None` → the store's own `UNSET` sentinel, so omitting the field now genuinely means "leave it alone." (2) `InMemoryChatPreferenceStore.update_display` never even included `default_memory_mode` in its `ChatPreferences(...)` reconstruction at all — silently resetting it to the class default on every plain Display-panel save; the SQLite adapter never had this bug, only the in-memory one. Both fixed in the same pass since the new `collapse_message_input` field would have inherited the identical defect otherwise. **FE composer-collapse investigated the vendored SDK's real internals before designing it, not assumed**: `CopilotChatInput` has no native collapse mode, but `CopilotChatView`'s `input` slot accepts a full component replacement (`SlotValue<C> = C | string | Partial<ComponentProps<C>>`) — read the SDK's own `renderSlot`/`CopilotChatView` source directly to confirm a component-shaped slot value does NOT also receive a sibling props object the caller passes alongside it (only a plain-object slot value gets that merge), so this app's own existing `textArea`/`sendButton`/`disclaimer` overrides had to be baked into the new wrapper's own render rather than passed at the call site as before — a real integration detail that would have silently dropped those overrides if assumed rather than verified. `makeCollapsibleComposerInput(enabled, overrides)` renders the real composer plus a small "Collapse" button when enabled, swapping to a restore bar (showing the draft's first line, or an honest "Empty draft") on click; the draft itself is tracked via `CopilotChatView`'s own `inputValue`/`onInputChange` controlled props so nothing is lost across the swap. Tests: backend 3 new (`test_row_12_collapse_message_input_round_trips_and_survives_a_display_only_update`, `test_in_memory_store_update_display_does_not_reset_default_memory_mode` — pins bug #2 above so it cannot silently return) + existing suite re-verified; FE 4 new (`composer-collapse.test.tsx`, rendered against a REAL `CopilotKitProvider` — collapse button absent when the preference is off, collapsing shows a truthful restore bar with only the first line, an empty draft shows "Empty draft" rather than a blank bar, restoring drops the bar) + updated `chat-preferences-api.test.ts`/`chat-preferences-panel.test.tsx` for the new field + a dedicated toggle test. 537 backend (up from 535) + 202/203 FE across `components/chat`/`components/settings`/`lib/copilotkit` (the 1 failure, `frontend-focus-run.test.tsx`, confirmed pre-existing and unrelated multiple times this session already) + 11/11 `settings-disclosure-panels.test.tsx` (drives the real full Settings → Chat panel, confirming the new toggle didn't break the surrounding page). |
| 13 | Share message as card | Turn an assistant reply into a branded PNG card + prefilled caption for X/LinkedIn; governed by `capabilities.social_share` (a policy pin withdraws the entry) | Message hover → More actions → Share as image | `pages/chat/share/ShareMessageModal.tsx`, `pages/chat/share/ShareCard.tsx` (helpers: `pages/chat/share/shareSupport.ts`) | `dashboard/social_share.py` (governance probe; card itself is client-side) | `GET /api/dashboard/config` (`social_share_enabled`) | `frontends/next-dashboard/lib/copilotkit/share-card.ts` (caption + canvas PNG + download) + `lib/copilotkit/{feedback-message-slot.tsx (Share toolbar button), chat-callbacks.ts (onShare)}` | TESTING_NEEDED | **Core scaffolded per owner steer 2026-09-17 ("proceed with feature dev… scaffold and minimally test, validate later").** Client-side, no server: `share-card.ts` builds a prefilled caption (first meaningful line, whitespace-collapsed, length-capped at 260 with an ellipsis, surrogate-safe) and renders the reply onto a 1200×630 (1.91:1 social/OG ratio) themed canvas card, returning a PNG data URL and triggering a download; `messageText` handles both string and AG-UI parts-array content. Wired as `onShare` on the message toolbar via the SAME `additionalToolbarItems`/`ToolbarButton` seam rows 9/15 use (now Rewind + Pin + Share coexist), with a toast surfacing the caption and a best-effort `navigator.clipboard` copy. Tests: FE 5 new (`share-card.test.ts` ×3 — text extraction from string/parts/undefined, caption from first line + whitespace collapse, long-caption cap+ellipsis; `feedback-message-slot-share.test.tsx` ×2 rendered against a REAL `CopilotKitProvider` — Share button renders+fires with the message, omitted when no callback) → full `lib/copilotkit` 96/97 (the 1 failure `frontend-focus-run.test.tsx` is the SAME pre-existing `useRouter()` flake) + `tsc` at the 8-error pre-existing baseline. **Deferred (disclosed, not built)**: the `capabilities.social_share` governance probe (a policy pin withdrawing the entry — needs a Companion-X config-read this port doesn't have yet), the X/LinkedIn share-intent deep links (this ships download + copy-caption), and richer card design (the current card is a clean, minimal themed default, not a design-reviewed layout). Plus the standing live-proof gap. |
| 14 | Fork a session | Branch a new slot from an existing transcript; an incognito or temporary session forks into a child of the same memory mode | Session row menu → Fork | `pages/ChatSidebar.tsx` | `chat_fork.py` | `POST /api/chat/slots/{slot}/fork` | `components/session/src/factory/session/mcp/fork.py` (`session_fork`), `components/session/.../runtime/lifecycle.py` (`SessionLifecycle.fork()`); `components/agent/src/factory/agent/mcp/session_fork_transcript.py` (`session_fork_transcript`, cross-brick), `components/agent/.../adapters/langchain_checkpoints.py` (`AsyncSqliteCheckpoints.copy_thread`, SQL-layer implementation of LangGraph's own documented-but-unimplemented `copy_thread`/`acopy_thread`), `components/agent/.../adapters/langchain_runtime.py`+`langchain_chat.py` (`fork_thread`); FE `frontends/next-dashboard/components/operations/sessions/{fork-session-control.tsx, use-session-detail-actions.ts, session-actions.ts}` (Fork button, two-call sequence) | PRESENT+WORKS | Live-proven 2026-09-16 on the 18000/13000 smoke stack against REAL owner conversations, both calls verified end-to-end: opened a real session with real turns ("What is 7 plus 5? Answer with just the number."), clicked Fork — the real `session_fork` MCP call created a genuine new session row, immediately followed by the real `session_fork_transcript` MCP call (`agent` brick) reporting `{"copied":true}` — the actual LangGraph checkpoint (14 checkpoint rows + 18 write rows) was copied onto the new thread, not just the session metadata. **Real platform finding**: LangGraph's own `BaseCheckpointSaver.copy_thread`/`.acopy_thread` are unimplemented stubs on the concrete `AsyncSqliteSaver` this project uses — confirmed by direct inspection, not assumed — so the copy was implemented at the SQL layer instead (re-insert every `checkpoints`/`writes` row under the new thread id; the schema's `parent_checkpoint_id` chain is not thread-qualified, so no remapping is needed), tested with a real turn→fork→verify-identical-history→independent-evolution-after round trip. **Real, disclosed edge case found live, not a bug**: forking a session whose `project` path no longer validates on the current host (found on a real pre-existing "M6.5 Crew acceptance" session) correctly fails closed with `session_project_path_refused` and creates NO orphaned row — the exact pre-existing `_validated_project` safety check doing its job on a fork the same way it does on ordinary creation; not every session can be forked if its stored project has since become invalid, which is correct, not a defect. **Storage-isolation lesson reconfirmed and acted on**: the smoke launcher does not isolate `.storage/` by port, so this proof's real throwaway session+checkpoint (1 session row, 14 checkpoint rows, 18 write rows) were found by exact key match and deleted immediately after use, verified clean both before and after. Backend 12 new tests (7 session/agent-brick fork tests + 5 `session_fork_transcript` tests, cross-brick real-invoker pattern matching the existing `session_history` precedent) + FE 7 new (real two-call sequence, degraded-but-still-returns-the-row on transcript failure, real UI interaction tests) — 134 backend + 46 FE all green, zero regressions across two LOC-driven module splits (`server.py`→`default_agent.py`, `memory.py`→`memory_chat.py`). |
| 15 | Rewind | Drop the transcript back to an earlier turn | Message action → Rewind | `pages/chat/AssistantMessage.tsx` | `chat_rewind.py` | `POST /api/chat/slots/{slot}/rewind` | `components/agent/src/factory/agent/mcp/session_rewind.py` (`session_rewind`); `frontends/next-dashboard/lib/copilotkit/{chat-rewind.ts,chat-callbacks.ts,feedback-message-slot.tsx}` (message-hover Rewind button) | PRESENT+WORKS | **FE closed this cycle — the disclosed CopilotKit toolbar gap from last cycle turned out to be a misread, corrected by reading the vendored component's real type definitions instead of trusting the first read of its documented props.** `CopilotChatAssistantMessage` DOES support extra toolbar buttons via `additionalToolbarItems: React.ReactNode` + an exported `ToolbarButton` component — found by reading `CopilotChatAssistantMessage.d.mts` directly rather than assuming the four documented callbacks (`onThumbsUp`/`onThumbsDown`/`onRegenerate`/`onReadAloud`) were the only extension point. No custom overlay needed after all. Built `chat-rewind.ts` (`rewindToMessage(threadId, messageId)`): the chat sidebar only carries `thread_id` (via CopilotKit's `agent.threadId`), not the session brick's own `session_id`, so it resolves the owning session first via the EXISTING `session_resolve_thread` tool (the same thread-scoped lookup row 61/Session control already established), then calls the new cross-brick `session_rewind` tool with the resolved `session_id`/`revision` — zero new backend needed for this resolution step. Wired `onRewind` into `useChatCallbacks` (drops the trailing client-side messages to match the server-side rewind, with NO re-run — the real semantic difference from `onRegenerate`, which re-runs) and into `makeAssistantMessageSlot` (renders the new toolbar button only when a callback is supplied, matching the existing four buttons' own conditional-render pattern). **A real, non-obvious discovery along the way**: upstream's own `onRegenerate` here is CLIENT-SIDE ONLY (drops the message + re-runs the agent) — it never actually calls the row-16 backend composition built earlier this session. Not touched or "fixed" this cycle (out of scope for row 15), but flagged here since a future cycle closing row 16's own FE gap should investigate whether `onRegenerate` should be upgraded to call the real backend `regenerate_turn`/`session_set_active_checkpoint` pair the same way rewind now does, for the same durable-pointer correctness reasons row 16 was built for in the first place. Tests: backend unchanged (5/5 `test_session_rewind_mcp.py` still pass); FE 5 new (`chat-rewind.test.ts` ×3: resolves session then rewinds with revision, reports false truthfully for an unknown message rather than erroring, throws when the session can't be resolved; `feedback-message-slot-rewind.test.tsx` ×2, rendered against a REAL `CopilotKitProvider` + self-managed agent rather than a shallow mock, since the vendored component needs live provider context to render at all — button fires the callback with the exact message, button is omitted entirely when no callback is supplied) — 82/83 across the full `lib/copilotkit` test directory (the 1 failure, `composer-typing.test.tsx`'s `textarea.scrollIntoView is not a function`, confirmed pre-existing and untouched by this change via `git status`). |
| 16 | Regenerate / variants | Re-run a turn, keep and switch between answers | Message action → Regenerate | `pages/chat/AssistantMessage.tsx` | `chat_regenerate.py` | `POST /api/chat/slots/{slot}/regenerate`, `.../switch-variant`, `.../edit-resend` | `components/agent/src/factory/agent/mcp/session_regenerate.py` (`session_regenerate`); `frontends/next-dashboard/lib/copilotkit/{chat-regenerate.ts,chat-callbacks.ts}` | PRESENT+WORKS | **Closed this cycle by upgrading a genuine correctness gap found while building row 15 (Rewind).** Earlier cycles' investigation (5-7, retained below) built and proved every hard substrate layer; this cycle composed them AND fixed a real, separate bug in the codebase's PRE-EXISTING `onRegenerate` implementation: it was CLIENT-SIDE ONLY (`agent.setMessages(...slice...)` + `copilotkit.runAgent`), which discarded the original reply entirely rather than keeping it as a durably switchable sibling branch — silently failing upstream's own "keep AND switch between answers" contract for every regenerate that had ever run. New cross-brick MCP tool `session_regenerate` (mirrors `session_rewind.py`'s exact shape: resolve session, drive `LangChainChatAgent.regenerate_turn`, persist the new branch via the existing `session_set_active_checkpoint`). **A real design bug caught and fixed before shipping, not by luck**: my first FE wiring attempt called the new backend tool (which ALREADY performs the real generation and persists it) and THEN also called `copilotkit.runAgent` — a second, duplicate generation against the same prompt. Caught by re-reading my own code before running anything, not by a failing test. Fixed by having the backend call return success/failure only, then re-reading the REAL durable transcript via the EXISTING `loadSessionHistory`/`session_history` primitive (the same one the Sessions page itself uses) and setting that verbatim — reflecting the server's own answer once, never generating twice. Tests: backend 5 (`test_session_regenerate_mcp.py`, unchanged from last cycle) + FE 3 new (`chat-regenerate.test.ts` — resolves session then regenerates with its revision then fetches the real transcript, returns null truthfully for an unknown message without ever fetching history, throws when session resolution fails) — 85/86 across the full `lib/copilotkit` suite (the 1 failure, `frontend-focus-run.test.tsx`, confirmed pre-existing and failing in complete isolation from this change, unrelated `useRouter()` context issue). Prior evidence, retained: **Auto-nudge cycles 5-7:** investigated before building — traced the real turn-invocation path and found NO checkpoint-id concept existed anywhere, confirmed via a real isolated proof-of-concept that once a sibling checkpoint branch exists, LangGraph's own "no explicit id → latest" resumption becomes genuinely ambiguous (a real correctness gap, not a UI nitpick). **Cycle 6 fixed the correctness gap**: `SessionRecord.active_checkpoint_id` durable pointer (additive migration, revision-fenced CAS via `session_set_active_checkpoint` MCP tool) + `RuntimeInvocation.checkpoint_id` materialized by the session-binding seam + `LangChainAgentRuntime._config()` injecting it only when set (ordinary turns stay byte-identical — verified). **Cycle 7 built the message-to-resume-point primitive**: `AsyncSqliteCheckpoints.checkpoint_before_message(key, message_id)` walks the real checkpoint chain (`alist()`, confirmed cumulative message state at every step via direct empirical verification against the installed LangGraph library) to find exactly which checkpoint to resume from to regenerate a specific message's reply; wired at the `LangChainAgentRuntime` level mirroring `fork_thread`'s existing shape. |
| 17 | Session title | Auto-generated and hand-editable slot titles | Header title → click to rename | `pages/ChatSidebar.tsx` | `chat_title.py` | `POST /api/chat/slots/{slot}/generate-title`, `PATCH .../title` | Sessions detail | PRESENT+WORKS | Cycle 64. Manual rename (pre-existing) + new `session_generate_title` MCP tool (session-owned LangChain completion, applies via existing revision-fenced `rename()`) + FE `use-auto-title.ts` hook detecting BOTH mechanical-title shapes (deck placeholder and `ensure_thread`'s first-line fallback). Verified live: real message → real title change ("Capital of France is Paris") on the smoke instance, genuine FE→MCP→LLM→rename round trip. |
| 18 | Session summary | Right-panel rolling summary of the conversation | Chat right panel → **Summary** tab | `pages/chat/SessionSummaryTab.tsx` | `chat_handlers.py` | `GET,POST /api/chat/slots/{slot}/summary` | `components/session/src/factory/session/runtime/summary_generation.py` (`generate_summary`) + `runtime/summary_lifecycle.py` (`set_summary` CAS write) + `mcp/summary.py` (`session_generate_summary`); durable `SessionRecord.summary` field (additive SQLite column + migration); FE `frontends/next-dashboard/components/operations/sessions/{session-detail.tsx (Summary section), use-session-detail-actions.ts (generateSummary), session-actions.ts (generateSessionSummary)}` | TESTING_NEEDED | **Full stack built and unit/FE-tested at every layer; live-browser proof on 18000/13000 pending, and a placement decision is flagged for the owner (below).** Backend mirrors row 17 (auto-title) exactly: durable `summary` field on `SessionRecord` (additive `summary TEXT NOT NULL DEFAULT ''` column + `SESSION_MIGRATIONS` entry, same shape as `tags`/`active_checkpoint_id`), `set_summary` as a revision-fenced `_cas_set` write (new `summary_lifecycle.py` mixin so `lifecycle.py` stays under the 200 LOC ceiling — landed at 198), `generate_summary(model_id, excerpt)` reusing the SAME session-owned `build_session_title_complete` LangChain completion (the session brick stays free of chat-transcript knowledge — the FE supplies the excerpt, exactly like `session_generate_title`), and `session_generate_summary` in its own `mcp/summary.py` module (mirrors `active_checkpoint.py`'s split precedent). Read side is free: the summary is an ordinary field of the session record already returned by `session_get`. **FE placed in the Sessions detail pane, NOT the chat right panel** — a real, disclosed deviation from upstream's reach point: Companion-X has no chat right-side-panel architecture (confirmed cycle 71), so the Summary surface lives where this port already put row 17 (title), row 14 (fork), and tags — the same Companion-X rail that accomplishes the upstream job (see/regenerate a rolling summary) using Companion-X rails rather than copying the layout. The "Generate/Regenerate summary" button loads the real transcript via the existing `loadSessionHistory`/`agent_session_history` primitive, builds a role-tagged excerpt, calls `session_generate_summary` fenced on the session's revision, and renders the returned `session.summary` (with an honest empty state and an empty-transcript guard that refuses to call the tool). Tests: backend 6 new (`test_generate_summary.py` — apply+persist via a real `session_get` re-read, revision-fenced, completion-failure reports `session_summary_generation_unavailable` without writing, foreign-owner fails closed, empty-summary default, and a real cross-restart SQL persistence proof) → full `components/session/test` **93/93** (up from 87), zero regressions, every touched/new file ≤200 LOC. FE 5 new (`session-actions.test.ts` +1: correct tool/excerpt/revision args → `session.summary`; `session-detail.test.tsx` +4, rendered against the REAL `SessionDetail` component: empty state + Generate button, full load-transcript→generate→onChanged round trip asserting the exact built excerpt, existing-summary renders + offers Regenerate, empty-transcript never calls the tool and says so) → full `components/operations/sessions` FE suite **51/51** + `use-auto-title` 7/7 (a shared fixture updated for the new required `summary` field); `tsc` shows only the same pre-existing unrelated errors (ApiError/graph-toolbar/model-picker/copilot-sidebar/feedback-slot-rewind), zero summary-caused. **Remaining before `PRESENT+WORKS`**: (1) a real 18000/13000 browser proof (open a session with a real transcript → Generate → a real LLM summary appears and survives a reload), the same live-proof bar row 17 met; (2) an owner call on whether the Sessions-detail placement satisfies this row or whether it must eventually also live in the chat right panel once that architecture exists. **UPDATE (cycle 8)**: the chat right-side-panel architecture is now scaffolded (`components/chat/chat-side-panel.tsx`), and the Summary now ALSO lives in its proper upstream home — a **Summary tab** in that panel (`components/chat/chat-summary-tab.tsx`, toggled from the chat header), reusing the exact `session_resolve_thread` → `agent_session_history` → `session_generate_summary` flow. So this row now has both surfaces (Sessions-detail interim + chat-panel proper); only the live browser proof remains. |
| 19 | Side chat | A scratch sub-conversation beside the main turn; read-only lookups (file reads, searches, fetches, read-only shell) run there without asking on the kiro-cli backend, changes are refused | Chat right panel → **Side**; `/side` or `/btw` in the composer; **Ask about this** on selected assistant text (main chat, split-view panes, Members threads — the seam is `chat-core/composer/selectionActions.ts`) | `pages/chat/SideChat.tsx`, `pages/chat/SidePanel.tsx`, `pages/members/MembersPage.tsx` (detail drawer's Side Chat view) | `handlers/side.py` | `POST /api/chat/slots/{slot}/side/open`, `.../side/turn`, `.../side/close` | `frontends/next-dashboard/components/chat/chat-side-tab.tsx` + `lib/chat/side-chat.ts` (seam) + registered in `copilot-sidebar.tsx` `panelTabs` | TESTING_NEEDED | **First slice scaffolded per owner ruling (16:20), cycle 9.** Greenfield row (no `side.py`, no prior seam). Shipped the entry-point seam + surface on the existing ChatSidePanel registry: `lib/chat/side-chat.ts` — pure `parseSideCommand` (`/side`·`/btw` detection+strip, ignores `/sidebar`) + `buildAskAboutPrompt` (the "Ask about this" seed, whitespace-collapsed, 500-char capped); and a **Side** tab (`chat-side-tab.tsx`) registered in `panelTabs` — a scratch surface with its own input, command parsing, optional `seed`, and a local scratch transcript. Tests: `side-chat.test.ts` 8/8 + `chat-side-tab.test.tsx` 2/2; `tsc` 8-baseline. **Deferred heavy half (disclosed in-UI + here)**: executing a side turn — read-only lookups run WITHOUT approval, changes REFUSED — needs backend `handlers/side.py` (`side/open`·`turn`·`close`) + a read-only tool-approval policy; and wiring `/side`·`/btw` into the main composer submit + the cross-surface "Ask about this" selection UI. Until then the tab records scratch questions and honestly marks answers pending. **Cycle 10: composer `/side`·`/btw` routing wired.** `routeComposerSubmit` (pure, tested) added to the seam; `makeCollapsibleComposerInput` now takes an optional `onSideCommand` that wraps `CopilotChatInput.onSubmitMessage` — a `/side`·`/btw` submit is diverted to the Side panel (opened + tab-selected + input seeded with the stripped query, main draft cleared) and NOT sent to the main agent; normal input is untouched. Tests: `side-chat.test.ts` 12/12 (+2 routeComposerSubmit), full chat suite 70/70 (composer-collapse 4/4 unregressed); `tsc` 8-baseline. Still deferred: backend side-turn execution + read-only tool policy, and the cross-surface "Ask about this" selection UI. **Cycle 11: "Ask about this" selection entry point wired.** `ask-about-this.tsx` — a selection overlay over the chat transcript (`transcriptRef`): selecting assistant text shows a floating "Ask about this" button that opens the Side panel seeded with `buildAskAboutPrompt(selection)`. Pure `AskAboutThisButton` + thin `useSelectionAnchor` DOM hook. Tests: `ask-about-this.test.tsx` 3/3; `tsc` 8-baseline. **All 3 entry points now land in the Side panel** (composer `/side`·`/btw`, Side tab input, selection "Ask about this"). Only the backend side-turn (`handlers/side.py` + read-only tool policy) + extending selection to split-view/Members remain. **Cycle 12 (headroom AMPLE): backend read-only tool policy foundation built.** `components/mcp_utils/src/factory/mcp_utils/runtime/side_tool_policy.py` — pure classifier grounded in the real EFFECT axis (`_mcp_category`, per `mcp_server.runtime.category_lookup`): `deterministic` tools (reads) are auto-runnable in a side turn; `operational`/`authoring` (the mutating verbs) are refused; uncategorised (native `shell`/`python_repl`) refused conservatively. `classify_side_tool`/`is_side_read_only`/`partition_side_tools` + a truthful refusal reason. Tests: `test_side_tool_policy.py` 5/5 (0.2s). **Scope correction**: split-view panes + Members threads do NOT exist in Companion-X (grep-confirmed), so extending selection there is N/A here, not a gap. Remaining: the side-turn executor (consumes this policy; needs chat-graph integration) + the open/turn/close endpoints. **Cycle 13: read-only enforcement GATE built.** `runtime/side_tool_gate.py::run_side_tool(tool_name, args, *, category_lookup, dispatch)` — the concrete "changes are refused" point: looks up the tool's category, applies `classify_side_tool`, dispatches ONLY read-only (`deterministic`) tools; mutating/uncategorised calls return `SideToolOutcome(ok=False, refusal=…)` and NEVER touch `dispatch` (no-change guarantee is structural). `dispatch` injected → pure/testable. Tests: `test_side_tool_gate.py` 4/4 (+policy suite = 9/9, 0.2s). Remaining: the LLM side-turn executor (NL→tool selection wrapping this gate) + open/turn/close endpoints. **Cycle 14: per-slot conversation lifecycle store built.** `runtime/side_conversation.py::SideConversationStore` — the scratch state the `side/{open,turn,close}` endpoints drive: `open` (idempotent — reopening keeps the transcript), `append` (records a turn; raises if no open side), `get`, `close` (discards, idempotent). In-memory/process-local by design (scratch, not durable). `SideTurn`/`SideConversation` dataclasses. Tests: `test_side_conversation.py` 4/4 (+gate+policy = 13/13, 0.19s). Row 19 backend now has: read-only policy + enforcement gate + conversation lifecycle. Remaining = the LLM turn executor (appends turns via the store, dispatches read-only tools through the gate) + the FastAPI/MCP endpoints wiring these to chat slots. **Cycle 15: side-turn EXECUTOR built (planner injected).** `runtime/side_turn.py::run_side_turn(store, slot, query, *, plan, category_lookup, dispatch)` composes all three primitives: opens the slot's side conversation, records the user turn, runs each planned tool through the gate (read-only dispatched, changes refused + recorded), returns `SideTurnResult(turns, refused)`. The one model-coupled piece — NL query → `[(tool, args)]` — is injected as `plan` (the deferred LLM step), so the orchestration is pure/testable. Tests: `test_side_turn.py` 4/4 (read-only runs+recorded; mutating refused+never dispatched; empty plan = user turn only; mixed partitions) — all four side suites 17/17 (0.21s). **Row 19 backend orchestration is now complete end-to-end minus two clearly-scoped seams**: (1) a real `plan` backed by the chat graph/LLM, (2) the `side/{open,turn,close}` FastAPI/MCP endpoints binding `SideConversationStore` + `run_side_turn` to live chat slots. **Cycle 26: service facade built.** `runtime/side_chat_service.py::SideChatService` is the composition root the endpoints expose — `open`/`turn`/`close`/`is_open` over a held `SideConversationStore`, with `planner` (LLM), `category_lookup` (resolved per turn — bricks load lazily), and `dispatch` (aggregator) all INJECTED so it stays pure/testable. Tests: `test_side_chat_service.py` 4/4; all five side suites 21/21 (0.21s). Now the ONLY remaining seam is transport: a gateway wiring that instantiates `SideChatService` with the real chat-graph planner + `build_category_lookup` + aggregator dispatch and exposes it as the `side/*` endpoints (needs live gateway + the deferred live-proof). **Cycle 28: HTTP transport layer built + tested.** `bases/api/.../runtime/side_routes.py::register_side_routes(app, service_provider)` exposes the exact `POST /api/chat/slots/{slot}/side/{open,turn,close}` contract over an INJECTED `SideChatService` provider (serializes turns + `refused`). Proven with a real `SideChatService` over fakes via FastAPI TestClient: `bases/api/test/.../test_side_routes.py` 3/3 (open→slot+turns, turn→read-only-runs/mutation-refused/3 turns recorded, close→open-state). **The sole remaining seam is now a 2-line gateway startup call** — `register_side_routes(app, lambda: SideChatService(planner=<chat-graph>, category_lookup=build_category_lookup, dispatch=<aggregator>))` — plus the deferred live-proof. Every layer beneath it (policy→gate→store→executor→service→HTTP routes) is built + tested. **LIVE WIRING COMPLETE (owner priority: Side chat first).** Built the LLM planner — `side_llm_complete.py::build_mcp_utils_complete` (mcp_utils' own local twin of `session/runtime/title_complete.py`'s credential-free `ChatProfile` pattern) + `side_planner.py::build_llm_side_planner` (prompts with ONLY read-only tools, parses a JSON plan, empty-plan on any non-JSON/unresolvable/exception). `bases/api/.../side_chat_wiring.py::register_side_chat(app)` composes the real `SideChatService` (ambient `openrouter` model via `SIDE_CHAT_MODEL_ID` env override, fresh `build_category_lookup()` per turn, real `agg.invoke_tool` dispatch) and mounts it via `register_side_routes`; wired into `rest.py`'s startup try/except chain (never fails app boot). No configured model degrades to an empty-plan fallback (200, zero tools) rather than 500. Tests: `side_planner` 8/8 + `side_chat_wiring` 2/2 — all side-* + wiring suites 34/34. **Row 19 is the first flagship with a real server-side seam wired end-to-end** — `POST /api/chat/slots/{slot}/side/{open,turn,close}` runs for real against the live aggregator once mounted at boot. Remaining: live browser proof (deferred) + prompt tuning against real model behavior. |
| 20 | Channel mirroring | Mirror a session into Slack / Discord / other channel | Header channel menu → Link channel | `pages/chat/ChatSettings.tsx` | `chat_mirror.py`, `chat_slack.py` | `POST /api/chat/slots/{slot}/mirror-link`, `.../slack-link`, `GET /api/chat/channel-targets` | — | MISSING | No behaviorally equivalent Session/chat MCP plus UI path was found in the current census. |
| 21 | Voice reply and dictation | TTS on replies; streaming mic transcription | Composer mic; Reply → More actions → Read aloud; Settings → Voice | `components/ChatInput.tsx`, `components/VoicePlaybackNotice.tsx`, `pages/chat/AssistantMessage.tsx` | `chat_voice.py`, `stt_stream.py` | `POST /api/voice/synthesize`, `POST /api/voice/cancel`, `GET /api/voice/system-voices`, `GET /api/ws/stt`, `POST /api/stt/transcribe` | — | MISSING | No behaviorally equivalent Session/chat MCP plus UI path was found in the current census. |
| 22 | Tool approvals | Approve or deny a tool call the agent proposes | Inline card in the transcript | `components/ApprovalCard.tsx` | `handlers/sessions.py` | `GET /api/approvals`, `POST /api/approvals/{id}/{action}` | Inline MCP elicitation card | PRESENT+WORKS | CopilotKit/MCP elicitation renders approve/deny inline and returns the typed boolean decision; confirmation tests pass. |
| 23 | Question cards | Agent asks a multiple-choice question in chat | Inline card in the transcript | `components/PendingQuestionCard.tsx` | `handlers/ask_question.py` | `POST /api/ask-question`, `GET /api/ask-question/pending`, `POST /api/ask-question/{ask_id}/answer` | Direct-MCP elicitation dialog (new) | TESTING_NEEDED | Cycle 68. The real gap was structural, not FE-only: `mcp_utils`'s `ElicitationForm` schema validator (`_ALLOWED_TYPES`) hard-rejected any non-boolean field, so no brick tool could EVER send a multi-choice question through MCP elicitation, regardless of FE support — confirmed via direct reproduction before writing any fix. Backend: `elicitation.py` now also accepts a single required `string` field with a bounded 2-8 option `enum` (mirrors a real `Literal[...]` pydantic field exactly — verified against `ChoiceDTO.model_json_schema()` output, not just a hand-built dict); free-text (`type: string` with no enum) is still rejected, unchanged. FE: `mcp-client.ts`'s `isEnumChoice()` detects the shape and routes it to a new `mcp-question.ts` store (same FIFO/timeout pattern as the existing `mcp-confirmation.ts`) + `McpQuestionHost` dialog (mirrors `McpConfirmationHost`), mounted in `provider.tsx` beside it. Tests: backend 6 new (`test_elicitation_security.py`) + 2 new full-transport end-to-end (`test_native_v2_elicitation.py`, via a new `mrtr_choice_surface` harness — a real `InputRequiredResult` round trip through the actual MCP server, not a mock); mcp_utils suite 252/252. FE 6 new (`mcp-client-v2.test.ts` +4, `mcp-question-host.test.tsx` +3 in its own file, one dropped for cross-file Radix-portal flakiness under `singleFork` — confirmed a test-runner isolation artifact, not a product bug, by re-running without `singleFork` where all 14 pass clean). Guardian unchanged (74). **Marked `TESTING_NEEDED`, not `PRESENT+WORKS`, on purpose**: confirmed via grep that ZERO production brick tools call `ElicitationFormRequest` yet (today it's test-only) — so there is no existing real caller to drive an actual live browser proof of a brick asking a multi-choice question end-to-end. The substrate (backend validation, full MCP transport, FE detection, FE dialog) is real and tested at every layer, but the "in the transcript" live proof needs a first real production caller, which is a separate scoping decision (which brick/tool should be the first), not assumed here. |
| 24 | Files in chat | Browse, attach and upload workspace files | Chat left rail → files; composer attach | `pages/chat/FileBrowserRail.tsx`, `pages/chat/FilesHomePanel.tsx` | `handlers/files.py` | `GET /api/file-read`, `POST /api/upload`, `POST /api/upload/file` | `frontends/next-dashboard/components/chat/chat-files-tab.tsx` (Files tab in the chat side panel), wired in `copilot-sidebar.tsx` | TESTING_NEEDED | **Browse + reference-insert scaffolded per owner steer, homed in the cycle-8 chat side panel.** A **Files tab** lists the workspace via the EXISTING `devtools_list_dir` MCP read (no new backend), navigates into directories (Up control + click-to-enter), and inserts a `@path` reference into the composer draft on file click (the sidebar wires `onInsert` to append to its `draft` state). Tests: FE 2 new (`chat-files-tab.test.tsx` — lists entries + inserts the exact `@README.md ` reference calling `devtools_list_dir` with the path; navigates into a dir) → `tsc` at the 8-error pre-existing baseline. **Deferred/disclosed**: upload (`POST /api/upload*` — no upload MCP surface in this port yet), inline attachment previews, and a left-rail placement (used the side-panel tab, the architecture that now exists, rather than a separate left rail) — plus the standing live proof. The `devtools_read_file` tool is available for a future inline-preview step. |
| 25 | Terminal panel | Two shells on the gateway host: an app-wide docked PTY, and a per-chat terminal whose tab lives in that chat's panel state (opens on the chat's working dir; switches with the session) | Header terminal toggle (docked); chat right panel → **+** menu → **Terminal** (per-chat) | `components/BottomTerminalPanel.tsx`, `pages/chat/SidePanel.tsx` | `handlers/terminal.py` | `POST /api/terminal/sessions`, `GET /api/ws/terminal/{session_id}` | Global bottom dock; status-bar toggle; `?terminal=1` | PRESENT+WORKS | Owner ruling 2026-09-15 05:54 supersedes upstream's two-surface contract: Companion-X exposes exactly one Terminal at the bottom. Per-chat UI/state/thread-cwd modules are deleted. The bottom dock is a real unrestricted login-shell PTY with tabs, stdin/stdout, resize, scrollback/reconnect, completion, saved default shell/font, owner-scoped 12-session cap, and one-live-socket displacement. Reported dead `+`/shell picker traced to a proven threaded `preexec_fn` deadlock that wedged the entire uvloop API; replaced with a fresh post-exec PTY child helper. Live 3000/8000 restarted healthy; exact BFF calls behind `+` and explicit zsh selection each created alive PTYs. StrictMode single-flight ensures one auto-open creates one session. Tests: frontend 34/34, Terminal 14/14, API/lifecycle 11/11. View-only frames: `/tmp/python-factory-evidence/m7-5-single-terminal-live.png` and `/tmp/python-factory-evidence/m7-5-bottom-terminal-final.png`. Gate A.5 `9e633f20` PASS_WITH_NITS with 59 independent tests; stale-comment nit applied. |
| 26 | Browser panel | Live in-panel browser the agent drives; the non-native address bar opens a real website in it; **Annotate** (desktop) picks page elements, takes a note per element in the panel, and adds them to chat with the agent's `eN` refs (`browser:annotate` IPC, `electron/browser-annotate.js`) | Right panel → **Browser** | `components/WebPreviewPanel.tsx` | `handlers/messaging.py` | `GET /api/browser/view`, `POST /api/browser/view/start`, `POST /api/browser/open`, `POST /api/browser/command`, `POST /api/browser/command-result` | `frontends/next-dashboard/components/chat/chat-browser-tab.tsx` + registered in `copilot-sidebar.tsx` `panelTabs` | TESTING_NEEDED | **First slice scaffolded per owner ruling (16:20), cycle 17.** The user-driven half the row names — "the non-native address bar opens a real website in it" — as a **Browser** tab on the ChatSidePanel registry: an address bar (`normalizeUrl` adds https:// to a bare host) loads the site in an in-panel iframe. Tests: `chat-browser-tab.test.tsx` 5/5 (normalizeUrl ×3, address-bar→iframe src, disclosure); `tsc` 8-baseline. **Deferred (needs backend/desktop, disclosed in-UI)**: the AGENT driving the browser (`/api/browser/*` view/command/open contract) and the desktop **Annotate** element-picker (`browser:annotate` IPC) — neither exists in Companion-X; and sites that refuse embedding (`X-Frame-Options`) are disclosed. |
| 27 | App-contributed panel tabs | Side-panel tabs an installed app declares in `contributes.panelTabs`; each mounts the app's own ESM bundle as the tab body through the in-process app host. Only enabled apps contribute, and no app means no rows | Chat right panel → **+** menu → the app's row; on an empty panel, its launcher card. Also under `/members` — the Crew Members side panel is the same `SidePanel`, so an app's row appears in its **+** menu once the member's thread is confirmed | `pages/chat/SidePanel.tsx`, `hooks/panelTabRegistry.ts`, `components/AppHost.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps`, `GET /apps/{name}/ui/{path}` | `frontends/next-dashboard/components/chat/panel-tab-registry.ts` (pure model) | MISSING (foundation slice started) | Flagship row, still largely MISSING (the in-process app host that mounts an app's ESM bundle as a tab body, `contributes.panelTabs` discovery, `/api/apps`). **Cycle 23: the merge model is built** — `panel-tab-registry.ts::mergeContributedTabs(base, contributed)` folds enabled apps' declared panel tabs into the base ChatSidePanel registry (Summary/Pins/Files/Side/Browser): built-in ids always win a collision (a contributed tab can never shadow a core tab), contributions append in stable order, duplicate contributed ids keep the earlier-loaded app. Pure/self-contained (reuses the real `ChatPanelTab` type). Tests: `panel-tab-registry.test.ts` 4/4; `tsc` 8-baseline. Deferred (needs backend): the app host + ESM bundle mounting + `contributes.panelTabs` discovery this merge consumes. **Cycle 33: merge model wired LIVE** — `copilot-sidebar.tsx` now builds `panelTabs` via `mergeContributedTabs(baseTabs, [])`, so the production chat registry consumes the row-27 primitive; once an app host supplies contributed tabs the array flows in with zero further sidebar changes. Chat suite 77/77 unregressed. |
| 28 | Route-history Back / Forward | Browser-style Back and Forward over the dashboard's own route history (desktop layout; on desktop a session switch pushes an entry, so it retraces sessions as well as pages). Each arrow disables when there is nowhere to go (the Forward watermark persists per tab in `sessionStorage` across a reload; it is discarded on a fresh navigation, and a cross-document Back/Forward return (or a BFCache restore) collapses the watermark to the current entry — persisted, so a reload cannot resurrect it — until an in-app navigation re-establishes the frontier — so Forward never leaves the dashboard) and every step asks the page's draft guard before an unarmed pop can unmount typed work | Topbar left cluster → **Back** / **Forward** arrows; `⌘←` / `⌘→` on macOS, `Ctrl+←` / `Ctrl+→` elsewhere (registry ids `history-back` / `history-forward`; left to text fields and terminals, claimed as a no-op on narrow viewports where the arrows are hidden) | `components/NavHistoryArrows.tsx`, `lib/routeHistoryPosition.ts`, `components/NavigationLeaveGuard.tsx` (`useGuardedHistoryStep`), `hooks/useKeyboardShortcuts.ts`, `lib/shortcutRegistry.ts` | client-only (no handler) | none | `frontends/next-dashboard/lib/view-history.ts` (reducer) + `lib/hooks/use-workbench.ts` (history state/wiring) + `components/layout/nav-history-arrows.tsx` (arrows) + `components/canvas/canvas.tsx` (mount) | TESTING_NEEDED | **Core scaffolded per owner steer (feature dev first, validate later).** Companion-X navigates by workbench VIEW switching, not URL routes, so "route history" here is a view-history stack. `view-history.ts` is a pure browser-history-semantics reducer (`pushView` truncates the forward branch + appends, capped at 50; `stepBack`/`stepForward`/`canBack`/`canForward`/`currentView`; push is a no-op when the view is already current). Wired into `use-workbench.ts` at the single view-change seam (`openTab`): every forward navigation records (suppress-guarded); `historyBack`/`historyForward` move the cursor and a sync effect replays the target through `switchView` with recording SUPPRESSED, so replay never corrupts the stack; `canHistoryBack`/`canHistoryForward` drive the arrow disabled states. `NavHistoryArrows` (pure) renders at the left of the Canvas header, consuming the API via `useWorkbenchContext` (single provider instance — no prop-threading through the 3 workbench pages). Tests: FE 8 new (`view-history.test.ts` ×6 — init, push/enable-back, no-op same view, back-then-forward walk, forward-after-back truncation, end no-ops; `nav-history-arrows.test.tsx` ×2 — fire + disabled states) → `tsc` at the 8-error pre-existing baseline; no existing test renders the Canvas shell, so the new context dependency is regression-safe. **Deferred/disclosed**: keyboard shortcuts (⌘←/⌘→ — no shortcut-registry exists in this FE yet), the page draft-guard before an unarmed pop, the per-tab `sessionStorage` Forward watermark across reloads, and the exact Topbar placement (mounted in the Canvas header, which carries workbench context, rather than the global Topbar which does not) — plus the standing live proof. |
| 29 | Notifications | Bell feed of agent-pushed notifications | Topbar bell → `/notifications` | `pages/NotificationsPage.tsx` | `handlers/messaging.py`, `handlers/notifications_push.py` | `GET /api/notifications`, `POST /api/notifications/ack`, `POST /api/notifications/push` | Topbar inbox popover | PRESENT+WORKS | Owner-scoped durable inbox, mark-read, topbar bell, and authorized deep links round-trip through Notification MCP; Option A popover is owner-approved. |
| 30 | Crew Members | One durable pinned DM thread per crew member, with the chat page's tabbed SidePanel docked beside it (permanent, no close control on wide windows; an overlay on narrow ones) whose first tab is the member's Crew summary, followed by the chat panel's pinned Artifacts and Files tabs, with the + menu offering its remaining views (Terminal / Browser / Side chat / Subagents / Workflows / Git, plus Logs / Context in Developer Mode) and any app-contributed panel tabs against the DM slot — all withheld until the thread endpoint confirms the slot, and Changes (pinned on the chat page) / Issues / Links / Pins / session Summary withheld outright (no transcript index feeds them here); the Crew summary folds the member's recent activity by calendar day (counts per day, project, a time strip behind each row, floors when the log is capped), lists the worker sessions the member is driving (live `slots` frames filtered on `created_by`) and the member's auto-patrol status — the nudge loop on its own `member-<slug>` slot (active / stopped with reason / none), with a roster avatar badge only while a loop is ACTIVE (a stopped loop and a never-armed member both show no badge — the stop reason lives in the Crew summary tab); active patrols also list under Wake sources; the roster carries a per-crew star (persisted on the crew record) and, in the sidebar's search row's sort/filter menu, persistent filters — starred-only, live state (working / needs you / unread / patrolling), origin (mine / built-in / from packages) — and a sort (recent activity / name), so the package-installed crews the agent sync writes can be collapsed. The panel's Side tab is the thread's **Side Chat** surface (selection toolbar → Ask about this lands there; its draft persists per slot in the chat-core store) | `/members` — rail row when the crew preview is on; also the sidebar create menu → **Crew Members** (always shown, its own separator group): with the preview on it opens `/members`, with it off it deep-links to Settings → Developer → Feature Previews with the Crew Members card ringed (`settingsPath` + `useSettingHighlight`, id `developer.crew-members`). The open member rides `?member=<name>` (a bare `/members` opens the remembered member, else the first row; below md it stays the roster); select text in a member reply → **Quote** / **Ask about this** | `pages/members/MembersPage.tsx`, `pages/members/rosterFilter.ts` (internal split of MembersPage — the pure filter/sort model over the roster array, no new user-facing feature), `components/listShell.ts` + `components/SearchFilterBar.tsx` (the Sessions sidebar's card / row / search-row recipes, shared so the two lists read as one surface), `pages/members/activityDays.ts` (pure helpers: fold the activity log by local day, label days, cap floors — no feature of its own), `pages/chat/SidePanel.tsx` (`leadingTab`), `hooks/usePanelDocumentActions.ts`, `components/autoNudgeLoop.ts` (shared cycle/countdown readouts) | `handlers/members.py`, `handlers/agents.py` (`starred`), `slot_projection.py` (`created_by`), `handlers/autonudge.py` (registry read), `handlers/side.py` (Side Chat) | `GET /api/members`, `POST /api/members/{slug}/thread`, `GET /api/members/{slug}/activity`, `GET,PUT /api/members/{slug}/rules`, `PUT /api/agents/{name}` (`starred`), `GET /api/autonudge`, `GET /api/ws` (`slots`, `autonudge_state`), `POST /api/chat/slots/{slot}/side/*` | `frontends/next-dashboard/components/operations/members/{roster-filter.ts, activity-days.ts, members-roster.tsx, crew-activity-summary.tsx, members-page.tsx}` + `app/members/page.tsx` | MISSING (navigable surface + full FE layer; backend deferred) | Flagship row, still largely MISSING the BACKEND (member DM threads, activity endpoint, autonudge/slots, docked SidePanel). **FE is now a navigable `/members` route** (cycles 18-31): 2 pure models + 3 presentational surfaces. `roster-filter.ts`+`members-roster.tsx` (facets: starred/live-state/origin/search + recent-activity|name sort, now with row selection). `activity-days.ts`+`crew-activity-summary.tsx` (fold by LOCAL day, per-day+per-project counts, Today/Yesterday/"Mon D", "≥N" capped floor). `members-page.tsx::MembersPage` composes them (pick a member → see their folded activity) with an honest empty state; mounted at `app/members/page.tsx`. Data (`members`/`activityFor`) is injected — the live crew→member mapping + activity endpoint are the deferred seam. Tests: 5 members suites 19/19; `tsc` 8-baseline. Deferred: the members/activity backend, DM threads, docked SidePanel, autonudge/slots wiring. |
| 31 | Channels | Group rooms with several agents in one thread | `/channels` (builtin app surface) | `pages/ChannelPage.tsx` | `handlers_channel.py` | `GET,POST /api/channels`, `POST /api/channels/{id}/messages`, `POST /api/channels/{id}/agents` | `frontends/next-dashboard/components/operations/channels/channel-mentions.ts` + `channel-room.tsx` + `channels-page.tsx` + `app/channels/page.tsx` | MISSING (navigable surface; backend deferred) | Flagship row, still largely MISSING the BACKEND (room/membership, multi-agent turn fan-out). **FE is now a navigable `/channels` route** (cycles 20/30/32). `channel-mentions.ts::parseChannelMentions` (resolve @mentions by id/stripped-name, broadcast when none). `channel-room.tsx::ChannelRoom` (participant chips + live "Goes to: …" addressing preview). `channels-page.tsx::ChannelsPage` composes a channel list + selected room; mounted at `app/channels/page.tsx` with an honest empty state. Data injected → testable. Tests: mentions 6 + room 3 + page 2 = 11/11; `tsc` 8-baseline. Deferred (needs backend): rooms/membership, the multi-agent turn fan-out, and the live channels data source. |
The Notifications surface is registered `hiddenFromNav`: its route and badge
stay wired, but it is entered from the topbar bell rather than a rail row.

## Agent Capabilities

Companion-X home: rail **Agent Capabilities** (single icon, sub-nav inside)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
One destination, pinned to the bottom of the rail, hosting eight tabs. Every tab
is a `?tab=` value on `/capabilities` (`pages/CapabilitiesPage.tsx`).
Any one of those tabs can also be **promoted to its own top-level rail row**, so
a sub-item someone uses daily is one click away instead of two. Each tab is
registered as a `pinnable` surface (`surfaces/builtins.tsx`,
`surfaces/registry.ts`), which keeps it OFF the rail until the user promotes it;
the promoted set is a per-browser preference held under `mc-nav-pinned` and
owned by `lib/navPinned.ts` (the sibling of `lib/appNavHidden.ts`, which does
the same job for app rows in the Apps group). Reach it from the pin control in
the Agent Capabilities page header (`components/PinSurfaceButton.tsx`), which
resolves its subject from the current `?tab=` value. Promotions are capped at
`NAV_PINNED_LIMIT`, and the rail applies the filter in `App.tsx`. No handler and
no endpoint: the preference never leaves the browser.
| 32 | Tab | What it is | Reach it | Page | Handler | Endpoints | — | PRESENT+WORKS | Generated upstream subtable header, not a user-facing capability; excluded from implementation scope. |
| 33 | Crews | Named agent bindings — which agent, model and workspace; per-crew custom avatar (hand-picked ghost traits and an optional per-state preset sound, edited in the crew editor's avatar builder, or an uploaded picture; a crew can also wear an appearance pack, picked on the builder's Library tab — see Crew appearance library). The preset sound is accepted on every tier; the record also accepts per-state ghost reaction `motions` (backend contract only — no picker edits them and no player draws them yet); the definition panel forks a private template copy on first edit (`components/crew/AgentTemplateDetail.tsx`) | `/capabilities?tab=crews` | `pages/KiroCrewAgentsPage.tsx`, `components/CrewAvatarBuilder.tsx`, `components/CrewAvatarLibraryTab.tsx`, `components/CrewAvatar.tsx`, `lib/appearancePacks/` | `handlers/agents.py` | `GET,PUT /api/config/default-agent`, `POST /api/agents`, `PUT,DELETE /api/agents/{name}`, `GET,POST /api/agents/{name}/avatar`, `POST /api/agents/detail/{name}/fork`, `POST /api/agents/detail/{name}/publish`, `POST /api/agents/detail/{name}/reset` | Agent Capabilities → Crews | PRESENT+BROKEN | **Fork/reset built 2026-09-17, closing half this row's gap** (avatar/appearance-pack half stays out — that's row 34's own separate large art-library feature, not built here). Row 35's Agent Templates pane was previously a hard dead end: built-in personas were unconditionally read-only with no path to customize one without directly overwriting the built-in — structurally impossible anyway, since `AgentRegistry.load()`'s own docstring states a firm existing invariant this work deliberately did NOT change ("built-ins WIN on id collision"). Built `agent_fork_agent` (copies any persona — built-in or user — to a NEW id) and `agent_reset_agent` (discards a fork's local edits, re-copying from the fork's recorded source's CURRENT definition, so a fork tracks a built-in that itself changes across releases rather than freezing a snapshot) in `components/agent/src/factory/agent/mcp/authoring.py`. **Provenance design decision**: fork lineage (`<id>` was forked from `<source_id>`) is NOT stored inside the YAML config itself — `AgentConfig` uses `extra="forbid"`, so adding a field there is a schema migration with a much wider blast radius than this row needs — instead it lives in a sidecar `<id>.fork.json` next to the config (`AuthoringManager.read/write/delete_fork_provenance` in `components/agent/src/factory/agent/authoring/manager.py`), read only by fork/reset, invisible to every other config consumer; `delete_yaml_config` now cleans up the sidecar automatically so a deleted fork never leaves orphaned provenance. FE: `agent-templates-view.tsx` gained a "Fork to edit a copy" button on the read-only built-in pane and a "Reset" button on the editable pane (`agent-template-api.ts`'s `forkTemplate`/`resetTemplate`, gated behind a `personaForkAvailable()` tool-presence check mirroring the existing `personaAuthoringAvailable()` pattern). Tests: backend 4 new (`test_agent_fork_reset.py` — provenance round-trip, delete cleans up the sidecar, a reset re-records provenance so a SECOND reset still works, and the same root-confinement guard every other authoring primitive gets) + FE 2 new (fork calls the tool with the right source/new-id pair, reset calls the tool with the selected id) — 888/889 backend (1 pre-existing unrelated flake) + 6/6 FE template suite, zero regressions across the broader 749/753 FE run (the 3 unrelated pre-existing failures are in `crews-navigation`/`frontend-focus-run`/`use-session-list`, confirmed untouched by this change via `git status`). **Still `PRESENT+BROKEN`, honestly**: avatar/appearance-pack UI (this row's OTHER half) is genuinely absent and depends on row 34's whole pack-library system (art + sound cues, its own MISSING row) — not attempted here, a different and much larger slice. Prior evidence: Core crew bindings work inside the unified parent; appearance/avatar/template fork-publish-reset behavior remains absent. |
| 34 | Crew appearance library | The dashboard's own library of appearance packs — art AND per-state sound cues — a crew can wear, rooted at `<data home>/appearance-library/`. Separate from Crew Companion's library: that app is independent and keeps its own packs; the two share only the pack format, so a pack exported from the Companion gallery imports here unchanged. A crew record names a pack as `avatar: {"kind":"pack","id":...}`, and the API can list, read, import, PetDex-fetch and delete packs; deleting a pack a crew wears is refused by name unless forced. The crew editor's avatar builder carries the picker on a **Library** tab: a grid of cards (thumbnail read through the per-slot route, name, author, built-in/custom badge, format), an "Import pack (.json)…" control that posts an exported bundle, and a two-click delete on a custom pack whose 409 names the crews still wearing it (`?force=1` is deliberately not offered). **Crews wear SVG packs only in v1** — the face is an `<img>` and core ships no Lottie or sprite player, so a pack in either format lists greyed and unselectable rather than hidden. A pack may also carry a per-state sound (`sounds` in its manifest, `.mp3`/`.ogg`/`.wav` under 512 KB, typed by sniffing the bytes), served one state at a time from `GET /api/appearances/{id}/sound/{state}` and travelling in the bundle with the art. That route is a backend contract today: nothing in the dashboard plays a pack's audio yet, and the picker does not author it | `/capabilities?tab=crews` → a crew → Edit avatar → Library | `components/CrewAvatarLibraryTab.tsx`, `components/CrewAvatarBuilder.tsx`, `components/CrewAvatar.tsx` (the pack tier), `lib/appearancePacks/library.ts`, `lib/appearancePacks/types.ts` | `handlers/appearances.py`, `dashboard/appearances.py` (crew store), `appearance_packs/store.py` (shared store class), `appearance_packs/transfer.py` (bundle import and PetDex fetch), `appearance_packs/sounds.py` (cue vocabulary and audio sniffing) | `GET /api/appearances`, `GET /api/appearances/{id}`, `GET /api/appearances/{id}/slot/{slot}`, `GET /api/appearances/{id}/sound/{state}`, `POST /api/appearances/import`, `POST /api/appearances/petdex/fetch`, `DELETE /api/appearances/{id}` | — | MISSING | No behaviorally equivalent Agent Capabilities management surface was found in the current census. |
| 35 | Agent Templates | The harness-level agent definitions crews bind to — viewed and edited inline in each agent's Template pane (`components/crew/AgentTemplateDetail.tsx`); the standalone tab was removed | `/capabilities?tab=crews` | `pages/KiroCrewAgentsPage.tsx` | `handlers/agents.py` | `GET /api/agents/installed`, `GET,PATCH /api/agents/detail/{name}`, `POST /api/agents/detail/{name}/fork`, `POST /api/agents/detail/{name}/publish`, `POST /api/agents/detail/{name}/reset` | Agent Capabilities → Agent Templates | PRESENT+WORKS | **Cycle 39 status correction:** this row's own evidence documented a live authenticated create→read→update→registry→delete→absence round-trip that already PASSED and was pixel-reviewed, but the Status field was never flipped off `PRESENT+BROKEN` after that work landed — a stale label, not a real gap. Re-verified before flipping: read `agent-templates-view.tsx` in full (create `+` button, edit form with ID/Name/Description/Model/System-prompt fields, Save, Delete, honest built-in-vs-user-template read-only distinction, authoring-gate awareness — all present and wired to real `agent_create_agent`/`agent_delete_agent`/etc. MCP tools), re-ran its FE suite (4/4) and the backend template/persona suite (27/27), and viewed the referenced evidence screenshot (`/tmp/python-factory-evidence/m7-agent-templates-crud.png` — a real blank create form, matching the code). OWNER 2026-09-15 13:41: Agent Templates is read-only — no create, no edit, no delete ("doesn't do shit… just informational"). Upstream page has create/edit/delete/duplicate. Prior evidence: `/capabilities?tab=agents` lists the merged Agent registry and, when the explicit Agent authoring gate is enabled, performs user-persona list/read/create/update/delete through public MCP (`agent_get_agent_registry`, `agent_list_config_items`, `agent_read_config`, `agent_create_agent`, `agent_delete_agent`). Built-ins remain truthfully read-only; unsupported upstream publish/reset actions are not fabricated. Live authenticated create→read→update→registry→delete→absence round-trip passed and screenshot `/tmp/python-factory-evidence/m7-agent-templates-crud.png` was pixel-reviewed. |
| 36 | Connections | MCP servers: install, sign in, enable, scope tools | `?tab=mcp` | `pages/connections/ConnectionsPage.tsx` | `handlers/mcp.py`, `handlers/connections.py`, `handlers/mcp_discover.py` | `GET /api/mcp`, `GET /api/mcp/discover`, `POST /api/connections/mint`, `POST /api/mcp/custom` | Agent Capabilities → Connections | PRESENT+WORKS | **Cycle 46: found the scroll bug was NOT actually fixed by cycle 32's shell fix — a genuine, deeper regression, re-verified live rather than trusting the prior status label.** Re-checked at the owner's originally reported 900px repro height: the outer scroll boundary's `scrollHeight === clientHeight` (not overflowing at all — no scrollbar could ever appear, matching "loses its scrollbar and its entries" exactly). Traced the full DOM ancestor chain live (not guessed): root cause is `components/canvas/canvas.tsx`'s own top-level flex column (`<div className="flex flex-1 flex-col min-w-0">` — the SHARED wrapper rendered for every Agent Capabilities tab, not Connections-specific) had no `min-h-0`. A flex item's default `min-height: auto` refuses to shrink below its own content size, so this one div silently grew to its tallest child's full content height instead of respecting `flex-1` sizing — breaking the height-clamp chain for every tab and making `main`'s intentional `overflow-hidden` (correct by itself) clip content with no scrollbar anywhere upstream. **Fix: one word, `min-h-0`, added to that one div.** `connections-view.tsx` itself needed zero changes — first attempted an inner scroll-boundary fix there, caught by the EXISTING regression test asserting "plain flow container, not a self-scrolling region" (cycle 32's own deliberate design decision to avoid competing boundaries) — reverted that attempt once live verification showed the real fix belonged upstream in `canvas.tsx` instead, confirmed the single upstream fix alone makes the pre-existing outer boundary genuinely work. **Verified live end-to-end** at the exact 900px repro height: before the fix, scrolling did nothing (stuck at `scrollTop=0`); after, the outer boundary genuinely overflows (`scrollHeight 2870 > clientHeight 800`) and scrolling reaches and visually shows the `workflow` brick (alphabetically last of all 44 registered bricks) — confirmed via a real screenshot, not just computed DOM state. Tests: new `canvas-height-clamp.test.tsx` (1, guards the one-word fix); existing `connections-view.test.tsx`'s 2 tests still pass unchanged (the file itself was reverted to its pre-session state). Guardian unchanged (74); `canvas.tsx` at 119 LOC. Prior evidence (add server / JSON import / enable-disable / reload) still holds: `/capabilities?tab=connections` uses native progressive MCP meta-tools: `list_bricks` lists every deployment power with namespace, health, loaded state, and authoritative tool count; `get_brick_tools` lazily enables an unloaded registered power and returns its admitted tool inventory. Cycle 3–5 (owner P1 (d)): upstream `POST /api/mcp/custom` parity landed — new `connections` brick (consults meta `404a26d5` + security `13978363` applied; Gate A.5 `f8b22e09` PASS_WITH_NITS). "+ Add server" (stdio command/args/env or Streamable-HTTP URL/headers) and "JSON" paste of a standard `{"mcpServers":…}` block; per-server enable/disable, remove (revision CAS), and a gateway-wide **Reload capabilities** meta-tool. Secrets are env-var NAMES only. Real stdio MCP subprocess round-trip test proves env injection by name. Tests: connections 14, meta-tool pins 20, FE 11. |
| 37 | Skills | Installed skills, the public registry, pending candidates | `?tab=skills` | `pages/overview/SkillsTab.tsx` | `handlers/prompts.py`, `handlers/discover.py`, `handlers/skill_budget.py` | `GET,POST /api/skills`, `GET /api/skills/-/discover`, `GET /api/skills/-/pending` | Agent Capabilities → Skills | PRESENT+WORKS | **Cycle 39 continued: delete added, closing the previously-confirmed remaining gap.** New `agent_delete_skill` MCP tool (`skills_tools.py::_delete_skill`, symlink/traversal-safe like read/add — a dedicated test confirms a symlinked "skill" directory can neither be listed, read, nor deleted) + a Delete button in the detail pane, gated by the same authoring capability as Add. **Verified live end-to-end**, not just unit-tested: on the isolated smoke instance with `SUPER_AGENT_ENABLE_AUTHORING_TOOLS=1`, created a real throwaway test skill through the actual UI, confirmed it listed, deleted it through the real Delete button, confirmed it vanished from the list AND its directory was gone from disk (not just hidden client-side) — never touched any of the real production skills already installed on this machine. Enable/disable toggle remains a genuine, disclosed gap: no per-skill enable state exists anywhere in the runtime skill store — a design-scope addition (would need a new persisted policy field, not a quick wire-up), left for its own cycle if the owner wants it; not fabricated as present. Tests: backend 4/4 new + 1 confirmed-unrelated pre-existing Hypothesis flake (CRLF newline normalization in an existing property test), FE 10/10 total (4 new this cycle). Guardian unchanged (74); files 80–173 LOC. Add (+ button) and read were already real (evidence: `/tmp/python-factory-evidence/m7-skills.png`) — the row was undercounted as fully broken due to a stale status label from an earlier cycle. OWNER 2026-09-15 13:44: Skills cannot be added or edited — read-only viewer. Upstream has create/edit/delete/enable toggle. |
| 38 | Knowledge | The document library: sources, sync, entities, graph | `?tab=knowledge` | `pages/knowledge/index.tsx` | `handlers/knowledge.py` | `GET /api/knowledge/items`, `POST /api/knowledge/sources`, `GET /api/knowledge/graph` | Agent Capabilities → Knowledge | PRESENT+WORKS | Cycle 47: built as a thin FE view over the EXISTING KB brick — zero new backend needed, same shape as the Memory browser's own history (`kb_list_documents`/`kb_search`/`kb_ingest`/`kb_delete_document`/`kb_get_collection_stats` all already existed and worked; the gap was purely a missing UI surface). New tab: browse/search (text query switches `kb_list_documents`→`kb_search`, mirroring Memory's list/retrieve split), add-document form (content + optional source), delete, and a stats panel (document count + total size). Entities/graph (the rest of the upstream description) deliberately NOT built as a separate surface — already reachable via the existing Graph canvas once documents are ingested with `extract_entities`, mirroring how row 46 (Explore memory) closed by reusing Graph instead of building a second visualizer; "sources/sync" maps onto KB's existing collection-config authoring tools (`authoring.upsert_collection`/`delete_collection`), not built into this tab since the owner hasn't asked for multi-collection management yet — disclosed, not fabricated. Tests: FE 6 new (`knowledge-view.test.tsx`) + 1 updated (`agent-capabilities-view.test.tsx`'s exact-tab-inventory list). Full canvas+knowledge+memory FE suite 89/89. Guardian unchanged (74); all touched/new files 69–153 LOC. **Verified live end-to-end** on the smoke instance against the REAL KB store (not mocked): the tab correctly showed all 29 real documents (the exact set this session's own M7.7 migration wrote earlier), a real search query correctly returned "No matches" for a genuinely non-matching term (proving real semantic search, not a stub), created a real throwaway "smoke-test" document through the actual ingest form, confirmed it appeared with a real generated UUID, deleted it through the real button, confirmed it vanished — never touched any of the 29 real production documents. |
| 39 | Prompts | Reusable prompt entries from the registry | `?tab=prompts` | `pages/overview/PromptsTab.tsx` | `handlers/prompts.py` | `GET /api/prompts`, `GET /api/prompts/{name}` | Agent Capabilities → Prompts | PRESENT+WORKS | `/capabilities?tab=prompts` discovers all healthy Factory prompt registries through real progressive MCP `list_bricks` → `get_brick_prompts`, searches 115 prompts across 35 registries in the isolated composition, collects declared required/optional arguments, and renders the selected entry through `render_brick_prompt` without loading prompt content before invocation. Partial registry failures remain visible instead of becoming a false empty state. Authenticated BFF initialize→Agent list→`agent_reason` render passed with the supplied task; screenshot `/tmp/python-factory-evidence/m7-prompts.png` was captured and pixel-reviewed via Playwright MCP. Prompt authoring is not fabricated; row 39’s upstream endpoint scope is list/detail. |
| 40 | Steering | Always-injected steering documents | `?tab=steering` | `pages/overview/SteeringTab.tsx` | `handlers/steering.py` | `GET,POST /api/steering`, `PUT,DELETE /api/steering/{key}` | Agent Capabilities → Steering | PRESENT+WORKS | Cycle 41: delete built (`agent_delete_steering` MCP tool, revision-fenced by `expected_sha256` matching the update pattern; `delete_steering` in `steering_documents.py` reuses the same symlink-refused/root-contained/CAS-checked path as create/update) + Delete button wired into the FE detail pane, gated by the same authoring capability as create/update. **Verified live end-to-end** on the smoke instance (18000/13000, `SUPER_AGENT_ENABLE_AUTHORING_TOOLS=1`): created a real "Smoke Delete Test" document through the actual UI, confirmed it listed, clicked Delete, confirmed it vanished from the list AND its `.md` file was gone from disk — never touched any real steering document. Tests: backend 3/3 (round-trip create→update→delete, stale-CAS conflict, not-found), FE 4/4 (delete round-trip, hidden-during-create). Guardian unchanged (74); all touched files 54–133 LOC. Prior evidence (create/edit) still holds: `/capabilities?tab=steering` lists, reads, creates, and edits global Agent steering documents through strict `agent_list_steering`, `agent_read_steering`, `agent_create_steering`, and revision-fenced `agent_update_steering`. Documents live in a configurable Agent-owned store with bounded UTF-8 markdown, slug confinement, symlink refusal, exclusive create, fsync+atomic replace, and SHA-256 CAS. LangChain prompt assembly injects active documents and includes their digest in graph caching so edits apply on the next invocation. Workspace-specific scopes are not fabricated (out of upstream's own scope for this row). |
| 41 | Hooks | Event-triggered agent runs | `?tab=hooks` | `pages/HooksPage.tsx` | `handlers/hooks.py` | `GET,POST /api/hooks`, `PUT,DELETE /api/hooks/{hook_id}`, `GET /api/kiro-hooks` | Agent Capabilities → Hooks tab (Events brick subscriptions) | PRESENT+WORKS | Cycle 42: rebuilt around the real job instead of the event-bus's internal vocabulary. **Key finding**: the backend capability ("run a tool when an event fires") already existed and already worked — `dispatch.py`'s generic fallback (`result = invoker(tool_name, **args)`) treats any handler name that isn't a hardcoded special-case as an arbitrary MCP tool to invoke on the event. Zero new backend plumbing needed; this was purely an honest-UX rebuild. New form: "When" is a real `<select>` of currently-firing agent lifecycle event types (grep-verified against actual `events_publish(event_type=...)` call sites in the agent/workflow/memory bricks — `system.run_started`, `system.run_cancelled`, `workflow.failed`, `user.approved`, `memory.store`, `learning.applied` — not an invented taxonomy), "Run this tool" is a searchable datalist sourced live from `listTools()` (filtered to exclude `events_*` tools to avoid a hook triggering itself), an enable/disable toggle, delete, and a "Last fired"/"No recent firings" line per hook sourced from `events_list_history(event_type=...)`. Existing production hooks using event types outside the curated list (e.g. `graph.projection.requested`, `swarm.completed`) degrade gracefully to showing the raw event type — confirmed live, nothing broke for pre-existing data. **Verified live end-to-end** on the smoke instance (18000/13000): found a real second env-gate along the way — `EVENTS_ENABLE_AUTHORING_TOOLS` is a separate switch from `SUPER_AGENT_ENABLE_AUTHORING_TOOLS`, surfaced by a real "Authoring tools are disabled" error on the first attempt; noted for future smoke sessions needing Hooks authoring. With both set: created a real "smoke-test-hook" (When: Workflow run fails → Runs: `agent_get_capabilities`) through the actual form, confirmed it listed with the correct plain-language event label and tool name, deleted it through the real Delete button, confirmed it vanished — never touched any of the pre-existing real production hooks (freshness/eval/reward dispatch, all rendered correctly throughout). Tests: FE 9/9 new/updated (API: list/save/delete/tool-filter/firings; view: create-via-picker, toggle, delete, no-firings state) — caught and fixed a real `await`-outside-`async` bug in the firings-loading effect during test authoring, before it ever reached a browser. Guardian unchanged (74); touched files 95–122 LOC. |
| 42 | Workflows | Saved dynamic-workflow definitions and their runs | `?tab=workflows` | `pages/overview/WorkflowLibraryTab.tsx` | `handlers/workflows.py` | `POST /api/workflows/run`, `GET,POST /api/workflows/definitions`, `POST /api/workflows/author` | Agent Capabilities → Workflows | PRESENT+WORKS | Cycle 48: built as a thin FE view over the EXISTING, already-complete Workflow brick backend — zero new backend needed, discovered by reading the runtime rather than assuming a gap: `workflow.get_workflow_registry` already lists real saved definitions (loaded from on-disk YAML at process start via `WorkflowRuntime._load_workflows`), `workflow.start_run(workflow_name_or_id=...)` already runs one by id, `workflow.list_runs`/`get_run`/`cancel_run` already complete the lifecycle, `workflow.authoring.upsert_workflow_definition`/`delete_workflow_definition` already create/delete. Built: definitions list with run/delete actions, a create-definition form (raw YAML, gated behind the authoring tool's presence in the live catalog — hidden entirely when disabled, matching Steering's own gating pattern), and a recent-runs panel with cancel. Tests: FE 7 new (`workflow-library-view.test.tsx`) + 1 updated (`agent-capabilities-view.test.tsx`'s exact-tab-inventory). Full canvas+knowledge+memory+workflows suite 96/96. Guardian unchanged (74); all touched/new files 69–165 LOC. **Verified live end-to-end** on the smoke instance: "No workflows yet" correctly rendered (the owner has no saved YAML definitions yet — an honest empty state, not fabricated), and the "Recent runs" panel showed REAL production run history (4 genuine runs: 2 failed migration-import attempts with their actual error `migration_target_failed` surfaced for the first time in any UI, 1 running LangGraph execution with a working Cancel button, 1 succeeded migration import) — confirmed via a real screenshot that spacing/layout render correctly, not just raw DOM text. |
`/agents`, `/mc-agents` and `/connections` are legacy paths that redirect here;
`/knowledge` redirects to `?tab=knowledge`. `/hooks` also stands alone as a
full page.

## Memory, lessons and usage

Companion-X home: Settings → Overview/Usage; Lessons under Agent Capabilities

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
Not a rail destination. The user-facing memory browser is a drill-in under
Settings → Overview; the graph visualizer is a Developer internals view.
The picker separates Global Memory V1 from each member's private V2. Member
creation allocates its empty memory automatically; the editor shows an immutable
binding and links directly to `/settings/overview?view=memory&store=<name>`. Legacy members
offer explicit empty initialization. The member panel scopes facts, directives,
experiences, recall evidence, preferences, projects, carve, retirement and backups
to its store. Copying selected starting knowledge is an explicit owner action
with provenance and no overwrite. Global settings and embedding controls appear
only on the global surface. Global V1's shared editor also keeps the Key/Value/Set
action for creating facts through `PUT /api/memory/semantic`; the form never
appears on a member's private store. An absent `?store=` continues to serve V1.
Private memory cards show the remembered content; their internal keys are in
Memory details. Selection controls occupy a separate row from Find and replace
and Forget. Legacy schedule rows state that the run uses Global Memory V1.
| 43 | Memory browser | Paged facts, rules and experiences; text search, memory-type filters, single correction and bulk preview/apply; preferences, history and diagnostics | `/settings/overview?view=memory` | `pages/overview/MemoryTab.tsx`, `pages/overview/MemoryRecordsEditor.tsx` | `handlers/memory.py`, `handlers/memory_edit.py` | `GET /api/memory/records`, `POST /api/memory/records/refresh`, `POST /api/memory/bulk/preview`, `POST /api/memory/bulk/apply` | Agent Capabilities → **Memory** tab | PRESENT+WORKS | **Cycle 24 (M7.7 scoping consult meta `8382bf2f`):** read path built and real — paged list (`memory_list`), text search (`memory_retrieve`), memory-type filter (short/long/**episodic**, satisfying row 49 in the same slice), stats panel (`memory_stats`), and delete (`memory_delete`) all round-trip through the actual MCP tools regardless of storage backend. **Auto-nudge cycle (backend) — single correction real**: `memory_update` MCP tool + `MemoryStore.update()` duck-typed for the in-memory and graph adapters. **Auto-nudge cycle (FE) — single correction wired end to end**: `use-memory.ts::correct()` + inline edit affordance in `memory-view.tsx`. **Owner answered directly: "Bulk delete of matched memories only."** New `memory_bulk_preview`/`memory_bulk_delete` MCP tools sharing one filter shape (`MemoryRuntime.bulk_match()`/`bulk_delete()`) so a preview count can never disagree with a real delete; FE "Delete matching…" arm-then-confirm control operating over the visible search/type/scope filter. Two real bugs found and fixed while testing: `retrieve()`'s 100-item cap needed clamping for bulk requests above that; `min_relevance=0` (a genuine data-loss hazard) was replaced with the library's real 0.3 default before shipping. Tests: memory brick 347/347 (backend), FE memory suite 31/31. Full `components/operations`/`settings`/`canvas` 277/277. Guardian unchanged. Row is now genuinely closed — read, single correction, and bulk delete are all real, tested, and live-wired, completing Slice 2 of the M7.7 consult's recommended build order. |
| 44 | Store picker | Choose Global Memory V1 or one member's private store | Memory browser → store card | `pages/overview/MemoryStoreCard.tsx` | `handlers/memory_admin.py` | `GET /api/memory/stores` | Built per owner ruling 2026-09-16 06:32 as a scope filter (Own + shared / Shared only) on the Memory tab, backed by `memory_list(metadata={"scope":...})`. Verified live: real network request confirmed sending `{"scope":"shared"}`. | PRESENT+WORKS | Cycle 63; `components/operations/memory/memory-view.tsx`, `use-memory.ts`, `memory-types.ts`; backend `memory_list`/`MemoryRuntime.list_all` gained the `metadata` filter. |
| 45 | Member memory V2 | Browse, correct and bulk-edit private facts, rules and experiences; inspect recall and explicitly copy starting knowledge | Member drawer/editor → Manage memory; `/settings/overview?view=memory&store=<name>` | `pages/overview/MemberMemoryPanel.tsx`, `pages/overview/MemoryRecordsEditor.tsx`, `pages/overview/MemoryDocCard.tsx` | `handlers/memory_member.py`, `handlers/memory_edit.py` | `GET /api/memory/records`, `POST /api/memory/bulk/preview`, `POST /api/memory/bulk/apply`, `GET /api/memory/recall`, `POST /api/memory/seed` | `memory_recall_inspect` MCP tool + Memory tab's "Why this surfaced" panel (new) | PRESENT+BROKEN | Cycle 69 (owner direction 2026-09-16): recall inspection built and closed — real owner/`FOLLOWED_BY`/`similar_to` graph edges with a genuinely recomputed cosine score, degrades honestly (`supported:false`) on non-graph adapters. Bulk-edit and seed-copy explicitly ruled OWNER_DEFERRED by the owner this cycle — not built, not planned unless re-opened. Row stays PRESENT+BROKEN (not WORKS) only because the deferred half is real scope this row's description still names; the built half is fully done and tested. |
| 46 | Explore memory | Group a store's memories by facet and drill into one group | Global memory → Explore memory; member memory → Recovery → Advanced | `pages/overview/MemoryCarveCard.tsx` | `handlers/memory.py` | `GET /api/memory/carve` | Agent Capabilities → Memory tab (type filter) + Analysis → Graph canvas (Memories preset) | PRESENT+WORKS | **Cycle 28 (M7.7, consult `8382bf2f`):** satisfied by composition rather than a dedicated "carve" UI — the Memory tab's type filter (row 43) already groups by facet (short_term/long_term/episodic, and category via the stats panel), and the new Memories graph preset (row 51, same cycle) is the "drill into one group" step: click a memory node to expand its real `HAS_MEMORY`/`FOLLOWED_BY`/`similar_to` relationships. No separate carve-specific page was built; the existing primitives already cover the upstream behavior once memories are real graph nodes (M7.7 Slice 1). |
| 47 | Replaced experiences | List experiences excluded from recall after replacement, and restore one | Global memory → Replaced experiences; member memory → Recovery | `pages/overview/MemoryRetiredCard.tsx` | `handlers/memory_admin.py` | `GET /api/memory/retired`, `POST /api/memory/retired/restore` | `memory_history` MCP tool + Memory tab's "Replaced experiences" history panel (new) | PRESENT+WORKS | Backend substrate (`supersede()`/`history()`) built, tested, and compx-auditor-reviewed cycle 62; PASS_WITH_NITS, all findings fixed. **Auto-nudge cycle — closed**: new `memory_history(memory_id)` MCP tool (mirrors `memory_recall_inspect`'s `supported: false` degradation shape) + `MemoryRuntime.history()` duck-typed delegation. FE: new `history-panel.tsx`/`history-types.ts` (mirrors `recall-inspect-panel.tsx`/`recall-path-types.ts`), a History button in `memory-view.tsx` beside Inspect/Edit/Delete, showing every version of the chain newest-first with Current/Replaced badges. **Deliberately informational only, no restore action**: upstream's "restore one" conflicts with a real owner ruling already recorded in this exact substrate (`graph_supersession.py`: "the curator... replaces memories — never a user-facing restore action") — the same reasoning that closed row 45's bulk-edit as `OWNER_DEFERRED`, applied consistently here rather than contradicted. Every test explicitly asserts no restore control exists. Tests: memory brick 343/343 (3 new backend), FE 28/28 (5 new history-panel tests + 1 memory-view integration test). Guardian unchanged. |
| 48 | Backups | Simple export/import of the whole graph to one file — no staged-restore-on-restart ceremony (owner-narrowed scope, 2026-09-16, replacing upstream's snapshot/stage/cancel-restore flow) | Memory browser → Backup card | `pages/overview/MemoryBackupsCard.tsx` (upstream ref only — replaced) | `handlers/memory_admin.py` (upstream ref only — replaced) | `graph_export`, `graph_import` MCP tools (new) | Memory tab → **Backup card** (new) | PRESENT+WORKS | Cycle 69 (owner direction 2026-09-16). Built at the `graph` brick level (the SAME shared graph memory/kb both write into, M7.7) rather than per-memory-store, since that's the real unit of durable state: `NetworkXGraph.export_snapshot()`/`import_snapshot()` reuse the exact checksummed envelope `persistent_networkx` already durably writes on every mutation — an export is a byte-identical twin of what a restart would reload, not a new format. `PersistentNetworkXGraph.import_snapshot()` overrides to route through the existing `_mutate()` durability path so an import survives restart. Split `networkx_adapter.py`'s run-scoped query methods into a new `NetworkXRunQueriesMixin` (LOC-ceiling fix, same doctrine as the file's own pre-existing `networkx_runs.py` delegation) to make room. New `graph_export`/`graph_import` MCP tools (base64-wire, honest `supported:false` on backends with no graph substrate e.g. neo4j, typed `error` on a corrupt/foreign file — never a silent bad import). FE: new `BackupCard` — Export downloads one file immediately; Import is arm-then-confirm (choose file → confirm "Replace whole graph with this file") since it replaces the WHOLE shared store, not a scoped subset. Tests: backend 4 new adapter-level (`test_graph_export_import.py`, including a real restart-survival proof against a temp blob store) + 4 new MCP-level (`test_backup_mcp.py`, a genuine export→reset→import→refetch round trip through the real tool catalog, not a mock) + 1 pre-existing catalog-count test bumped 32→34; graph suite 289/289. FE 4 new (`backup-card.test.tsx`) — caught and fixed a real environment gap along the way: this project's jsdom has no `File.prototype.text()` (confirmed absent by direct check, not assumed), so the component uses `FileReader` instead, which works identically in real browsers and in the test environment; memory FE suite 18/18. Guardian unchanged (74). |
| 49 | Episodic search | Search past episodic memories | Memory browser → search | `pages/overview/MemoryTab.tsx` | `handlers/memory.py` | `GET /api/memory/episodic/search`, `GET /api/memory/episodic`, `DELETE /api/memory/episodic/{id}` | Agent Capabilities → **Memory** tab (type filter = Episodic) | PRESENT+WORKS | **Cycle 24 (consult meta `8382bf2f`):** built in the same slice as row 43 — the Memory tab's type filter selects `episodic` (a live `memory_type` on the existing model) and its search box calls `memory_retrieve` with that filter; delete works via `memory_delete`. No edit-path gap here (unlike row 43's bulk/correction), so this row is fully parity — list, search, filter, AND delete. |
| 50 | Embeddings | Enable the vector store and pick its model | Memory browser → vector card | `pages/overview/VectorMemoryCard.tsx` | `handlers/memory.py` | `GET /api/memory/embedding-status`, `POST /api/memory/enable-embeddings`, `POST /api/memory/embedding-model` | Agent Capabilities → Memory tab → **Embeddings card** | PRESENT+BROKEN | **Cycle 27 (M7.7, consult `8382bf2f`):** the upstream "enable/pick a model" framing doesn't map onto this architecture honestly — backend (`MEMORY_BACKEND`) and embedder (`MEMORY_LOCAL_EMBED_MODEL`) are BOTH process-start config choices with no live toggle, exactly like Settings → Browser's engine selection (same precedent, `browser.get_engine_status`). Built the honest half: `memory_get_embedding_status` MCP tool (`mcp/deterministic.py`) reports the EFFECTIVE state truthfully — backend, whether the graph adapter is active, `is_semantic` (real local GGUF model vs. keyword-hash fallback), model path, dimensions, and a `change_hint` telling the operator which env vars to set + restart. Required two small plumbing additions to introspect the composed runtime honestly: `MemoryRuntime.adapter` (read-only property; named `adapter` not `store` — collides with the pre-existing `store()` mutation method, a real bug caught by the test suite before it shipped) and `GraphMemoryStore.embedder`/`LlamaCppEmbedder.model_path` accessors. FE: `components/operations/memory/embeddings-card.tsx`, mounted in the Memory tab below the stats panel. **Still BROKEN, not WORKS**: no live "enable" or "pick a model" action exists or can exist without a config-write capability this brick doesn't have (out of scope for this slice) — this row is a truthful status card, not the full upstream control set, so it stays short of parity. Tests: memory brick 311/311 (7 new across `test_embedding_status_and_stats_scoping.py`, extracted into its own file after the additions pushed `test_typed_mcp_boundary.py` over the 200-line Guardian ceiling), FE 13/13. Guardian back to baseline 74 after the extraction (briefly hit 75 mid-cycle, caught and fixed before finishing). |
| 51 | Memory graph | Entity/relation visualizer over the memory store | `/developer?tab=memory` | `pages/overview/MemoryGraphTab.tsx` | `handlers/memory.py` | `GET /api/memory/graph`, `GET /api/memory/observability` | Analysis → Graph canvas → **Memories** preset | PRESENT+WORKS | **Cycle 28 (M7.7, consult `8382bf2f`):** reused the EXISTING general-purpose Graph canvas (`components/canvas/graph-view.tsx`) rather than building a dedicated memory-graph page — it already renders arbitrary entity types via `graph_find_entities`/`graph_get_neighbors`, the same MCP tools the M7.7 `GraphMemoryStore` writes into. Added a "Memories" preset button (`graph-toolbar.tsx`) calling `loadTopology("memory")`; fixed two real display bugs this surfaced: (1) `colorForType`'s `TYPE_COLORS` map had `Memory` capitalized while the adapter writes lowercase `"memory"` — case mismatch fell through to gray `default`, fixed by adding the lowercase key; (2) `deriveGraphNodeDisplayName` had no case for memory nodes, so they'd render their raw uuid — added a `content`-preview case (truncated at 48 chars). Clicking a memory node expands its real `HAS_MEMORY`/`FOLLOWED_BY`/`similar_to` edges via the existing `graph_get_neighbors` path — no new backend needed, the M7.7 substrate already produces real relationships. Tests: `graph-toolbar-memories-preset.test.tsx` (1, new), `graph-node-display.test.ts` (2 new, node:test runner) — both green, plus existing `graph-camera`/`graph-focused-isolation` suites unaffected (2 files, 4 tests). **Scope boundary, disclosed not silently dropped:** did not wire a direct navigation link FROM the Memory tab TO the graph canvas (`syncCanvasRoute("graph")`) — the canvas is view-state-driven via a workbench context provider whose availability from the standalone Memory tab route wasn't verified; the graph itself is real and reachable via the rail's existing Analysis entry point today, just not one click closer from Memory. |
| 52 | Usage | Token and turn usage over time | `/settings/overview?view=usage` | `pages/overview/UsageTab.tsx` | `handlers/usage.py`, `handlers/telemetry.py` | `GET /api/usage`, `GET /api/usage/kiro`, `GET /api/usage/turns` | Settings → Usage | PRESENT+WORKS | Cycle 51: same "backend already exists" pattern as rows 38/42/63/64 — `telemetry.get_metrics_summary` already tracked real cumulative LLM turns/tokens/cost/tool-invocations/errors (`record_llm_interaction`, called from the real chat runtime), just never surfaced anywhere. **Honest scope boundary, disclosed not silently faked**: the real backend is a process-lifetime running total (in-memory dataclass, resets on restart) — NOT a persisted, day-bucketed time series. Building genuine calendar-range history would mean new persisted rollup storage, a real design decision on its own (parallel to why rows 54/66/68 stayed blocked this session). Rather than fake a date-range picker with nothing behind it, shipped the truthful minimum slice: a new Settings → Usage tab showing the real running counters (turns, tokens in/out, estimated cost, tool invocations, last-turn latency, errors) explicitly labeled "running totals since the gateway process started," with the exact reason a date range isn't offered stated in plain text. **Found and fixed a real live bug while verifying**: `callTool`'s own `extractCallBrickToolValue` helper unwraps MCP transport layers but stops one level short of this tool's actual payload — the telemetry brick's `structured_content` field is ITSELF another `{schema_version,ok,data}` wrapper (confirmed via live network inspection of the real `/mcp` response body, not guessed), so a naive `res.result` read crashed with "Cannot read properties of undefined." Fixed by routing through the existing `unwrapToolData` helper (already built to peel exactly this kind of extra wrapper) instead of patching the shared, ~10-call-site `extractCallBrickToolValue` function itself — smaller, correctly-scoped fix, no blast radius on other callers. Tests: 3 new (`usage-settings-panel.test.tsx`: real counters render, truthful unavailable state on tool failure, null-latency guard) + 2 fixed (`settings-view.test.tsx` tab-inventory + `settings-disclosure-panels.test.tsx`, both needed the corrected real-shape `callTool` mock once the bug was found — verifying the fix against the ACTUAL wire shape, not a shape I'd assumed twice). Full canvas+settings suite 123/123; Guardian unchanged (74); both touched files ≤142 LOC. **Verified live end-to-end**: reproduced the real crash on the smoke instance BEFORE the fix (a genuine live regression I introduced, caught before calling the row done — not shipped blind), confirmed the exact response body via live network inspection, applied the `unwrapToolData` fix, reloaded, and confirmed the tab renders the real telemetry snapshot correctly (all zeros on this freshly-started smoke process, which is the honest, correct state — not a fabricated number). |
| 53 | WakaTime coding activity and export | Coding stats for a named range plus project-grouped hours over a date range as a CSV or JSON download, for the productivity view and billable-hours invoicing | `/settings/overview?view=wakatime` | `pages/overview/WakaTimeTab.tsx` | `handlers/wakatime.py` | `GET /api/wakatime/stats`, `GET /api/wakatime/export` | Owner ruling 2026-09-16 06:32: "forget wakatime for now... im not paying for api key and shit." Owner does not use WakaTime and will not pay for a third-party API key. | OWNER_DEFERRED | Owner ruling — do not build, do not re-ask. |
| 54 | Portability | Export and import the whole memory/config bundle | `/settings/imports` | `pages/overview/PortabilityTab.tsx` | `handlers/portability.py` | `GET /api/portability/export`, `POST /api/portability/import`, `POST /api/portability/preview` | Settings → Import & Export → Export a backup / Restore from a backup | PRESENT+WORKS | **Cycles 52-55 (design consult `ecd02cb1`):** split by direction per the consult's recommendation — new `portability` brick owns EXPORT (reads owner-scoped content through each brick's own public tools), `migration` gained a `companion-x-v1` adapter for IMPORT (reuses the existing merge-only, receipt-idempotent apply engine wholesale — proven with a test driving a real `companion-x-v1`-adapter `PlanRecord` through the completely unmodified `apply_plan_page`/`prepare_target_call`). Bundle: single inspectable JSON manifest, 5 allowlisted kinds, two-layer secret exclusion, atomic path-contained writes. **Full export→restore round trip verified live end-to-end against the owner's real data**: Preview export (real counts: memory 0, kb 29, lessons 41, schedules 1, preferences 1) → Write backup file (real 29,748-byte `.cxbundle.json`) → Preview restore reading that same file back (42 eligible: lessons 41, memory 0, schedules 1) → confirm → Start merge-only restore → "Restore succeeded"/"Finished: completed" with every record correctly reported as already-present-and-skipped (not duplicated) — the merge-only idempotency guarantee proven, not just claimed. Along the way found and fixed two real bugs: an error-swallowing bug in the export MCP tools (specific failure codes were collapsed into one generic string, hiding the real cause) and the actual root cause it then revealed — `lessons_list`'s own `limit` field caps at 1000 (unlike memory's uncapped or kb's 10,000-cap), and a blanket `limit=10_000` for every kind silently failed lessons' own validation. Both fixed and regression-tested. **Honest, disclosed, non-blocking gap**: `kb`/`preferences` have no import TARGET yet (`kb_import_record` + a preferences-import tool don't exist — would follow the exact pattern already proven for memory/lessons/schedules) — both the backend preview tool and the FE explicitly say "Knowledge base, Display preferences cannot be restored yet — no import target exists for those kinds" rather than silently omitting or fabricating success; export of those 2 kinds still works fully today (an inspectable, portable snapshot), just not yet auto-restorable. Tests: portability 26 + migration 12 backend, FE 13 (export-api/export-panel/bundle-import-panel) + 2 fixed = 38 backend + 15 FE, all green; Guardian unchanged (74) across every cycle. |
Private-memory boundary coverage lives in `test/test_member_memory_ownership.py`,
`test/test_member_memory_runtime.py`, `test/test_member_memory_algorithm.py`,
`test/test_member_memory_api.py` and `test/test_member_memory_backup.py`.
Pooled member authority is covered by `test/test_mcp_caller.py`,
`test/test_mcp_gateway_recaller.py` and `test/test_mcp_gateway_backend_coverage.py`:
kernel-bound per-call proof issuance, forged caller removal and concurrent
header isolation.
`website/src/test/MemberMemoryIdentity.test.tsx` covers automatic identity and
scheduled-member binding; `MemberMemoryPanel.test.tsx` covers scoped lifecycle
requests, server search, explicit copy limits and partial outcomes, structured
correction (including stable-identity experiences), full detail expansion from
three-line card previews, retired-memory pagination and live refresh, persisted
recovery staging/cancellation, unavailable-store retry, exact member avatars,
empty-member conversation links, and profile drafts across tab/store/browser
navigation and saves in flight.
`MemoryStorePicker.test.tsx` retains the global-versus-member read assertions.
`MemoryRecordsEditor.test.tsx` covers cross-page selection, stale-preview draft
refresh, proposal acceptance/retention, JSON scalar editing and recall query
changes. `website/playwright/member-memory.spec.ts` exercises the real isolated
gateway: member lifecycle, recovery staging, persisted conversations and both
V1/V2 65-record Email batches with concurrent edits and mobile previews.
It also verifies accepting and rejecting conflict proposals in each lineage.

## Schedules and loops

Companion-X home: Agent Capabilities → **Schedules** (no own rail icon — owner decision)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| 55 | Schedule | Cron jobs: recurring agent turns, scripts, commands | `/schedule` — rail **Schedule**; also created inline from the crew editor's "What wakes this crew" section (`/capabilities?tab=crews`) | `pages/SchedulePage.tsx`, `components/CrewWakeSection.tsx` | `handlers/cron.py` | `GET,POST /api/crons`, `DELETE /api/crons/{job_id}`, `GET /api/crons/history` | Agent Capabilities → Schedules | PRESENT+WORKS | **Cycle 17 (consult meta `dab287c6`):** the missing piece was creation — `schedules-view.tsx`'s empty state literally said "Ask Companion X"; there was no `+` control at all (same class of defect as the earlier Connections/Agent Capabilities lesson). Added `schedule-create.tsx` (dialog: agent, task, timing kind — interval/one_shot/cron — with the matching field), wired via `use-schedules.ts::create()` → the EXISTING `scheduler_add` tool, zero backend change (today's fire dispatch is agent-turn only, so an agent-turn-only create form is honest end-to-end). Header "New schedule" button + empty-state CTA, both open the same dialog. Full parity for this kind: list AND create AND edit(pause/resume) AND delete AND trigger. **Scope note (execution axis, not timing):** upstream's "scripts" and "commands" job KINDS are a different axis (what runs, not how often) and are explicitly NOT this row's backend today — `AddScheduleInput.kind` is timing-only. Consult verdict: script kind = OWNER_NA (upstream's `~/.kiro/crew/crons/` Python-function convention has no counterpart substrate here; faking it via Devtools would be a different feature wearing the same label). Command kind = buildable but real work, deferred as slice 2 (would reuse `devtools_run_command`'s scoped project executor — argv not raw shell, 120s bound — needs an execution-axis DTO field + `_run_command` dispatch branch + an owner ruling on bounded-executor vs "real terminal" semantics). Tests: `schedule-create.test.tsx` 5/5 (dialog entry points, interval create round-trip, timing-kind switch + field validation, error keeps dialog open, cancel is a no-op). Schedules suite 28/28; FE operations/settings/canvas 192/192; tsc baseline; Guardian unchanged. UI frame batched per owner ruling. |
| 56 | Cron secret grants | Owner-approved vault-secret env grants for script crons: agent requests via `cron_secret_request`, the owner approves/denies/revokes on the job's Secrets panel | `/schedule` → job → **Secrets** | `pages/SchedulePage.tsx` (`JobSecretsPanel`) | `handlers/cron.py` | `PUT /api/crons/{job_id}/secrets` | — | OWNER_NA | OWNER RULING 2026-09-16 21:20: confirmed N/A — no script-cron kind in Companion-X. Prior: Direct consequence of row 55's ruling: this panel exists upstream ONLY to grant vault-secret env vars to **script crons** — a job kind Companion-X's scheduler does not have (row 55: script kind is OWNER_NA, no `~/.kiro/crew/crons/` substrate). There is no script cron for a secret grant to attach to. The underlying vault primitive already exists and is unrelated to this row: `components/storage`'s `owner_secrets` (write-only-back, no reveal tool) backs Settings → Secrets (row 98, already PRESENT+WORKS). Revisit only if the owner reopens the script-kind ruling. |
| 57 | Template update signal | A job created from a Schedule template records the template's id and prompt snapshot; the job detail panel shows a dismissible hint (naming the template) when that template's prompt has since changed | `/schedule` → job detail | `pages/SchedulePage.tsx` (`TemplateUpdatedNotice`), `utils/schedulePresets.tsx` (`templateUpdate`) | `handlers/cron.py` | `POST /api/crons` (accepts `source_preset`, `source_template_prompt`), `GET,POST /api/crons` | — | MISSING | No behaviorally equivalent Scheduler/loop contract and user surface was found in the current census. **Cycle 18 scope finding: GATED on a missing prerequisite.** Row 57's premise ("a job created from a Schedule TEMPLATE records the template's id + prompt snapshot") requires a Schedule Templates / presets feature that does NOT exist in Companion-X — grep-confirmed zero preset/template refs across the schedules FE (`components/operations/schedules/*`) and no `source_preset`/`source_template_prompt` on the scheduler brick's `ScheduleRecord`. The "update signal" cannot be built standalone; it presupposes the templates themselves (a separate, larger MISSING capability). Not buildable until schedule templates land. |
| 58 | Monitor loops | Same-session bounded monitors and legacy nudge loops watching an external thing; a member's loop is also surfaced read-only in the Crew Members side panel's Crew summary tab | Chat composer → monitor popover; Crew Members → side panel → Crew summary → Auto patrol; agent-armed | `components/SessionAutomationPopover.tsx`, `components/AutoNudgePopover.tsx`, `components/autoNudgeLoop.ts`, `pages/members/MembersPage.tsx` (read-only) | `handlers/autonudge.py` | `GET,POST /api/monitors`, `PATCH /api/monitors/{id}`, `GET /api/monitors/slot/{slot_key}`, `POST /api/monitors/{id}/stop`, `POST /api/monitors/{id}/clear`, `POST /api/monitors/{id}/restart`, `GET,POST /api/autonudge`, `PATCH,DELETE /api/autonudge/{loop_id}`, `POST /api/autonudge/{loop_id}/fire` | Status bar → Monitors popover | PRESENT+WORKS | **Cycle 18 (consult meta `afe02757`):** zero new backend — `workflow.start_loop` already accepted `kind: Literal["goal","monitor"]` with `LoopKind.MONITOR` defined and tested, unused by any UI. Verified session-scoping is real, not assumed: `LoopRecord`'s model validator hard-enforces `initiation_envelope.session_id == origin_thread_id` (`loop_models.py`), so a loop cannot drift off the chat that armed it. `workflow.list_loops` scopes by tenant/owner only (no thread filter), so `monitor-item.tsx` filters client-side on `origin_thread_id === agent.threadId` — load-bearing, not cosmetic. Built `components/layout/monitor-item.tsx`: status-bar popover (Radar icon + active count) beside Subagents, arm form (objective + per-cycle instructions → `workflow.start_loop(kind="monitor", ...)`), live list filtered to this thread's non-terminal monitor loops, Stop per row (`workflow.stop_loop` with `expected_revision`). Current-thread identity resolved via CopilotKit's `useAgent({agentId: "companion_x"}).threadId` — `StatusBar` sits inside the same `CopilotProvider` as the chat surface, so no new prop plumbing was needed (verified by reading `app/layout.tsx`'s provider tree; the consult's claim of an existing `canvas.tsx` thread-prop pattern did not check out on inspection — this was the actually-correct path, found by tracing `ModelPicker`'s own `useAgent` usage). **Deferred (consult ruling):** the Crew Members read-only loop surface — Crews UI has no per-member panel yet to hang it on; building one now would be net-new Crews layout, not this row. Legacy "nudge loop" naming alias also deferred (cosmetic). Tests: `monitor-item.test.tsx` 6/6 (thread filtering excludes other threads, kind/state filtering, arm round-trip, stop with revision, empty state, error state). FE layout/settings/canvas/operations 232/232; tsc baseline; Guardian unchanged. UI frame batched per owner ruling. |
| 59 | Session ledger | Durable per-session work state surviving compaction | Agent-written; no dashboard page | — | `handlers/session_ledger.py` | `GET /api/session-ledger`, `POST /api/session-ledger/record` | — | OWNER_NA | OWNER RULING 2026-09-16 21:20: confirmed N/A — KiroCrew-internal scratch ledger, no product surface. Prior: Consult meta `68c1726c`: KiroCrew-internal agent-tooling convention — the `kirocrew`-agent's own compaction-survival scratch ledger; agent-written, no dashboard page even upstream. Not a Companion-X end-user product feature. Chat working-state durability is served by the memory brick + LangGraph thread checkpointers, not by porting this endpoint. Confirmed no existing substrate satisfies it "for free" either: `LoopRecord`/`LoopCycleRecord` (`workflow/runtime/loop_models.py:44,113`) persist goal/monitor-loop orchestration state keyed by `loop_id`, a different concept from a chat agent's per-session working memory. Same reasoning as row 81 Worktrees / row 56 Cron secret grants. |
| 60 | Work ledger | The shared record between a conductor session and the worker sessions it dispatched: a worker reports schema-bounded status against its own item, and its conductor reads that record plus the acceptance batch it decides from. Used by `kirocrew-conductor` (and its deprecated `kirocrew-ledger-conductor` alias, the same spec under the flow's old name for one release) plus `kirocrew-worker` — `kirocrew-pipeline-conductor` and `kirocrew-security-conductor` deliberately do not mount `kirocrew-work`, because their children report through their own skills' scripts | Agent-written via the `kirocrew-work` MCP tools; no dashboard page yet (Phase 4 adds the Crew page item table) | — | `handlers/work_ledger.py` | `GET /api/work-ledger`, `POST /api/work-ledger/record`, `GET /api/work-ledger/brief`, `POST /api/work-ledger/report` | — | OWNER_NA | OWNER RULING 2026-09-16 21:20: confirmed N/A — Workflow brick is Companion-X's multi-agent dispatch. Prior: Consult meta `68c1726c`: KiroCrew's internal `kirocrew-conductor`/`kirocrew-worker` multi-agent dispatch protocol (agent-written MCP convention, no dashboard page upstream either). Companion-X built its own multi-agent substrate this session — Workflow's durable loop engine (rows 55/58/77), `spawn_background`, and the Subagents rail (row 80) — which serves the equivalent product need. No product-facing gap hides under this row. |
| 61 | Session control | Create / stop / send-to a session from outside it | Agent and app callers, not a UI | — | `session_control.py` | `POST /api/session-control/create`, `.../stop`, `.../send`, `GET .../read` | Session MCP: `session_create`/`session_stop`/`session_steer`/`session_get` | PRESENT+WORKS | **Cycle 20 (consult meta `f2cd0dd5`):** the cancellation mechanism already existed (`LangChainAgentRuntime.cancel(invocation_id)` + a real `except asyncio.CancelledError` path) but was unreachable from outside the turn — `_active` was keyed by the random per-turn `invocation_id`, and an external caller only knows `session_id`(=`thread_id`). Small wiring slice, not a framework fight (LangGraph's own `astream` is already cancelled cooperatively via ordinary `asyncio.Task.cancel()` — no special LangGraph interrupt primitive needed). Added: `runtime/adapters/langchain_active_threads.py` (`ActiveThreadRegistry`: thread_id→{invocation_id} index + `cancel_all`; `cancel_and_drain` helper), wired into `LangChainAgentRuntime.invoke`/`stream`/`close`; `AgentRuntimePort.cancel_thread` on the port; `LangChainChatAgent.cancel(thread_id)` / `ChatAgentPort.cancel`; `runtime/chat.py::cancel_chat_turn(thread_id)` exported from `interface.py`; new deterministic-adjacent operational tool `agent.cancel_turn` (`mcp/cancel_tool.py` + `contracts/cancel_turn.py`). Session brick: new `mcp/session_stop.py` — `session_stop(session_id, expected_revision)` CAS-checks the session's own revision (same semantics as `session_archive`, no store mutation — stopping cancels the turn, it doesn't change session state) then cross-brick-calls `agent.cancel_turn` via the SAME `tool_invoker_for_caller` seam used by `spawn_background`/`list_background_runs` this session — no direct import. Explicitly deferred per consult: mid-tool-call cancellation (only cancels at chunk/tool boundaries — a `CancelledError` inside a side-effecting tool could half-complete), cross-process/worker-turn cancellation (`_active` is process-local; `runtime/managed_cancellation.py`'s loop-owner pattern is the promotion path if spawn/worker turns need it later), and a UI stop button (upstream itself says "agent and app callers, not end-user UI" — no FE work needed for this row). Tests: `test_cancel_running_turn.py` 3/3 (REAL async cancellation with a slow-streaming fake model — cancels the live stream, is thread-scoped and does not touch other threads' turns, reports false when nothing is running); `test_cancel_turn_tool.py` 2/2; session `test_session_stop.py` 5/5 (cross-brick call shape, honest false report, stale-revision rejection, unknown-session rejection, graceful degradation with no invoker registered). Session 56/56; agent 162/162 (incl. fixing one `AgentRuntimePort` protocol-conformance fake). All touched files ≤200 LOC (two were right at the 200-line ceiling with zero slack — extracted `ActiveThreadRegistry` and removed a runtime-layer CAS helper in favor of doing the check once in the MCP layer). Guardian unchanged. |
| 62 | Agent chat egress | Post, edit and delete the bot's own chat messages from an agent turn. For POST and EDIT, a room target must be in the tracked-channel allowlist and a DM must be the owner's resolved DM, and outbound text and blocks pass the display-form redaction floor before reaching the provider. DELETE carries NO target authorization today: it validates only that `channel` and `ts` are present and forwards to the provider, so it reaches any message the bot token can delete (documented gap, not a described boundary) | Agent-written via the `kirocrew-core` messaging MCP tools; no dashboard page | — | `handlers/messaging.py` | `POST /api/send-message`, `POST /api/update-message`, `POST /api/delete-message` | Notification delivery only | OWNER_DEFERRED | OWNER RULING 2026-09-16 21:20: no Slack bot token; deferred to M9 with the rest of channels. Prior: Consult+security pairing meta `d68eb456`: confirmed zero bot-token Slack Web API client anywhere in `components/` — Slack integration today is incoming-webhook only (`components/notification`'s `AppriseBackend`/`apprise.notify()`), which is genuinely one-shot (`notify()` returns `bool`, never a `channel`+`ts` reference) — edit/delete are architecturally impossible on this substrate, not a design gap. This is **configuration-blocked, not product-identity-mismatched**: Companion-X already delivers to external channels, so bot-token messaging is a legitimate future extension, unlike rows 56/59/60/81 which were upstream dev-tooling identity. **Owner decision needed before any code:** does the owner want a Slack BOT TOKEN configured (distinct from the webhooks already supported)? If yes: slice 1 = provider-neutral `messaging.post_message`/`messaging.edit_message` only (POST+EDIT return `{channel,ts}`, the edit handle webhooks can never provide), bot token via the existing `owner_secrets` write-only-back env-name pattern (row 98 exemplar), authorization = tracked-channel allowlist (owner-editable config, not hardcoded) OR DM resolved against the ambient envelope's owner principal (never trust tool-input identity). **DELETE explicitly NOT to be built with upstream's posture** — reproducing an unauthorized "reaches any bot-token-visible message" gap was rejected; DELETE stays OWNER_NA-pending-a-decision unless carrying the same authorization as POST/EDIT. Ledger asks the bot-token question once; does not re-ask. |

## Artifacts

Companion-X home: Agent Capabilities → **Artifacts** (no own rail icon — owner decision)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| 63 | Artifact library | Saved widgets, HTML and documents, versioned | `/artifacts` — rail **Artifacts** | `pages/ArtifactsPage.tsx` | `handlers/artifacts.py` | `GET,POST /api/artifacts`, `GET /api/artifact-folders`, `PATCH /api/artifacts/{slug}/folder` | Agent Capabilities → Artifacts | PRESENT+WORKS | Cycle 49: closed the remaining gap disclosed in cycle 38 — folder-tree navigation and folder-create UI. **Zero new backend needed**: `artifacts_folder_create`/`rename`/`move`/`delete`/`list` and `artifacts_move` (assign an artifact to a folder) all already existed with a complete, tested implementation (`FolderSQLMixin`, real revision-CAS, depth/path tracking) — confirmed by reading the runtime before writing any FE code, same "backend already complete" pattern as rows 38/42 this session. Built a dedicated `FolderSidebar` component: flat depth-ordered list (each folder's own `path`/`depth` from the real model renders as a tree via indentation, no client-side tree-building needed), create/rename/delete inline, and artifact-list filtering by `folder_id` (added to the FE `Artifact` type — was already on the backend model, just never exposed). Tests: FE 2 new (folder listing+filtering, folder creation) + 2 fixed (a genuinely-broken "callTool never called" assertion that the new sidebar's own independent fetch legitimately invalidated, and a missing `folder_id` field on test fixtures). Full artifacts suite 19/19. Guardian unchanged (74); all touched/new files 50–108 LOC. **Verified live end-to-end**: created a real "Smoke Test Folder" through the actual UI, confirmed it listed with a real generated id and 0 item count, clicked into it and confirmed the honest "Empty folder" state (correctly filtered out the two real pre-existing production artifacts, which stayed reachable under "All artifacts"), deleted it through the real button, confirmed it vanished — never touched the real production artifacts or folders. Move-into-folder and rename were built and unit-tested but not separately live-verified this cycle (create/delete/filter cover the same underlying MCP call pattern); disclosed, not silently assumed. |
| 64 | Artifact detail | View, edit, version history, companion chat | `/artifacts/:slug` | `pages/ArtifactDetailPage.tsx` | `handlers/artifacts.py` | `GET,PATCH /api/artifacts/{slug}`, `GET /api/artifacts/{slug}/versions`, `GET /api/artifacts/{slug}/events` | Agent Capabilities → Artifacts detail | PRESENT+WORKS | Cycle 50: closed the "companion-chat iteration pane" gap — but not by building a second, duplicate chat surface. Companion-X already has exactly ONE global, always-visible chat sidebar (unlike upstream KiroCrew's per-page embedded chat); the correct "how" on these rails is making that ONE chat artifact-aware, not cloning it per page. Found the exact mechanism already in use for the same job elsewhere: `CanvasContextBridge`'s `useAgentContext` publishes workbench state into the agent's context every turn (bd-vw04) — applied the identical pattern locally inside `ArtifactDetail`, publishing the open artifact's slug/name/kind/version/description/content (content capped at 4000 chars and marked truncated beyond that, so a large document/widget artifact can't balloon every turn's token budget). Tests: 3 new (`artifact-detail-agent-context.test.tsx`: context shape, truncation, re-publish on artifact change) + 2 pre-existing test files fixed to mock the now-real `useAgentContext` import. Full artifacts suite 22/22. Guardian unchanged (74); `artifact-detail.tsx` at 82 LOC. **Verified live end-to-end with the strongest possible proof for this feature**: opened a real artifact through the actual UI, then asked the REAL chat agent "What artifact am I currently looking at? Just tell me its slug, nothing else." — with zero mention of the artifact's name or slug in the question — and the agent answered correctly: "The slug is live-verification-notes." This is a genuine full-stack round-trip proof (FE context publish → CopilotKit forwardedProps → real LLM turn → correct answer), not a screenshot or a unit test assertion. |
| 65 | Artifact comments | Threaded, anchored comments on an artifact | Artifact detail → select text → comment | `pages/ArtifactDetailPage.tsx` | `handlers/artifacts.py` | `GET,POST /api/artifacts/{slug}/comments`, `PATCH,DELETE .../comments/{comment_id}` | Agent Capabilities → Artifacts detail comments | PRESENT+BROKEN | Threaded comments and moderation round-trip; anchored text-selection comments now work for DOM-rendered kinds (markdown/json/text) — verified live: select text in the preview → "Commenting on" banner appears → post → comment shows the quoted span, orphan-flagged if the anchor text is later edited away. Disclosed, real, honest gap: `widget`/`html`/`svg` kinds render inside a sandboxed iframe (`McpUiFrame`) whose content the parent page cannot read a selection from without a postMessage bridge into the sandboxed content itself — a separate, larger change, not attempted this cycle. Backend: `anchor_text` threaded through the comment model/store/lifecycle/MCP contract (additive column migration, nullable, existing rows unaffected). Tests: artifacts brick 46/46 (4 new), FE 15/15 (5 new). **Cycle 34: the deferred sandboxed-kind bridge is started** — `lib/artifacts/iframe-selection.ts` defines the postMessage protocol that closes the disclosed `widget`/`html`/`svg` gap: `parseSelectionMessage` (parent-side validator — ignores hostile/unrelated messages, collapses whitespace, caps 1000 chars) + `selectionReporterScript` (the mouseup→postMessage reporter injected into the sandbox). Tests: `iframe-selection.test.ts` 5/5; `tsc` 8-baseline. Remaining wiring (deferred): inject the reporter into `McpUiFrame`'s sandboxed srcdoc + a parent listener hook feeding the existing anchor flow. **Cycle 35: parent listener hook built** — `lib/hooks/use-iframe-selection.ts::useIframeSelection(onSelect)` attaches a `window` message listener, validates each via `parseSelectionMessage`, and hands the clean text to the anchor flow (ignores unrelated/malformed; detaches on unmount). Tests: `use-iframe-selection.test.tsx` 3/3; `tsc` 8-baseline. **Only the McpUiFrame srcdoc injection remains** to make the bridge live (inject `selectionReporterScript()` + call `useIframeSelection` from the artifact-detail comment layer). **Cycle 36: bridge WIRED LIVE.** `artifact-preview.tsx` now, for iframe kinds (widget/html/svg), appends `<script>${selectionReporterScript()}</script>` to the sandboxed frame's content AND calls `useIframeSelection(onAnchorSelect)` — so a text selection inside a sandboxed artifact posts up and reaches the SAME `onAnchorSelect` anchor flow the DOM kinds use. Anchored comments now work for EVERY artifact kind. Tests: `artifact-preview.test.tsx` 7/7 (incl. new bridge case: reporter injected + posted selection → onAnchorSelect; containment/sandbox assertions still pass) + `iframe-selection` 5/5 + `use-iframe-selection` 3/3; `tsc` 8-baseline. **Row 65's disclosed iframe-kind gap is closed** (client-side; the only PRESENT+BROKEN row with a buildable-without-backend gap). | |
| 66 | Remote artifacts | Provider-hosted docs browsed and commented in place | `/artifacts/remote/:provider/:externalId` | `pages/RemoteArtifactDetailPage.tsx` | `handlers/artifacts.py` | `GET /api/remote-artifacts/{provider}/browse`, `GET .../{external_id}`, `GET .../comments` | `frontends/next-dashboard/components/operations/artifacts/remote-artifact-ref.ts` (pure model) | MISSING (foundation slice started) | Flagship row, still largely MISSING (provider adapters that browse/fetch/comment on a remote doc, `/api/remote-artifacts/*`, the detail route). **Cycle 25: the reference model is built** — `remote-artifact-ref.ts::parseRemoteArtifactRef(provider, externalId)` validates a (provider ∈ gdocs/gdrive/notion/confluence, safe single-segment externalId — rejects slashes/traversal/spaces) pair and `remoteArtifactPath` builds the canonical `/artifacts/remote/:provider/:externalId` route. Pure/self-contained. Tests: `remote-artifact-ref.test.ts` 4/4; `tsc` 8-baseline. Deferred (needs backend): the provider integrations that fetch + render + comment on a remote doc. **Every non-deferred feature-map row now has at least a tested foundation.** |
| 67 | Publishing | Push an artifact out to a configured provider, then re-check a recorded notice | Artifact detail → share menu, plus the publication banner's "Check again" | `pages/ArtifactDetailPage.tsx`, `components/PublishHub.tsx` | `handlers/artifacts.py` | `GET /api/artifacts/publish-providers`, `POST /api/artifacts/{slug}/publish`, `DELETE .../publish`, `POST .../publish/refresh`, `POST .../publish/reprobe-notice`, `PATCH /api/artifacts/{slug}/sharing` | Artifacts MCP: `artifacts_publish_providers`/`publish`/`publish_refresh`/`get_publication`/`unpublish` | PRESENT+WORKS | **Cycle 23 (consult meta `747358d5`):** "publish to a configured provider" was incoherent without a first concrete provider — the consult's minimal honest slice: a provider-neutral `PublishProvider` port (`runtime/publish.py`: `publish(artifact) -> (external_ref, detail)` / `refresh(external_ref) -> (status, detail)`, `PublishProviderRegistry` name→provider lookup) with ONE trivial in-tree reference adapter (`runtime/adapters/local_publish_provider.py`, provider_id `"local"`) proving the shape end-to-end — real providers (GitHub/Notion) are connector packs added later without touching the port, per doctrine's three-layer tool model. `PublicationNotice` model (`runtime/models.py`) persisted via `PublicationSQLMixin`/`sql_publications.py` (mirrors `sql_comments.py`'s self-contained schema+mixin pattern), orchestrated by `PublishLifecycle` (`runtime/publish_lifecycle.py`) which never fabricates a "published" status the provider didn't actually return — `refresh()` on a provider error reports `unavailable` with the real error, not a stale "still published." 5 MCP tools (`mcp/publish.py` + `mcp/contracts_publish.py`): `artifacts_publish_providers` (@deterministic, lists configured providers), `artifacts_publish`/`artifacts_publish_refresh` (@operational), `artifacts_get_publication`, `artifacts_unpublish`. No provider token crosses the port by value (secrets would resolve by env-var NAME per the `owner_secrets` pattern once a real provider adapter exists). **FE deferred**: a share-menu button that only offers "local" (a self-test adapter, not a real destination) would mislead the owner; the share-menu UI ships once a first real provider is named. Tests: `test_publish.py` 9/9 (registry unknown-provider rejection, local-adapter publish/refresh round-trip, lifecycle persists a truthful notice, unknown-artifact fails closed, refresh never fabricates "published" on a provider error, unpublish removes the notice, MCP round-trip, MCP unknown-provider typed failure). Artifacts brick 42/42; Guardian unchanged; all touched files ≤200 LOC. UI frame deferred (backend-only slice, no owner-visible surface yet). |
| 68 | Deploy | Ship a webapp artifact to a public URL on the user's AWS | `/deploy` (`/artifacts/deploy` redirects) | `pages/ArtifactDeployPage.tsx` | `src/kiro_crew/deploy/handlers.py` | `GET,PUT /api/deploy/config`, `GET,POST /api/deploy/profiles`, `POST /api/deploy/deploy` | — | OWNER_DEFERRED | OWNER RULING 2026-09-16 21:20: deferred to M9 — no host-level AWS deploy inside a local feature; no paid/keyed defaults. Prior: Consult meta `747358d5`: this is a product-identity conflation, not a simple config gap. KiroCrew's `artifact-deploy` skill is HOST-level tooling — it deploys the KiroCrew chat app's OWN artifacts into an AWS account via `~/.kiro/crew/deploy/profiles.json`, gated by the KiroCrew dashboard. A Companion-X PRODUCT Deploy feature means Companion-X's own end users shipping THEIR webapp artifacts to a public URL — different product, different payer, different trust boundary. Reusing KiroCrew's pipeline would hard-couple the engine to its host (doctrine violation: "the engine is built once... zero `if domain ==` anywhere," and analogously zero hard dependency on the specific host running it). **Owner ruling needed before any code:** whose AWS account pays (single owner-configured account vs. per-end-user BYO-account), what base-stack model, and whether public unauthenticated hosting is in scope for Companion-X's product at all — a product+cost decision, not an engineering one. No slice is safe to build until it lands. |

## Apps

Companion-X home: OWNER DECISION PENDING (Agent Capabilities → Apps, or OWNER_NA)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
Third-party and builtin apps that add their own pages, crons and MCP tools.
| 69 | Discover | Browse the app registries and install | `/apps` — **Apps** section header link | `pages/apps/DiscoverPage.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps/registry`, `POST /api/apps/registry/install`, `GET /api/apps/registries` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 70 | Updates | Apps with a newer version available | `/apps/-/updates` | `pages/apps/DiscoverPage.tsx`, `pages/apps/UpdatesList.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps`, `POST /api/apps/{name}/update` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 71 | Library | Installed apps as launchpad tiles, with rail pinning. Lists enabled apps by default; a labelled toggle ("Show N disabled") reveals the disabled ones too, and the choice is remembered per browser in `localStorage` (not synced across devices or origins) | `/apps/library` | `pages/apps/LibraryPage.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps`, `POST /api/apps/{name}/enable`, `POST /api/apps/{name}/disable` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 72 | App detail | One app's manifest, config, permissions, uninstall | `/apps/detail/:name` | `pages/AppDetailPage.tsx` | `src/kiro_crew/apps/routes.py` | `GET /api/apps/{name}`, `GET /api/apps/{name}/manifest`, `GET /api/apps/{name}/config` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 73 | Installed app page | An app's own UI, served by the app | `/apps/:name` | `pages/AppPage.tsx` | app-owned | `POST /api/apps/{name}/token`, `POST /api/apps/{name}/open` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 74 | App migration | Move an app's data after a packaging change | `/apps/migrate/:name` | `pages/MigrationPage.tsx` | `src/kiro_crew/apps/routes.py` | `DELETE /api/apps/{name}/migrate-cleanup` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 75 | Builtin app surfaces | Top-level routes builtin apps claim | `/<app>` via the `/:builtinApp` catch-all | `apps/builtinRegistry.ts` → per-app page | per-app `backend/routes.py` | `POST /api/apps/<app>/...` per app | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
| 76 | App session controls | A compact per-chat control an app contributes to the composer, handed the active session's identity | Chip in the composer bar of any chat, when an enabled app declares `contributes.sessionControls` — no route of its own | `hooks/useSessionControls.ts`, `components/SessionControlHost.tsx`, `components/ChatInput.tsx` (chips), `pages/ChatPage.tsx` (wiring) | `src/kiro_crew/apps/manifest.py` (declaration + validation); the status route is app-owned | `GET /api/apps`, `GET /api/apps/{name}/{statusPath}` (in-gateway) or `GET /apps/{name}/api/{statusPath}` (process-backed) | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "i dont need an app store, we can have a 'brick store' and defer this to later". Re-spec as Brick Store in M9 Deferred features. |
`apps/builtinRegistry.ts` is the path→component table for builtin surfaces
(22 entries: Worlds, Channels, Auto Improvement, Auto Research, AWS Control,
File Explorer, Code Review Sage, Workflows, Dev Fleet, Issue Radar, Meetings,
Papyrus, PPTX Maker, Ops Mission Control, Design Critique, Crew Companion,
Task Runner, MD Notebook, Mochi, Spec Builder, Personal Shopper, Design Tweak).
Adding a builtin surface means an entry there plus `ui.pages` in the manifest —
`App.tsx` needs no change, which is why the router-delta half of the freshness
check cannot see it and the pages-dir half can.

## Task runner and subagents

Companion-X home: Agent Capabilities → Agent Templates (subagents table) + Terminal panel

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| 77 | Task Runner | Autonomous multi-step runs from a spec | `/projects` — **Apps** group rail row | `pages/ProjectsPage.tsx` | `handlers/taskrunner.py`, `handlers_project.py` | `GET,POST /api/projects`, `POST /api/taskrunner/plan`, `POST /api/taskrunner/from-chat` | Agent Capabilities → **Projects** tab | PRESENT+WORKS | **Cycle 14 (consult meta `952b54e8`):** built as a thin FE view over the EXISTING durable Workflow loop engine — zero new backend. The M7.6 loop ledger (`workflow.start_loop`/`list_loops`/`get_loop`/`get_loop_cycle` + pause/resume/stop transitions) is the same engine driving this repo's own autonomous goal loop; a spec's title/body map onto the loop's existing `objective`/`cycle_instructions` fields (no new "start from freeform spec" tool needed — a pure FE mapping problem per the consult). `components/operations/projects/{project-types.ts,project-card.tsx,project-detail.tsx,projects-view.tsx}`, `lib/hooks/use-projects.ts`. Mounted as a new **Projects** tab inside Agent Capabilities (matches the owner's existing IA collapse of Crews/Schedules/Artifacts/Lessons under one icon — no new activity-bar item, no new route). Full parity: create (spec form → `start_loop`), list (`list_loops`), detail (latest settled cycle via `get_loop_cycle`), pause/resume/stop (existing transitions), truthful conflict/deleted/error states (never fabricates success). Tests: `projects-view.test.tsx` 7/7 (start round-trip, empty/error/retry, cycle detail, pause/stop revision fencing, conflict refresh); agent-capabilities tab inventory updated. FE operations/settings/canvas 187/187; tsc baseline; Guardian unchanged (no backend touched). UI frame batched per owner ruling. **Cycle 44 (P1 BLOCK #2 item 8) — reproduced and fixed a real live regression**: the owner's smoke test found the tab showing "Projects unavailable" — reproduced exactly on a live smoke instance (not a stale/theoretical report). Root cause: `workflow/mcp/loop_support.py::authority()` and `runtime/loop_runtime.py::_identity()` both required `session_id` on the ambient envelope for EVERY loop MCP call (`get_loop`/`list_loops`/`get_loop_cycle`/pause/resume/stop), but a page-level dashboard call (the Projects tab isn't inside a chat turn) never carries a `session_id` — only `tenant_id`/`principal_id` are set by `GatewayAccessController.envelope()` for a normal MCP connection. `list_loops`/`get_loop`/`get_loop_cycle`/`transition_loop` all silently discarded the `session_id` value anyway (never used for scoping); only `start_loop` genuinely needs it (as `origin_thread_id`), and validates it separately via its own `resolve_origin()` call. Fixed by relaxing `_identity()`/`authority()` to only require tenant+principal, and adding an explicit, narrower `loop_session_context_required` check inside `start_loop` itself so the write path's real requirement is preserved. Verified: 28 workflow loop tests pass (2 new regression tests: read succeeds without a session, `start_loop` still rejects a session-less call), Guardian unchanged (74). **Verified live end-to-end**: reproduced the exact "Projects unavailable" error on a smoke instance before the fix, confirmed the fix resolves it after a restart — the tab now lists a real existing loop (`m76live31789482921907`, this session's own M7.6 dogfood loop) and its full detail (spec, cycle 0/1, Pause/Stop controls) renders correctly. This bug affected the OWNER'S real production stack too (same code path) — fix is in the working tree, awaiting the batched restart. |
| 78 | Task detail | One run's steps, gates and approvals | `/projects` → a project row | `pages/ProjectDetailPage.tsx` | `handlers_project.py` | `GET /api/projects/{id}`, `GET /api/activities`, `GET,POST /api/comments` | Agent Capabilities → Projects → detail panel | PRESENT+WORKS | **Cycle 14 (same slice as row 77):** `project-detail.tsx` shows objective, spec (cycle_instructions), interval, cycle progress (`lastSettledCycle`/`maxCycles`), terminal reason, and the latest settled cycle's disposition + summary via `workflow.get_loop_cycle` — the generic Workflow run-activity primitives satisfy this row's contract per the consult, no new backend. Upstream's separate comments thread has no counterpart; not claimed. Pause/Resume/Stop are the gates this engine actually exposes. Tests in `projects-view.test.tsx` (cycle detail assertion) + `project-detail.tsx` itself. |
| 79 | Spec refinement | Interactive tightening of a task spec before running | Task Runner → refine | `pages/ProjectsPage.tsx` | `handlers/taskrunner.py` | `GET,POST /api/taskrunner/refine`, `POST /api/taskrunner/refine/answer` | — | OWNER_NA | OWNER RULING 2026-09-16 21:20: confirmed — defer until the owner asks for it. Prior: Consult meta `952b54e8`: the owner never asked for interactive spec tightening; it is a layer on top of row 77 (Task Runner), not a prerequisite. Deferred until requested — revisit if the owner wants it once Projects is in daily use. |
| 80 | Subagents | Background agent runs spawned from a session | Chat activity viewer; rail badge | `pages/chat/ActivityViewer.tsx` | `handlers/messaging.py` | `GET,POST /api/spawn`, `POST /api/spawn/stop-all`, `GET /api/spawn/{agent_id}`, `POST /api/spawn/{agent_id}/steer` | Status bar → Subagents badge/popover | PRESENT+WORKS | **Cycle 15 (consult meta `5602b4f2`):** the badge+popover IS the Activity Viewer — no new page/route, mounted beside the existing Terminal toggle in the status bar (consistent chrome, not invented). One small new deterministic tool was the honest minimum: `workflow.list_runs`'s server-side filter only honors tenant/workflow/status (no kind/origin), but `launch_metadata.kind` (`background_subagent`/`workflow_loop_cycle`) already lands on every background-launched run's `input` (`launch.py:57-65`) — so `agent.list_background_runs` (`components/agent/src/factory/agent/{mcp/list_background_runs_tools.py,mcp/contracts/list_background_runs.py,runtime/background/list_runs.py}`) reads via the SAME in-process invoker seam `launch.py` already uses and filters client-side; zero Workflow backend changes. Cancel/Stop-all needed NO new backend: reuses the existing `workflow.cancel_run(run_id, reason)`. FE: `components/layout/subagents-item.tsx` (10s poll, active-count badge, popover list, per-row Cancel, Stop all iterating cancel over active runs only), mounted in `status-bar.tsx` next to Terminal. Full parity: list (badge+popover) AND per-row cancel AND stop-all — matches upstream's GET/spawn, stop-all, GET/{agent_id} (steer already existed via `steer_background_tool`, unchanged). Tests: `test_list_background_runs.py` 6/6 (kind filter, origin_thread_id filter, active_only, limit, typed errors); `subagents-item.test.tsx` 5/5 (badge count, list, cancel, stop-all-active-only, error state). Agent brick 64/64; FE layout/settings/canvas/operations 221/221; tsc baseline; Guardian unchanged. UI frame batched per owner ruling. |
| 81 | Worktrees | Create a git worktree for a follow-up session | Chat follow-up card → new worktree | `pages/ChatPage.tsx` | `handlers/worktree.py` | `POST /api/worktree/create` | — | OWNER_NA | OWNER RULING 2026-09-16 21:20: confirmed N/A — one operator project root; revisit only for the Rust cockpit if ever. Prior: Consult meta `f13f8094`: Companion-X is a domain-agnostic expert-augmentation product for end users. Its working directory is a single operator-configured project root (`COMPANION_X_PROJECT_ROOT`, default `cwd`, resolved+containment-checked in `workflow/runtime/loop_runtime.py:90-98`; `project_root_digest` is a sha256 admission guard, not a per-session checkout handle) that is not assumed to be — and frequently is not — a git repository; it may point at a knowledge base, a scan target, or nothing filesystem-related. Terminal's `cwd` (`terminal/runtime/session_registry.py:40`) is the same single working directory with no git/worktree awareness. Per-session `git worktree add` is a dev-tooling concept from KiroCrew's own coding-agent dashboard with no coherent counterpart here: no per-session checkout model, no default-branch assumption, no guarantee a repo exists to branch from. Not a stale-description descope — the feature is well-defined upstream, it simply has no honest mapping onto this substrate. Revisit only if the owner explicitly makes Companion-X a git-repo-scoped tool. |

## Settings

Companion-X home: rail **Settings** gear (single icon, full suite inside)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
`/settings/*` is a splat route; `pages/SettingsPage.tsx` parses the trailing
segments itself (`segment[0]` = tab, `segment[1]` = sub-nav). Every row below
is `/settings/<key>`. Panels live in `pages/settings/`.
| 82 | Tab | What it is | Panel | Handler | Endpoints |  | — | PRESENT+WORKS | Generated upstream subtable header, not a user-facing capability; excluded from implementation scope. |
| 83 | `overview` | Health hero, stat cards, memory and usage drill-ins | `OverviewPanel.tsx` → `pages/OverviewPage.tsx` | `handlers_system.py` | `GET /api/status`, `GET /api/system` |  | Settings → Overview | PRESENT+BROKEN | Cycle 8: rebuilt as `components/settings/overview-panel.tsx` — live health **hero** (connected/unreachable/checking, healthy-brick ratio, last-checked time), four **stat cards** with real counts that drill into the owning surface (Tools → Connections, External MCP servers → Connections, Sessions → Sessions, Schedules → Schedules with active count), and the owner's saved Theme/Density/Language read from the durable Display preference (the previous hardcoded “System · dark default” row was untruthful once Display preferences existed) with an Open Display link. Works with or without the workbench provider (`useOptionalWorkbenchContext`). Dead `settings-panels.tsx` removed. FE canvas+settings 106/106; tsc baseline only. **Stays PRESENT+BROKEN honestly**: upstream memory/usage drill-ins need the Memory (M7.7, rows 43–51) and Usage (row 52) surfaces, which do not exist yet; the row flips when M7.7 lands. |
| 84 | `imports` | Import config and history from another tool | `ImportPanel.tsx` | `handlers/onboarding_import.py`, `handlers/portability.py` | `GET /api/onboarding/import/scan`, `POST /api/onboarding/import/apply` |  | Settings → Import & Export | PRESENT+WORKS | `/settings?section=imports` now selects supported Memory/Lessons/Schedules/Markdown inputs, previews a bounded credential-redacted immutable `kirocrew-v1` plan through `migration_preview`, requires explicit confirmation, starts the exact plan through Workflow-owned `migration_start`, and reads typed per-kind counts/terminal reason through `migration_get`; imports are merge-only and schedules arrive paused. A live isolated source/store proof initially exposed and then fixed nested `NativeEnvelopeInvoker` self-deadlock on the two-worker protocol pool by routing trusted nested awaits through the existing internal agent pool. Post-fix authenticated MCP preview→start→get completed one disposable Lesson with status `succeeded`, terminal reason `completed`, 1 imported/0 failed; all temporary data was removed. Screenshot `/tmp/python-factory-evidence/m7-settings-import.png` was captured and pixel-reviewed via Playwright MCP. |
| 85 | `chat` | Chat behavior preferences, plain vs highlighted diffs, selectable model visibility | `ChatPanel.tsx` | `handlers/core.py`, `handlers/files.py` | `GET,PUT,PATCH /api/config/kirocrew`, `GET,PUT /api/dashboard/config` |  | Settings → Chat | PRESENT+WORKS | `/settings?section=chat` reads and revision-CAS updates owner-scoped `plain_diffs` and bounded `hidden_models` through strict `ui_get_chat_preferences` / `ui_update_chat_preferences` MCP tools. UI owns the semantics with in-memory and durable SQLite adapters; owner/tenant isolation, restart persistence, strict ingress, CAS conflict, and Hypothesis transitions pass. Plain mode changes the real docked Terminal between raw and added/removed/hunk highlighting; hidden models remain configured but disappear from the actual chat picker. Isolated authenticated get→update→get persisted revision 1 with plain diffs on and `openrouter` hidden, then survived a full API/Next restart byte-logically. Screenshot `/tmp/python-factory-evidence/m7-settings-chat.png` was captured and pixel-reviewed via Playwright MCP. |
| 86 | `display` | Theme, density, language, terminal (font, default shell, command completion toggle) | `DisplayPanel.tsx` | `handlers/themes.py`, `handlers/core.py` | `GET,POST /api/themes`, `GET,PUT /api/config/theme`, `GET,PATCH /api/config/kirocrew` |  | Settings → Display | PRESENT+WORKS | `/settings?section=display` now revision-CAS persists owner-scoped System/Dark/Light theme and bounded 10–18 px Terminal font size through strict `ui_get_display_preferences` / `ui_update_display_preferences` MCP. A root synchronizer applies theme on every page; the real docked Terminal consumes font size immediately. Shared SQLite storage additively migrates prior Chat-only records and preserves Chat/Display fields across independent updates. Isolated authenticated get→update→get saved Light/16 px/revision 1; Playwright MCP screenshot `/tmp/python-factory-evidence/m7-settings-display.png` shows the light theme and saved controls. Cycle 6: **Density** (Comfortable/Compact — Tailwind v4 `--spacing` root override via `html[data-density]`, so every spacing utility tightens uniformly) and **Language** (BCP-47 select of 12 common tags, any well-formed tag accepted; applied as `<html lang>` for locale formatting and assistive tech; copy states interface text is English for now) added as durable owner-scoped fields on the same Display preference object (strict full-object CAS contract extended; SQLite additive migration proven on pre-density rows). Default shell and completion controls landed with Terminal row 25. Every upstream Display control now round-trips. Backend ui prefs 83 pass; FE settings/layout/terminal 22 pass; tsc baseline only; Guardian unchanged. UI frame batched to the end-of-run verification per owner ruling. |
| 87 | `voice` | TTS voice and dictation engine | `VoicePanel.tsx`, `SttSettings.tsx` | `chat_voice.py`, `handlers/core.py` | `GET,PUT /api/voice/config`, `GET /api/voice/voices`, `GET /api/voice/system-voices`, `GET,PUT /api/config/stt`, `GET /api/stt/status`, `POST /api/stt/ffmpeg/download` |  | Settings → Voice | OWNER_DEFERRED | Owner 2026-09-14 12:39: "we can defer voice/channels (i want these but we can put these at the back since they're features)". Build in M9 Deferred features. |
| 88 | `notifications` | Which events notify, and on which channel | `NotificationsPanel.tsx` | `handlers/messaging.py` | `GET /api/notifications/channels`, `PUT /api/notifications/channels/settings` |  | Settings → Notifications | PRESENT+WORKS | `/settings?section=notifications` combines sanitized read-only workspace channel inventory with owner-scoped event delivery controls. Strict `notification_get_preferences` / `notification_update_preferences` MCP persist global mute, bounded per-kind mute, passive/default/critical overrides, and revision CAS from ambient tenant/principal identity. Enforcement occurs only after durable inbox creation: mute returns `skipped_muted` while preserving inbox truth; priority overrides affect delivery only, leaving immutable stored priority/content digest and replay idempotency unchanged. Live isolated proof showed revision 1→2→3, muted record persisted without send, passive delivered as low while stored priority stayed critical, and revision/override survived a fresh store. Final screenshot `/tmp/python-factory-evidence/m7-settings-notifications-final.png` was pixel-reviewed after correcting footer clearance. Secret-bearing channel YAML is never reconstructed or changed; workspace channel enablement remains intentionally administrator-owned rather than an owner preference. |
| 89 | `shortcuts` | Keyboard shortcut reference and overrides | `ShortcutsPanel.tsx` | `handlers/files.py` | `GET,PUT /api/dashboard/config` |  | Settings → Shortcuts | PRESENT+WORKS | `/settings?section=shortcuts` now mounts a real keyboard reference derived from implemented consumers: Cmd/Ctrl+K opens the command palette, Up/Down moves among focused session rows, and Left/Right/Home/End navigate Settings and Agent Capabilities tabs. Focused Settings tests pass and `/tmp/python-factory-evidence/m7-settings-shortcuts.png` was captured from an isolated 18000/13000 stack and pixel-reviewed with all rows and the limitation disclosure visible. Cycle 7: **overrides landed.** Registry `lib/shortcuts.ts` (actions, normalized `mod+shift+alt+<key>` chords, ⌘/Ctrl → `mod`, `chordFromEvent`/`matchesChord`/`effectiveChord`/`formatChord`); overrides persist as `shortcuts` on the durable owner-scoped Display preference (full-object CAS; strict backend validation of action ids and chords; SQLite additive column). Real consumers: command palette binds its effective chord; new global `ShortcutListener` binds chat (`mod+j`) and Terminal dock (``mod+` ``) toggles to the workbench context, skipping unmodified keys in editable fields. Settings → Shortcuts records a chord per remappable action, refuses conflicts with a named message, resets to default, and lists structural arrow/Home/End keys as fixed. Backend ui prefs 84 pass; FE 75 pass across shortcuts/settings/layout/command/terminal; tsc baseline only; Guardian unchanged. UI frame batched to end-of-run per owner ruling. |
| 90 | `skills` | Skill enablement and context budget | `SkillsPanel.tsx` | `handlers/prompts.py`, `handlers/skill_budget.py` | `GET /api/skills`, `GET /api/skills/-/budget` |  | Settings → Skills | PRESENT+BROKEN | `/settings?section=skills` now provides a clear handoff to the canonical Agent Capabilities → Skills workflow at `/capabilities?tab=skills`, where real `agent_list_skills`, `agent_read_skill`, and authoring-gated `agent_add_skill` MCP support browse/search/read/create without duplicating registry ownership in Settings. The panel explicitly discloses that per-owner enablement and context-budget controls do not exist in the current Agent contracts. Unified Settings tests pass; isolated screenshot `/tmp/python-factory-evidence/m7-settings-skills.png` was pixel-reviewed with the handoff and limitation fully visible. Row remains broken for enablement/budget only. **Cycle 10 (consult meta `740c7a80` AMEND_PLAN applied):** owner-scoped **skill enablement shipped** — `SkillPolicy{disabled_skills, revision}` mirroring the approval-policy rail (separate model/SQLite table in the same DB, revision CAS, ids validated against on-disk skills); typed `agent_get_skill_policy`/`agent_update_skill_policy`; consumer at manifest compile (`compile_agents.owner_enabled_skills`: persona skills minus the ambient owner's disabled set BEFORE `freeze_skills`, so the frozen bundle digest changes and replay evidence shows what attached; no owner/service → no-op). Settings → Skills is now per-skill On/Off switches + browse/create handoff; Agent Capabilities → Skills badges “Off for you”. Backend 53 (policy/store/MCP/manifest + approval/server suites); FE 115 (settings/skills/canvas). **Verified during design: skill text is never injected into the chat model today** (`SkillMaterializer` has no consumers; `AgentConfig.skills` is only frozen for evidence; chat `_graph` attaches none) — so upstream's “context budget” has nothing to cap. Row stays PRESENT+BROKEN pending owner ruling: mark the budget OWNER_NA (Companion-X attaches skills as on-demand files, disclosed in Settings) → PRESENT+WORKS; or build chat skill injection + budget as its own scoped item. |
| 91 | `channels` | Slack, Discord, Telegram, WhatsApp, Teams, and more | `ChannelsPanel.tsx` + one panel per provider | `handlers/messaging.py` | `GET,PUT /api/slack/config`, `GET,PUT /api/discord/config`, `GET /api/slack/manifest` |  | Settings → Channels | OWNER_DEFERRED | Owner 2026-09-14 12:39: "we can defer voice/channels (i want these but we can put these at the back since they're features)". Build in M9 Deferred features. |
| 92 | `browser` | Install playwright-cli, attach token, engine choice | `BrowserPanel.tsx` | `handlers/messaging.py` | `GET,POST /api/browser/install`, `PUT /api/browser/token`, `POST /api/browser/engine` |  | Settings → Browser | PRESENT+WORKS | `/settings?section=browser` now identifies the real strict Browser MCP brick and links to Agent Capabilities → Connections for its admitted launch/navigation/content/interaction/screenshot tools. It documents that interactive page-operation authorization belongs to the dashboard Browser panel's per-session Globe control and that Settings never displays or stores its credential. Exact audit found no Playwright CLI installer, attach-token, or engine-selection contracts; default Browser brick composition uses `MockAdapter` unless a runtime is injected. Unified Settings tests pass; isolated screenshot `/tmp/python-factory-evidence/m7-settings-browser.png` was accessibility- and pixel-reviewed without clipping. Row remains broken for install/token/engine controls. Owner 2026-09-14 12:39: "browser - core, how else do i configure the agents". **Cycle 11 (consult meta `b2256fc8` AMEND_PLAN applied):** root cause fixed — `browser/server.py:_runtime` always composed `MockAdapter`, so agents' browser tools were placeholders in production. **Engine choice** now rides the established composition rail (`FACTORY_BROWSER_ADAPTER=mock|cdp`, exemplar `security/server.py:19`; unknown values fail closed to mock). New tools: `browser.get_engine_status` (@deterministic pure detection: effective engine, Chrome path via existing `_find_chrome`, websockets availability, change hint) and `browser.engine_smoke` (@operational: launch+close one headless session on the active engine, truthful ok/error — split from status per consult because launching Chrome is a side effect). Phantom `playwright` adapter removed from BRICK.yaml/index/capabilities (only mock+cdp exist). Settings → Browser shows the EFFECTIVE engine (per-owner toggle would misrepresent a process-wide adapter), amber warning when mock, readiness rows, a Test engine button, and how to change it. OWNER_NA (consult wording): *Install engine* — Companion-X drives the system Chrome via CDP; there is no playwright-cli to install; replaced by readiness detection + launch smoke. *Attach token* — no KiroCrew-hosted browser engine exists; CDP uses local Chrome, so no token applies (optional future: `cdp_endpoint` for a remote Chrome, secret by env-name). Browser brick 34/34 (strict same-brick contracts incl. new tools); FE settings/canvas 112/112; tsc baseline only; Guardian unchanged. UI frame batched per owner ruling. |
| 93 | `computer-use` | Enable and scope native desktop automation | `ComputerUsePanel.tsx` | `handlers/computer_use.py` | `GET,PUT /api/computer-use/config`, `POST /api/computer-use/invoke` |  | Settings → Computer Use | PRESENT+WORKS | `/settings?section=computer-use` now explains the real host-owned macOS capability and its safety boundaries: the host dashboard owns enablement, password fields are never readable/writable/captured, accessible controls are pointer-safe by default, and pointer-moving actions require explicit permission. Exact repository audit found no Companion X Computer Use config or invoke contract, so no fake toggle or duplicate authorization state was added. Unified Settings tests pass; isolated screenshot `/tmp/python-factory-evidence/m7-settings-computer-use.png` was accessibility- and pixel-reviewed with all safeguards visible. Row remains broken for project-owned enable/scope controls. Owner 2026-09-14 12:39: "computer use --> CORE, agents are worthless if they can't do shit for me". **Cycle 12 (consult meta `a247cdd5` — Option A REUSE, Option B rejected as 23k-LOC bespoke FFI re-implementation violating north star #1/#2):** Computer Use = a desktop-automation MCP server mounted through Connections; every agent reaches `computer_*` tools over the normal MCP rails (ONE approval list, telemetry, scoping). `connections/runtime/computer_use.py` (pure detection: mounted server by `computer_*` tool names, pluggable preset `kirocrew mcp-computer` — upstream launch `src/kiro_crew/agent.py:1141` / `cli.py:2424` — command-on-PATH, macOS check, Accessibility hint), `mcp/computer_use.py` tool `connections_computer_use_status` (@deterministic), runtime now records tool names per mount. Settings → Computer Use: live mounted/not, amber warning while agents have no desktop tools, one-click **Register the KiroCrew computer server** via `connections_add_server` (disabled with reason when `kirocrew` is not on PATH; reports "did not mount" truthfully), Accessibility readiness hint, Manage-in-Connections link. Scope control = the owner's ONE approval list (Security). OWNER_NA (owner's words): upstream's separate allowed/denied-app config — "agents are worthless if they can't do shit for me"; the one approval list is the only gate. NOT NA'd: secure-field refusal + own-dashboard refusal, enforced by the server (`policy.py::check_input_target`). Known coupling (consult): the KiroCrew shim forwards to the KiroCrew gateway, so it requires the KiroCrew app running with Computer Use enabled; preset is pluggable for a self-contained server later. Tests: connections 15/15 incl. REAL stdio mount of a fake `computer_*` server flipping status; FE settings/canvas 115/115; tsc baseline; Guardian unchanged. UI frame batched per owner ruling. |
| 94 | `webhooks` | Inbound webhook tokens and contexts | `WebhooksPanel.tsx` | `handlers/hooks.py` | `GET /api/webhooks`, `POST /api/webhooks/tokens`, `POST /api/webhooks/test` |  | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "remotecrew/webhooks sound super interesting but not core. Defer like features (e.g. slack etc)". Build in M9 Deferred features. |
| 95 | `instances` | Remote Kiro Crew instances to connect to, and the provisioning lanes that create them (built-in EC2 plus edition lanes from the `remote_provisioners` CPP seam) | `RemoteCrewPanel.tsx`, `InstancesPanel.tsx`, `components/remoteProvisionerRenderers.tsx` | `handlers_instances.py`, `handlers_cloud.py` | `GET,POST /api/instances`, `POST /api/instances/{id}/connect`, `GET /api/cloud/provisioners`, `GET /api/cloud/preflight` |  | Settings → Remote Crew | OWNER_DEFERRED | Owner 2026-09-14 12:39: "remotecrew/webhooks sound super interesting but not core. Defer like features (e.g. slack etc)". Build in M9 Deferred features. |
| 96 | `privacy` | Telemetry disclosure and opt-out | `PrivacyPanel.tsx` | `handlers/telemetry.py` | `GET /api/telemetry/collection`, `GET /api/telemetry/beacon` |  | Settings → Privacy | PRESENT+WORKS | `/settings?section=privacy` plainly discloses technical activity that may be recorded (logs, timing, tool/agent activity, model-usage counts), server sampling/field hiding, and operator-owned retention. It explicitly states that this version does not guarantee conversation text or personal data is excluded from logs/traces. Exact audit found no owner collection-status or opt-out contract, so no fake switch was added; users are told they cannot disable collection only for their account. Unified Settings tests pass; isolated screenshot `/tmp/python-factory-evidence/m7-settings-privacy.png` was accessibility- and pixel-reviewed with all disclosures visible. Row remains broken for collection/beacon status and opt-out. **Cycle 13 (consult meta `d84e6417`):** root cause found — `RetentionPolicy()` was instantiated with pure defaults at the only two call sites (`provenance_runtime.py`, `provenance_ingest.py`); no code anywhere composed `sample_rate`/`redact_fields`/retention days from anything but hardcoded literals, so the panel's claim "the operator... can keep only a sample... hide fields... chooses how long data is kept" was FALSE in production (100% sampling, zero redaction, hardcoded 7d/365d, no lever). Fixed by adding the missing composition seam: `telemetry/runtime/policy_config.py` (`compose_retention_policy()` reads `TELEMETRY_SAMPLE_RATE`/`TELEMETRY_REDACT_FIELDS`, `compose_retention_days()` reads `TELEMETRY_RAW_DAYS`/`TELEMETRY_ROLLUP_DAYS`, both fail-closed/clamped on bad input — mirrors `FACTORY_BROWSER_ADAPTER`), wired into both `RetentionPolicy()` sites, `RetentionRuntime.run_retention` defaults, and the `RetentionInput` MCP DTO defaults (the Scheduler's maintenance dispatch calls `telemetry_run_retention` with no args, so the DTO default is where the real lever had to land). New `@deterministic telemetry_get_collection_status` tool (pure) reports `collecting=true` (no per-account opt-out — matches the panel's existing honest disclosure), `sample_rate`, `redacted_field_names` (names only, never values), `raw/rollup_retention_days`, `configurable`. Settings → Privacy now shows this live status alongside the disclosure text. OWNER_NA (consult wording): upstream's `/api/telemetry/beacon` — "Beacon/heartbeat opt-in is upstream-specific; Companion-X telemetry runs as one always-on runtime surfaced by health_check, so there is no separate beacon to expose." Telemetry brick 200/200 (incl. new `test_collection_status.py`: env composition, fail-closed on bad input, clamping, names-only egress); FE settings/canvas 118/118; tsc baseline; Guardian unchanged. UI frame batched per owner ruling. |
| 97 | `security` | Denied commands, sensitive paths, approval posture | `SecurityPanel.tsx`, `PostureDisclosure.tsx` | `handlers/security.py`, `handlers/tailnet.py`, `handlers/file_delivery_consent.py` | `GET /api/security/denied-commands`, `PATCH .../builtins/{id}`, `POST .../user`, `GET,POST,DELETE /api/file-delivery/consent` |  | Settings → Security | PRESENT+WORKS | `/settings?section=security` now includes the owner-ruled single approval list. Agent owns a strict owner-scoped empty-default policy, durable SQLite revision CAS, canonical tool-name validation, and pre-invocation LangGraph interrupts; Settings searches catalog names and writes through `agent_get_approval_policy` / `agent_update_approval_policy`. Listed tools pause through the existing chat approval card; denial prevents invocation, approval resumes once, and empty remains unattended autonomy. Live MCP proved revision 0→1, stale conflict, restart persistence, and browser-owner isolation; selected `devtools_run_command` rendered checked after stack restart. Focused tests cover policy/MCP/runtime 11, real graph resume 1, existing frontend resume 2, mapper 4, panel 2, integration 11; Guardian has zero new MCP violations. Evidence: `/tmp/python-factory-evidence/m7-settings-security-approval-{list,selected}.png`. Existing command/path restrictions and embedded-content allowlists remain disclosed. Row stays broken only because denied-command/sensitive-path editing and file-delivery consent controls are absent. **Cycle 8 ruling application:** the only remaining upstream controls — denied-command and sensitive-path editors — are exactly what the owner's autonomy ruling excludes (north_star Non-negotiables: full autonomy, ONE approval list, no allowlists/denylists). Per the map's own rule (“each section either works or is `OWNER_NA` in the owner's words”), those controls are OWNER_NA and the approval list is the complete, working security posture; the section is disclosed as such. File-delivery consent is likewise covered by the approval list (any tool can be listed). Row is PRESENT+WORKS. |
| 98 | `secrets` | Managed integration credentials plus other encrypted vault entries | `SecretsPanel.tsx` | `handlers/secrets.py` | `GET,POST /api/secrets`, `DELETE /api/secrets/{name}` |  | Settings → Secrets | PRESENT+WORKS | `/settings?section=secrets` lets the owner store an arbitrary named secret, list stored names, and delete by name — matching upstream's write-only-back posture exactly (no reveal/read tool exists anywhere in the stack). New owner-scoped `owner_secrets` data plane added to the `storage` brick (architecture-consulted, `APPROVE_WITH_AMENDMENTS`): AES-256-GCM envelope via a new shared `AeadEnvelopeCodec` (extracted rather than a third copy of the credential_slots seal/open loop), AAD-bound to tenant_id+principal_id+name+key_id, SQLite table keyed by (tenant_id, principal_id, name), last-write-wins (no generation fence — an owner-managed KV has no second authority to invalidate, per the consult's amendment). Public strict MCP: `storage.owner_secret_list` (deterministic, names only), `storage.owner_secret_set` / `storage.owner_secret_delete` (operational). Identity comes only from the ambient envelope (tenant_id/principal_id) — never from tool input — and fails closed on a blank/missing identity rather than defaulting into a shared namespace. 18 backend tests (crypto round-trip, cross-owner isolation, at-rest ciphertext never contains plaintext, fail-closed on missing envelope, no read/reveal method exists) + 5 frontend tests (add/list/delete, no reveal affordance) + full existing storage (230) and settings/canvas (30) suites green. Guardian: zero new MCP/LOC violations. Owner ruling 2026-09-14: "secrets management page sounds core too, nice way to manage secrets." Independent Gate A.5 `0333098b`: `PASS_WITH_NITS` — all 6 security-critical checks and 5 secondary checks confirmed against actual code with tests independently re-executed. Two nits applied: added a direct codec-level AAD-transplant test and dropped a dead parameter. Final: 19 backend owner_secrets tests, 231/232 full storage suite (1 pre-existing unrelated blob-store Unicode flake). No live-browser screenshot this cycle (isolated smoke launch was blocked by shell safety policy on the `rm -rf` cleanup step) — backend+frontend contract tests plus independent security review are the evidence instead. |
| 99 | `developer` | Developer Mode gate, Feature Previews opt-ins (client flags), local-gateway switch | `DeveloperPanel.tsx`, `FeaturePreviewsSection.tsx` | — | — |  | Settings → Developer | PRESENT+BROKEN | `/settings?section=developer` now discloses actual behavior: CopilotKit developer-console visibility is automatic and the footer Web Inspector remains available when supported; service feature flags are configured outside the client; the dashboard API address is selected server-side at startup. Exact audit found no owner Developer Mode, client preview registry/opt-ins, or runtime local/remote gateway switch, so no fake toggles were added. Disclosure tests pass; isolated screenshot `/tmp/python-factory-evidence/m7-settings-developer.png` was accessibility- and pixel-reviewed with all status cards visible. Row remains broken for those user controls. |
| 100 | `releases` | Release channel, update check, changelog | `ReleasesPanel.tsx` | `handlers/updates.py` | `GET /api/update/check`, `POST /api/update`, `GET /api/changelog`, `GET /api/releases` | `frontends/next-dashboard/components/settings/releases-settings-panel.tsx` (Settings → Releases) | TESTING_NEEDED | **Re-spec'd against Companion-X's release story per owner (2026-09-14), cycle 16.** Companion-X on this port has NO auto-update service — the dashboard updates by redeploying the gateway, not an in-app updater — so upstream's release-channel/update-check/changelog endpoints have nothing real to bind to. The honest panel surfaces what IS real: the running dashboard version (`package.json`) + the build identifier (row 101's `NEXT_PUBLIC_BUILD_ID` via `getBuildId`), and discloses that the in-app release-channel/update-check/changelog controls are not part of this deployment (no fake update button). Registered as a new **Releases** section in `settings-view.tsx`. Tests: `releases-settings-panel.test.tsx` 2/2; `tsc` 8-baseline. Deferred (needs backend + a real release story): an actual update-check/changelog service, if Companion-X ever ships a self-updating build. |
| 101 | `about` | Version, build, diagnostics bundle | `AboutPanel.tsx`, `ReportProblemCard.tsx` | `handlers/diagnostics.py`, `handlers/feedback.py` | `POST /api/diagnostics/collect`, `GET /api/diagnostics/download/{filename}` | Settings → About; `frontends/next-dashboard/components/settings/diagnostics-bundle-panel.tsx` | PRESENT+BROKEN | `/settings?section=about` now shows the dashboard package version from `package.json` plus live API health, registered-brick count, and available-tool count through the existing health hook. **Diagnostics bundle built**: a real "Download diagnostics bundle" button composes version + live health (status, registered/healthy capability counts, available tools) + basic runtime context (user agent, dashboard origin, generation timestamp) into a downloaded JSON file — zero new backend endpoint, since every field was already fetched by this same panel's existing `useHealth()` hook (matching row 102's own just-established "host-side file, nothing new stored server-side" pattern). Explicitly never includes conversation content or credentials, disclosed directly under the button. Build identifier and report-problem workflow remain genuinely absent — no fake action was added for either; the panel's disclosure text was updated to name only the two REAL remaining gaps, not the diagnostics bundle that now works. Tests: FE 2 new (`diagnostics-bundle-panel.test.tsx` — a real download call composed from mocked health data, the no-secrets disclosure text is present) + updated `settings-disclosure-panels.test.tsx`'s About assertion (previously asserted "diagnostics bundle... not available", now asserts the real download button exists and only the two genuinely-absent items are disclosed) — 69/69 across the full `components/settings` suite, zero regressions. Row remains broken for build identifier and report-problem only. **Auto-nudge cycle 8 (2026-09-17): build identifier shipped.** `lib/build-info.ts#getBuildId()` reads `NEXT_PUBLIC_BUILD_ID` (Next inlines `NEXT_PUBLIC_*` at build, so the release pipeline sets a git-SHA+timestamp) and falls back to "development" for local/unbuilt runs — an honest reflection of the running build, not a fabricated string. Surfaced as a new "Build" stat card in `about-settings-panel.tsx`; disclosure narrowed to name only the report-problem gap. Tests: `build-info.test.ts` 2/2, `settings-disclosure-panels.test.tsx` About assertion updated (Build card + only report-problem disclosed) 11/11; `tsc` clean. **Only report-problem (needs a backend feedback endpoint) remains.** |
| 102 | — (cross-cutting) | Host-side backup of the browser-held settings the tabs above write, so they survive a moved dashboard port or a relocated Electron `userData`. Restores on a profile that has never reached the host; allowlist-scoped (`DURABLE_PREF_KEYS`), not every browser key | — (no panel; `lib/uiPrefs.ts` is the client) | `handlers/ui_prefs.py` | `GET,PUT /api/ui-prefs` | `components/ui/src/factory/ui/runtime/preference_backup.py` (`export_preferences`/`restore_preferences`); `components/ui/src/factory/ui/mcp/preference_backup.py` (`ui_export_preferences`/`ui_import_preferences`); `frontends/next-dashboard/components/settings/preference-backup-panel.tsx` (Settings → Import & Export → Back up your preferences) | PRESENT+WORKS | **Re-specced against Companion-X's own architecture rather than copying upstream's shape** (per this session's own migration doctrine): upstream keys this off a `DURABLE_PREF_KEYS` browser-localStorage allowlist because upstream's settings live client-side first. Companion-X's equivalent settings (`ChatPreferences`: chat/display/shortcuts/memory-mode) were ALREADY fully durable server-side in SQLite before this row — confirmed by direct code read, not assumed — so "host-side backup" here means exporting/restoring that existing durable record to a file the owner keeps, not inventing a new durable store. Found and confirmed the ONE genuinely client-only setting left (chat panel width, `use-chat-width.ts` localStorage) is cosmetic UI chrome, not a real preference row — correctly out of scope. Built `export_preferences()`/`restore_preferences()` composing the EXISTING `ChatPreferenceStore.update`/`update_display` calls (no new store primitive added — both existing files were already near the 200 LOC ceiling, so a third file `preference_backup.py` holds the composition, matching this session's established extraction doctrine). Restore CAS-fences against the store's CURRENT revision, never the snapshot's own stale revision — verified by a dedicated test proving a restore still succeeds after an intervening edit changed the live revision. New typed MCP tools `ui_export_preferences` (deterministic)/`ui_import_preferences` (operational), mounted in `tools.py` alongside the existing chat/display preference tools. **A real, larger discovery made mid-build, not chased blind**: Companion-X already has an entire, more complete "companion-x-v1" cross-product bundle export/import system (`components/portability`, `components/migration`) with its OWN `preferences` kind — export already calls `ui_get_display_preferences` (a partial subset: theme/density/language/font-size only), but import explicitly disclosed `preferences` as `unsupported_bundle_kinds` in `migration/runtime/parsers/bundle.py`'s own docstring ("no preferences-import target tool exists yet") — this row's new `ui_import_preferences` is now exactly that missing target, but wiring it into the FULL bundle pipeline (`SourceKind` enum, `PreferencesTargetArgs`, `target_prepare.py`'s builder pattern, the bundle parser) is a distinct, larger follow-up deliberately NOT attempted this cycle — this row's own literal scope ("host-side backup... survive a moved dashboard port") is satisfied standalone by the new tools, and half-wiring shared migration infrastructure under tight host memory was the wrong tradeoff. FE lives in Settings → Import & Export (upstream has no dedicated panel either — `lib/uiPrefs.ts` is client-only there too) as a new `PreferenceBackupPanel`: Download preferences (JSON file) / Restore from file, alongside the existing bundle Export/Import panels. **A real jsdom-vs-real-browser gap caught by a test, not shipped blind**: `File.prototype.text()` is unimplemented in this project's jsdom test environment (confirmed via a throwaway diagnostic test before touching the component) — switched to `FileReader.readAsText()`, which works identically in both real browsers and jsdom, rather than special-casing test vs. production code paths. Tests: backend 4 new (`test_preference_backup.py`: full round trip through the real SQLite store, restore-after-intervening-edit CAS proof, MCP-tool-level round trip, ingress rejects duplicate hidden models) + updated the whole-server exact-tool-count boundary test (39→41, the two new tools named explicitly) — 535/535 real `components/ui` tests pass (14 errors are a pre-existing, unrelated `MCP_PERMISSIONS_CONFIG_DIR` environment gap, confirmed absent from every test config in the repo, not caused by this change). FE 4 new (`preference-backup-panel.test.tsx`: real download, real upload-and-restore, truthful error on export failure, truthful error on a malformed restore file) — 67/67 across the full settings test directory (19 files), zero regressions. |
Flagged-file delivery consent (`GET,POST,DELETE /api/file-delivery/consent`,
owner-gated) is listed on the `security` row because that is the tab it belongs
to, but **no panel is wired to it yet** -- the endpoints are the only way to
record or withdraw the grant today (#8793). Stated rather than implied: the
backend control landed first so the delivery gates could read it, and the panel
is a follow-up. Nothing about the grant is reachable from an agent either way;
the record sits on the sandbox-sealed keystone floor.
Instances is deliberately not a rail row: it is set up here once and switched
from the header tab strip. Webhooks carries both a preview flag and
`hiddenFromNav`, so it surfaces as this Settings tab rather than a rail row.
The UI-preference backup row carries no tab because it has no panel and nothing
for a user to reach: it is the mechanism behind the tabs above, listed so its
handler has an owner in this map. Its allowlist is explicit, so a preference
absent from `DURABLE_PREF_KEYS` is still lost on an origin change, and a value
that gates a safety confirmation (`mc-yolo-ack`) is deliberately excluded. The
contract is in
[../system-specs/modules/config.md](../system-specs/modules/config.md).

## Developer

Companion-X home: Settings → Developer

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
`/developer` (`pages/DeveloperPage.tsx`), ten `?tab=` values. Internals views;
not where a user manages their own data. The former `feature-previews` tab moved
to Settings > Developer (`pages/settings/FeaturePreviewsSection.tsx`); the old
`?tab=feature-previews` link redirects there.
| 103 | Tab | What it is | Page | Handler | Endpoints |  | — | PRESENT+WORKS | Generated upstream subtable header, not a user-facing capability; excluded from implementation scope. |
| 104 | `logs` | Live gateway log stream and level control | `pages/LogsPage.tsx` (`LogViewer`) | `handlers/updates.py` | `GET /api/logs`, `GET,POST /api/logs/level` |  | `frontends/next-dashboard/components/settings/logs-panel.tsx` (Settings → Developer) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** `LogsPanel` renders the live tool-event SSE stream (`useLiveToolStream`, no new backend) as a newest-first activity log — timestamp + status glyph + title — with a streaming/idle badge and empty state. Tests: `logs-panel.test.tsx` (activity + empty/idle) + `settings-disclosure-panels` still 11/11; `tsc` at the 8-error baseline. Deferred: raw gateway log-line streaming + the server-side log-level control (`/api/logs*` — no such endpoint in this port yet) + live proof. |
| 105 | `system` | Host runtime, services, sessions, performance | `pages/SystemPage.tsx`, `pages/system/` | `handlers_system.py`, `handlers/session_storage.py` | `GET /api/system`, `GET /api/system/session-storage` |  | `frontends/next-dashboard/components/settings/system-panel.tsx` (Settings → Developer) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** `SystemPanel` reuses `useHealth` (now also exposing `gateway`): live gateway address + status + service census (registered bricks, total tools) + last-checked. Tests: covered in `system-telemetry-panels.test.tsx`; `settings-disclosure-panels` still 11/11; `tsc` at the 8-error baseline. Deferred: per-session/performance metrics (no host-runtime endpoint in this port) + live proof. |
| 106 | `telemetry` | Startup timings and context traces | `pages/TelemetryPanel.tsx` | `handlers/telemetry.py` | `GET /api/telemetry/startup`, `GET /api/telemetry/context-trace` |  | `frontends/next-dashboard/components/settings/telemetry-panel.tsx` (Settings → Developer) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** `TelemetryPanel` reuses `useHealth`'s timeline capabilities (no new backend): live tool-event transport + history backend/persistence, degrading honestly to the reason when unavailable. Tests: `system-telemetry-panels.test.tsx` (live/history + unavailable states); `tsc` at the 8-error baseline. Deferred: startup timings + per-turn context traces (no telemetry endpoint yet — the timeline is the substrate) + live proof. |
| 107 | `storage` | Raw localStorage inspector | `pages/LocalStorageDebug.tsx` | — (client only) | — |  | `frontends/next-dashboard/components/settings/local-storage-inspector.tsx` (mounted in Settings → Developer) | TESTING_NEEDED | **Built per owner steer — client-only, matching upstream's own handler-less design.** `LocalStorageInspector` lists this browser's `localStorage` keys (sorted) with a value preview + count, a refresh, and per-key delete; empty state + SSR guard. Mounted in the existing `DeveloperSettingsPanel` (Settings → Developer) below its status cards. Tests: FE 3 new (`local-storage-inspector.test.tsx` — lists seeded keys/values, deletes a key and removes it from real `window.localStorage`, empty state) + the existing `settings-disclosure-panels.test.tsx` (renders the real Developer panel) still 11/11; `tsc` at the 8-error pre-existing baseline. Essentially feature-complete for a read/prune diagnostic; only the live browser proof (deferred per owner steer) stands between it and `PRESENT+WORKS`. |
| 108 | `mcp-pool` | MCP connection pool state | `pages/settings/McpManagement.tsx` | `handlers/mcp.py` | `GET /api/mcp/active`, `GET /api/mcp/scopes`, `POST /api/mcp/probe` |  | `frontends/next-dashboard/components/settings/mcp-pool-panel.tsx` (mounted in Settings → Developer) | TESTING_NEEDED | **Built per owner steer, reusing the EXISTING `useHealth` poll (no new backend).** `McpPoolPanel` shows the live connection status badge, healthy/total brick counts, total tool count, and a per-brick health list (with the error text when a brick is unhealthy) — the at-a-glance diagnostic for exactly the MCP-401/transport outages seen earlier this session. Mounted in the existing Settings → Developer panel. Tests: FE 2 new (`mcp-pool-panel.test.tsx` — healthy state shows status/counts/bricks; unhealthy state surfaces a brick's error) + `settings-disclosure-panels.test.tsx` still 11/11; `tsc` at the 8-error pre-existing baseline. **Deferred**: the active/scopes/probe controls (`/api/mcp/*` — a richer management surface than the read-only health view this ships) + the live proof. |
| 109 | `memory` | Memory graph visualizer | `pages/overview/MemoryGraphTab.tsx` | `handlers/memory.py` | `GET /api/memory/graph` |  | `frontends/next-dashboard/components/settings/memory-stats-panel.tsx` (Settings → Developer) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** `MemoryStatsPanel` reads the existing `memory_stats` MCP tool (no new backend): total-memory count + by-type breakdown, with an honest unavailable state. Tests: `memory-stats-panel.test.tsx` (stats + error) + `settings-disclosure-panels` 11/11; `tsc` at the 8-error baseline. Deferred: the interactive node/edge graph visualization (heavy viz; the Memory operations surface owns browse/inspect) + live proof. |
| 110 | `config` | Raw Kiro Crew and agent config editors | `pages/overview/KiroCrewCfgTab.tsx`, `AgentCfgTab.tsx` | `handlers/core.py`, `handlers/agents.py` | `GET,PUT,PATCH /api/config/kirocrew`, `GET,PUT /api/agent/config` |  | `frontends/next-dashboard/components/settings/config-panel.tsx` (Settings → Developer) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20) as the honest read-only equivalent** — Companion-X deliberately doesn't surface raw-config editing (owner ruling: settings, not raw config). `ConfigPanel` reuses `useHealth`+`useModelCatalog` to show resolved runtime config (gateway, status, model providers/count) with an explicit "set via env/config brick, not editable here" note. Tests: `config-agent-backend-panels.test.tsx`; `settings-disclosure-panels` 11/11; `tsc` at the 8-error baseline. Deferred: raw config EDIT surfaces (intentionally out of scope) + live proof. |
| 111 | `agent-backend` | Which agent harness backend is live; hosts the **Kiro sign-in** card (see Standalone operator surfaces) while KAS is an offered backend | `pages/developer/AgentBackendTab.tsx`, `pages/developer/KiroSignInCard.tsx` | `handlers/acp_backend_status.py`, `handlers/kiro_prerequisite.py` | `GET /api/acp-backends`, `GET /api/kiro-prerequisite` |  | `frontends/next-dashboard/components/settings/agent-backend-panel.tsx` (Settings → Developer) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20) as an honest status panel** — Companion-X runs a SINGLE built-in harness (LangChain/LangGraph over MCP v2, Track B rails), no pluggable ACP/KAS backend and no Kiro sign-in. `AgentBackendPanel` states the live harness + transport + health, with an explicit not-applicable note for backend switching. Tests: `config-agent-backend-panels.test.tsx`; `settings-disclosure-panels` 11/11; `tsc` at the 8-error baseline. Deferred: pluggable-backend switching + Kiro sign-in (KiroCrew-identity / M8 rebrand territory) + live proof. |
| 112 | `debug-tools` | Diagnostic overlays, currently the chat scroll inspector | `pages/developer/DebugToolsTab.tsx` | — (client only) | — |  | `frontends/next-dashboard/components/settings/debug-tools-panel.tsx` (mounted in Settings → Developer) | TESTING_NEEDED | **Built per owner steer — client-only, matching upstream's handler-less design.** `DebugToolsPanel` reuses the existing `useLiveToolStream` to show the tool-event SSE connection state + recent event count, plus this client's runtime context (viewport, dashboard origin, user agent) — the browser-side companion to the mcp-pool panel for tracing tool-transport issues. Mounted in Settings → Developer. **Real robustness fix found via this build**: mounting a `useLiveToolStream` consumer in a test surfaced that the hook threw when `EventSource` is absent (jsdom/SSR) — added a `typeof EventSource === "undefined"` guard so it degrades to a disconnected stream instead of crashing the render (a latent SSR/test hazard, now fixed). Tests: FE 2 new (`debug-tools-panel.test.tsx`, useLiveToolStream mocked — connected + disconnected states) + `settings-disclosure-panels.test.tsx` still 11/11 (renders the real Developer panel, now with three diagnostics); `tsc` at the 8-error pre-existing baseline. **Deferred**: the specific chat-scroll inspector overlay + live proof. |
| 113 | `archive` | Consolidated session archive browser | `pages/SessionArchive.tsx` | `handlers/sessions.py` | `GET /api/session/archive`, `GET /api/session/archive/{name}` |  | Sessions → Archived | PRESENT+WORKS | Sessions archived filter, revision-fenced archive/reopen, and archived badges provide the consolidated archive behavior. |

## Standalone operator surfaces

Companion-X home: Settings → Developer / Terminal panel (case by case)

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
| 114 | Logs | Full-page log viewer | `/logs` | `pages/LogsPage.tsx` | `handlers/updates.py` | `GET /api/logs`, `GET /api/stream` | `frontends/next-dashboard/app/logs/page.tsx` (+ `components/settings/logs-panel.tsx` `variant="page"`) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Full-page `/logs` operator route reusing the row-104 `LogsPanel` (new `page` variant, full-height) over the live tool-event SSE stream. Tests: `logs-panel.test.tsx` (+ page-variant assertion) 3/3, Developer render 11/11; `tsc` clean. Deferred: raw gateway log-line streaming + server-side log-level control (no `/api/logs` on this port yet). |
| 115 | Hooks | Full-page hook manager | `/hooks` | `pages/HooksPage.tsx` | `handlers/hooks.py` | `GET,POST /api/hooks`, `GET /api/kiro-hooks` | `frontends/next-dashboard/app/hooks/page.tsx` (+ `components/operations/hooks/hooks-page.tsx`) | TESTING_NEEDED | **Honest scaffold per owner ruling (16:20).** `/hooks` operator route. Companion-X has NO persistent hook registry on this port — webhooks are ephemeral agent handoffs via the `register_hook` MCP tool, so the page documents that mechanism + renders an honest empty-registry state rather than faking CRUD over a nonexistent store. Tests: `hooks-page.test.tsx` 2/2; `tsc` clean. Deferred (needs backend): a persistent hook store + `/api/hooks` endpoints. |
| 116 | Webhooks | Inbound webhook tokens, contexts, run history | `/webhooks` (preview-gated) | `pages/WebhooksPage.tsx` | `handlers/hooks.py` | `GET /api/webhooks`, `POST /api/webhooks/tokens`, `POST /api/hooks/agent` | — | OWNER_DEFERRED | Owner 2026-09-14 12:39: "remotecrew/webhooks sound super interesting but not core. Defer like features (e.g. slack etc)". Build in M9 Deferred features. |
| 117 | Cloud launch | Provision a remote Kiro Crew instance through a provisioner lane: the built-in lane creates an EC2 instance in the user's own AWS account; an edition may add lanes via the `remote_provisioners` CPP seam, and the tab shows a lane selector only when more than one drawable lane exists (`registerRemoteProvisionerRenderer`) | Settings → Remote Instances → Set up a new one | `pages/settings/RemoteCrewPanel.tsx`, `components/remoteProvisionerRenderers.tsx` | `handlers_cloud.py`, `platform/defaults.py` (`DefaultRemoteProvisionerProvider`) | `GET /api/cloud/provisioners`, `GET /api/cloud/preflight`, `POST /api/cloud/launch` (`provider_id`), `GET /api/cloud/iam-policy` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |
| 118 | Mobile connect | Pair a phone to this gateway | Left rail → **Connect your phone** (governed methods + QR); Settings → Security → mobile card (sign-in link only) | `App.tsx`, `components/MobileConnectModal.tsx`, `pages/settings/MobileLoginCard.tsx` | `handlers/mobile_connect.py`, `handlers/auth_mobile.py`, `handlers/tailnet_mobile.py` | `GET /api/mobile-connect/methods`, `POST /api/auth/mobile-link`, `POST /api/tailnet/mobile/qr` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |
| 119 | Kiro sign-in | Sign in to the Kiro identity agents run as under the KAS backend, without a kiro-cli login: Google/GitHub via loopback (local) or device code (remote), Builder ID and company SSO via device code; signed-in summary with sign-out; explicit "sign-in expired" state (no silent fallback to kiro-cli login) | Developer → Agent Backend → **Kiro sign-in** card, under the backend switch and only while KAS is an offered backend (Developer Mode preview, so deliberately not on Settings → Overview and not in Settings search; Overview shows only a one-line **Kiro sign-in moved to Developer > Agent Backend** signpost while KAS is the selected backend); chat error row "not signed in" → **Sign in to Kiro** deep link (`/developer?tab=agent-backend&highlight=key:kiro-sign-in`, which rings the card). The full-screen `KasLoginGate` form is still not mounted at the app root | `pages/developer/KiroSignInCard.tsx`, `pages/developer/kiroSignInLink.ts`, `pages/developer/AgentBackendTab.tsx`, `pages/OverviewPage.tsx` (`KiroSignInMovedPointer`), `components/KasLoginGate.tsx`, `pages/chat/ErrorCard.tsx` | `handlers/kas_login.py`, `auth/service.py` (`status()`), `auth/refresh.py` (`RefreshRejected`) | `GET /api/kas-login`, `POST /api/kas-login/device`, `POST /api/kas-login/loopback`, `POST /api/kas-login/poll`, `POST /api/kas-login/cancel`, `POST /api/kas-login/logout` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |
| 120 | Source-provider review | PR state, checks and review threads in the Changes panel | Chat right panel → **Changes** | `components/PullRequestPanel.tsx`, `components/CommentThreads.tsx` | `handlers/source_providers.py` | `POST /api/source/pull-request`, `.../checks`, `.../status`, `.../resolve` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |
| 121 | Crash report notice | Desktop-only banner on the launch after a crash: a count of new own-app crash artifacts, **Show diagnostics** (reveals the crash ledger in the OS file manager) and dismiss | Appears automatically on the next launch after a desktop crash — no navigation | `components/CrashReportNotice.tsx` (mounted app-wide in `App.tsx`) | `website/electron/crash-collector.js`, `website/electron/ipc-registrar.js`, `website/electron/preload.js` (`crashReportsAPI`) | IPC, not HTTP: `crash-reports:get`, `crash-reports:reveal` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |
| 122 | Startup feature video | One short clip introducing a new feature, shown once on the launch after it ships and retired permanently by a `seen` or `dismissed` verdict; yields the whole launch to release notes, an update popup or first-run onboarding, and is skipped in incognito/temporary sessions. A clip is always played from disk -- the release folder for a downloaded one, the shipped assets for a built-in one -- and never streamed from the CDN: the browser only plays bytes the gateway downloaded and sha256-checked first, so an uncached hosted clip waits for the next launch. Every offer's files were proved on disk by the request that offered it, so the dialog opens on the `/next` answer alone | Appears by itself at startup, with no navigation, on the first launch where nothing else claimed the screen and the catalog still has an entry this install has not used | `components/StartupVideoModal.tsx`, `components/startupVideoGate.ts` (sequencing policy), mounted app-wide in `App.tsx` | `feature_videos.py`, `feature_videos_manifest.py` (signed hosted catalog), `feature_videos_cache.py` (local clip cache + serving) | `GET /api/feature-videos/next`, `POST /api/feature-videos/feedback`, `GET /feature-videos/<release>/<file>` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |
| 123 | OpenAI-compatible API | Chat-completions shim for external clients | External clients only | — | `openai_compat.py` | `POST /v1/chat/completions` | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "standalone operator surface - sounds like marketing shit we dont need that i can either ditch or set as 'needs to be rebranded' and deferred before the last features". Decide ditch-vs-rebrand per row in M8 Rebrand. |

## Popouts and embeds

Companion-X home: case by case; chat popout + artifact embed first

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
Separate top-level route trees `App.tsx` selects before the dashboard chrome
renders, so nothing in them carries the sidebar.
| 124 | Route | What it is | Page |  |  |  | — | PRESENT+WORKS | Generated upstream subtable header, not a user-facing capability; excluded from implementation scope. |
| 125 | `/popout/chat/:slug?` | One session in its own OS window | `pages/PopoutFrame.tsx` |  |  |  | `frontends/next-dashboard/app/popout/chat/[[...slug]]/page.tsx` (+ `components/chat/standalone-chat.tsx`) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Thin route rendering the existing `CopilotChatSidebar` full-viewport (no-op close/resize) via `StandaloneChat` inside `StandaloneSurface`. Tests: `standalone-surface.test.tsx`; `tsc` clean (route + StandaloneChat compile). Deferred: `:slug` session-binding + true route-group chrome-strip + live proof. |
| 126 | `/popout/artifact/:slug` | One artifact in its own window | `pages/ArtifactPopoutFrame.tsx` |  |  |  | `frontends/next-dashboard/app/popout/artifact/[[...slug]]/page.tsx` | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Thin route rendering the Artifacts `OperationsPage` view in `StandaloneSurface`. `tsc` clean; `standalone-surface.test.tsx` covers the overlay. Deferred: slug-focus (open one artifact directly) + chrome-strip + live proof. |
| 127 | `/popout/terminal` | The docked terminal, detached | `pages/TerminalPopoutFrame.tsx` |  |  |  | `frontends/next-dashboard/app/popout/terminal/page.tsx` | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Thin route rendering the existing `TerminalPanel` (no-op onClose — the OS window closes it) in `StandaloneSurface`. `tsc` clean; `standalone-surface.test.tsx` covers the overlay. **Completes the 125-130 popout/embed batch.** Deferred: chrome-strip route-group layout + live proof. |
| 128 | `/embed/chat/:slug?` | Chat embedded in a host surface | `pages/ChatPage.tsx` (`embedded`) |  |  |  | `frontends/next-dashboard/app/embed/chat/[[...slug]]/page.tsx` (+ `standalone-chat.tsx`) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Thin route rendering `StandaloneChat` (the existing chat sidebar, full-viewport) in `StandaloneSurface`. `tsc` clean. Deferred: `:slug` session-binding + chrome-strip + live proof. |
| 129 | `/embed/sessions` | Session list embedded in a host surface | `pages/ChatPage.tsx` (`embedded`) |  |  |  | `frontends/next-dashboard/app/embed/sessions/page.tsx` (+ `components/layout/standalone-surface.tsx`) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Thin App Router route rendering the existing Sessions `OperationsPage` inside a new `StandaloneSurface` (a full-viewport opaque overlay that covers the workbench chrome for an embed-style presentation). Tests: `standalone-surface.test.tsx` (the reusable overlay); `tsc` clean (route compiles). Deferred: a dedicated route-group layout for true chrome-stripping + live proof. |
| 130 | `/embed/settings` | Reduced settings for an embedded host | `pages/EmbedSettingsPage.tsx` |  |  |  | `frontends/next-dashboard/app/embed/settings/page.tsx` (+ `standalone-surface.tsx`) | TESTING_NEEDED | **Scaffolded per owner ruling (16:20).** Thin route rendering the existing Settings `OperationsPage` inside `StandaloneSurface`. Tests: `standalone-surface.test.tsx`; `tsc` clean. Deferred: route-group chrome-strip layout + live proof. |

## Redirects

Companion-X home: N/A unless a Companion-X route is renamed

| # | Feature | What it is | Reach it (upstream) | Page (`website/src/`) | Handler (`src/kiro_crew/dashboard/`) | Endpoints (upstream) | Companion-X location | Status | Evidence |
|---|---|---|---|---|---|---|---|---|---|
Kept so old bookmarks and deep links still resolve. Adding a feature never
means adding a row here; retiring one usually does.
| 131 | From | To |  |  |  |  | — | PRESENT+WORKS | Generated upstream subtable header, not a user-facing capability; excluded from implementation scope. |
| 132 | `/knowledge` | `/capabilities?tab=knowledge` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 133 | `/agents`, `/mc-agents` | `/capabilities` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 134 | `/connections` | `/capabilities?tab=mcp` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 135 | `/overview` | `/settings/overview` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 136 | `/instances` | `/settings/instances` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 137 | `/artifacts/deploy` | `/deploy` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 138 | `/orchestrated/:slug?` | `/chat/:slug?` (orchestrator slot) |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 139 | `/tasks` | Task Runner |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
| 140 | anything unmatched | `/chat` |  |  |  |  | — | OWNER_REBRAND | Owner 2026-09-14 12:39: "old kirocrew urls wont work because im making this my own thing. I'll eventually point to my own github and other shit (even rebrand). So we can rebrand/whatever last but before the deferred features". Handle in M8 Rebrand. |
