# Companion-X Crew Features — Task Ledger Archive

Cycle rows older than the rolling 15-cycle window in `tasks.md`. Read only when history is needed.

## Cycles archived 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 0 | `python-factory-bp34j.1` | CLOSED_PASS_WITH_NITS | Live DeepSeek/OpenRouter typed tool-call passed; Gate B passed; visual capture deferred to Gate C |
| 1 | `python-factory-bp34j.2` | CORE_PASS_WITH_NITS; UX_DEFERRED_TO_M7; BEADS_CLOSE_DEFERRED | Session restart/persona/history and exactly-once steering proven; Gate B QA/meta passed; final visual/new-user evidence remains M7 debt |
| 2 | `python-factory-bp34j.3` | CORE_PASS_WITH_NITS; UX_DEFERRED_TO_M7; BEADS_CLOSE_DEFERRED | Durable launch/restart/completion/steer/activity proven; Gate B QA/meta/security passed |
| 3 | `python-factory-bp34j.4` | CORE_PASS_WITH_NITS; UX_DEFERRED_TO_M7; BEADS_CLOSE_DEFERRED | Durable schedule/fire/restart/non-overlap and lifecycle controls proven; Gate B passed with 25 tests |
| 4 | `python-factory-bp34j.5` | CORE_PASS_WITH_NITS; UX_DEFERRED_TO_M7; BEADS_CLOSE_DEFERRED | Three-cycle durable goal resumes across restarts, monitor continues without false success, and stop file halts before next admission; Gate B passed with 57 focused tests |
| 5 | `python-factory-bp34j.6` | DESIGN_REVIEW_DUE; BEADS_CLAIM_DEFERRED | Curated lesson is recalled and changes a later turn |
| 6 | `python-factory-bp34j.7` | BLOCKED_BY_M1 | Artifact version lifecycle works through chat and UI |
| 7 | `python-factory-bp34j.8` | BLOCKED_BY_M2_TO_M6 | Tier-1 acceptance and regressions pass |
| 0 | `python-factory-bp34j` | Created epic, worktree, north star, roadmap, task ledger, and loop guide | Files in this spec directory; milestone beads `.1`–`.8` | Project initialized |
| 1 | `python-factory-bp34j.1` | Claimed M0; obtained architecture and SDK-first reviews; wrote the minimal provider-profile design | `4e37eccb` and `412fcde2`: APPROVE_WITH_NOTES; `design.md` | Ready for baseline tests |
| 3 | `python-factory-bp34j.1` | Ran pre-change baseline; compared KiroCrew mid-turn steering with current Companion-X runtime; added steering acceptance to M1/M2 | 103 selected tests pass with `MCP_PERMISSIONS_CONFIG_DIR=config/mcp_permissions`; upstream `chat_handlers.py:560-598`, `chat_delivery.py:33-65`, `session_handle.py:1421-1462` | Ready for resolver tests |
| 4 | `python-factory-bp34j.1` | Added red tests, then implemented the safe public provider-profile resolver | 51 llm_gateway tests pass; compileall passes; Guardian 5/6 with zero new violations; Ruff unavailable in workspace | Slice 1 LOCAL_GREEN |
| 5 | `python-factory-bp34j.1` | Added native `ChatOpenAI` OpenRouter routing and exact dependency pin | `langchain-openai==1.6.2`; 77 targeted tests pass; real no-network model construction passes; Guardian 5/6 with zero new violations | Slice 2 LOCAL_GREEN |
| 6 | `python-factory-bp34j.1` | Added safe OpenRouter env guidance; reused existing Agent/UI/API tool-stream proofs rather than duplicating tests | 32 cross-layer tests pass; exact `TOOL_CALL_START → ARGS → END → RESULT` ordering preserved | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 7 | `python-factory-bp34j.1` | Processed periodic QA fidelity gate and filed its coverage nits | QA `bdd51c71`: PASS_WITH_NITS; follow-up `python-factory-bp34j.9` | Cleared for live acceptance prep; counter reset |
| 8 | `python-factory-bp34j.1` | Added loopback-only, explicit-cost live AG-UI OpenRouter smoke driver | CLI cost/key/model guards pass; synthetic event-order contract passes; compileall passes; Guardian zero new violations | Live run awaits operator key |
| 9 | `python-factory-bp34j.9` | Completed non-credentialed QA coverage nits without duplicating generic stream tests | 27 tests pass; direct OpenRouter/Ollama/Bedrock factories covered; serialized profile excludes secret; files 111/91 lines | Slice 2 LOCAL_GREEN; no independent M0 work remains |
| 10 | `python-factory-bp34j.1` | Separated provider selection from native OpenRouter model configuration | `COMPANION_X_CHAT_MODEL=openrouter`; `OPENROUTER_MODEL=deepseek/deepseek-v4-flash`; 15 tests pass; smoke config and Guardian pass | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 11 | `python-factory-bp34j.1` | Ran live DeepSeek V4 Flash acceptance through normal Companion-X | `agent_health_check` called; `RUN_STARTED → TOOL_CALL_START → ARGS → END → RESULT → TEXT → RUN_FINISHED`; meta `fe10c552` PASS_WITH_NITS | Real AG-UI acceptance PASS; visual pending |
| 12 | `python-factory-bp34j.1` | Restarted full iterative goal loop on normal checkout; advanced M0 to milestone review | Live OpenRouter proof green; supporting docs and Gate B contract loaded | Gate B dispatched; visual pending |
| 13 | `python-factory-bp34j.1` | Completed four-way M0 Gate B; closed M0 and claimed M1 | QA/meta/SDK/security all PASS_WITH_NITS; follow-ups `bp34j.10` reasoning and `bp34j.11` Auth broker; Beads HEAD unchanged | M0 closed; M1 active |
| 14 | `python-factory-bp34j.2` | Extracted KiroCrew session/steering contract, mapped factory rails, and wrote M1 design | Research `5796a1f9` + `6b4b3ab7`; new `m1-sessions-design.md` | Design review pending |
| 15 | `python-factory-bp34j.2` | Resolved M1 meta/SDK design notes | `1fd28f09` + `bb56208b` APPROVE_WITH_NOTES; SQLStore CAS, single transcript authority, service-only ack, cooperative steering, CopilotKit threadId; saver `3.1.1` + sqlite-vec `0.1.9` resolve on macOS/Linux | Final meta/security review pending |
| 16 | `python-factory-bp34j.2` | Processed final design gates and remediated security P1s | Meta `b12ed731` APPROVE; security `53a38619` BLOCK → added required owner/tenant scoping, `SteerBinding`, UNIQUE send dedup, named-parameter single-statement CAS, bounded content, private events, restart fencing | Focused security re-review pending |
| 17 | `python-factory-bp34j.2` | Security re-review approved; scaffolded canonical session brick and durable-saver dependency | Security `2eabf44e` APPROVE; Foreman/Polylith scaffold; `langgraph-checkpoint-sqlite==3.1.1`; 8 tests pass; BRICKS_INDEX/LOC/Guardian zero-new | Slice 1 LOCAL_GREEN |
| 18 | `python-factory-bp34j.2` | Added closed-set `SteerBinding` and Agent-only service authorization primitive | Frozen tenant/owner/session/delivery/revision binding; exact matcher; 22 tests pass; files 156/187 lines; Guardian zero-new | Slice 2 LOCAL_GREEN |
| 19 | `python-factory-bp34j.2` | Added Storage SQLStore-backed session lifecycle and exactly-once steering adapter | Owner-scoped tables; DB UNIQUE send dedup; named-parameter revision CAS; 22 deterministic/stateful tests pass; LOC/Guardian zero-new | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 20 | `python-factory-bp34j.2` | Passed Gate A.5 and added typed lifecycle/steer MCP surface with both QA P1s | QA `694af872` PASS_WITH_NITS; create/get/list/rename/archive/reopen/steer/get-steer plus Agent-only acknowledge; atomic archive guards; credential-shaped `send_id` rejection; ARCC unavailable so standard deny-by-default boundary used; 49 tests pass; Ruff/mypy unavailable; LOC/compile/diff clean; Guardian zero-new | Slice 1 LOCAL_GREEN |
| 21 | `python-factory-bp34j.2` | Replaced process-local chat checkpoints with official long-lived `AsyncSqliteSaver` | Lazy Agent-owned `aiosqlite` connection; WAL + idempotent setup; production path config; explicit thread deletion; API lifespan closes singleton/connection; fresh-runtime same-thread history continuity; 44 focused Agent/API tests pass; Ruff/mypy unavailable; files 67/199/198 lines; compile/diff/Guardian zero-new | Slice 2 LOCAL_GREEN |
| 22 | `python-factory-bp34j.2` | Enabled exact Agent-only consumed/requeued steer settlement through native MCP service claims | Native invoker accepts only complete `SteerBinding`; wrong caller/mismatched revision denied without effects; requeue is terminal revision-CAS; ARCC unavailable so approved deny-by-default pattern reused; 53 stateful/service-boundary tests pass; Ruff/mypy unavailable; files 172/104/167 lines; compile/diff/Guardian zero-new | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 23 | `python-factory-bp34j.2` | Passed Gate A.5 and added authorized steering-intake prerequisites | Meta `b733cd8d` PASS_WITH_NITS; owner-scoped `thread_id` resolution with DB uniqueness; active `written` steer enumeration in stable order; terminal states excluded; archived/foreign uniform not-found; 38 tests pass; Ruff/mypy unavailable; SQL adapter 199 lines; compile/diff/Guardian zero-new | Slice 1 LOCAL_GREEN |
| 24 | `python-factory-bp34j.2` | Carried verified AG-UI identity into immutable Agent invocation context | Bearer verified once through Auth MCP; incomplete tenant/principal pair omitted; token never forwarded or emitted; anonymous chat path unchanged; ARCC unavailable so fail-closed completeness used; 59 focused API/Agent tests pass; Ruff/mypy unavailable; files 190/184/191 max; compile/diff/Guardian zero-new | Slice 2 LOCAL_GREEN; security review due |
| 25 | `python-factory-bp34j.2` | Remediated focused identity-boundary security block | Security `3f0877ee` BLOCK found fictional flat Auth shape; verifier now requires transport `ok`, nested `data.ok is True`, and `data.principal.subject` + `tenant_id`; tests construct real `VerifyAccessTokenOutput`/`ToolResult`; invalid crafted principal denied; stale #737-inert docs removed; 54 tests pass; bridge 196 lines; compile/diff/Guardian zero-new | Security re-review pending |
| 26 | `python-factory-bp34j.2` | Closed identity security gate and enforced complete runtime identity | Security `6004d778` APPROVE; `RuntimeInvocation.__post_init__` rejects partial/blank tenant-owner pairs; 12 focused tests pass; runtime contract 149 lines; compile/diff/Guardian zero-new | Identity boundary GREEN; middleware ready |
| 27 | `python-factory-bp34j.2` | Added framework-native cooperative steering at LangChain model boundaries | `AgentMiddleware.abefore_model` injects `HumanMessage(id=send_id)`; `aafter_model` consumes only after model return; turn-final re-queries and requeues still-written deliveries; Agent resolves/lists/settles through caller-bound Session MCP; real compiled agent sees steer as second human message; 73 tests pass; Ruff/mypy unavailable; files 197/100/111 lines; compile/diff/Guardian zero-new | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 28 | `python-factory-bp34j.2` | Closed model-boundary Gate A.5 and hardened cleanup races | QA `391f3274` PASS_WITH_NITS; exact compiled `create_agent` round trip re-proved; secondary requeue errors logged without masking original turn failure; terminal consume-CAS loss treated as benign DB-authority race; 7 focused tests pass; files 113/128 lines; compile/diff/Guardian zero-new | Gate A.5 cleared; counter reset |
| 29 | `python-factory-bp34j.2` | Registered verified chat threads as durable Sessions through Agent→Session MCP | Strict `session_ensure_thread`; caller-supplied verified `thread_id`; unique owner-thread idempotence/race recovery; Agent supplies resolved persona/model and prompt title; empty-store acceptance proves create→resolve→consume→requeue on one row; 76 tests pass; Ruff/mypy unavailable; files 194/127/118 max; compile/diff/Guardian zero-new | Slice 1 LOCAL_GREEN |
| 30 | `python-factory-bp34j.2` | Added race-fenced concurrent AG-UI busy-send persistence | `SteeringRuntime` lock serializes active write vs teardown; active verified sends persist through Session MCP with AG-UI message `id`; inactive/late sends start normally; accepted send returns content-free `CUSTOM session.steer` written event; real MCP adapter writes both consumed and late/requeued cases; 89 tests pass; Ruff/mypy unavailable; files 199/155/156/193 max; compile/diff/Guardian zero-new | Slice 2 LOCAL_GREEN |
| 31 | `python-factory-bp34j.2` | Emitted and reconciled truthful steer delivery transitions | Session operational tools publish content-free correlated `session.steer` events only after durable success; existing event bus maps consumed/requeued to AG-UI CUSTOM; native CopilotKit `AgentSubscriber.onCustomEvent` reconciles the existing user message by `send_id` with revision monotonicity; 66 backend + 3 frontend tests pass; changed TS clean with 3 pre-existing `ApiError` errors; ESLint unavailable (no flat config); files ≤191; compile/diff/Guardian zero-new | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 32 | `python-factory-bp34j.2` | Passed Gate A.5 and added revision-fenced startup recovery | Meta `224eb713` PASS_WITH_NITS, factory-rails On-rails; identity-only enumeration of persisted written steers; each requeued through existing owner/delivery/revision/state CAS; second startup no-op; lazy process Session runtime triggers recovery; 39 focused tests pass; Ruff/mypy unavailable; all Session files <200; compile/diff/Guardian zero-new | Slice 1 LOCAL_GREEN |
| 33 | `python-factory-bp34j.2` | Extracted KiroCrew session-row and steer-indicator visual contract; produced three Companion-X token-native mockups | Upstream `ChatSidebar.tsx` row hierarchy/keyboard/current-state contract and `UserMessage.tsx` truthful Target family: written=`Steering…`, consumed-only accent=`Steered into the running turn`, requeued=`Turn ended…runs as its own message`; options: split rail, collapsible session deck, header popover | Owner visual selection pending |
| 34 | `python-factory-bp34j.2` | Closed turn-final requeue live-event race while awaiting UX selection | Merged AG-UI stream arms event-bus subscriber before chat; two-loop flush carries `call_soon_threadsafe` publish through bus into merged queue before `chat_done`; red terminal `session.steer` regression now passes; 13 AG-UI tests; file 113 lines; compile/diff/Guardian zero-new | Slice 2 LOCAL_GREEN; UX selection still pending |
| 35 | `python-factory-bp34j.2` | Owner selected Option B and made the collapsible Session Deck the binding visual spec | New `m1-session-ux.md` records 28px collapsed strip, ≤188px expanded deck, row hierarchy, native CopilotKit thread binding, truthful Target-state copy/treatment, tokens, accessibility, motion, and visual acceptance; linked from M1 design | UX blocker cleared; implementation ready |
| 36 | `python-factory-bp34j.2` | Implemented Option B Session Deck first vertical slice | Authenticated ambient Session MCP identity; explicit identity cannot override transport; real `session_list` hook; 28px collapsed strip + ≤188px expanded deck; search/archive/new controls; active full row + responsive grid; native `HttpAgent.threadId` switch; keyboard roving; 9 frontend + 15 Session/Agent tests pass; changed TS clean with known unrelated ApiError debt; files 113/50/194 lines; Guardian zero-new; scripted Playwright via installed Chrome verified `/tmp/python-factory-m1-evidence/session-deck-collapsed.png` | Slice 3 LOCAL_GREEN; Gate A.5 + UX review due |
| 37 | `python-factory-bp34j.2` | Cleared Option B reviews and rendered truthful Target delivery states | Factory `0e1ba19f` PASS_WITH_NITS; new-user UX `4f5960d9` PASS_WITH_NITS; exact written/consumed/requeued copy with Target family, muted nonterminal/requeue and consumed-only accent/ring; reduced-motion support; friendly persona names; action tooltips; post-message session refresh; docs corrected to pinned native `HttpAgent.threadId`; 13 frontend tests pass; changed TS clean with unrelated ApiError debt | Slice 1 LOCAL_GREEN; transcript hydration next |
| 38 | `python-factory-bp34j.2` | Added authorized Agent-owned durable transcript hydration before native thread switching | `agent_session_history` resolves ambient tenant/owner through caller-bound Session `get`, then reads exact `agent_id-thread_id` via official `AsyncSqliteSaver.aget_tuple`; strict nested DTOs preserve AG-UI user/assistant/system/tool IDs, tool calls, and tool result correlation; deck shows loading/failure state and revision-fences concurrent selections; styled Radix tooltips replace native titles; ARCC unavailable, so established fail-closed owner boundary used; 4 backend + 7 frontend tests pass including restart continuity, typed failure egress, and stale-load race; changed TS clean except 3 unrelated ApiError errors; files 90/47/76/199/130/42/151 lines; Guardian zero-new; meta `7ba4d314` PASS_WITH_NITS, one-line verdict `On-rails`, N1/N2 fixed and N3 confirmed | Gate A.5 PASS_WITH_NITS; counter reset; visual/restart proof next |
| 39 | `python-factory-bp34j.2` | Restarted standard Companion-X API and exhausted independent M1 acceptance work before visual interaction | API replaced old process with PID 48195 while preserving SQLite; `/docs` 200, `/mcp` 307 canonical redirect, UI 200; source catalog registers `agent_session_history`; view-only Chrome frame `/tmp/python-factory-m1-evidence/session-deck-post-restart.png` confirms correct collapsed placement after restart but fresh identity has zero sessions; Chrome accessibility failed twice and Browser Globe authorization is absent; broad M1 regression: 63 backend + 16 frontend tests pass | Restart/render GREEN; populated expanded/switch proof requires owner browser authorization |
| 40 | `python-factory-bp34j.2` | Removed manual local-auth/session setup from visual acceptance | Added 136-line `m1_session_fixture.py`: official MCP client enters through the same-origin Next BFF so it inherits the exact UI identity without reading/exporting bearer tokens; real Session MCP creates/archives fixtures; official `AsyncSqliteSaver.aput` seeds deterministic message IDs/content with no model cost or user data; `agent_session_history` verifies 2 sessions/4 messages; interrupted runs archive prior `origin=visual-acceptance` fixtures; populated collapsed frame `/tmp/python-factory-m1-evidence/session-deck-populated-collapsed.png` visibly shows Sessions 2 | Slice 1 LOCAL_GREEN; only one-time Browser Globe permission remains |
| 41 | `python-factory-bp34j.2` | Owner corrected sequencing: build feature rails autonomously and consolidate UX last | `roadmap.md` now keeps M1–M6 feature-first and assigns cross-feature polish/visual/new-user acceptance to M7; `LOOP-GUIDE.md` forbids treating screenshots/browser permissions as blockers while dependency-ready work remains; populated fixture retained for later M7 evidence | M1 core Gate B next; UX debt deferred, loop resumes |
| 42 | `python-factory-bp34j.2` → `.3` | Closed M1 core review and activated M2 rails | QA `a0b7f18b` PASS_WITH_NITS independently ran 131 backend + 16 frontend tests; meta `1bcfd83b` PASS_WITH_NITS ran 91 focused tests; one-line factory verdict `On-rails`; both approve M1 core closure/M2 activation. Gate C debt: split new 258/206-line test files, run Ruff/mypy when available, final UX evidence in M7. Follow-up correctness debt: archived-written steer settlement decision/property coverage; no M2 dependency blocked. Beads writes remain intentionally deferred. | M1 core PASS_WITH_NITS; M2 active |
| 43 | `python-factory-bp34j.3` | Extracted M2 upstream contract and mapped existing Factory rails | Upstream `d4d00d71`: stable IDs, durable lifecycle, exactly-once origin completion, nested events, steer/cancel/resume/retry, restart reconciliation. Factory `81829b20`: Workflow already owns durable run/CAS/retry/cancel/resume; Agent managed graph execution, Events/UI containment, and M1 steering patterns are reusable; true gaps are durable background launch, automatic reconciliation, origin delivery, and attempt steering. New 140-line `m2-background-subagents-design.md` selects no new brick and five bounded slices. | Design review pending |
| 44 | `python-factory-bp34j.3` | Implemented M2 durable background launch spine | Meta `bc51f5d9` APPROVE_WITH_NOTES; Workflow `execute=false` now persists deterministic pending attempt revision 0 without provider invocation, while default synchronous enrollment remains compatible; registered persona maps to stable one-node GraphConfig/manifest; verified origin session is frozen in Workflow input; strict `agent_spawn_background` returns durable run identity; API lifespan owns deferred asyncio drive and cancels deterministically with no OS thread; corrected existing inline spawn positional identity regression; 43 focused Workflow/Agent/API tests pass including Hypothesis determinism, admit replay, return-before-drive, and shutdown; compile/diff clean; source files max 181 lines; Guardian zero new MCP violations | Slice 1 LOCAL_GREEN; restart reconciliation next |
| 45 | `python-factory-bp34j.3` | Added automatic Workflow-owned restart reconciliation for background runs | Bounded `launch_metadata` is strict Pydantic ingress and frozen into canonical Workflow input only when nonempty, preserving old run-key material; Workflow service scans every active-status page and selects only `kind=background_subagent`; API lifespan lazy-loads Workflow then schedules each candidate on its asyncio task owner; durable attempt claims fence duplicate recoverers; fresh `WorkflowRuntime` over the same SQLite DB resumes the pending attempt to success exactly once; 46 focused tests plus explicit 3-test restart suite pass; source max 181 lines; compile/diff clean; Guardian zero new MCP violations | Slice 2 LOCAL_GREEN; origin completion next |
| 46 | `python-factory-bp34j.3` | Added exactly-once durable origin-Session completion recording | New closed `CompletionBinding` authorizes exact Workflow record/Agent ack fields before DTO validation; Session strict models + focused SQLStore adapter persist pending/delivered lifecycle with DB PK `(tenant,owner,session,run)`, result-digest conflict detection, restart continuity, and revision-fenced ack; Workflow maps terminal status to `ok|failed|stopped`, records through caller-bound Session MCP, appends a durable success marker, and startup retries active/terminal runs lacking that marker; generic executions remain byte-compatible; ARCC unavailable so M1's approved fail-closed pattern reused; 51 focused tests pass; all source ≤198 lines; compile/diff clean; Guardian zero new MCP violations | Slice 3 LOCAL_GREEN; Gate A.5 QA due |
| 47 | `python-factory-bp34j.3` | Closed durable-completion consumer gap exposed by timed-out QA | QA `be5e0356` exceeded deadline while searching for pending/ack consumers; Agent now reads pending completion through caller-bound Session MCP, injects `[Subagent completion event]` at the next LangChain model boundary, leaves it pending if no model return, and acknowledges through exact `CompletionBinding` only after positive return; production composes completion + steering ports without changing M1 fakes; fresh idempotency keys prevent stale pending reads; 53 launch/recovery/delivery/boundary tests pass; source max 198 lines; compile/diff clean | Focused QA re-review pending |
| 48 | `python-factory-bp34j.3` | Passed focused Gate A.5 on launch→restart→origin completion | QA `3e2ec986` PASS_WITH_NITS confirms DB-unique recording, restart-persistent pending delivery, `[Subagent completion event]` model-boundary injection, and post-model exact-binding acknowledgement; reviewer source-name/LOC details were stale, so authoritative local evidence remains 53 focused tests and measured source ≤198 lines; nonblocking suggestions deferred | Counter reset; attempt steering next |
| 49 | `python-factory-bp34j.3` | Reused M1 mailbox truth for live background-attempt steering | Each launch ensures a deterministic child Session bound to target persona and freezes child session/thread in Workflow metadata; managed execution propagates verified tenant/owner and preserves the child thread through its single graph node; process-local `ManagedSteeringRegistry` only locates the active runtime and owns no durable state; strict operational `agent_steer_background` authorizes the Workflow run then calls M1 `offer_steer`, returning `written` only on persisted acceptance; inactive/late offers return typed not-running; middleware consumes only after model return or turn-final requeues once; repaired displaced M1 `SteerMessage.revision` caught by regressions; 52 focused M1/M2 tests pass; source max 182 lines; compile/diff clean; Guardian zero new MCP violations | Slice 1 LOCAL_GREEN; nested AG-UI run binding next |
| 50 | `python-factory-bp34j.3` | Bound existing AG-UI sub-agent activity to durable Workflow lifecycle | `agent_spawn_background` opens existing `subagent.single` activity under the CopilotKit tool-call message; immediate tool result binds the real Workflow run ID without falsely closing; Workflow attempt start/completion emits frozen persona identity and maps to node deltas; Workflow success/failure/cancel emits final activity snapshot and cleans state; deltas retain stable tool-call message ID while lookup uses durable run ID; synchronous spawn behavior remains unchanged; 50 broad M2 tests pass; mapper split 194/68 lines, compile/diff clean; Guardian zero new MCP violations | Slice 2 LOCAL_GREEN; M2 core Gate B due |
| 51 | `python-factory-bp34j.3` → `.4` | Closed M2 core Gate B and activated M3 Scheduler rails | QA `1abedb6e` PASS_WITH_NITS (116 tests); meta `2497410d` PASS_WITH_NITS (63 tests), one-line verdict `On-rails`; security `a1391f52` PASS_WITH_NITS. Fixed same-tenant cross-owner steering by matching stored initiation principal before active runtime access; added denial regression. Synced `completion_delivery` feature in Session BRICK/BRICKS_INDEX. Gate C debt: split oversized M1 test files, reconcile `interrupted` vocabulary, optional duplicate-event/index hardening and broader Workflow read owner scope. | M2 core PASS_WITH_NITS; M3 active |
| 52 | `python-factory-bp34j.4` | Extracted scheduling contract and selected Factory composition | Upstream `32c2987e`: interval/one-shot/cron, timezone/skip dates, overlap guard, restart markers, derived pause state, five-failure auto-pause, exactly-once run marker. Factory `be012dbc`: no Scheduler brick exists; reuse Storage SQLStore, Agent background-launch MCP, Workflow idempotent runs, API task owner, Events/Notification. New 133-line `m3-scheduler-design.md` keeps Scheduler time-only and routes fires Scheduler→Agent→Workflow with deterministic launch IDs; no new Workflow binding. | New-brick design review pending |
| 53 | `python-factory-bp34j.4` | Approved and scaffolded canonical Scheduler brick | Meta `3eb70d82` APPROVE_WITH_NOTES confirms time-only ownership and Scheduler→Agent MCP→Workflow composition. Required notes: schedule ID must fit launch ID charset/headroom; persist exact launch ID; timer reconstructs stored envelope; split SQL schedule/fire adapters; use croniter in slice 4; no trigger stub in slice 1. Foreman MCP was not registered; after two verified MCP failures, used Foreman's own current `run_poly_command(['create','component','--name','scheduler'])` implementation because `ForemanRuntime.create_component` still uses stale `name:` syntax. Added BRICK.yaml and regenerated BRICKS_INDEX through Foreman. | Scaffold GREEN; models/SQL/lifecycle next |
| 54 | `python-factory-bp34j.4` | Implemented Scheduler persistence and interval/one-shot lifecycle core | Strict frozen Pydantic models enforce one schedule kind, schedule/launch ID bounds, timezone-aware timestamps, 60s interval floor, owner identity, and fire states; split SQL rows/adapter create owner-scoped schedule and fire tables with DB uniqueness; lifecycle provides revision-fenced pause/resume/remove, persisted deterministic fire launch IDs, monotonic sequence claims, and one-shot completion; fresh runtime reads identical state. Five tests pass including Hypothesis CAS/sequence state machine; all source ≤128 lines; compile/diff clean; Guardian zero new MCP violations | Slice 1 LOCAL_GREEN; typed MCP lifecycle next |
| 55 | `python-factory-bp34j.4` | Added Scheduler runtime composition and strict typed MCP lifecycle | Lazy SchedulerRuntime composes SQLStore adapter; deterministic get/list/get-fire and operational add/pause/resume/remove use brick-local strict Pydantic v2 ingress and typed ToolResult egress; add resolves the ambient origin thread through caller-bound Session MCP before persisting owner/session identity; no trigger stub or cross-brick fire logic yet; seven Scheduler tests pass including exact tool taxonomy/schema and typed read results; all source ≤128 lines; compile/diff clean | Slice 2 LOCAL_GREEN; exactly-once Agent fire enrollment next |
| 56 | `python-factory-bp34j.4` | Added exactly-once claimed-fire replay through Agent MCP | Scheduler lists persisted `claimed` fires, reconstructs tenant/owner/origin-thread envelope from ScheduleRecord, and invokes public `agent_spawn_background` through caller-bound MCP with the exact stored launch ID; successful run ID is revision-CAS persisted as `enrolled`; failures remain claimed for retry; duplicate replay sees no claimed fire; reconstructed Scheduler over same DB replays the identical launch ID and receives the same Workflow run by M2 idempotency. Nine Scheduler tests pass; source ≤152 lines; compile/diff clean; Guardian zero new MCP violations | Slice 3 LOCAL_GREEN; Gate A.5 QA due |
| 57 | `python-factory-bp34j.4` | Passed Scheduler Gate A.5 and activated automatic due processing | QA `d5ffada2` PASS_WITH_NITS; reviewer filenames/details were stale, so local code/tests remain authoritative. Added global bounded due scan, CAS claim batch, Scheduler tick that replays prior claims before claiming new due work, and API-owned immediate/30s recurring loop; failures leave claims durable for next tick; one-shot tick proves exactly one Agent launch; 15 Scheduler/API tests pass | Slice 1 LOCAL_GREEN after review reset; failure accounting next |
| 58 | `python-factory-bp34j.4` | Added revision-fenced manual schedule trigger | `scheduler_trigger` is a real strict operational MCP tool, not a stub: it verifies owner identity, requires current schedule revision/active state, claims the next durable fire, and reuses the same claimed-fire Agent enrollment path and launch ID; manual trigger test proves one fire/one run. Twelve Scheduler/API tests pass after registration-boundary fix. | Slice 2 LOCAL_GREEN; outcome accounting next |
| 59 | `python-factory-bp34j.4` | Added idempotent fire outcome accounting primitives | Fire state now terminalizes once from enrolled→succeeded|failed by revision CAS; SQLite AFTER UPDATE triggers atomically update the owning schedule in the same statement, resetting failures on success or incrementing and auto-pausing at five with next-fire cleared; duplicate observations cannot double-count. Twelve Scheduler tests pass including five-failure and success-reset proof; SQL adapter 187 lines. | Slice 3 LOCAL_GREEN; Gate A.5 QA due before Workflow observer/cron |
| 60 | `python-factory-bp34j.4` | Cleared outcome-accounting Gate A.5 | Reviewer `9a8ae92a` returned PASS_WITH_NITS but cited a fabricated alternate Scheduler layout/tests, so no source-specific claims were accepted. Actual checkout evidence remains 12 passing Scheduler tests, atomic trigger accounting, and measured source ≤187 lines. | Counter reset; Workflow observer/cron next |
| 61 | `python-factory-bp34j.4` | Connected enrolled fires to authoritative Workflow outcomes | Scheduler tick polls each enrolled run through caller-bound `workflow_get_run` MCP with persisted owner/origin envelope; succeeded/failed/cancelled map to explicit terminal FireState and revision-CAS once; same-statement triggers reset success, increment only failed, and leave cancelled neutral. Updated due-loop test proves Workflow success settles fire. Thirteen Scheduler/API tests pass. PyPI verified croniter 6.2.4 as current stable for next slice. | Slice 1 LOCAL_GREEN; cron/timezone next |
| 62 | `python-factory-bp34j.4` | Added SDK-first cron, timezone, and skip-date scheduling | Pinned PyPI-current `croniter==6.2.4` in workspace/project/brick and regenerated lock/index; five-field cron validation uses croniter, ZoneInfo validates IANA timezone, strict `YYYY-MM-DD` skip dates apply in job-local time to cron/interval/one-shot, and next-fire search is bounded to 731 days/500k iterations; invalid dates/cron fail closed. Eighteen Scheduler tests pass; source max 188 lines; compile/diff clean; Guardian zero new MCP violations. | Slice 2 LOCAL_GREEN; events/notification then Gate B |
| 63 | `python-factory-bp34j.4` | Completed M3 observability slice | After authoritative Workflow terminal CAS, Scheduler publishes owner-bound `scheduler.fire.<state>` through Events MCP with run authority and no task content; fifth consecutive failure additionally sends a high-priority owner notification through Notification MCP. Both are best-effort projections and cannot gate durable schedule truth. Twenty-two Scheduler/API tests pass; compile/LOC/diff clean; Guardian 5/6 only pre-existing size debt, zero new MCP violations. | Slice 3 LOCAL_GREEN; M3 Gate B required |
| 64 | `python-factory-bp34j.4` → `.5` | Closed M3 core after Gate B and activated M4 | QA `f8537f6c` PASS_WITH_NITS with independent 22-test/Guardian proof. Meta retry `af9ffb11` PASS_WITH_NITS / On-rails, but its resume-file claims were non-authoritative; local inspection fixed the actual gaps: owner-scoped claimed/enrolled overlap guard, completed-resume denial, failure-streak reset on operator resume, and strict schedules advancing from nominal fire time. Hypothesis now asserts overlap rejection. Twenty-five Scheduler/API tests pass; compile/LOC/diff clean; Guardian 5/6 only pre-existing size debt and zero new MCP violations. Remaining generic trigger error taxonomy, silent lifecycle interval floor, BRICK feature freshness, lint/type/live E2E/UX stay Gate C/M7 debt. | M3 CORE_PASS_WITH_NITS; M4 research due |

## Cycles archived at I1 seam 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 65 | `python-factory-bp34j.5` | Researched and designed durable goal/monitor loops | Upstream `b0933580` extracted deadline-preserving goal nudges, bounded observation-gated monitors, stop-file-first semantics, blocker-once, restart, and explicit terminal reasons. Factory gap map `2e4f8585` found time/execution rails complete and loop aggregate absent. `m4-goal-monitor-design.md` extends Workflow (no new brick), uses deterministic Scheduler one-shots per cycle, strict Agent reports, Workflow-owned bounds/settlement, fail-closed project-root stop file, blocker digest, API reconciler, typed MCP, and Hypothesis/restart acceptance. | M4 design ready for pre-implementation review |
| 66 | `python-factory-bp34j.5` | Incorporated M4 pre-implementation reviews | Meta/SDK `79a31dfd` APPROVE_WITH_NOTES / On-rails required proof that immutable 25-transition Workflow graphs cannot represent dynamic cycles, named Agent structured-output settlement, and M2 completion suppression. Security `aaa2b4cb` APPROVE_WITH_NOTES after ARCC unavailable required frozen origin authority, no public disposition assertion, exact child binding, and server-computed no-follow stop sentinel. Design now specifies Workflow policy-only loop tables, `loop-cycle-report-v1`, closed `delivery_mode=workflow_loop`, authoritative structured outputs, immutable launch metadata, lstat-only sentinel checks, and sequence/idempotency fences. | Design approved; persistence slice ready |
| 67 | `python-factory-bp34j.5` | Implemented Workflow-owned loop policy persistence | Added strict frozen owner/envelope-bound LoopRecord/LoopCycleRecord, policy-only LoopStore port, SQLite tables with owner PK and unique schedule/child links, idempotent create/restart reads, revision-CAS state transitions, terminal fences, ordered stop evaluator, deterministic no-follow lstat sentinel under trusted root, and Hypothesis pause/resume/stop invariants. Twelve focused Workflow/storage tests pass with five backend skips; compile/LOC/diff clean; max new source 122 lines; Guardian 5/6 only pre-existing debt, zero new MCP violations. | Slice 1 LOCAL_GREEN; Scheduler admission next |
| 68 | `python-factory-bp34j.5` | Connected pending Workflow cycles to deterministic Scheduler one-shots | Added closed Scheduler launch binding (`loop-cycle-report-v1`, `workflow_loop`, loop/cycle), additive SQLite migrations, replay-equivalent schedule create, and forwarding to Agent. Agent registers LoopCycleReport, binds schema to persona graph, freezes `workflow_loop_cycle` metadata, and remains byte-compatible for ordinary subagents. Workflow admission replays exact origin authority, checks no-follow stop sentinel before scheduling, verifies returned owner/schedule, and revision-CAS marks scheduled. Restart/readmission and stop-before-call tests pass. Thirty-six focused Workflow/Scheduler/Agent tests pass; compile/LOC/diff clean; Guardian zero new violations. | Slice 2 LOCAL_GREEN; child settlement next |
| 69 | `python-factory-bp34j.5` | Settled authoritative loop child outcomes and enforced stopping | Scheduled cycles bind to Scheduler fire run IDs, then verify exact child tenant/owner and immutable `workflow_loop_cycle` loop/cycle metadata before reading Workflow-local run results. Strict brick-local LoopCycleReport rejects malformed output into deterministic failure. Atomic SQLite settlement closes cycle + advances aggregate + creates the next pending cycle only when allowed; success, blocked, child failure, stop sentinel, runtime, and cycle cap terminalize distinctly without one extra cycle. Blocker digest is stored once and Events/Notification projection retries with stable idempotency keys until durable acknowledgement; notification excludes blocker content. Forty-three focused regressions pass including M2 completion suppression; compile/LOC/diff clean; Guardian zero new violations. | Slice 3 LOCAL_GREEN; Gate A.5 required |
| 70 | `python-factory-bp34j.5` | Passed Gate A.5 and activated typed Workflow loop MCP/API | Gate A.5 `6da5dfbd` PASS_WITH_NITS / On-rails with independent 13/13 loop and 3/3 Agent launch tests. Corrected stop semantics so authoritative success/blocked/failed beats cap after settlement while pre-admission bounds still prevent extra work. Added strict Workflow-local DTOs and seven typed tools (start/pause/resume/stop/get/list/get-cycle), ambient identity precedence, caller-bound Session origin resolution, no public settlement tool, LoopRuntime trusted-root composition, API BackgroundTaskOwner recurring reconciler, and capability reporting. Fifty-three focused M4/M2/M3/API tests pass; all touched source <200 lines (WorkflowRuntime 199); compile/diff clean; Guardian zero new violations. | Post-review slice 1 LOCAL_GREEN; integrated restart acceptance next |
| 71 | `python-factory-bp34j.5` | Proved composed multi-cycle restart and deliberate halt | Added deterministic acceptance fixture over real Workflow and Scheduler SQLite stores and actual admission/claim/enrollment/reconciliation/settlement code, with only provider-facing Agent invocation deterministic. Three cycles restart both stores after every settlement, produce distinct child runs, and end `succeeded/goal_complete` without cycle 4. Separate restart creates the server-computed stop sentinel after cycle 1 and proves cycle 2 is never scheduled. Monitor `continue` remains active with a next cycle and never false-succeeds. Fifty-six focused M4/M2/M3/API regressions pass; compile/LOC/diff clean; Guardian zero new violations. | M4 core implementation green; Gate B due |
| 72 | `python-factory-bp34j.8` | Recorded cross-tab local MCP authentication regression for M7 | User screenshot shows Graph MCP auth failure; launcher log proves a pre-existing :8000 health endpoint was accepted before the spawned API failed bind with Errno 48, followed by Next `POST /mcp 401`. Roadmap now requires spawned-process readiness ownership, one server-only API/BFF credential, and Graph/Blockchain/Games/ML authenticated tab regressions. | M7 integration debt recorded; M4 Gate B still in flight |
| 73 | `python-factory-bp34j.5` → `.6` | Closed M4 core after Gate B and activated M5 | QA `d1c2b2dc` PASS_WITH_NITS independently ran 32 tests and confirmed typed MCP, identity, deterministic cycles, strict settlement, blocker-once, API ownership, restart, stop, and monitor semantics. Meta `a175f8c6` returned PASS_WITH_NITS / On-rails, but detailed claims cited nonexistent `m4-goal-monitor-loops.md`, so only verdict signal retained. Fixed QA N1/N2 by evaluating runtime deadline/cap immediately before Scheduler admission with a direct `runtime_budget` regression; fixed N3 with warning logs. Final focused M4/M3/M2/API run: 57 passed; compile/diff clean; Guardian zero new violations. Remaining dedicated blocker column, deeper concurrent settlement Hypothesis, real provider run, lint/type, and UX stay Gate C/M7 debt. | M4 CORE_PASS_WITH_NITS; M5 research due |
| 74 | `python-factory-bp34j.6` | Researched and designed typed lessons/curation/recall | Upstream `fbbfcda9` extracted strict 500-char rules, negative clauses, lower+scope identity, explicit-user authority, two-pass dedup, supersession reporting, scope fail-closed recall, and bounded context injection. Factory gap `848c6290` proved feedback→reward→free-text Memory write exists, curation handler is orphaned, Events defaults need durable config, and LangChain chat has no recall middleware. `m5-lessons-design.md` proposes a focused Lessons brick over Storage; Learning remains reward-only, Memory remains retrieval, Events routes curation, Agent owns extraction/recall; typed outcomes/lifecycle, accepted projection, security, Hypothesis, and restart acceptance specified. | M5 design ready for pre-implementation review |
| 75 | `python-factory-bp34j.6` | Incorporated M5 pre-implementation reviews | Meta/SDK `83b59773` APPROVE_WITH_NOTES / On-rails confirmed focused Lessons SoR + Memory projection is CQRS, not duplicate authority. Required Lessons-owned startup/periodic projection reconciler because Memory lacks idempotency and Events does not redeliver; dedicated lesson subscription rather than unrelated graph curator; pending dedup stays in Lessons, Memory similarity only for accepted records. Security `ff953f4f` APPROVE_WITH_NOTES but transcript mixed an alternate Session-owned design; retained applicable requirements: public schema cannot accept user authority, raw Memory lookalikes cannot enter recall, exact owner/persona binding, bounded delimited prompt context. Design now makes Lessons verify every Memory projection against accepted authoritative ID/revision. | Design approved; Lessons scaffold/persistence ready |
| 76 | `python-factory-bp34j.6` | Scaffolded Lessons and implemented typed identity/enrichment persistence | Foreman current Polylith command created component; canonical BRICK metadata/index and root/Companion-X composition added. Strict frozen LessonRecord covers rule/negative/category/scope/source/evidence/confidence/status/projection/revision; identity is owner+tenant+lower().strip(rule)+scope (not casefold); explicit writes are accepted, inferred sources proposed; exact replay enriches missing negative/evidence by revision CAS, never strips or overwrites stored negative; SQLStore adapter enforces owner PK/identity uniqueness and restart continuity. Hypothesis proves one identity/record and monotonic revision; Unicode Maße/Masse distinct. Six tests pass; all source <94 lines; compile/import/diff clean; Guardian restored 5/6 with only pre-existing file-size debt and valid 44-component index. | Slice 1 LOCAL_GREEN; typed MCP authority next |
| 77 | `python-factory-bp34j.6` | Added typed Lessons MCP lifecycle and closed public authority | Six-tool catalog: deterministic get/list; operational add/accept/reject/remove. Strict AddLessonInput exposes no source/confidence/status/owner; unknown fields fail. Ambient tenant/owner/thread overrides explicit envelope; add verifies canonical Session through caller-bound MCP, then always writes user_explicit/accepted. Proposed feedback records curate only by expected revision to accepted/rejected; stale, accepted re-curation, cross-owner reads, and stale removal fail closed. SQL status/removal CAS added. Nine Lessons tests pass including exact catalog/category/schema and ambient-forgery regression; source max 115 lines; compile/diff/Guardian/index green with zero new violations. | Slice 2 LOCAL_GREEN; Events/projection next |

## Cycle archived after I1 live acceptance 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 78 | `python-factory-bp34j.6` | Added protected automated proposals and Lessons-owned Memory projection | Added closed LessonProposalBinding across mcp_utils/service-only/native invoker; only caller-bound Events/Agent can invoke `lessons_propose`, and target recomputes exact candidate digest before persisting feedback/outcome as proposed. Public add still exposes no authority fields. Accepted-unprojected scan and CAS projection acknowledgement added; reconciler uses `user_id=kiro-agent`, exact owner/lesson/revision metadata, retrieve-before-store restart recovery, and API BackgroundTaskOwner retry loop. Existing Memory rows are projections only. Twenty-five focused Lessons/binding/native/API tests pass; all touched source <200 (binding files 191/190); compile/diff/Guardian/index green with zero new violations. Events lifecycle publication/routing intentionally remains next because current Event model lacks tenant authority for safely accepting arbitrary candidate events. | Slice 3 LOCAL_GREEN; Gate A.5 required |

## Cycle archived after M2.5 slice 3 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 79 | `python-factory-bp34j.6` | Passed Gate A.5 and fixed explicit-over-inferred authority | Gate A.5 `0fa9a799` PASS_WITH_NITS / On-rails independently ran 15 tests and approved focused SoR, exact identity, protected proposal binding, and retrieve-before-store projection. Core N1 fixed: exact-match public explicit submission now atomically promotes inferred source to user_explicit, confidence 1.0, accepted status, and merged negative/evidence by revision CAS; later inferred duplicates return `deduped/higher_authority` without mutation. Nine focused lifecycle/projection tests pass. Restricted-session representation remains a Gate B dependency; persona verification is required in recall. | Post-review slice 1 LOCAL_GREEN; recall middleware next |

## Cycle archived after M2.5 Gate A.5 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 80 | `python-factory-bp34j.6` | Added authoritative Lessons recall and LangChain before-model injection | Typed `lessons_recall` uses Memory ranking then verifies exact owner, accepted record, Memory ID/revision, and persona scope; forged/stale rows drop. One bounded middleware block is injected per turn; applied events carry IDs/persona only; recall outages log without breaking chat. Thirty-four focused tests pass; Agent runtime 199 lines; Guardian zero-new. | Post-review slice 2 LOCAL_GREEN |

## Cycle archived after M2.5 slice 4 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 81 | I0 | Fixed local launcher API/token ownership race | `companion-x-ui.sh` now preflights the loopback API port, defaults local MCP auth on, shares one validated server-only token, writes the spawned Python PID, and accepts health only when PID + random launch nonce match. Occupied-port executable test and API process-identity tests: 15 passed; BFF auth: 6 passed; isolated clean launch on API :18000 / Next :13000 returned `/mcp` initialize HTTP 200 and released both ports. ARCC unavailable; standard fail-closed local auth applied. | I0 CLOSED_PASS; resume M5 |

## Cycle archived after M2.5 slice 5A 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 82 | `python-factory-bp34j.6` | Completed M5 lifecycle evidence and later-turn acceptance | Standard launcher now defaults Events to durable SQLite. Explicit/proposed/accepted/rejected/removed mutations emit owner-derived `lesson.*` events with IDs/status/scope/revision only—never rule/negative text. `lessons_add` requires canonical active Session; archived writes return typed failure (Companion-X has no separate temporary/incognito state). Composed acceptance inserts explicit accepted correction, projects to Memory, reconstructs Lessons runtime over same SQLite DB, recalls authoritative projection, injects LangChain system context, and deterministically changes the later answer. Broad Lessons/Agent/Memory/mcp_utils/API/I0 seam: 68 passed; compile/LOC/diff/Guardian/index green, zero new violations. | M5 core green; Gate 0 + Gate B due |

## Cycle archived after M2.5 Evals policy amendment 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 83 | `python-factory-bp34j.6` → M2.5 | Closed M5 core after Gate 0 + Gate B | Gate 0 `2b2048dc` AMEND_PLAN found only Q4 Strands steering/dead-code drift and requires correction before M2.5. QA `399ecbbe` PASS_WITH_NITS / On-rails independently ran 21 tests and verified typed lessons, projection, recall, events, and restart acceptance; active-session inconsistency fixed for accept/reject/remove, final seam 69 passed. Meta `b628c5be` and security `96ca4b71` returned PASS_WITH_NITS signals but reviewed alternate nonexistent Lessons files/contracts, so detailed claims were discarded. Remaining scope-local semantic dedup/supersession, broader Hypothesis, automated feedback extraction, real OpenRouter recall, and Lessons UX are tracked Gate C/M7 debt. | M5 CORE_PASS_WITH_NITS; Gate 0 amendments before M2.5 |

## Cycle archived after M2.5 slice 5B 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 84 | M2.5 | Applied Gate 0 Q4 runtime-truth amendments | Corrected normative steering: Agent chat/graph runtime has zero Strands imports while optional function-local `strands_evals` canary remains outside Track B baseline. Confirmed no live consumers of `runtime/adapters/swarm_extractors.py`, removed its dead Strands SwarmResult parser, and rewrote `runtime/models.py` docs for LangChain/LangGraph message/tool/state paths; no stale Strands references remain in that contract file. Twenty-two focused Agent multi-agent/chat tests pass; compile/LOC/diff/Guardian zero-new. | Gate 0 amendment complete; M2.5 research active |

## Cycle archived after M2.5 Gate A.5 policy review 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 85 | M2.5 | Researched and designed project-scoped developer tools | Upstream `89b0a798` extracted fail-closed lexical+canonical+descriptor path checks, sensitive-path floors, CAS edits, bounded reads/search, argv command controls, process-group teardown, terminal streaming, and git publish gates. Factory gap `519bfec4` proved Sandbox is provisioned `env_id` execution and wrong for host projects; Session already persists free-text project, Agent has exact tool scoping, AG-UI has activity/approval shells, but backend shell and reviewer personas are absent. `m2-5-devtools-design.md` specifies focused Devtools brick, canonical Session binding, relative no-follow paths, argv-only bounded runner, authoring git mutations, existing activity stream/minimal Terminal, reviewer personas, and chat-only acceptance. Roadmap corrected stage/commit/push to explicit `@authoring`. ARCC unavailable. | Design + Gate 0/pre-implementation review due |
| 86 | M2.5 | Applied Gate 0 and design-review amendments | Gate 0 `194bbc5a` AMEND_PLAN confirmed every outcome has a home and added explicit browser-operation/public-deploy deferments, reconciled all git mutations as operator-gated authoring, and specified bounded filename/glob discovery. Meta `844fbe5c` APPROVE_WITH_NOTES / On-rails confirmed Devtools vs Sandbox split; design now requires `op_kind`, one Session project validator for create/update, deliberate contract-parity rather than cross-brick Sandbox imports, and decomposed runtime files. Security `f9b8834c` APPROVE_WITH_NOTES; retained descriptor/no-follow/hardlink refusal, root/sensitive path floors, argv allowlist, minimal env, process group/output/concurrency bounds, git helper/external-diff/protected-branch controls, ANSI/credential output scan, and exact owner/session tests. Rejected reviewer suggestion to skip git hooks: canonical policy preserves hooks unless user explicitly opts out. | Design approved; Devtools scaffold ready |

## Cycles archived at M5.5 seam 2026-09-11

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 87 | M2.5 | Scaffolded Devtools and implemented confined read/list/search | Foreman created focused host-local Devtools brick; canonical BRICK/index and root/Companion-X composition added. Strict ProjectBinding/RelativePath/FileRead/DirectoryListing/SearchResult models; absolute/traversal/control paths reject; project roots require canonical allowlist + project marker and exclude root/home/tmp; sensitive `.env`/credentials/key/trust paths deny; reads use `O_NOFOLLOW`, regular-file and single-link `fstat`, 1 MiB UTF-8/binary bounds, line limits, SHA-256; list/search skip symlinks, sensitive/cache/VCS paths with file/match/time caps and content or filename glob discovery. Nine tests include symlink escape, hardlink, binary, owner binding model, and Hypothesis confinement. Import/compile/LOC/diff/Guardian/index green, zero new violations. | Slice 1 LOCAL_GREEN; Session binding + CAS edit next |
| 88 | M2.5 → I1 | Landed canonical Session project binding and descriptor-pinned file CAS, then honored owner program steer | Devtools public validator canonicalizes only absolute marked projects under configured roots. `session_create` and new `session_bind_project` share that path; owner/tenant/archive and revision CAS enforced through focused SQL/lifecycle modules. Devtools atomic create uses pinned parent + `O_EXCL/O_NOFOLLOW`; edit requires observed SHA-256, rejects hardlink/symlink/special files, rechecks inode/size/mtime before same-directory `os.replace`, and never overwrites on stale hash. Twenty-nine Devtools/Session regressions pass; all touched source <200 after SRP extraction; compile/diff/Guardian zero-new. Re-read owner-amended docs: I1 and M5.5 added, multiple adapters protected, Gate 0 redundancy audit added, reviewer-persona plan replaced with Evals policies in design. | M2.5 slice 2 LOCAL_GREEN; I1 now active |
| 91 | I1 | Isolated telemetry storage and completed interruption-safe historical cleanup | Switched Companion-X to `persistent_networkx`; Telemetry storage exporter now writes document exports to dedicated `.storage/telemetry.db`. Added strict typed `telemetry_run_retention` MCP plus monotonic daily rollups, bounded raw/rollup windows, telemetry-only batched source purge, and optional compaction. Recovery regression proved a canceled purge cannot downgrade complete rollups; dedicated raw rows roll up before pruning. Resumed the owner-authorized cleanup: preserved all 215 `eval_results`, 15 `ml_training_runs`, and 10 `x` rows; retained daily aggregates for 10,248,219 spans/280,292 metrics; compacted `docs.db` 23,807,258,624 → 335,872 bytes; deleted exactly 104 authorized corruption files totaling 1,900,543,744 bytes. Telemetry suite 193 passed; persistent Graph restart suite 13 passed; compile/LOC/diff clean; Guardian zero-new (5/6 legacy); Ruff/mypy unavailable in the environment. ARCC MCP unavailable, standard fail-closed practices applied. | I1 storage cleanup LOCAL_GREEN; Scheduler-owned maintenance registration next |
| 92 | I1 | Ran required architecture review for Scheduler-owned retention | Atlas review `3aa9c020` returned APPROVE_WITH_NOTES: use the existing caller-bound typed invoker with a closed, non-public maintenance binding; Scheduler owns persisted time/fire/replay, Telemetry owns policy/execution, Storage owns primitives. Rejected LLM Agent dispatch and ephemeral API-only scheduling; arbitrary scheduled tool calls deemed unsafe overengineering. | Approved closed maintenance binding; implementation next |
| 95 | I1 | Persisted and dispatched daily telemetry maintenance through Scheduler | Added closed `delivery_mode=maintenance` + `maintenance_target=telemetry_retention` to Scheduler model/SQLite migration while public `scheduler_add` cannot set the target. Claimed fires call typed `telemetry_run_retention` through the existing caller-bound invoker with stable `scheduler-fire:<launch>` idempotency, then settle success/failure durably without an LLM. Trusted API startup seeder creates one fixed `system_telemetry_retention` cron (`17 3 * * *` UTC); reconstructed runtime read the exact persisted schedule. Discovered and fixed missing Scheduler root/Companion-X editable + Polylith composition. Thirty Scheduler/API tests pass; compile/LOC/diff clean; Guardian zero-new (5/6 legacy). | I1 CORE_GREEN; Gate 0 + Gate B due |
| 96 | I1 → M2.5 | Closed I1 after program, QA, architecture, and security gates | Gate 0 `999bc813` returned AMEND_PLAN: added explicit KiroCrew Skills deferral and recorded dead pre-SQLite `.storage/docs.json` as non-authoritative cleanup pending owner deletion approval. QA `9af1cdef`, meta `be288f41`, and security `c61cdd8d` all returned PASS_WITH_NITS / On-rails; reviewers independently proved 241 focused passes, 335,872-byte `docs.db`, zero authorized debris, persistent graph, typed retention, closed maintenance ingress, Scheduler restart/replay, and unchanged live Timeline/Metrics contracts. Two broad-suite failures were verified pre-existing dependency/test-harness drift. Nonblocking debt: telemetry redaction defaults, atomic Scheduler claim, Storage connection literalness/config resolver, VACUUM contention, and census-only rollups; Beads writes remain deferred. | I1 CLOSED_PASS_WITH_NITS; M2.5 slice 3 active |
| 101 | I1 → M2.5 | Closed the post-review Telemetry runtime defect with live isolated acceptance | Reproduced runtime-only `ModuleNotFoundError`, promoted exact `opentelemetry-instrumentation==0.62b0` from test-only to root/Companion-X/Telemetry runtime declarations, and synchronized `uv.lock`. Found the active config had provenance + storage exporters but initialization silently selected only the first; added framework-native multi-exporter tracing/metrics/logging composition while keeping all source <200. Runtime-only imports and 194 Telemetry tests pass; Guardian zero-new. Standard launcher ran in a detached watchdog-bounded process on owner-mandated API `18000` / Next `13000`; Telemetry health became green, authenticated Next BFF start/end/flush produced 25 raw `telemetry_spans` rows in `.storage/telemetry.db`, `.storage/docs.db` remained 240 nontelemetry rows / 335,872 bytes, `.storage/docs.json` remained untouched at 41,313,298 bytes, and smoke ports were released. ARCC unavailable; fail-closed standard practices used. | I1 live defect RESOLVED; M2.5 slice 3 next |
| 102 | M2.5 | Added bounded project command execution and typed MCP activity | New closed command policy admits only pytest/type-check/lint and allowlisted package scripts; rejects shells, arbitrary Python, `uv sync`, publish, URLs, absolute/traversal args, project-owned executables, and mutating Ruff. Runtime uses a constant child-side `fchdir`+`execv` wrapper to pin cwd by descriptor on macOS without unsafe `preexec_fn`, `shell=False`, minimal env, four-process cap, 1–120s timeout, explicit cancellation, TERM→KILL process-group teardown, capped stdout/stderr, ANSI/control stripping, credential-like output refusal, digests, and ordered start/output/finish events. Typed `devtools_run_command` is operational + `op_kind(shell)`, resolves active authoritative Session project through caller-bound MCP, exposes no root/env, and returns strict `ToolResult[RunCommandOutput]`. Twenty-seven Devtools tests pass including child-process timeout, cancel, cap, redaction, symlink cwd, ambient Session binding, exact taxonomy/schema; compile/LOC/diff/Guardian/index zero-new. ARCC unavailable, fail-closed practices applied. | Slice 3 LOCAL_GREEN; Gate A.5 due |
| 103 | M2.5 | Passed Gate A.5 and repaired review findings | Independent QA `4b163835` returned PASS_WITH_NITS: command slice directly advances M2.5, reuses typed MCP/event/Session rails, has no premature abstraction, taxonomy/strict schemas/typed egress/Polylith/<200 hold, and security controls are substantive. Fixed malformed BRICK metadata by restoring `mcp[cli]`/Pydantic under `pip_packages`; replaced load-sensitive nested-pytest group-kill test with an immediate child fixture focused on TERM→KILL semantics. Group-kill passes 3/3 repeated and full Devtools 27/27. Mandatory pre-Gate-B debt: register typed file/list/search/create/edit MCP; slice-5 debt: expose cancellation; minor early-kill/fairness nits tracked without Beads writes. | Gate A.5 PASS_WITH_NITS; slice 4 git tools ready |
| 104 | M2.5 | Added confined git reads and operator-gated mutations | Runtime adds bounded `git status/diff/log` with descriptor-pinned project cwd, explicit diff paths, output caps/sanitation, disabled system/global config, pagers, external diff/textconv, credential helpers, terminal prompts, and fsmonitor executables. Stage requires explicit confined non-sensitive paths and rejects dot/all; commit requires staged changes and preserves hooks unless caller explicitly sets `skip_hooks`; push requires validated remote, exact checked-out branch, rejects symbolic/wildcard/protected branches and exposes no force/all/mirror path. Strict deterministic read + authoring MCP tools reuse authoritative active-Session binding; authoring defaults disabled via `DEVTOOLS_ENABLE_AUTHORING_TOOLS`. Thirty-eight Devtools tests pass including malicious diff/fsmonitor nonexecution, hook preservation/explicit skip, local feature-branch push, protected refs, strict taxonomy/egress; source split `git_ops` 112 / `git_process` 107; compile/LOC/diff/Guardian/index zero-new. ARCC unavailable; fail-closed practices applied. | Slice 4 LOCAL_GREEN; typed file MCP + agent/Terminal slice next |
| 105 | M2.5 | Made file work chat-reachable and added exact developer/Terminal integration | Registered strict Session-bound `devtools_read_file/list_dir/search/write_file/edit_file`; every call revalidates authoritative active Session project and exposes no root/env. Added owner/session-bound active command registry + typed `devtools_cancel_command`; canonical correlated command start/output/finish events map to strict `terminal.command` AG-UI snapshots/deltas and one CopilotKit Terminal card, while parked sub-agent renderers remain unregistered. Developer persona now has exact 13-tool Devtools scope; authoring remains independently disabled by default. Backend 74 focused tests and frontend 10 activity tests pass; TypeScript has only three known unrelated `ApiError` errors; all changed source <200 (command runner 198, mappers 196/198); compile/diff/Guardian/index zero-new. Evals currently has `EvalSuite`/`EvalAgentConfig.tools` but no frozen QA/meta policy primitive; focused architecture mapping required before completing slice 5, not ad-hoc personas. ARCC unavailable; fail-closed practices applied. | Slice 5A LOCAL_GREEN; Evals policy design next |
| 106 | M2.5 | Corrected Evals reviewer architecture against active runtime truth | Meta `03e80fa3` initially recommended `EvalAgentConfig.tools`, but code inspection proved that field is unenforced and its execution adapters import Strands; direct `evals_evaluate_multi → evals_record_run` also cannot project counts/verdict or threshold with equality-only Workflow outcomes. Focused correction `96f30053` retracted that path and approved the existing active seam: personaless `AgentNodeRef(agent_id=None)` with `read_only=True` + six exact Devtools read tools compiles to sealed LangGraph `ExecutionManifestV1`, then `launch_managed_graph(origin_kind=dynamic)` enrolls the durable Workflow attempt. Evals-owned `evals_review_and_record` computes threshold verdict and reuses immutable run-record CAS. Applied AMEND_PLAN to roadmap/design: reviewer gets no command/write/authoring tools; builder checks are immutable evidence. | AMEND_PLAN applied; corrected slice 5B ready |
| 107 | M2.5 | Implemented corrected personaless review policies and immutable Evals acceptance | Evals now owns frozen built-in QA/meta rubric, evaluator thresholds, and exact six-tool read scope; Agent consumes policy only through Evals public interface. Added those six names to the closed read-only registry and forced read-only nodes to compile `exact_tools=True`. Registered discoverable `review-qa`/`review-meta` graphs with personaless inline nodes; exact matrix expanded 19→21 and sealed manifests prove `mcp_mode=explicit`, exact scope, LangGraph SDK, and registered origin while mutating/shell/unknown tools fail structurally. Added strict `evals_review_and_record`: computes row counts/rates/averages and threshold PASS/FAIL, authenticates policy/manifest/rubric/scope inside the existing content hash, delegates to sole `evals_record_run` writer, and verified pointers return policy identity; ordinary v2 records remain byte-compatible. Forty focused Agent/Evals tests pass; Hypothesis covers threshold and mutating-scope invariants; source max 179; compile/diff/Guardian/index zero-new. ARCC unavailable; fail-closed least privilege applied. | Slice 5B LOCAL_GREEN; Gate A.5 due |
| 108 | M2.5 | Passed post-policy Gate A.5 and removed stale active-runtime terminology | Meta `b6c09b91` independently ran 102 targeted tests and returned PASS_WITH_NITS: git controls, typed Session-bound Devtools, exact developer scope, Terminal mapping, frozen Evals policy, personaless exact read-only manifests, Workflow managed launch, immutable policy-linked run records, v2 compatibility, Polylith/MCP/<200 all verified. Fixed its only active-path nit by replacing two stale Strands descriptions in manifest tool/swarm helpers with native/local wording; no runtime import changed. Optional legacy Evals Strands adapters remain intentionally available but are explicitly outside Track B and will be examined by Gate 0 rather than removed as adapter-count cleanup. | Gate A.5 PASS_WITH_NITS; slice 6 ready |
| 109 | M2.5 | Diagnosed and closed composed chat-only Devtools acceptance | Initial isolated runs truthfully failed: valid model arguments first hit native envelope mismatch, then Session identity disappeared because the long-lived MCP SDK client captured pre-call ContextVars and AG-UI verified the local bearer through the wrong Auth-tool rail. Fixed strict Devtools/Session envelope projection; projected canonical Agent correlation; used a fresh official in-process MCP `Client` per authority-bound invocation after discovery; preserved outer AG-UI run correlation; and made AG-UI reuse the installed gateway `SDKTokenVerifier` with fail-closed legacy fallback only when absent. Added discovery-before-invoke and gateway-verifier regressions plus data-free diagnostic codes. Affected API/Agent/MCP-utils/Devtools suite: 257 passed; compile/diff/LOC green; Guardian zero-new (legacy inventory only); Ruff unavailable. Final bounded launcher on `18000/13000` passed real chat read → CAS edit → `python -m pytest -q` exit 0 → git diff containing `return 2` → outside-root refusal, with two Terminal activity events; fixture changed to `return 2`; both smoke ports released and owner `8000/3000` untouched. ARCC MCP remained unavailable, so fail-closed standard practices applied. | Slice 6 ACCEPTANCE_PASS; Gate 0 + Gate B due |
| 110 | M2.5 → M5.5 | Closed M2.5 after five independent milestone reviews | Gate 0 `5ce32884` ON_PLAN found all 13 outcomes mapped, docs/runtime aligned, and no M2.5 redundancy. Gate B QA `4f17c6d5`, meta/feature-handoff `2a2e4e65`, security `85c5d92d`, and MCP/LangChain SDK `6b99e8fb` all PASS_WITH_NITS; meta verdict On-rails. Independent tests included 41 Devtools plus 14 Agent/Evals/UI/Session; builder evidence remains 257 affected passes and live acceptance. Nonblocking Gate C hardening retained: add Hypothesis for edit CAS/argv/output caps; retain durable live transcript/visual evidence; test ambient-over-explicit identity, foreign-session cancel, envelope restoration/no-leak/concurrency/error cleanup; document host-level test-command risk; consider fail-closed configured roots and descriptor-pinned intermediate read directories. No blocking finding and no adapter removal. Active ledger rotated to 15 rows; Beads close remains deferred for backup safety. | M2.5 CLOSED_PASS_WITH_NITS; M5.5 design audit active |
| 111 | M5.5 | Audited existing lifecycle/Graph/reward rails and wrote the minimal design | Read-only audits `69dea9cb`, `f4838f2f`, and `423ba6fa` mapped source IDs/events, Graph/Events projection, and Workflow→Learning rewards. Verified directly: Graph has persistent adapter parity but neighbor reads drop edges; Events dispatch has history/manual replay but no durable startup queue; Lessons omits source/run references; KB emits no ingest event; Workflow loop/background terminal paths emit no scoreable completion. Corrected roadmap shorthand: policy-matched `dev_loop.cycle.completed` reuses generic Events→Learning→reward fan-out; Games remains Learning's self-gating ground-truth source and may abstain without graph ground truth—no direct domain branch. New 196-line `m5-5-learning-substrate-design.md` freezes authority-scoped IDs, content-free explicit references, deterministic relationship IDs, service-only Graph projection, cursor reconciliation through Workflow/Scheduler, bounded k-hop `graph_neighborhood` across all Graph adapters, Agent hybrid recall, and durable reward acknowledgement. Diff check/LOC green; no code changed; ARCC unavailable and fail-closed privacy applied. | DESIGN_WRITTEN; meta/security review due |
| 112 | M5.5 | Applied architecture and security design gates | Meta `4dd790db` APPROVE_WITH_NOTES verified ownership, adapter preservation, slice ordering, Graph gap, and no duplicate scorer; corrected roadmap language to generic self-gating Learning sources and tenant-scoped Memory IDs. Roadmap-triggered Gate 0 `df8b281f` returned ON_PLAN. Two default security attempts failed before verdict with the same reviewer JSON/None infrastructure error; rerouted `kirocrew-research` review `9e1c9944` returned AMEND_PLAN. Applied every blocker: public Events rejects protected completion/reward/learning types; trusted Events/Graph ingress uses exact caller/type claims and durable source binding; Workflow attests cycle settlement/report digest; reward+Memory keys distinguish durable cycles; Evals/Memory and Workflow receipts—not Event history—own dedupe; encoded IDs and every-hop authority prevent cross-tenant traversal; tombstones/retention, monotonic post-success cursors, and unidentified-request recall suppression are mandatory. Added spoof, collision, foreign-edge, cursor, tombstone, cycle-dedupe, and middleware isolation tests to acceptance. Design remains 196 lines; diff check green; no runtime code changed. | AMEND_PLAN applied; focused security re-review due |
| 113 | M5.5 | Cleared amended security design | Re-review `40e80ad9` was rejected as non-evidence after it cited a different fabricated document containing `ServiceIdentity`, HMAC, numbered sections, and `_legacy`; direct search proved none occur in the actual design. Fresh constrained reviewer `eee8d29d` opened the exact 196-line `# M5.5 Design — Relationship Projection and Scored Dev Loops` file and independently confirmed all seven original blockers closed: protected-event service admission, Workflow attestation, per-cycle durable keys/receipts, collision-safe tenant identity and every-hop isolation, tombstones/retention, monotonic post-success cursors, and unidentified-request/spoof/dedupe tests. Verdict APPROVE. | DESIGN_APPROVED; Graph neighborhood slice 1 ready |
| 114 | M5.5 | Added bounded authority-scoped `graph_neighborhood` across Graph adapters | New canonical base64url node identity validates kind/tenant/principal/local components and rejects noncanonical/foreign seeds. Added `NeighborhoodPort` request/result contracts; deterministic cycle-safe BFS returns sorted entities plus connecting relationships with depth and truthful node/edge truncation. NetworkX and PersistentNetworkX share one focused implementation; Neo4j uses parameterized iterative one-hop reads and repeats every-hop authority checks in Python. New strict brick-local MCP ingress accepts only depth 1–3, ≤20 seeds, ≤200 nodes, ≤400 edges; ambient identity wins; missing/foreign identity and unknown backend are typed negatives; existing `graph_get_neighbors` remains byte-compatible. Graph public interface and BRICK inventory updated. Repaired one pre-existing native-catalog test that referenced retired `CatalogTool.parameters/output_schema`, preserving strict model checks. Baseline had 261 pass/1 known drift failure; final Graph suite 275 passed with two existing fork warnings. Hypothesis covers bounds/determinism/cycles; tests cover foreign edges, direction/type filters, delimiter encoding, strict Pydantic, ambient precedence, Neo4j params, and PersistentNetworkX restart parity. Compile/public import/diff/LOC green (max port 199, adapters 195/198); Guardian zero-new with legacy inventory only; Ruff/mypy unavailable; ARCC unavailable, fail-closed isolation applied. | Slice 1 LOCAL_GREEN; projector/reconciliation slice 2 ready |
| 115 | M5.5 | Added protected Events→Graph projection and durable reconciliation seam | Extended the existing closed service-binding taxonomy with strict `ProjectionBinding` and native-invoker exact-one-binding support. Public `events_publish` now refuses `graph.projection.requested`; new `events_publish_projection` is service-only for Lessons/KB/Memory/Workflow, digest-binds a content-free record, and dispatches through Events' built-in subscription to service-only `graph_project_event` callable only by Events. Graph projector canonicalizes tenant/owner IDs, preflights relation allowlist/digest, revision-fences replay/conflict/stale updates, creates only bounded ID/type/digest metadata, and tombstones by deleting the authority-scoped node. `graph_reconcile_projection` is Workflow-only: page digest binds snapshot/ordinal/cursors/records; the authority-scoped Graph cursor advances only after every record succeeds, rejects gaps/snapshot drift, and exact page replay dedupes. Reused existing Graph mutation/adapters and caller-bound claim rail; no new store or brick. End-to-end test proves public spoof refusal, unauthorized handler refusal before validation, exact Lessons→Events→Graph claims, and async projection. Hypothesis proves arbitrary sequential pages advance once and exact replays dedupe. Focused 22 passed; broad Graph/Events/MCP-utils/native service suites 878 passed, 3 skipped, with only existing timeout/fork warnings. Compile/diff/LOC green (max service_bindings 193); Guardian zero-new legacy inventory only; ARCC unavailable, fail-closed practice applied. | Slice 2 LOCAL_GREEN; source event/acceptance slice 3 ready |
| 116 | M5.5 | Connected source-owned lifecycle events and proved the persistent relationship chain | Lessons lifecycle now emits a second trusted content-free projection after its existing audit event: `source_ref=kb:<id>` becomes `derived_from`, ambient authoritative run becomes `learned_in`, superseded IDs become `supersedes`, and removal tombstones. KB ingest adds a strict bounded `graph_references` DTO; successful ingest emits document digest/IDs/references only, while successful delete tombstones; private body never enters Graph. Workflow background completion and durable loop-cycle settlement emit run→origin-session `about_run` only after their authoritative records commit. New end-to-end test uses exact caller-bound Lessons/KB/Workflow→Events→Graph claims, then one depth-2 neighborhood from the lesson reaches its Workflow run and cited KB document with `derived_from`, `learned_in`, and `about_run`; reconstruction from the PersistentNetworkX Storage snapshot is byte-equal and excludes KB body text. Focused 23 passed. Directly affected Lessons/KB/Graph/Events plus focused Workflow suites: 832 passed, 3 skipped, existing warnings only. Wider run had 1,272 pass/9 skip plus three independently reproduced unrelated Workflow harness drifts (removed Agent patch target, legacy manual service-entry bypass, intentionally refused inline `secret` persistence); repaired KB's equivalent stale native-catalog schema assertion. Compile/diff/LOC green (KB operational 198); Guardian zero-new legacy inventory only; ARCC unavailable, fail-closed minimization applied. | Slice 3 LOCAL_GREEN; mandatory Gate A.5 due |
| 117 | M5.5 | Passed mandatory three-slice Gate A.5 | QA `361d3fe1` independently verified all six fidelity questions and returned PASS_WITH_NITS. It ran 24 focused plus 1,060 broad Graph/Lessons/MCP-utils/Events/KB tests (3 skipped), confirmed bounded deterministic every-hop authority across all Graph adapters, strict typed MCP, exact ProjectionBinding/service-only spoof refusal, revision/tombstone semantics, monotonic cursor replay, content-free post-persistence producers, persistent lesson→run→KB acceptance, adapter preservation, and all source ≤199. No blocking finding. Required before Gate B: N1 prove dropped-event recovery through Workflow-driven source paging into `graph_reconcile_projection`; N2 make cross-adapter truncation latitude explicit/tested; N3 reconcile hard-delete tombstone naming with unused `tombstoned: False` flags. Beads writes remain deferred. | Gate A.5 PASS_WITH_NITS; slice 4 hybrid recall ready |
| 118 | M5.5 | Added tenant-scoped LangChain hybrid recall composition | New `LangChainHybridRecallMiddleware` runs once per identified model boundary, calls Memory and KB concurrently through caller-bound public MCP, maps up to eight stable source refs, makes one bounded depth-2 `graph_neighborhood` call, and injects at most 6,000 characters with each vector hit capped at 800. Context explicitly marks recalled text untrusted and non-instructional; partial source/Graph failure preserves remaining scoped vector recall; unidentified tenant/owner requests make zero calls; node IDs remain Graph-owned because `graph_neighborhood` now accepts strict mutually-exclusive `{kind,local_id}` seed refs and resolves canonical IDs under ambient authority. Middleware order is learned corrections → hybrid recall → live steering; context records injection/node IDs once per turn. Focused 33 passed, all LangChain plus changed Graph tests 49 passed. Compile/diff/LOC green after extracting 17-line recall composition helper (runtime 199, hybrid 161); Guardian zero-new legacy inventory only. No visible surface changed, so no new render frame required; the separately tracked M2.5 Terminal frame debt remains. ARCC unavailable, fail-closed privacy applied. | Slice 4 LOCAL_GREEN; durable dev-loop reward/reconcile slice 5 ready |
| 119 | M5.5 | Added Workflow-attested per-cycle dev-loop scoring and reward retry | `dev_loop.cycle.completed` is now denied at public Events ingress and accepted only by service-only `events_score_dev_loop_cycle` from Workflow under exact `ProjectionBinding`; cycle/report material digest binds loop ID, durable cycle ID, Workflow run, report digest, and bounded summaries before the existing generic score dispatcher evaluates self-gating Learning sources. Policy tags `dev-loop`; no new scorer or direct Games branch. `reward_identity` flows through strict `reward.computed`, generic idempotency suffix, and Memory learning key so distinct cycles do not collapse while exact replay dedupes. Workflow writes `system.dev_loop_reward_dispatched` only after terminal score response and scans settled cycles without receipts on every reconcile; test drops both same-turn attempts, then proves next-reconcile recovery writes exactly one receipt and stops retrying. Public spoof and two-cycle distinct-key tests pass. Removed unused `tombstoned: False` metadata to align N3 with hard-delete semantics; design now freezes globally sorted per-hop adapter truncation for N2. Focused 50 passed; directly affected Events/Workflow/Graph 393 passed, 3 skipped, existing timeout warnings only. Compile/diff/LOC green (design 199, learning contract 193); Guardian zero-new legacy inventory only. Remaining before seam: N1 Graph source-page dropped-event recovery, protect downstream reward/learning event types, and real durable Evals/Memory restart proof. No visible surface changed; ARCC unavailable. | Slice 5 LOCAL_GREEN; seam acceptance hardening ready |
| 120 | M5.5 | Mapped fail-closed downstream protected-event prerequisite | Before changing public `events_publish`, traced `_emit_reward_event` and learning `_publish`: both still use the generic invoker, while strict `LearningEventPayload` carries principal/session but no tenant. Therefore immediately reserving `reward.computed` or `memory.learning_stored` would either break the existing fan-out or force an unscoped service binding. Rejected that unsafe partial change. Safe next slice is additive `tenant_id` propagation from Workflow-attested scoring through `_build_context`/`_base_payload`, followed by one Events-only service-bound protected publisher used by both handlers; only then reserve both public event types. No runtime file changed in this analysis cycle; ARCC unavailable and fail-closed behavior preserved. | Protection design READY; implementation next |
| 121 | M5.5 | Added canonical tenant identity through reward and learning fan-out | Added backward-compatible `tenant_id` to strict `LearningEventPayload`; Workflow-only dev-loop scorer stamps attested tenant/principal; reward `_build_context` and learning `_base_payload` preserve tenant through `reward.computed` and downstream Memory/Evals handlers. Existing producers remain byte-compatible via empty default. Test asserts tenant and principal survive into the scored reward. Focused reward/learning suite 110 passed; compile/diff/LOC green (learning contracts 194, rewards 178); Guardian zero-new legacy inventory only. No visible surface changed. ARCC unavailable, fail-closed tenant propagation applied. | Tenant prerequisite LOCAL_GREEN; mandatory Gate A.5 due before protected publisher |
| 122 | M5.5 | Passed post-reward Gate A.5 | QA `d79f186f` reviewed hybrid recall, Workflow-attested per-cycle scoring/retry, and canonical tenant propagation; verdict PASS_WITH_NITS with no blockers. It confirmed identified-only bounded Memory/KB→Graph context, public dev-loop spoof refusal, exact ProjectionBinding/report digest, distinct per-cycle reward/Memory keys, settled-cycle receipt retry, tenant preservation through strict contracts, no direct Games branch, all source <200, and no visible surface change. Explicitly cleared Events-only service-bound downstream publisher. Nits: optional local RewardMetadata typing and clearer binding cross-reference; no behavior remediation required. | Gate A.5 PASS_WITH_NITS; protected publisher ready |
| 123 | M5.5 | Protected downstream reward and learning publication | Added strict service-only `events_publish_learning_signal`, callable only by Events under exact tenant/owner/type/subject/revision/payload-digest `ProjectionBinding`, for exactly `reward.computed` and `memory.learning_stored`. Public `events_publish` now rejects both types. Reward emitter and learning `_publish` migrate to the caller-bound service rail; reviewer-added direct fallback was rejected and removed so missing composition fails closed. Publisher revalidates digest, tenant/principal consistency, and canonical learning payload before Events runtime publication. Seven adversarial tests cover public spoof, direct denial, exact internal success, wrong tenant/digest/type; migrated legacy mocks now exercise service capture. Subagent deterministic evidence: 624 Events/MCP-utils passes (3 skipped), 677 affected passes (3 skipped), compile/LOC/diff green; independent focused 20 passed after fallback removal; Guardian zero-new legacy inventory only. No Games branch, no server launch, no visible surface. | Protected publisher LOCAL_GREEN; source-page/restart/render seam evidence next |
| 124 | M5.5 | Proved Workflow-driven authoritative source paging recovers dropped Graph events | Added Workflow-owned `reconcile_source_pages`: bounded 1–256 records/page and 1–100 pages, strict immutable page DTO, fixed snapshot ID/digest across pages, globally monotonic non-cyclic cursors, source-system binding, canonical page digest, exact Workflow→Graph `ProjectionBinding`, authority envelope, and data-safe failure. New acceptance starts with an empty persistent Graph to model dropped asynchronous lifecycle dispatch, pages Lessons in two pages plus KB and Workflow durable records through service-only `graph_reconcile_projection`, then proves one depth-2 lesson neighborhood restores KB/run/session nodes and `derived_from`/`learned_in`/`about_run`; PersistentNetworkX reconstruction is byte-equal and KB body is absent. Mixed-source pages reject before Graph invocation. Baseline 12 passed; final new/affected tests 16 passed plus isolated async lifecycle acceptance 1 passed. One combined run reproduced the previously documented order-sensitive missing-`learned_in` contamination, then passed isolated without code/test weakening. Compile/LOC (100/141)/diff green; Guardian zero-new, legacy LOC inventory only. ARCC unavailable; fail-closed practices applied. No visible surface. | Source-page recovery LOCAL_GREEN; durable Evals/Memory reward restart next |

## Cycle archived at M6 seam 2026-09-12

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 125 | M5.5 | Proved real durable per-cycle Memory/Evals reward persistence and restart dedupe | Memory `memory_store` adds strict bounded `idempotency_key`; `MemoryRuntime` searches the configured adapter's durable owner records, returns the same record for an equal key/content/type/category retry, rejects conflicting reuse, and stamps the key into metadata before first write, preserving every adapter. Reward Memory handler passes its existing per-cycle key. Reward→Evals uses full `reward_identity` as immutable document identity and lets the canonical writer derive/recover timestamps. Acceptance exposed and fixed an older invalid Evals projection: one passing reward case had `pass_rate=score`; it now records binary case pass rate while retaining continuous `avg_score`. Real acceptance writes through A-MEM/Chroma and SQLite Storage-backed Evals, reconstructs both adapters, replays with empty Events history, proves same Memory ID and one Evals record, then proves cycle 2 creates distinct records. Baseline 37 passed; final affected 76 passed including adversarial Evals tests and public equal-retry/conflict contract. Compile/LOC/diff green; Guardian zero-new legacy inventory only. No Games branch, no visible surface, no server. ARCC unavailable; fail-closed practices applied. | Reward restart LOCAL_GREEN; mandatory Gate A.5 due |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 126 | M5.5 | Passed mandatory post-restart Gate A.5 with two pre-Gate-B blockers | Meta `c867021a` independently inspected cycles 123–125, reran 10 focused tests, checked service-only claim enforcement and all source <200, and returned PASS_WITH_NITS. Confirmed direct M5.5 advancement, reuse of ProjectionBinding/Graph reconcile/Storage CAS/Evals sole writer, strict typed boundaries, correct reward identity, immutable timestamp replay, and fail-closed protected publication. B1: `reconcile_source_pages` has zero production callers or real source page loaders, so dropped-event recovery is test-only. B2: Memory dedupe scans at most `max_memories_per_user` (default 1,000) and is non-atomic, allowing old-key/concurrent duplicates. Nits: durable failure signal for unavailable protected publisher, optional RewardMetadata typing, and zero-reward Evals policy. M2.5 Terminal frame still owed. | Gate A.5 PASS_WITH_NITS; clear B1 then B2 before Gate B |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (2)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 127 | M5.5 | Resolved B1 through the reviewer-approved honest descope instead of unsafe mutable polling | Production inspection showed existing Lessons/KB list APIs are mutable snapshots while Graph reconciliation permanently fences snapshot ID/digest; wiring them to a daily Scheduler target would pass once and reject after the next source mutation. Amended M5.5 design and roadmap: the service-only Graph rebuild seam and bounded Workflow fixture driver remain proven, but automatic dropped-event scheduling/loaders are explicitly deferred until sources expose immutable change feeds; no production automatic-recovery claim remains. This is the exact B1 option (b) allowed by Gate A.5 `c867021a`, avoids a fake/unreachable trigger, and preserves Workflow/Scheduler future ownership. Design compacted to 199 lines; diff clean; Guardian zero-new legacy inventory only. Roadmap change triggers immediate Gate 0. | B1 RESOLVED_BY_DESCOPING; Gate 0 due, then B2 |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (3)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 128 | M5.5 | Passed roadmap-triggered Gate 0 after one reviewer infrastructure timeout | Atlas `a3b2ab08` stalled in a read operation and timed out after 30 minutes without verdict; it is non-evidence. Constrained replacement `6cdb5c82` read all six authoritative docs plus the Workflow page driver and returned ON_PLAN. It confirmed all 13 north-star outcomes remain mapped; automatic dropped-event repair is internal plumbing, not an omitted user-visible feature or Tier-1 criterion; live Events→Graph remains primary; M5.5 exit requires relationship/reward restart durability, not automatic recovery; docs match zero production callers; Workflow/Graph/Scheduler ownership remains correct; and the bounded fixture driver is deliberate seam evidence, not accidental dead code. Optional top-level deferred-list duplication was not needed because the M5.5 roadmap bullet is explicit. | Gate 0 ON_PLAN; B2 sole pre-Gate-B blocker |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (4)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 129 | M5.5 | Replaced capped/non-atomic Memory dedupe with Storage CAS claims and receipts | Added Memory-owned idempotency runtime over existing `storage_doc_create_or_match`: SHA-256 request identity derives O(1) claim/receipt IDs; the atomic claim serializes equal-key writers before any adapter write, conflict hashes reject changed payloads, matched claims without receipts return truthful `memory idempotency in progress`, and receipts bind request hash to the durable Memory ID for restart replay. Removed the 1,000-record `list_all` scan from `MemoryRuntime`; unkeyed behavior and every adapter remain unchanged. Public `memory_store` fails closed when Storage is unavailable and returns only allowlisted domain errors. New public-boundary tests prove replay after 1,005 newer records still returns the original ID and two concurrent writers create exactly one Memory; real A-MEM/Chroma+SQLite Evals restart acceptance remains green. Focused 10 passed; complete Memory suite 262 passed; affected Events 13 passed; compile/LOC/diff green (idempotency 115, runtime 164, MCP 115, test 103); Guardian zero-new legacy inventory only. ARCC unavailable; fail-closed practices applied. No visible surface. | B2 LOCAL_GREEN; Terminal frame then Gate B |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (5)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 130 | M2.5 evidence → M5.5 seam | Captured the owed production Terminal activity frame | Ran bounded `uv run pytest -q components/memory/test/factory/memory/test_idempotency.py` (2 passed in 0.25s). Started the standard launcher in a bounded background process group on API `18000` / Next `13000` with a 240-second TERM→KILL watchdog. A temporary evidence-only Next route imported the real `terminalActivityRenderer`, rendered the exact completed command/output, and was removed after capture. View-only Playwright frame shows Companion X, Terminal command title, `Done`, and `Exit 0`; inspected image saved at `/tmp/python-factory-evidence/m2.5-terminal.png`, SHA-256 `c8eb3f34fef9f1327662668dbb8bd5b93afa0055773551ce66e6d090b54e0d5d`. Both isolated ports released; owner ports untouched; repo contains no temporary route/PNG. | Render debt RESOLVED; M5.5 Gate 0/Gate B due |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (6)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 131 | M5.5 → M6 | Closed M5.5 after milestone-seam program, QA, architecture, and security reviews | Gate 0 `2d41f1c5` ON_PLAN. Replacement QA `d09cd7bf` PASS_WITH_NITS ran 39 targeted tests (14 + 24 + 1) and verified one-call lesson→run→KB restart parity, real A-MEM/SQLite Evals reward replay, Storage-CAS high-volume/concurrency idempotency, strict contracts, and protected refusals. Meta/handoff `4625b3f7` PASS_WITH_NITS / On-rails verified Polylith ownership, MCP taxonomy, adapter preservation, no duplicate scorer/store, and all changed source <200. Replacement security `87627b43` PASS_WITH_NITS verified canonical authority IDs, every-hop isolation, exact ProjectionBinding, content-free projection/tombstones, identified-only untrusted recall, settlement/receipt retry, CAS fail-closed behavior, and no public fallback; ARCC remained unavailable. Original QA `1c3ef3b0` timed out and original security `ac30cb45` returned no verdict, so both are non-evidence. Nonblocking reliability/test/documentation nits retained for Gate C without Beads writes. | M5.5 CLOSED_PASS_WITH_NITS; M6 design audit active |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (7)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 132 | M6 | Audited upstream artifact behavior and Factory rails, then wrote the minimal M6 design | Upstream contract audit `bd383ada` mapped stable collision-suffixed slugs, explicit immutable snapshots, additive revert, 50-version retention, bounded kinds/tags/folders/comments, one-level threads, agent review-only status, gallery/detail/deep links, and typed failure guarantees; remote publication/provider sync, file-backed reload, binary/image, webapp, and deploy remain deferred. Factory gap audit `f0d0d7fe` confirmed no existing Artifacts brick/UI and selected a focused Artifacts owner over Storage public `SQLStore`, preserving protected-artifact, Dataset, Evals, and ML authorities. Direct inspection confirmed Session/Lessons/Scheduler already use `factory.storage.interface.get_sql_store` with revision CAS. New 186-line `m6-artifacts-design.md` freezes strict models, SQLite trigger-backed atomic immutable versions, ambient authority, typed MCP taxonomy, safe sandboxed rendering, UI/chat acceptance, and five slices. `roadmap.md` now explicitly defers omitted upstream remote/file/binary lifecycle, triggering Gate 0. No runtime code changed; Beads untouched. | DESIGN_WRITTEN; Gate 0 + meta/security reviews due |

## Cycle archived during M6.5 rolling-window maintenance 2026-09-12 (8)

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 133 | M6 | Applied Gate 0, architecture, and security design verdicts | Gate 0 `c60d2f3d` ON_PLAN confirmed all 13 outcomes remain mapped and remote/file/binary/webapp artifact lifecycle is honestly deferred. Meta `304d74ea` APPROVE_WITH_NOTES / On-rails confirmed the focused Artifacts owner, Storage public SQLStore, SQLite-trigger precedent, exact Agent tools, typed Events, and separate protected/Dataset/Evals/ML authorities; required adapter-contract, split-file, Tier-1 priority, and prune properties. Security `f8754032` APPROVE_WITH_NOTES under ARCC-unavailable fail-closed review found public SQLStore auto-commits each statement and required single-statement CAS/triggers, bounded owner-only slug inserts, exact idempotency, retained-version immutability, structural content-free event DTOs, server-derived actors, UTF-8 byte caps, opaque tombstones, folder guards, and HTML/widget/SVG sandbox canaries. All amendments and adversarial tests are now frozen in the 191-line design. No runtime code changed; Beads untouched. | DESIGN_APPROVED; scaffold slice 1 ready |

## Cycle archived at M6.5 slice 4 seam 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 134 | M6 | Scaffolded Artifacts and completed owner-scoped save/get/list persistence slice | Foreman MCP was unavailable after two scoped tool lookups; used Foreman current `run_poly_command(["create","component","--name","artifacts"])`, then canonical `write_bricks_index` generated 46 components/5 bases with zero missing/invalid metadata. Added compliant BRICK/interface/server, strict 1 MiB UTF-8 Pydantic models, public Storage SQLStore runtime, bounded NFKD slug allocator, owner-scoped `ON CONFLICT DO NOTHING` save, exact idempotency match/conflict, atomic v1 trigger, immutable-version trigger, typed deterministic get/list and operational save MCP, root/Companion-X composition, and split adapter files. Tests prove v1/restart, same-owner suffixes, cross-owner base-slug parity/opaque reads, filters, exact replay/conflict, concurrent distinct slugs, immutable version rows, strict unknown-field/surrogate/oversize refusal, ambient authority, typed failures, and Hypothesis canonical 64-candidate bounds. Baseline Storage SQL 8 passed; final Artifacts+Storage 19 passed; compile/public import/diff clean; all Artifacts source ≤113 lines; Ruff/mypy unavailable. Guardian 5/6 only legacy LOC inventory, Artifacts absent from violations, BRICKS_INDEX and MCP contracts green, zero new MCP violations. No visible surface; `.storage/docs.json` untouched. | SLICE_1_LOCAL_GREEN; immediate Gate A.5 due |
| 135 | M6 | Passed immediate new-brick/persistence Gate A.5 | QA `3c7c7dc5` independently inspected the design, diff, Storage SQLStore, composition/index, MCP contracts, and all slice-1 source; reran the focused suite with 11 passes and returned PASS_WITH_NITS. It confirmed compliant Artifacts ownership/anatomy, public Storage interface, strict ingress/typed egress/taxonomy, ambient authority/server actor, canonical 64-candidate slugs, owner isolation, concurrent `ON CONFLICT` allocation, exact idempotency, atomic v1 trigger, immutable version row, restart parity, UTF-8 bounds, source ≤113, valid 46-component composition, and untouched adapter breadth. Nonblocking forward nits: push tag filtering before pagination for UI scale; tighten actor kind type; optionally centralize duplicated content validation; and make slice-2 tombstoned version/comment reads opaque. | Gate A.5 PASS_WITH_NITS; slice 2 ready |
| 136 | M6 | Added revision-fenced updates, immutable history, additive revert, opaque tombstones, and content-free lifecycle events | Extended Artifacts with single-statement owner-scoped content/metadata CAS; atomic update trigger inserts snapshots and prunes only that artifact outside the latest-50 window; retained versions reject update/delete; version reads join active heads; revert reads immutable history then creates a new fenced head; tombstones reserve slugs and make head/version/foreign reads uniformly opaque. Added strict `artifacts_versions`, `artifacts_update`, `artifacts_revert`, and `artifacts_tombstone` MCP contracts with JSON-array tag ingress converted to immutable tuples. Typed Events payload structurally allows only authority/slug/version/revision/kind/IDs/content digest and excludes content/name/description/tags/comment body/digest; best-effort publication returns truthful status without rolling back source state. Tightened ActorKind, centralized UTF-8 validation, pushed tag filtering before pagination, and updated BRICK/index capabilities. Tests cover stale conflict, metadata no-snapshot, v1→v2→revert v3, 60-update contiguous latest-50 prune/restart/other-artifact isolation, retained delete denial, tombstone opacity/reservation, strict Events minimization and failure durability, public typed lifecycle, tag pagination, plus Hypothesis prune windows. Artifacts+Storage: 29 passed; compile/public import/diff clean; source max 138; Ruff/mypy remain unavailable; Guardian 5/6 only legacy LOC inventory, zero Artifacts violations, index/MCP green. No visible surface; `.storage/docs.json` untouched. | SLICE_2_LOCAL_GREEN; folders/comments slice 3 ready |
| 137 | M6 | Added owner-scoped bounded folders, one-level comments, and human-gated authoring; passed Gate A.5 | Added strict ArtifactFolder/ArtifactComment records and 20-depth/500-folder/500-comment bounds; focused SQL mixins over public Storage SQLStore; owner-scoped recursive CTE cycle/depth guards; exact RETURNING-based folder-move CAS; active sibling uniqueness; safe fenced folder tombstones whose same-statement trigger reparents direct children/artifacts; revision-fenced artifact moves; conditional one-statement root/reply inserts rejecting reply-to-reply; root review/resolve/delete transitions with reply cascade; opaque tombstoned/foreign comment reads; and one-statement tombstone-only permanent purge. Added deterministic folder/comment reads, operational folder/comment/move tools, authoring-category resolve/delete/purge with ARTIFACTS_ENABLE_AUTHORING_TOOLS kill switch and runtime human defense-in-depth. Actor remains ambient-only; comment events carry structural comment ID but no body/body digest or artifact digest. Tests prove cycle/depth/cap/foreign/CAS, exact concurrent folder winner, safe reparent, one-level nesting rejection, concurrent comment 500/501 exactly 500, agent resolve/delete/purge denial, human resolve/delete/purge, strict forged-actor refusal, typed public MCP lifecycle, and Hypothesis folder depth. Final Artifacts+Storage Gate A: 40 passed; compile/public import/diff clean; max source 188; Ruff/mypy unavailable; Guardian 5/6 only legacy LOC inventory with zero Artifacts violations; index/MCP green; no visible surface; docs.json unchanged. Meta Gate A.5 485b40b1 PASS_WITH_NITS, independently 32 focused green; its four nits (move CAS ambiguity, fabricated validation record, moved label, comment digest minimization) resolved and 32 focused rerun green. ARCC unavailable; fail-closed practices applied. | SLICE_3_GATE_A5_PASS; gallery/navigation/sandbox slice 4 ready |
| 138 | M6 | Added exact Artifacts Agent composition, workbench gallery/deep links, CopilotKit navigation, and sandboxed previews | Registered built-in artifact-curator with an exact 17-tool Artifacts allowlist excluding human-only resolve/delete/purge while preserving the intentionally broad Companion-X default persona. Added Artifacts CanvasView/tab/activity navigation, direct `/artifacts` and `/artifacts/<slug>` route, dedicated non-mutating `fe_navigate_artifacts`, context suggestions, authoritative `artifacts_list` gallery with search/empty/error/retry states, and selected deep-link preview. Text/Markdown/JSON render as escaped host text; widget/HTML/SVG route only through existing McpUiFrame text/html srcdoc. Hostile payload tests assert exact sandbox `allow-scripts`, no `allow-same-origin`, referrerPolicy no-referrer, no parent mutation, and escaped host text. Corrected touched stale Strands comments to active LangChain. Baselines: Agent 12 and frontend 26 passed. Final targeted Agent/Artifacts/Storage: 54 passed; full dashboard Vitest 304 passed across 65 files. TypeScript emitted only pre-existing stale terminal-evidence and ApiError test diagnostics; ESLint unavailable because ESLint 9 has no flat config. Compile/diff clean; Guardian 5/6 only legacy LOC inventory, zero slice-4 violations; docs.json unchanged. Bounded scripts/companion-x-ui.sh launches used API 18000/Next 13000 with 240s/180s watchdogs and TERM→KILL cleanup; both ports connect-refused after each. Real gallery frame `/tmp/python-factory-evidence/m6-artifacts-gallery-settled.png` SHA-256 `18c86e9a535d93523117d7ebb8ffe6737fee090a2e7d3bf7e115815f1c62a645` shows two authoritative cards, tags, filter, active nav, and chat suggestions. Direct sandbox frame `/tmp/python-factory-evidence/m6-artifact-sandbox-preview.png` SHA-256 `5ad71b206c47d1ec0af3b32929abe850a89e443e1fa2dd8301801f6b4560548f` shows widget content inside External UI from Artifacts with gallery/chat intact. Initial progressive MCP 400 retried successfully before authoritative render. ARCC unavailable; fail-closed practices applied. | SLICE_4_LOCAL_GREEN; detail/chat acceptance slice 5 ready |
| 139 | M6 | Added artifact detail/version/comment/revert UI and exact chat lifecycle acceptance | Added shared frontend artifact/version/comment contracts; revision-fenced content editor; immutable version timeline with current-version disable and additive restore; owner comment composer/list/mark-review; authoritative per-slug versions/comments refresh; and explicit stale-revision refresh/retry messaging. Gallery deep links now mount the complete detail while keeping sandboxed preview and chat visible. New project integration drives the exact artifact-curator MCP surface through save v1→update v2→versions→agent comment→revert v1 as new v3→reopen→SQLite restart, proving bytes/history survive; dedicated CopilotKit navigation test proves `/artifacts/<slug>`. Tests cover stale failure UI, restore targeting, post/review calls, hostile containment, gallery states, and exact tool exclusion. Final Gate A: 55 Python passed; full dashboard 307 passed across 66 files; compile/diff clean; TypeScript only three pre-existing ApiError test typing errors; ESLint unavailable because ESLint 9 lacks flat config; Guardian 5/6 only legacy LOC inventory with zero M6 violations; docs.json unchanged. Bounded scripts/companion-x-ui.sh detail launch used API 18000/Next 13000 with 180s watchdog and TERM→KILL cleanup; both ports connect-refused after. Inspected full-workbench frame `/tmp/python-factory-evidence/m6-artifact-detail.png` SHA-256 `c5386b34ca58e914de1a2d33484c1910432daece109eb46d905abfd0a7b926b1`; complete panel frame `/tmp/python-factory-evidence/m6-artifact-detail-panel.png` SHA-256 `f05c5b61ea9924de495c42e285aaee5b3d987383258b254d8e8bf7ed6cd5da62` shows content, revision/version, v2 current/v1 restore, human open comment, and mark-review. ARCC unavailable; fail-closed practices applied. | SLICE_5_LOCAL_GREEN; Gate 0/B pending |
| 140 | M6 → M6.5 | Applied the M6 closure handoff and recorded the completed seam reviews | Corrected false host iframe-listener claims to match the pinned SDK source check and pre-render origin gate; narrowed internal artifact mutation events to `updated|reverted`; aligned runtime capability labels with `BRICK.yaml` and added a regression; corrected idempotency and purge-audit design wording. Baselines 33 Python/33 frontend passed; final 34 Python/33 frontend passed; compile and diff clean. Ruff unavailable. TypeScript retained only the same three pre-existing `ApiError` test diagnostics. Guardian 5/6 with zero new MCP violations and only 74 legacy file-size violations. Existing reviews recorded without rerun: Gate 0 `c9fc6775` ON_PLAN; QA `f0093486`, meta/handoff `16937749`, security `7ea99cd3`, UX `8c02ab4e` PASS_WITH_NITS. Beads close succeeded, but its backup hook unexpectedly created and pushed metadata-only commit `ea35fe4ed` despite `--sandbox --dolt-auto-commit=off`; no migration code was committed and further Beads writes are suspended. No visible behavior changed, so no new frame was required. ARCC unavailable; existing fail-closed policy preserved. | M6 CLOSED_PASS_WITH_NITS; M6.5 design audit active |
| 141 | M6.5 | Audited upstream Crew behavior and Factory rails, then wrote the minimal M6.5 design | Upstream audit `ad1e1206` distinguished Crew configuration data from out-of-scope `CrewOrchestrator`; mapped typed bundle/default/resolution/CRUD/roster/model-picker behavior, truthful catalog failures, credential-shaped-name refusal, and workspace confinement. Factory audit `e694e437` confirmed reusable Agent persona registry, Session project/model persistence, Devtools confinement, safe LLM Gateway profiles, official LangChain model clients, CopilotKit properties, and exact gaps: no Crew record, logical memory scope, compatible profile, per-session runtime model, model-keyed graph cache, catalog, page, or picker. New 198-line `m6-5-crews-design.md` keeps Crew in Agent registry + Session materialization, Memory as logical namespace over the project-selected adapter, provider parsing in LLM Gateway, and execution in the existing LangChain runtime. It freezes secret-by-env-name profiles, loopback/allowlisted HTTPS base URLs, Session authority, five slices, SDK pins/dispatcher source, strict MCP/UI failures, Hypothesis properties, restart/live Devtools acceptance, and explicit deferrals. ARCC unavailable; fail-closed standard guidance applied. No runtime code or visible surface changed; no tests/frame required. Beads writes remain suspended. | DESIGN_WRITTEN; meta/SDK/security reviews due |

## Cycle archived during M7 slice-2 rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 142 | M6.5 | Applied mandatory pre-implementation design amendments | Meta `0a4783fa` APPROVE_WITH_NOTES confirmed Crew-as-data/no-new-brick ownership and required a focused owner-scoped `CrewStore` rather than widening persona `RegistryStore`, Session-only materialization, SRP splits, partition-key Memory scope, and empty-model refusal. SDK `779b2bbb` APPROVE_WITH_NOTES confirmed official ChatOpenAI/CopilotKit/LangGraph reuse and required explicit compat-before-Bedrock branching, first-class `RuntimeInvocation.model_id`, complete constructor call-site migration, model-specific Bedrock refresh eviction, and SessionDeck field plumbing. Security `6a831fb0` AMEND_PLAN found four blockers: collision-unsafe Memory composition, globally unscoped Crew records, decorative revision without CAS, and possible cross-session model confusion. Rewrote the design to 196 lines with owner-derived authority on every Crew path; focused CAS store with owner lock + atomic replace + default/delete fence; fixed-width owner hash + validated scope as the adapter partition key; model ID selecting both model instance and graph cache; allowlist-before-env identical safe errors; loopback/remote URL policy; server-side set-model validation; credential-clean IDs; secret-free error enums; two-provider and eight adversarial acceptance cases. No runtime/UI code changed. ARCC unavailable. Beads writes suspended. | AMEND_PLAN_APPLIED; narrow security re-review required |

## Cycle archived during M7 plan-store rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 143 | M6.5 | Cleared the amended security design gate | Narrow re-review `9494fc01` returned APPROVE with no remaining text amendments. It verified A1 fixed-width owner-hash + validated Memory scope partition, A2 tenant/owner-scoped focused CrewStore with no global fallback, A3 store-layer CAS/atomic replace/default-delete fence, and A4 first-class per-request model selection + model-keyed graph cache + targeted refresh eviction. It confirmed all eight adversarial test classes and slice-level N1–N5 commitments are present. ARCC unavailable; fail-closed guidance applied. Design remains 196 lines; no runtime/UI code changed; Beads writes suspended. | DESIGN_APPROVED; slice 1 safe compatible profiles ready |

## Cycle archived during M7 target-preparation rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 144 | M6.5 | Implemented safe named OpenAI-compatible profiles and official ChatOpenAI routing | Added focused `openai_compat_policy.py` with explicit profile allowlist before env reads, credential-shaped-name rejection, deterministic env stems, loopback-address HTTP policy, exact operator-allowlisted remote HTTPS, malformed URL refusal, and one stable non-secret error. `resolve_chat_profile` now handles `openai-compat/<name>` before Bedrock; `ChatProfile` retains only API-key env name. Added non-secret configured model catalog via `llm_gateway.interface`. Agent factory explicitly builds official `ChatOpenAI` for compat profiles and cannot fall through to Bedrock; existing OpenRouter/Ollama/Bedrock branches remain behavior-compatible. Gate A exposed and fixed allowlisted-remote DNS dependence and raw `socket.gaierror` leakage. Added Hypothesis identifier coverage, env-existence error parity, URL/credential/secret traceback/catalog tests, and native model construction tests. Baseline 15 passed; final broader LLM Gateway + Agent model suite 102 passed; compile/diff clean; all touched files 35–145 lines. Ruff/mypy unavailable. Guardian 5/6 with zero new violations and only 74 legacy LOC violations. No visible surface, so no frame required. `.storage/docs.json` untouched. ARCC unavailable; fail-closed guidance applied. Beads writes suspended. | SLICE_1_LOCAL_GREEN; per-session model runtime slice 2 ready |

## Cycle archived during M7 plan-cursor rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 145 | M6.5 | Added first-class per-session model authority and model-isolated LangChain caches | `RuntimeInvocation.model_id` is now a validated first-class field. New focused `LangChainModelCache` resolves request→persona→process default, builds official clients by ID, and supports targeted replacement. Runtime graph identity now includes model ID and uses that exact client; checkpoint identity remains persona+thread. Root chat/runtime construction injects the existing official builder; Bedrock expiry refresh replaces only the request model and its graphs. Chat, squad, graph-node, spawn, coordination, and managed-graph paths all propagate model ID; AST validation found zero production constructors missing it. Concurrent two-model and targeted-refresh tests prove no client/graph sharing; facade test proves refresh retries the same selected ID. Gate A found and fixed neutral test/local chat default `unknown`→empty precedence delegation. Baseline 35 passed with five pre-existing missing-`langchain_community` squad failures. Final focused 42 + selected squad 3 passed; broad Agent 755 passed/1 xfailed with only three pre-existing missing-`langchain_community` local-toolbelt failures. Compile/diff clean; runtime 199 and squad 200 lines. Ruff/mypy unavailable. Guardian 5/6, zero new violations, 74 legacy LOC only. No visible surface/frame. `.storage/docs.json` untouched. ARCC unavailable; fail-closed isolation applied. Beads writes suspended. | SLICE_2_LOCAL_GREEN; owner-scoped Crew/Session/Memory slice 3 ready |

## Cycle archived during M7 apply-execution rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 146 | M6.5 | Integrated owner-scoped Crew storage, Session materialization, and structural Memory namespaces | Added strict owner-derived CrewConfig; focused CrewStore instead of widening persona RegistryStore; in-memory/disk adapters with authority-hashed namespace, exclusive create, revision CAS, owner lock, atomic fsync+replace, and non-dangling default/delete. Added prefixed typed Agent Crew list/get/resolve/create/update/default/delete MCP; create/update validate persona, project, and configured model; disk errors log safe types only. Session schema migrates backward-compat crew_id/memory_scope; sole create/ensure path materializes them; owner-scoped CAS set-model/rebind validates model and atomically updates crew/persona/model/project/workspace/scope. Memory owns credential-clean owner/scope partition `sha256(owner)+"."+scope`; every RuntimeInvocation propagates memory_scope and hybrid recall uses it as adapter user_id, never post-filter. Parent integration fixed stale Session metadata model registration and now binds existing Session persona/model/scope before LangChain graph selection, so frontend hints cannot override durable state. It also restored generic Session model-ID compatibility, completed full project/workspace rebind, prefixed Crew tools, and added missing definition/model validation. Tests: subagents 22 Crew, 48 Session, 69 Memory/propagation; merged baseline 129; final focused 106 and structural 53; full Session 48, Memory 280, broad Agent 789 passed/1 xfailed. Compile/diff/AST/LOC clean; all production invocations carry model+memory scope; all source ≤200. Ruff/mypy unavailable. Guardian 5/6 with zero new violations and 74 legacy LOC only. No visible surface/frame. `.storage/docs.json` untouched. ARCC unavailable. Beads writes suspended. | SLICE_3_LOCAL_GREEN; immediate Gate A.5 due |

## Cycle archived during M7 Workflow-continuation rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 147 | M6.5 | Passed immediate three-slice architecture/persistence Gate A.5 | Meta `a2ad7085` returned PASS_WITH_NITS and independently reran 65 representative compatible-profile, Memory partition, Session binding, Crew lifecycle, pre-graph Session, and scope propagation tests; Guardian 5/6 with zero new violations. It confirmed direct milestone advancement, official SDK/Factory reuse, justified focused CrewStore/ModelCache, typed prefixed MCP, ambient authority, source ≤199, Session authority before graph/model selection, owner/CAS/default-delete fences, genuine Memory partition wiring, safe logging, generic Session compatibility, and UI readiness. Parent integration additionally ran 106 merged tests, all 48 Session, all 280 Memory, broad Agent 789/1 xfailed, plus 53 final structural tests; fixed stale metadata model registration, durable Session pre-graph binding, Crew prefix/definition validation, full persona/model/project/workspace/scope rebind, and explicit constructor propagation. Nits applied: local Crew lock claims are now explicitly single-process with distributed conditional-write requirement; unidentified hint passthrough documented; slice 4 must group mixed providers. No blocker, no visible frame yet. Ruff/mypy unavailable. `.storage/docs.json` untouched. ARCC unavailable. Beads writes suspended. | GATE_A5_PASS_WITH_NITS; slice 4 UI ready |

## Cycle archived during M7 engine-registration rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 148 | M6.5 | Paused at the required frontend visual-spec gate | Frontend-design workflow requires a selected mockup before coding when layout/interaction choices remain. Rendered three theme-aware options: A card gallery (recommended; matches Artifacts and is most discoverable), B master-detail, C dense table. Sent one blocking dashboard question card; cycles 33–36 arrived without an answer. No UI code was written and no question was repeated. Independent backend acceptance remains sequenced after UI and does not remove the required visual decision. Beads writes remain suspended. | BLOCKED_ON_OWNER_LAYOUT_SELECTION; auto-nudge halted to prevent repeated no-progress cycles |

## Cycle archived during M7 multi-kind rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 149 | M6.5 | Completed card-gallery/model-picker slice 4, live render, usability remediation, and macOS Crew durability hardening | Added secret-free `agent_list_models`, grouped provider picker, selected card gallery, complete Crew create/edit/default UI, and New Session default-Crew materialization through Agent resolve → Session create; AG-UI next-session model hint remains subordinate to durable Session binding. Fixed the component test’s accidental removal of `parseSession`. Full dashboard Vitest 325/325; focused final frontend 17/17; backend catalog/hint 29 passed; final Crew durability/CAS/MCP 31 passed; TypeScript retained only three known `ApiError` test diagnostics. Guardian ratchet against `ea35fe4edee92ba6c671e6ebf13145167e1e5449` passed 6/6 with zero new violations. Authenticated live MCP on API 18000/Next 13000 created three owner-scoped Crew cards and fenced default `m65-platform`; discovered local directory-fsync false-failure portability and added unsupported-errno handling with fail-closed unknown I/O plus type-only safe logging. Playwright MCP streamed and captured `/tmp/python-factory-evidence/m6.5-crews-gallery.png`; pixel review found/fixed project overflow with ellipsis/full-value tooltip. New-user reviewer `8120a9da` PASS; added plain-language Persona/Project/Model/Memory hints and screen-reader text. ARCC unavailable; fail-closed practices applied. `.storage/docs.json` untouched. Beads writes suspended. | SLICE_4_LOCAL_GREEN; slice 5 live non-OpenRouter acceptance ready |

## Cycle archived during M7 lifecycle rolling-window maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 150 | M6.5 | Probed the frozen slice-5 live provider prerequisite and stopped at truthful external dependency | Read the accepted design and existing chat-only Devtools harness. Project `.env` has no `COMPANION_X_OPENAI_COMPAT_PROFILES`; presence-only checks found no OpenAI/LM Studio/vLLM/LocalAI credential; loopback ports 1234, 11434, 8001, and 8080 are closed. The contract explicitly requires a tool-capable non-OpenRouter endpoint for real Devtools read → CAS edit → test → diff → outside-root refusal and restart, so a fake protocol server or OpenRouter substitution would not satisfy acceptance. No provider call, project-code transmission, file mutation, or cost occurred. Beads writes remain suspended. | BLOCKED_ON_COMPAT_ENDPOINT; owner must configure/start one compatible endpoint |

## Cycle archived at M7 Migration slice-2 closure 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 151 | M6.5 → M7 | Completed live distinct-model Crew acceptance, restart continuity, Gate 0 amendments, and full Gate B | Fixed Companion-X production Agent composition to persist owner-scoped Crew/persona config under `.storage/agent-config` while generic Agent defaults stay temporary; added a 174-line two-phase authenticated acceptance driver. Live `openrouter/cohere/north-mini-code:free` Crew (process default remained `openrouter`→DeepSeek) materialized developer/project/workspace/`m65-live` scope through Session, then executed Devtools read→CAS edit→pytest→git diff→outside-root refusal on an isolated fixture. Fresh API/Next PIDs on 18000/13000 recovered Crew, default, resolved model, Session model/project/memory, and edit; all smoke groups released. Evidence: 104 representative cross-seam tests, 40 composition tests, independent fixture pytest 1 pass, final UI 16 pass, TypeScript only three known `ApiError` diagnostics, API amendment 11 pass, Guardian ratchet 6/6, source ≤200, Ruff/mypy unavailable, docs.json unchanged. Frames `/tmp/python-factory-evidence/m6.5-crew-live-restart.png` and `...-final.png` inspected. Gate 0 `5df3bd54` AMEND_PLAN corrected stale exit wording/garbled design; QA `cd40b97b`, meta `540630a8`, SDK `0ee48694`, security `e9190830`, replacement UX `3de70d1c` PASS_WITH_NITS; initial UX `9d535ccd` timed out after violating its no-search scope. Fixed meta LOC nit (`ag_ui_routes.py` 200) and UX dt/dd, name-tooltip, canonical active-model semantics. Remaining nits are nonblocking/documented assumptions: local-only CAS, operator-trusted URL/DNS/root config, future distributed conditional writes, optional atomic set-default return. ARCC unavailable; fail-closed practices applied. No Beads write or commit. | M6.5 CLOSED_PASS_WITH_NITS; M7 design audit ready |

## Cycle archived at M7 Notification audit 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 152 | M7 | Audited upstream import formats, Factory product rails, and Tier-1 UX debt; wrote the minimal M7 design | Parallel audits `0ede6ab2`, `91172b25`, and `5abe8547` mapped KiroCrew SQLite/JSONL/cron/markdown formats and exclusions, existing typed target MCP rails, and missing Schedule/Lessons/Notifications/direct-Session UX plus Gate C evidence. New 176-line `m7-product-integration-design.md` selects a focused Migration brick with `kirocrew-v1` adapter, operator-configured no-follow temp snapshot, immutable plans/receipts, Workflow-owned attempts/restart, target-owned protected merge-only Memory/Lessons/paused-Scheduler writes, Notification-owned durable inbox/closed deep-link union, mockup-gated Operations UI, import Settings flow, and one unified Gate C recipe. Corrected roadmap: upstream canonical onboarding excludes `sessions/*.jsonl`; M7 does not fabricate foreign LangGraph checkpoints. Explicitly excludes secrets, command/script/runtime cron fields, skills/MCP/config/workspaces, transcripts, overwrite, and external URLs. ARCC queried and unavailable; fail-closed limits/adversarial fixtures specified. No runtime/UI code changed; no frame/tests required. Beads writes remain suspended. | DESIGN_WRITTEN; pre-implementation meta/security/UX review due |

## Cycle archived during M7 Notification baseline maintenance 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 153 | M7 | Applied pre-implementation architecture, security, and UX design verdicts | Meta `b7dab063` APPROVE_WITH_NOTES / On-rails approved a focused Migration brick over Dataset/Integrations/Agent and required existing Workflow checkpoint progression, Storage SQLStore receipts, existing Memory semantics, and split parser files. Security `81fad35c` AMEND_PLAN found dirty-WAL data-loss risk, whole-root secret/model/session staging, and under-specified TOCTOU defenses; design now uses SQLite online backup + integrity check, stages only enumerated allowlisted paths, and validates path components/leaf descriptors with lstat/O_NOFOLLOW/fstat. It also supports canonical `cron/jobs.json`, atomically paused imports, source-identity idempotency, target authorization on deep-link open, credential/PII inbox redaction, and operational read-only preview taxonomy. UX `212d0660` AMEND_PLAN expanded mockups to grouped Operations + topbar inbox + Settings import, kept accepted Artifacts/Crews stable, and froze keyboard/a11y/focus/stale-target/plain-language merge-only/source-disabled/replay states. Amended design is 189 lines; no runtime/UI code changed and no frame required. ARCC unavailable; fail-closed practices applied. | AMENDMENTS_APPLIED; narrow security/UX re-review due |

## Cycle archived at M7 Notification persistence Gate A.5 2026-09-13

| Cycle | Bead | Action | Evidence | Result |
|---:|---|---|---|---|
| 154 | M7 | Cleared amended security and UX design gates | Narrow security re-review `25a8f4f6` APPROVE confirmed WAL-inclusive online backup/integrity check, allowlisted-only staging, lstat/O_NOFOLLOW/fstat TOCTOU closure, paused canonical cron import, source-identity idempotency, deep-link reauthorization, credential/PII redaction, operational preview taxonomy, and adversarial fixtures. Narrow UX re-review `3d40cb60` APPROVE confirmed grouped mockup scope, additive Artifacts/Crews treatment, accessible topbar inbox, plain merge-only/import/replay/source-disabled copy, accessible names/aria-current/live regions/focus, and deleted-target handling. Meta `b7dab063` remained APPROVE_WITH_NOTES / On-rails. Final design 189 lines; diff clean; no runtime/UI change or frame. | DESIGN_APPROVED; Migration slice 1 ready |

| 155 | M7 | Scaffolded and completed Migration slice 1 source preview and durable receipt substrate | Foreman MCP was unavailable after two scoped lookups; Foreman current `run_poly_command(["create","component","--name","migration"])` scaffolded the brick and canonical `write_bricks_index` produced 47 components/5 bases with zero missing/invalid metadata. Added compliant BRICK/interface/server, root/Companion-X composition, strict operational `migration_preview`, ambient authority, safe fixed errors, redacted bounded samples/reports, immutable owner-scoped plans, public Storage SQLStore receipts/cursor CAS, and exact replay/conflict/restart/concurrency properties. `kirocrew-v1` stages only allowlisted paths through lstat/openat/O_NOFOLLOW/fstat, rejects links/devices/owner/size/mutation, backs up dirty-WAL SQLite online with integrity/post-mutation/size checks, and splits Memory/Lessons/Schedules/Markdown parsers. Parent hardening bounded diagnostics, removed raw DB errors, separated scoped lesson identity, stripped imported auto-approval while preserving skip dates, fixed descriptor leaks, made selectable kind plans distinct, and kept strict SQL enum reconstruction. Full Migration 82 passed; public catalog exposes only operational preview; compile/diff clean; all production source 4–199 lines; Guardian ratchet 6/6; Ruff/mypy unavailable; docs.json unchanged. No visible surface/frame. No Beads write or commit. | SLICE_1_LOCAL_GREEN; immediate Gate A.5 due |

| 156 | M7 | Passed immediate Migration new-brick/persistence Gate A.5 and applied nits | Meta `904b3064` PASS_WITH_NITS / On-rails independently reran 82 Migration tests and Guardian, confirming focused ownership, Workflow progression deferral, public Storage SQLStore, owner-scoped receipt CAS/restart, WAL online backup/integrity, fd-safe allowlisted staging, bounded redacted output, scoped lesson identity, paused/no-auto-approval schedules, strict operational typed MCP, 47-component composition, all source ≤199, and zero new violations. Applied substantive nits: corrected design receipt uniqueness to source_record_id and normalized every receipt adapter exception to fixed `migration_preview_unavailable`; added non-leak regression. Cosmetic schedule found-count note remains truthful. Final Migration 83 passed; Guardian ratchet 6/6; diff clean. No visible change/frame; no Beads write or commit. | GATE_A5_PASS_WITH_NITS; slice 2 target writes/Workflow ready |

| 157 | M7 | Split oversized protected-import tests before durable orchestration | Confirmed both halt sentinels absent and refreshed all M7 contracts. Split Memory 206 lines into 183/33, Lessons 257 into 173/97, and Scheduler 209 into 178/45 without changing behavior; package-relative helper reuse preserves the exact protected native-dispatch fixtures. All six shards remain under 200 lines; the complete protected Memory/Lessons/Scheduler import suite passed 41 tests in 7.21s; `git diff --check` clean. Guardian baseline ratchet against `ea35fe4edee92ba6c671e6ebf13145167e1e5449` passed 6/6 with no new LOC violations; strict Guardian remains 5/6 solely for 74 known legacy LOC files, none touched here. No production/UI behavior changed, so no frame required. `.storage/docs.json` untouched; no Beads write or commit. | TARGET_TEST_LOC_CLEAN; immutable Migration plan-record persistence ready |

| 158 | M7 | Persisted immutable parser-approved plan material and passed immediate persistence Gate A.5 | Added strict frozen `PlanRecord`/commit models, canonical payload JSON+digest, selected-kind/payload/identity validation, and owner/tenant/adapter/source-fingerprint/plan-digest binding. Split SQL plan material into a 141-line mixin over public Storage `SQLStore`; insert-only rows replay exact material, conflict without overwrite, verify digest on read, page deterministically at ≤100, and survive fresh `StorageRuntime` restart. Preview now reparses the trusted temp snapshot, persists only parser-approved bounded records, and binds the plan last; source mutation creates a new plan while old material remains byte-logically unchanged. Added Hypothesis canonical round-trip plus replay/conflict/owner/paging/restart/source-change tests. Baseline 83; full Migration 89 passed; focused after nits 21 passed; final selected-kind guard 6 passed; compile/diff clean; all Migration Python ≤200. Guardian ratchet 6/6. Immediate QA Gate A.5 `135d8de6` PASS_WITH_NITS and approved Workflow progression; removed unused import, trimmed receipt model to 196 lines, and replaced test-only Storage runtime internals with public `StorageRuntime`. No UI/frame, Beads write, commit, or docs.json change. | PLAN_MATERIAL_GATE_A5_PASS; Workflow apply/enrollment ready |

| 159 | M7 | Added deterministic target payload/digest preparation for immutable plan records | Added strict Migration-owned Memory/Lessons/Scheduler target argument models and deterministic preparation with no production imports from target bricks. It converts prefixed plan IDs to the shared raw-hex wire representation, maps Markdown to source-path-free semantic Memory, canonicalizes Memory tags, maps unknown lesson categories safely to knowledge, derives exact lesson identity/digest, maps every supported schedule shape to paused-target ingress with deterministic source schedule ID, and returns one fixed unsupported-record error for target-bound violations. `PreparedTargetCall` now emits Python-mode native kwargs and exactly the eight `migration_import` binding fields from one validated object. Cross-brick tests validate every payload through the target owner DTO and compare digests against target owner helpers, including all schedule kinds and oversized refusal. Initial validation exposed JSON-vs-strict-Python representation drift; corrected Memory lists and retained Lesson/Scheduler tuples/datetimes while hashing Scheduler JSON-normalized definitions. Final focused Migration/mcp_utils/Memory/Lessons/Scheduler suite 35 passed; compile/diff clean; touched files 120–151 lines; no target-brick production imports; Guardian ratchet 6/6. No UI/frame, Beads write, commit, or docs.json change. | TARGET_PREPARATION_LOCAL_GREEN; Workflow apply execution ready |

| 160 | M7 | Hardened Migration page cursors to exact plan identity before Workflow apply | While auditing Workflow execution, found the existing cursor key `(tenant, owner, adapter, source_fingerprint, kind)` could share progress between distinct kind-selection plans over the same snapshot. Added strict prefixed `plan_digest` to `PageCursor`, its port, every load/save CAS, and a new `migration_plan_cursors` table whose primary key includes the plan digest; the old pre-plan table is no longer read. Added explicit same-source/different-plan isolation plus restart continuity. Compressed only the receipt-model docstring to retain 195-line headroom. Targeted receipt model/store/state-machine suite 33 passed; compile/diff clean; touched production files 90–195 lines; Guardian ratchet 6/6. No UI/frame, Beads write, commit, or docs.json change. | PLAN_CURSOR_LOCAL_GREEN; exact Workflow apply execution ready |

| 161 | M7 | Implemented exact Workflow-authorized Migration apply execution and passed immediate Gate A.5 | Added strict execution/page/output DTOs and service-only `migration_apply_execution` under `callers={workflow}` + `binding=execution`; ambient tenant/owner is reasserted before effects. The bounded executor reads one exact plan-scoped cursor page, prepares target-owned calls, invokes only through caller-bound `migration_import`, writes no receipt/cursor on malformed/transport failure, stores typed failures without cursor advancement, supersedes failed receipts on retry, skips exact settled replays, conflicts on changed target material, and CAS-advances only after all page records settle. Added data-level Memory refusal handling and completed-page no-op behavior. True `NativeEnvelopeInvoker` integration proves Workflow → Migration → protected Memory binding. Composed Migration/Memory/Lessons/Scheduler/native suite 148 passed; focused final 33 passed; native integration 1 passed; compile/diff clean; files 56–158 lines; Guardian 6/6. Immediate meta Gate A.5 `ca423559` PASS_WITH_NITS confirmed ownership/authority/idempotency but found Workflow hardcodes `max_attempts=2`; partial-page continuation would wrongly consume failure retries and truncate imports. Engine remains unregistered. No UI/frame, Beads write, commit, or docs.json change. | APPLY_EXECUTION_GATE_A5_PASS; Workflow continuation outcome required before enrollment |
