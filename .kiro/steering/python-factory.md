---
inclusion: always
---
# Python Software Factory — Agent Operating Manual

Polylith monorepo. Brick counts are derived from `BRICKS_INDEX.yaml`. Every brick = full MCP interface. Agents interact through MCP, never direct imports.

## NORTH STAR

The vision every session and sub-agent optimizes toward. When a decision is ambiguous, these win.

1. **Emergent best practices.** Use the current, proven way — not yesterday's habit. Check what the framework/SDK ships today before hand-rolling.
2. **Engineering excellence.** No bandaids, no shims, no "temporary" hacks. Elegant and simple over clever. Prefer framework primitives over bespoke code. Polymorphic where possible. Avoid both under- and over-engineering — solve the actual problem cleanly.
3. **Recursively learning system.** Companion-X is a self-contained loop: it acts, observes outcomes, scores them, and improves itself the next cycle. Every feature should feed the loop, not dead-end.
4. **Game theory · RL · blockchain are first-class.** Games model strategic/adversarial dynamics; Reinforcement Learning drives system evolution; blockchain provides the trust/economy/provenance layer. Reach for them when a problem fits — they are core substrate, not bolt-ons.
5. **UX is paramount — the assistant drives everything.** The user should be able to do anything in the app *through* the assistant, and the assistant has full control of the app. Theme: minimal, clean, elegant, animation-forward.

## AGENTIC CONTROL PLANE — NORMATIVE

> Freeze the handshake, not the intelligence.
>
> Write only enough code to define safe handoffs, preserve evidence, and execute repeatable operations. Let agents perform work that requires intelligence: research, interpretation, planning, and adaptation.
>
> Agents decide and compose; bricks execute and guarantee; Evals challenge; Workflow remembers.

This file is the single normative authority for the control-plane split. Secondary docs describe current implementation and link here; they do not redefine ownership.

- **Agent brick — intelligent runtime.** Owns the runtime and persona registry, skills, model/tool scoping, structured outputs, native LangChain/LangGraph agent, middleware, checkpoint, and multi-agent primitives, and a small catalog promoted only from repeated evidence. Within one attempt, a registered LangGraph graph owns bounded research, synthesis, and revision. Exploratory multi-agent composition must remain bounded; it is not the durable process ledger.
- **Companion-X — control plane, not another runtime.** Dynamically assembles bounded, objective-specific teams through Agent surfaces, invokes brick capabilities through MCP, and renders evidence. It does not own a parallel agent runtime or static domain team.
- **Capability bricks — deterministic execution.** Execute typed, bounded operations through MCP and guarantee their declared contracts. They do not adaptively plan, revise objectives, or own cross-brick lifecycle.
- **Workflow brick — durable attempts.** Owns cross-brick attempt history, global budgets, retries, cancellation, recovery, stopping rules, and terminal reasons. It remembers what ran; it does not perform intelligent research or synthesis.
- **Dataset brick — immutable materialization.** Validates and materializes immutable blueprints through allowlisted references. Its only retries are bounded, idempotent internal replays over frozen inputs or checkpoints; Workflow owns global retries, and Agent Graph owns deficiency-driven revision.
- **Games brick — reward and evidence.** Emits reward signals and supporting evidence for strategic outcomes. It does not accept or promote artifacts, trajectories, datasets, or models.
- **Evals brick — independent challenge.** Owns frozen evaluation policies, immutable evidence-bound reports, typed deficiencies, acceptance, and promotion. Agents may propose work but cannot supply executable validators or change criteria after results are observed.

**Domain-neutral acceptance gate.** Prove every generic control-plane path with two unrelated domains using the identical Agent → Workflow → capability bricks → Evals runtime path. Registered specialized computational adapters are allowed when selected through typed, immutable references; domain-specific orchestration branches are not.

## RULES

PLATFORM DOCTRINE — Companion-X is a domain-agnostic expert-augmentation engine wrapped by a single interaction surface: Companion-X Web (Next.js). Depth is a mode of that cockpit (embedded Monaco + the existing agent + the `sandbox` brick), added when a task warrants it — there is one cockpit until a second surface earns its place. The engine is built once; a domain is pure data (persona/skills/SOP + taxonomy + game config + presentation manifest + eventually a LoRA adapter). Zero `if domain == ...` anywhere; security is domain pack #1. Tools are three layers: expertise→agents, capabilities/connectors→MCP (the connector marketplace), visualization→render surfaces. Details: `.agents/steering/platform-doctrine.md`

ARCHITECTURE — Polymorphic, clean, agnostic. <200 LOC per file. SRP. Business logic in `runtime/`, MCP in `mcp/`, public API in `interface.py`. Bases are pure transport shells. No cross-imports (use `factory.<brick>.interface`). Details: `.agents/steering/dev-principles.md`

SDK-FIRST — Favor framework primitives over bespoke wrappers. **Runtime truth (2026-09-11): the agent chat/graph runtime is LangChain 1.x + LangGraph 1.x over MCP v2 and has zero Strands imports. A gated optional `strands_evals` SDK canary remains in `components/evals` behind function-local imports; it is not the Agent runtime baseline or a Track B runtime dependency. Do not reintroduce Strands into Agent execution or cite its primitives as the baseline.** LangChain/LangGraph ship `create_agent`, `AgentMiddleware` (before/after-model hooks), `bind_tools`, structured output, `langchain-mcp-adapters` for MCP v2 tool loading, `AsyncSqliteSaver`/checkpointers for durable threads, `StateGraph`/subgraphs for multi-agent composition, and provider-native chat models (`ChatOpenAI`, `ChatOllama`, `ChatBedrockConverse`). CopilotKit ships dispatcher + frontend tools + activity messages. If the SDK has a primitive, USE IT. Reimplementing what the SDK provides is a tenet violation. Bespoke executor/registry/tool-wrapper drift is the cautionary tale (epic `python-factory-sd72`). Before writing new code that wraps a third-party FE SDK with a dispatcher (CopilotKit, Vercel AI SDK), the design phase MUST cite the dispatcher source path + version pin. Note: `@ag-ui/client` is the wire-protocol layer CopilotKit's dispatcher consumes — it does not ship its own dispatcher. If a primitive is genuinely missing, build it AND document the gap with a file/line reference and an upstream issue link. Details: `.agents/steering/dev-principles.md` SDK-First section. Installed FE SDK pins on the chat surface: `@copilotkit/*@1.53.0` (dispatcher: `frontends/next-dashboard/node_modules/@copilotkit/react-core/dist/`), `@ag-ui/[email protected]` (wire), `@mcp-ui/[email protected]` (mcp-ui carrier — dispatcher: `frontends/next-dashboard/node_modules/@mcp-ui/client/dist/index.mjs` exports `UIResourceRenderer` which fans out internally to `dist/src/components/{HTMLResourceRenderer,RemoteDOMResourceRenderer}.d.ts` by `resource.mimeType` — `text/html` → iframe srcdoc/src; `text/uri-list` → iframe src; `application/vnd.mcp-ui.remote-dom+javascript` → RemoteDOM script). Python first-party producer: `mcp-ui-server>=1.0.0,<2.0.0` (PyPI 1.0.0 = internal `__version__` 5.2.0; entrypoint `mcp_ui_server.create_ui_resource`). meta-architect verdict 26d6cd24. security-engineer verdict d580bfdd flagged the SDK default sandbox `allow-scripts allow-same-origin` for `external_url` mode → MUST override at mount time (lo1g9.4). Sunset triggers and dispatcher pin tables: `.agents/steering/upstream-sdk-shims.md`.

BRICKS — Every brick follows the same structure: BRICK.yaml, `runtime/ports.py` (Protocol), `mcp/` primitives, `interface.py`. Workflow brick is the exemplar. Details: `.agents/steering/brick-anatomy.md`

SCAFFOLDING — Use foreman MCP tools for all brick creation, compliance checks, and index management. `foreman_guardian_check` is the gate. Details: `.agents/steering/workflow.md`

TESTING — Hypothesis property tests required for all stateful bricks. Recipes in `.agents/recipes/` define E2E scenarios. Run existing tests before writing new ones. Details: `.agents/steering/hypothesis-testing.md`

MEMORY — Use companion-x memory brick (NOT a-mem). `memory_store`, `memory_retrieve` via `call_brick_tool`. Always pass `user_id: "kiro-agent"`. Details: `.agents/steering/companion-memory.md`

ISSUE TRACKING — Beads (`bd`) is the single source of truth. `bd ready --json` | `bd update <id> --claim` | `bd close <id> --reason "..."` | `bd create "title" -p <0-4> --deps discovered-from:<id>`. No markdown TODOs. Details: `.agents/steering/beads-workflow.md`

SHELL SAFETY — Always use non-interactive flags: `cp -f`, `mv -f`, `rm -f`, `rm -rf`. Never let shell commands hang on prompts.

MODELS — Chat models are built by the single LangChain factory `components/agent/.../runtime/adapters/langchain_model.py` from a safe `llm_gateway.resolve_chat_profile()` profile: `openrouter/<vendor>/<model>` (or `COMPANION_X_CHAT_MODEL=openrouter` + `OPENROUTER_MODEL`) → `ChatOpenAI`; `ollama/<model>` → `ChatOllama`; bare Bedrock id → `ChatBedrockConverse`. Secrets are referenced by env-var *name* in profiles, never by value. The historical Strands Mantle adapter (`strands_mantle_model.py`) is deleted; `.agents/steering/gpt5-mantle-models.md` is historical. Any OpenAI-compatible endpoint (opencode, LM Studio, vLLM) is the M6.5 target of epic `python-factory-bp34j`.

UI — Views are data, not code. Bricks declare views in `mcp/views.py`. A2UI is a payload schema, AG-UI is the wire. The chat surface uses four carriers — see `.agents/steering/a2ui-protocol.md` for the canonical model. The active frontend is Companion-X (Next.js + shadcn/ui + recharts). HTMX/DaisyUI and Flet dashboards are legacy adapters — do NOT reference them as the current UI. Details: `.agents/steering/a2ui-protocol.md`

LOCAL COMPANION-X DEV — For the lightweight local stack, source `projects/companion_x/.env`, then start the API with `uv run python -m factory.api.main`. The editable install puts every brick's `src/` on the Python path automatically via Hatchling's `[tool.hatch.build] dev-mode-dirs` declaration in `pyproject.toml` — no `PYTHONPATH` overrides needed for `uv run`, ad-hoc `python -m`, Kiro powers, or the API process (bd:python-factory-9wcd). The Next dashboard should point at `API_URL=http://127.0.0.1:8000` via `frontends/next-dashboard/.env.local` and be restarted after env changes. Chat uses `COMPANION_X_CHAT_MODEL` (see MODELS); `ollama/<model>` needs the Python `ollama`/`langchain-ollama` packages installed, otherwise the LangChain model factory falls through to the Bedrock path and fails with credential/profile errors even when Ollama itself is running. Optionally set `COMPANION_X_CHAT_THINKING_BUDGET=16000` to enable Claude extended thinking on Sonnet 4.6 (Haiku/Nova/Llama no-op); the v2 chat sidebar auto-renders the Thinking panel via REASONING_MESSAGE_* events (bd:python-factory-tpfk). Local durable document/telemetry storage now uses `STORAGE_DOC_BACKEND=sqlite`, which resolves to `SQLiteDocumentStore` at `./.storage/docs.db`. Old TinyDB `JSONDecodeError` output usually means a stale process or shell is still running with the pre-switch env.

MCP PATTERNS — Tool categories: `@deterministic` (pure), `@operational` (stateful), `@authoring` (gated). Prefix with brick name. Details: `.agents/steering/mcp-tools.md`

MCP TOOL SCOPING — `MCP_INCLUDE_BRICKS=<comma-separated brick names>` on any process running `factory.mcp_server` (base or a project's own `server.py` calling `get_flat_view`) restricts the served tool surface to EXACTLY those bricks — nothing more. Total tools served = sum of the named bricks' tools, always. There is no bloat you're stuck with: composing N bricks in a project's `pyproject.toml` and setting `MCP_INCLUDE_BRICKS` to those same N brick names gives you only their tools. The "600+ tools" companion-x is sometimes cited with is what you get ONLY if you set `MCP_INCLUDE_BRICKS` to companion-x's own ~20-brick list (see `.kiro/specs/standalone-brick-servers/design.md`) — it is not a property of the `agent`/`mcp_server` bricks themselves, and no other project inherits it by composing those bricks. `MCP_DISCOVERY_MODE=progressive` (default) additionally exposes only 9 meta-tools until a brick is force-loaded via `get_brick_tools`; `flat` mounts everything in `MCP_INCLUDE_BRICKS` immediately. Same scoping applies in-process to the LangChain agent's tool wiring via `AgentConfig.tools` / `exact_tools=True` — pass an explicit allowlist and the agent sees only those tools, never the whole registry. Unscoped (`allowlist=None`) is the chat-persona default, not a brick limitation.

INVENTORY — Component and base counts are generated from `BRICKS_INDEX.yaml` (including `oracle`, the domain-agnostic verification oracle, bd:python-factory-216ti). Full catalog with adapter matrix. Details: `.agents/steering/brick-inventory.md`

ARCHITECTURE DEEP DIVE — Gateway pattern, progressive discovery (9 meta-tools), envelope context, CDK generation, Companion-X. Details: `.agents/steering/project-overview.md`

PROGRESSIVE DISCOVERY — Project-level MCP servers (e.g. Companion-X) use lazy loading. `list_bricks` only shows already-loaded bricks; `tools_count: -1` means the brick is registered but not yet loaded. Call `get_brick_tools(brick_name)` to trigger loading.

DOMAIN-AGNOSTIC SUBSTRATE — **Historical changelog (Strands era; retained for bead/verdict provenance). The design outcomes (registry-driven persona, per-thread persona selection, agnostic graph/games/memory boundaries) still hold; every Strands-specific mechanism named below (`AgentSkills` plugin, `@tool` async generators, `translate_strands_event`, `StrandsMCPChatAgent`, `agent.agent_id` plugin recall) has been re-implemented on LangChain/LangGraph — read the code, not this paragraph, for the current mechanism.** Epic `python-factory-hadbi` (qa `1e7ce24b`, APPROVE_WITH_FOLLOWUPS) made three boundaries domain-agnostic. AGENT: chat persona is registry-driven — `COMPANION_X_CHAT_AGENT_ID` env (default `companion-x-default`, LOUD-FAIL on miss) selects an `AgentConfig` whose new `skills:list[str]` field gates Strands `AgentSkills` attach (non-empty only); cache key + `Agent.name` + `session_manager.session_id` all use `f"{agent_id}-{thread_id}"` so swapping the env yields clean session boundaries (bd:`hadbi.1`). The persona registry is now UNIFIED (epic bd:`python-factory-d4roe`): built-ins=CODE (`AGENTS_TYPED`), user personas=DATA behind a `RegistryStore` port (disk locally; AgentCore seam), merged built-ins-first via `registry/unified.py::unified_personas` — the single read path for both chat resolve + discovery. `agent_create_agent` `@authoring` tool creates a persona with NO restart (gated; chat-callable as of bd:`python-factory-czpw.1` — the chat-exclusion was WAIVED by explicit user decision, `@authoring` kept as operator kill-switch). `AgentConfig.id` charset-locked `^[a-z0-9][a-z0-9_-]{0,127}$` (REJECT not coerce, bd:`67qvz`). Persona is selectable PER-THREAD via `forwardedProps.companion_x_agent_id` (`GET /api/personas` + FE `/` palette, bd:`d4roe.3`); env stays the default (byte-identical when absent). CHAT-AGENT CRUD+SPAWN (epic bd:`python-factory-czpw`, uncommitted): the chat agent can now create/spawn/delete/compose sub-agents conversationally. Three native Strands async-generator `@tool`s (NOT MCP tools): `spawn_subagent(agent_id, task)` (bd:`czpw.1`) runs a single registered persona inline; `spawn_swarm(agent_ids, task)` (bd:`python-factory-t8o1g`) assembles a `Swarm` for free multi-agent collaboration; `spawn_graph(agent_ids, edges, task)` (bd:`python-factory-x2k0b`) builds a directed `Graph` pipeline — `edges=[{"from":id,"to":id},...]`, nodes with no incoming edges auto-detected as entry points; `spawn_registered_graph(graph_id, task)` (bd:`python-factory-4bqcz`) resolves the graph topology from the built-in registry by `graph_id` — runs the 17+ pre-built pipelines (redteam, recon, sast, dast, etc.) without inline node+edge description. All four stream via the existing `tool_stream` rail; `translate_strands_event` handles `multiagent_node_stream` events (G2) by re-tagging inner events `message_id=f"swarm-{node_id}"` so each node renders in its OWN sub-bubble. `AgentSkills` must be passed via `Agent(plugins=[...])` at ctor time, NOT `hooks.add_hook()` — `AgentSkills` is a `Plugin` (has `init_agent`), not a `HookProvider`. Plus `agent_delete_agent` `@authoring` and `agent_list_skills` `@deterministic`; security gate waived by user (strands-expert `209fb838`/`6b9c1482`, meta-architect `dbae0646`). GRAPH: `graph.get_findings_for_run` / `graph.get_recent_findings` accept `taxonomy_edges: list[TaxonomyEdgeSpec] | None = None` — `None` = back-compat security CWE/OCSF defaults, `[]` = no taxonomy join, custom = caller-driven row columns (`TaxonomyEdgeSpec` in `runtime/models.py`; bd:`ecph9`). GAMES: RL pipeline reads `config["domain_class"]` with `domain_class → vuln_class → game_type` back-compat fallback, memory tags dual-emit `[domain_class, "scan-learnings", f"{domain_class}-learnings", run_id]`, and per-call `count_labels` threads through `_collect_findings` / `graph_counts` / `build_findings_detail` (bd:`qer1z`). Follow-up wave (PRs #600 / #601) threaded `domain_class` / `count_labels` / `taxonomy_edges` through the MCP boundary itself (`games_process_workflow_rl`, `games_write_experiment_report`, `graph_get_workflow_summary`, `evals_persist_score`), the canonical `LearningEventPayload.domain_class` field (alias `vuln_class`), `mcp_utils.correlation._CANONICAL_KEYS`, and per-node `skills:list[str]` on `AgentNodeRef` + `SwarmAgentConfig` via the neutral `runtime/adapters/skills_attach.py::build_skills_plugin` helper. New domain agents only need their own `defaults_<domain>.py` + per-call config; walkthrough at `.agents/recipes/domain-agnostic-substrate.md`. P3 burndown wave 1 (bd:`qm07q` + bd:`lin6p`, qa `96d9e8ac` APPROVE_WITH_FOLLOWUPS) closed two follow-ups: `graph` ships a per-domain taxonomy registry (`components/graph/src/factory/graph/runtime/taxonomy_registry.py` — `register_extension` / `get_extensions` / `resolve_domain_taxonomy` / `merge_taxonomies` / `reset_extensions`) surfaced through a new `graph://schemas/taxonomy/{domain}` MCP resource template, and `memory_retrieve` accepts `tags: list[str] | None = None` (None=no filter, []=match nothing, list=ANY-match on `metadata.tags`) post-filtered at `MemoryRuntime.retrieve` so all 7 adapters inherit it for free. Per-domain RL recall closed end-to-end (this PR, bd:`vs1vu` + bd:`zj026` superseded by bd:`ziwqs`, qa `9c4846a4` then `3c837989` APPROVE): `games/runtime/game_pipeline.py::_store_learnings` now persists `tags` inside `metadata={"tags": [...]}` (was a silently-dropped top-level kwarg since #550, meta-architect `2efd2a40` Q9). Per-agent recall reads `agent.agent_id` directly (Strands canonical primitive — `strands/agent/agent.py:227`) — bd:`ziwqs` retracted the prior `domain_class` field per strands-expert verdict `68ea2c63` + meta-architect verdict `afdb867a`. `LearningRecallPlugin(agent)` reads `self._agent.agent_id` at invocation; `StrandsMCPChatAgent._get_or_create` binds `agent_id=cfg.id` to the Strands `Agent` ctor (the load-bearing fix; pre-ziwqs the kwarg was missing and Strands' default `'default'` leaked through, structurally breaking per-agent recall regardless of `domain_class`). Empty/`'default'` `agent_id` omits the tags filter (broad recall back-compat). The agent IS the domain — skills + tools + system_prompt + agent_id are the entire expertise contract; no separate `domain_class` knob. Client-side `_has_learnings_tag` retained as defense-in-depth. P3 burndown wave 2 (this PR, bds:`26e2a` + `b2d2o` + `nley3`, qa `8862f537` BLOCK→APPROVE) closes the last three follow-ups: bd:`26e2a` ships Tier-1 native `tags` push-down on `neo4j` + `neo4j_embedding` via the shared `_neo4j_filters.build_filter_clauses` helper (chromadb stays Tier-2 per `flatten_meta` JSON-encode limitation; runtime post-filter retained as defense-in-depth + Tier-2/3 fallback), bd:`b2d2o` adds a generic `metadata: dict[str, str] | None = None` per-key AND filter with **asymmetric default `metadata={}≡None`** (distinct from `tags=[]` strict-empty, verdict `f279063c` Q4) and a Cypher injection guard at TWO sites (`MemoryQuery._validate_metadata_keys` + `_neo4j_filters.safe_property_key`, both `re.fullmatch` on `^[a-zA-Z_][a-zA-Z0-9_]*$` — `re.match` was a discovered trailing-newline bypass), and bd:`nley3` is a test-only Hypothesis flake fix on `test_p5_preserves_user_text`.

## SESSION PROTOCOL

1. Check `bd ready` for available work
2. Claim before starting: `bd update <id> --claim`
3. When done: `bd close <id> --reason "..."`
4. Discovered new work? `bd create "title" --deps discovered-from:<parent-id>`
5. Before finishing: `foreman_guardian_check` if code changed
6. Push: `git pull --rebase && bd sync && git push`
