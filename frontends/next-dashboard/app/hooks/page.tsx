"use client";
import { HooksPage } from "@/components/operations/hooks/hooks-page";

// Row 115 (feature-map) — /hooks: full-page hook manager (honest scaffold).
export default function Page() {
  return <HooksPage />;
}
